name: apply
description: '*(ALPHA)*  Create a PipelineRun/TaskRun.'
hidden: true
flags:
    --file=!: The YAML file to use as the PipelineRun/TaskRun configuration file.
    --region=: ID of the region or fully qualified identifier for the region.
documentation:
    command: '*(ALPHA)*  Create a PipelineRun/TaskRun.'
    flag:
        file: The YAML file to use as the PipelineRun/TaskRun configuration file.
        region: |-
            ID of the region or fully qualified identifier for the region.
            +
            To set the `region` attribute:
            * provide the argument `--region` on the command line;
            * set the property `builds/region`.
