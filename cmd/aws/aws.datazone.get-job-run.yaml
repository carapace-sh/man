name: get-job-run
description: The details of the job run.
flags:
    --domain-identifier=!: The ID of the domain.
    --identifier=!: The ID of the job run.
documentation:
    command: The details of the job run.
    flag:
        domain-identifier: The ID of the domain.
        identifier: The ID of the job run.
