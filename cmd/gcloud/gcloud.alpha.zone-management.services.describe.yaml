name: describe
description: '*(ALPHA)*  describe zonal service.'
flags:
    --location=: The location name.
documentation:
    command: '*(ALPHA)*  Describes a zonal service.'
    flag:
        location: |-
            The location name.
            +
            To set the `location` attribute:
            * provide the argument `zonal_service` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
