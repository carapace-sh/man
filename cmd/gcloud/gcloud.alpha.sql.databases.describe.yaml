name: describe
description: '*(ALPHA)*  Displays configuration and metadata about a Cloud SQL database.'
flags:
    --instance=!: Cloud SQL instance ID.
documentation:
    command: '*(ALPHA)*  Information such as database name, charset, and collation will be displayed.'
    flag:
        instance: Cloud SQL instance ID.
