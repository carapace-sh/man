name: describe
description: '*(ALPHA)*  Obtain details about Speech-to-text recognizer.'
flags:
    --location=: Location of the recognizer.
documentation:
    command: '*(ALPHA)*  Obtain details about Speech-to-text recognizer.'
    flag:
        location: |-
            Location of the recognizer.
            +

            To set the `location` attribute:
            * provide the argument `recognizer` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
