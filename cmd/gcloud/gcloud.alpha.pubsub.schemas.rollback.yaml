name: rollback
description: '*(ALPHA)*  Roll back a Pub/Sub schema to a specified revision.'
flags:
    --revision-id=!: The revision to roll back to.
documentation:
    command: '*(ALPHA)*  Roll back a Pub/Sub schema to a specified revision.'
    flag:
        revision-id: The revision to roll back to.
