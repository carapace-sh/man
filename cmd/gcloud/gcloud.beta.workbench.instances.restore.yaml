name: restore
description: '*(BETA)*  Restores the workbench instance to a snapshot state.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --location=: Google Cloud location of this environment https://cloud.google.com/compute/docs/regions-zones/#locations.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --snapshot-project=!: The project id of the snapshot to be restored from.
    --snapshot=!: The snapshot name to be restored from.
documentation:
    command: |
        *(BETA)*  Restores the workbench instance to a snapshot state.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        location: |-
            Google Cloud location of this environment https://cloud.google.com/compute/docs/regions-zones/#locations.
            +
            To set the `location` attribute:
            * provide the argument `instance` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `notebooks/location`.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        snapshot: The snapshot name to be restored from.
        snapshot-project: The project id of the snapshot to be restored from.
