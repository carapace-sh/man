name: create
description: '*(BETA)*  Create a Mirroring Endpoint Group Association.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: Location of the mirroring endpoint group association.
    --max-wait=: Time to synchronously wait for the operation to complete, after which the operation continues asynchronously.
    --mirroring-endpoint-group-location=: Location of the mirroring endpoint group.
    --mirroring-endpoint-group=!: ID of the mirroring endpoint group or fully qualified identifier for the mirroring endpoint group.
    --network=!: ID of the network or fully qualified identifier for the network.
    --no-async&: Return immediately, without waiting for the operation in progress to
documentation:
    command: |+
        *(BETA)*  Create a mirroring endpoint group association. Successful creation of an association results
        in an association in ACTIVE state. Check the progress of association creation
        by using `gcloud network-security mirroring-endpoint-group-associations list`.

        For more examples, refer to the EXAMPLES section below.

    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete. The default is *True*. Enabled by default, use *--no-async* to disable.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            Location of the mirroring endpoint group association.
            +
            To set the `location` attribute:
            * provide the argument `MIRRORING_ENDPOINT_GROUP_ASSOCIATION` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        max-wait: Time to synchronously wait for the operation to complete, after which the operation continues asynchronously. Ignored if --no-async isn't specified. See $ gcloud topic datetimes for information on time formats.
        mirroring-endpoint-group: |-
            ID of the mirroring endpoint group or fully qualified identifier for the mirroring endpoint group.
            +
            To set the `id` attribute:
            * provide the argument `--mirroring-endpoint-group` on the command line.
        mirroring-endpoint-group-location: |-
            Location of the mirroring endpoint group.
            +
            To set the `location` attribute:
            * provide the argument `--mirroring-endpoint-group` on the command line with a fully specified name;
            * provide the argument `--mirroring-endpoint-group-location` on the command line;
            * provide the argument `--location` on the command line;
            * provide the argument `networksecurity.projects.locations.mirroringEndpointGroupAssociations` on the command line with a fully specified name.
        network: |-
            ID of the network or fully qualified identifier for the network.
            +
            To set the `network-name` attribute:
            * provide the argument `--network` on the command line.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
