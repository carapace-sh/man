# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: stop [NAME]
description: Stop builder instance
documentation:
    command: '[AI] The `docker buildx` is a Docker extension for building multi-stage and production-ready Docker images using multiple buildx contexts. The `docker buildx stop` command is used to gracefully stop all running builds, tests, and other background tasks associated with the buildx service. It does not delete any data or configurations, allowing users to restart the services later when needed.'
