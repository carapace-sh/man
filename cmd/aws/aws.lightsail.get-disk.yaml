name: get-disk
description: Returns information about a specific block storage disk.
flags:
    --disk-name=!: The name of the disk (`my-disk`).
documentation:
    command: Returns information about a specific block storage disk.
    flag:
        disk-name: The name of the disk (`my-disk`).
