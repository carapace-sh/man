name: mediastore
description: An AWS Elemental MediaStore container is a namespace that holds folders and objects.
documentation:
    command: An AWS Elemental MediaStore container is a namespace that holds folders and objects. You use a container endpoint to create, read, and delete objects.
