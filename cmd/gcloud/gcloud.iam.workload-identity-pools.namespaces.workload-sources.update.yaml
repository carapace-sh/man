name: update
description: Update a workload source for a workload identity pool namespace.
hidden: true
flags:
    --allow-identity-self-selection: Allows matched workloads to request any identity within the namespace.
    --async: Return immediately, without waiting for the operation in progress to
    --location=: The location name.
    --namespace=: The ID to use for the namespace.
    --no-allow-identity-self-selection&: Allows matched workloads to request any identity within the namespace.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --single-attribute-selectors=: An attribute that a workload can attest for it to be allow to receive a managed identity.
    --workload-identity-pool=: The ID to use for the pool, which becomes the final component of the resource name.
documentation:
    command: Update a workload source for a workload identity pool namespace.
    flag:
        allow-identity-self-selection: Allows matched workloads to request any identity within the namespace. Use *--allow-identity-self-selection* to enable and *--no-allow-identity-self-selection* to disable.
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        location: |-
            The location name.
            +
            To set the `location` attribute:
            * provide the argument `workload_source` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        namespace: |-
            The ID to use for the namespace. This value must be 2-63 characters, and may contain the
            characters [a-z0-9-]. The prefix `gcp-` is reserved for use by Google, and may not be
            specified.
            +

            To set the `namespace` attribute:
            * provide the argument `workload_source` on the command line with a fully specified name;
            * provide the argument `--namespace` on the command line.
        no-allow-identity-self-selection: Allows matched workloads to request any identity within the namespace.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        single-attribute-selectors: An attribute that a workload can attest for it to be allow to receive a managed identity.
        workload-identity-pool: |-
            The ID to use for the pool, which becomes the final component of the resource name. This value
            should be 4-32 characters, and may contain the characters [a-z0-9-]. The prefix `gcp-` is
            reserved for use by Google, and may not be specified.
            +

            To set the `workload-identity-pool` attribute:
            * provide the argument `workload_source` on the command line with a fully specified name;
            * provide the argument `--workload-identity-pool` on the command line.
