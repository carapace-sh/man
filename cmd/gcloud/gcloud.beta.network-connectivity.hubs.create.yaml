name: create
description: '*(BETA)*  Create a new hub.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --description=: Description of the hub.
    --export-psc: This boolean controls whether Private Service Connect transitivity is enabled for the hub.
    --export-psc-global-google-apis&: This boolean controls whether Private Service Connect endpoints for global Google APIs should be propagated.
    --export-psc-published-services-and-regional-google-apis&: This boolean control whether Private Service Connect endpoints for regional ILBs and regional Google APIs should be propagated.
    --labels=: List of label KEY=VALUE pairs to add.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-export-psc&: This boolean controls whether Private Service Connect transitivity is enabled for the hub.
    --no-export-psc-global-google-apis&: This boolean controls whether Private Service Connect endpoints for global Google APIs should be propagated.
    --no-export-psc-published-services-and-regional-google-apis&: This boolean control whether Private Service Connect endpoints for regional ILBs and regional Google APIs should be propagated.
    --policy-mode=: Policy mode of the hub.
    --preset-topology=: Topology of the hub.
completion:
    flag:
        policy-mode:
            - policy-mode-unspecified
            - preset
        preset-topology:
            - hybrid-inspection
            - mesh
            - preset-topology-unspecified
            - star
documentation:
    command: '*(BETA)*  Create a new hub with the given name.'
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        description: Description of the hub.
        export-psc: This boolean controls whether Private Service Connect transitivity is enabled for the hub.
        export-psc-global-google-apis: This boolean controls whether Private Service Connect endpoints for global Google APIs should be propagated.
        export-psc-published-services-and-regional-google-apis: This boolean control whether Private Service Connect endpoints for regional ILBs and regional Google APIs should be propagated.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-export-psc: This boolean controls whether Private Service Connect transitivity is enabled for the hub.
        no-export-psc-global-google-apis: This boolean controls whether Private Service Connect endpoints for global Google APIs should be propagated.
        no-export-psc-published-services-and-regional-google-apis: This boolean control whether Private Service Connect endpoints for regional ILBs and regional Google APIs should be propagated.
        policy-mode: 'Policy mode of the hub. _POLICY_MODE_ must be one of: *policy-mode-unspecified*, *preset*.'
        preset-topology: 'Topology of the hub. Only applicable when ``--policy-mode=PRESET''''. _PRESET_TOPOLOGY_ must be one of: *hybrid-inspection*, *mesh*, *preset-topology-unspecified*, *star*.'
