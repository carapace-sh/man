# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: describe TARGET
aliases:
    - desc
description: Edit the commit message of the specified commit
group: editing commits
flags:
    -h, --help: Print help
