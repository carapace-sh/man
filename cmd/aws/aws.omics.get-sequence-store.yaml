name: get-sequence-store
description: Retrieves metadata for a sequence store using its ID and returns it in JSON format.
flags:
    --id=!: The store's ID.
documentation:
    command: Retrieves metadata for a sequence store using its ID and returns it in JSON format.
    flag:
        id: The store's ID.
