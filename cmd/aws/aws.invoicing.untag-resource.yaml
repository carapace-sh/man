name: untag-resource
description: Removes a tag from a resource.
flags:
    --resource-arn=!: The Amazon Resource Name (ARN) to untag.
    --resource-tag-keys=!: Keys for the tags to be removed.
documentation:
    command: Removes a tag from a resource.
    flag:
        resource-arn: The Amazon Resource Name (ARN) to untag.
        resource-tag-keys: Keys for the tags to be removed.
