name: list-deployment-configs
description: Lists the deployment configurations with the user or Amazon Web Services account.
flags:
    --next-token=: An identifier returned from the previous `ListDeploymentConfigs` call.
documentation:
    command: Lists the deployment configurations with the user or Amazon Web Services account.
    flag:
        next-token: An identifier returned from the previous `ListDeploymentConfigs` call. It can be used to return the next set of deployment configurations in the list.
