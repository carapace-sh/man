name: reset-landing-zone
description: This API call resets a landing zone.
flags:
    --landing-zone-identifier=!: The unique identifier of the landing zone.
documentation:
    command: This API call resets a landing zone. It starts an asynchronous operation that resets the landing zone to the parameters specified in the original configuration, which you specified in the manifest file. Nothing in the manifest file's original landing zone configuration is changed during the reset process, by default. This API is not the same as a rollback of a landing zone version, which is not a supported operation.
    flag:
        landing-zone-identifier: The unique identifier of the landing zone.
