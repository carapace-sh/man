name: start-guisession
description: Initiates a graphical user interface (GUI) session that’s used to access a virtual computer’s operating system and application.
flags:
    --resource-name=!: The resource name.
documentation:
    command: Initiates a graphical user interface (GUI) session that’s used to access a virtual computer’s operating system and application. The session will be active for 1 hour. Use this action to resume the session after it expires.
    flag:
        resource-name: The resource name.
