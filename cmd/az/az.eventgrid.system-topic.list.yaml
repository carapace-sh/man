# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List available system topics.
flags:
    --odata-query=: The OData query used for filtering the list results.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
documentation:
    flag:
        odata-query: 'The OData query used for filtering the list results. Filtering is currently allowed on the Name property only. The supported operations include: CONTAINS, eq (for equal), ne (for not equal), AND, OR and NOT.'
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List all system topics in a resource group except the system topic with name "name1": az eventgrid system-topic list -g rg1 --odata-query "NOT (name eq 'name1')"
    List all system topics in a resource group whose name contains the pattern "XYZ": az eventgrid system-topic list -g rg1 --odata-query "Contains(name, 'XYZ')"
    List all system topics in a resource group.: az eventgrid system-topic list -g rg1
    List all system topics in the current Azure subscription.: az eventgrid system-topic list
