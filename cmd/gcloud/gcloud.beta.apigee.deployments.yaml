name: deployments
description: '*(BETA)*  Manage deployments of Apigee API proxies in runtime environments.'
documentation:
    command: |
        *(BETA)*  Manage deployments of Apigee API proxies in runtime environments.

        `gcloud beta apigee deployments` contains commands for enumerating and checking the status
        of deployments of proxies to runtime environments.
