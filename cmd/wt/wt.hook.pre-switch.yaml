name: pre-switch
description: Run pre-switch hooks
flags:
    --var=*: Override built-in template variable (KEY=VALUE)
    -h, --help: Print help (see more with '--help')
    -y, --yes: Skip approval prompts
completion:
    flag:
        C:
            - $files
        config:
            - $files
    positional:
        - []
documentation:
    command: |-
        Run pre-switch hooks

        Blocking — waits for completion before continuing.
    flag:
        help: Print help (see a summary with '-h')
