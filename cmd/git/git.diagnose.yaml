name: diagnose
description: Generate a zip archive of diagnostic information
group: interrogator
flags:
    --mode=: specify the type of diagnostics that should be collected
    -o, --output-directory=: place the resulting diagnostics archive in <path>
    -s, --suffix=: specify an alternate suffix for the diagnostics archive name
