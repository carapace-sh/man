name: search
description: '*(ALPHA)*  Search for and list all Privileged Access Manager (PAM) grants you have created, have approved, or can approve.'
flags:
    --caller-relationship=!: Whether to return grants you have created, have approved, or can approve.
    --entitlement=!: ID of the entitlement or fully qualified identifier for the entitlement.
    --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
    --folder=: The name of the folder
    --limit=: Maximum number of resources to list.
    --location=: The resource location
    --organization=: The name of the organization
    --page-size=: Some services group resource list output into pages.
    --sort-by=: Comma-separated list of resource field key names to sort by.
completion:
    flag:
        caller-relationship:
            - can-approve
            - had-approved
            - had-created
documentation:
    command: '*(ALPHA)*  Search for and list all Privileged Access Manager (PAM) grants you have created, have approved, or can approve.'
    flag:
        caller-relationship: 'Whether to return grants you have created, have approved, or can approve. _CALLER_RELATIONSHIP_ must be one of: *can-approve*, *had-approved*, *had-created*.'
        entitlement: |-
            ID of the entitlement or fully qualified identifier for the entitlement.
            +
            To set the `entitlement` attribute:
            * provide the argument `--entitlement` on the command line.
        filter: |-
            Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            If the expression evaluates `True`, then that item is listed. For more
            details and examples of filter expressions, run $ gcloud topic filters. This
            flag interacts with other flags that are applied in this order: *--flatten*,
            *--sort-by*, *--filter*, *--limit*.
        folder: |-
            The name of the folder
            +
            To set the `folder` attribute:
            * provide the argument `--entitlement` on the command line with a fully specified name;
            * provide the argument `--folder` on the command line. Must be specified for resource of type [privilegedaccessmanager.folders.locations.entitlements].
        limit: |-
            Maximum number of resources to list. The default is *unlimited*.
            This flag interacts with other flags that are applied in this order:
            *--flatten*, *--sort-by*, *--filter*, *--limit*.
        location: |-
            The resource location
            +
            To set the `location` attribute:
            * provide the argument `--entitlement` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        organization: |-
            The name of the organization
            +
            To set the `organization` attribute:
            * provide the argument `--entitlement` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line. Must be specified for resource of type [privilegedaccessmanager.organizations.locations.entitlements].
        page-size: |-
            Some services group resource list output into pages. This flag specifies
            the maximum number of resources per page. The default is determined by the
            service if it supports paging, otherwise it is *unlimited* (no paging).
            Paging may be applied before or after *--filter* and *--limit* depending
            on the service.
        sort-by: |-
            Comma-separated list of resource field key names to sort by. The
            default order is ascending. Prefix a field with ``~'' for descending
            order on that field. This flag interacts with other flags that are applied
            in this order: *--flatten*, *--sort-by*, *--filter*, *--limit*.
