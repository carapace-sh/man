name: add-labels
description: '*(ALPHA)*  Add labels to Google Compute Engine images.'
flags:
    --labels=!: A list of labels to add.
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute images add-labels* adds labels to a Google Compute Engine image.
    flag:
        labels: A list of labels to add.
