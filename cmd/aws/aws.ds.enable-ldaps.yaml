name: enable-ldaps
description: Activates the switch for the specific directory to always use LDAP secure calls.
flags:
    --directory-id=!: The identifier of the directory.
    --type=!: The type of LDAP security to enable.
completion:
    flag:
        type:
            - Client
documentation:
    command: Activates the switch for the specific directory to always use LDAP secure calls.
    flag:
        directory-id: The identifier of the directory.
        type: The type of LDAP security to enable. Currently only the value `Client` is supported.
