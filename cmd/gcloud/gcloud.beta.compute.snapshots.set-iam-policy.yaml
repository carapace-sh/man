name: set-iam-policy
description: '*(BETA)*  Set the IAM policy for a Compute Engine snapshot.'
documentation:
    command: |
        *(BETA)*  Sets the IAM policy for the given snapshot as defined in a JSON or YAML file.
