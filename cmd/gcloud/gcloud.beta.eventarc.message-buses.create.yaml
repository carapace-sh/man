name: create
description: '*(BETA)*  Create an Eventarc message bus.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --crypto-key=: Fully qualified name of the crypto key to use for customer-managed encryption.
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: The location for the Eventarc message bus, which should be one of the supported regions.
    --logging-config=: The logging config of the message bus.
    --no-async&: Return immediately, without waiting for the operation in progress to
completion:
    flag:
        logging-config:
            - NONE
            - DEBUG
            - INFO
            - NOTICE
            - WARNING
            - ERROR
            - CRITICAL
            - ALERT
            - EMERGENCY
documentation:
    command: '*(BETA)*  Create an Eventarc message bus.'
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        crypto-key: Fully qualified name of the crypto key to use for customer-managed encryption. If this is unspecified, Google-managed keys will be used for encryption.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            The location for the Eventarc message bus, which should be one of the supported regions. Alternatively, set the [eventarc/location] property.
            +
            To set the `location` attribute:
            * provide the argument `message_bus` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `eventarc/location`.
        logging-config: 'The logging config of the message bus. _LOGGING_CONFIG_ must be one of: *NONE*, *DEBUG*, *INFO*, *NOTICE*, *WARNING*, *ERROR*, *CRITICAL*, *ALERT*, *EMERGENCY*.'
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
