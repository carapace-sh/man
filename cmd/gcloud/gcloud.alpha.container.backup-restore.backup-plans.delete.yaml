name: delete
description: '*(ALPHA)*  Delete a backup plan.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --etag=: The etag is used for optimistic concurrency control as a way to help
    --location=: Google Cloud location.
    --no-async&: Return immediately, without waiting for the operation in progress to
documentation:
    command: |
        *(ALPHA)*  Delete a Backup for GKE backup plan.

        ## EXAMPLES

        To delete a backup plan ``my-backup-plan'' in project ``my-project'' in location ``us-central1'', run:

          $ gcloud alpha container backup-restore backup-plans delete my-backup-plan --project=my-project --location=us-central1
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        etag: |-
            The etag is used for optimistic concurrency control as a way to help
            prevent simultaneous updates or deletes of a resource from overwriting each other.
        location: |-
            Google Cloud location.
            +
            To set the `location` attribute:
            * provide the argument `backup_plan` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `gkebackup/location`.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
