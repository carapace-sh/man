name: deployments
description: '*(ALPHA)*  Manage deployments of Apigee API proxies in runtime environments.'
documentation:
    command: |
        *(ALPHA)*  Manage deployments of Apigee API proxies in runtime environments.

        `gcloud alpha apigee deployments` contains commands for enumerating and checking the status
        of deployments of proxies to runtime environments.
