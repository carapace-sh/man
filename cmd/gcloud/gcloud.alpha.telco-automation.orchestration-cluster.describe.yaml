name: describe
description: '*(ALPHA)*  Show details about the orchestration cluster.'
flags:
    --location=: The location name.
documentation:
    command: '*(ALPHA)*  Show details about the orchestration cluster.'
    flag:
        location: |-
            The location name.
            +
            To set the `location` attribute:
            * provide the argument `orchestration_cluster` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
