name: describe
description: '*(ALPHA)*  Show details about a `WasmPlugin` resource.'
flags:
    --location=: The location ID.
documentation:
    command: '*(ALPHA)*  Show details about a `WasmPlugin` resource.'
    flag:
        location: |-
            The location ID.
            +

            To set the `location` attribute:
            * provide the argument `wasm_plugin` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * use global location.
