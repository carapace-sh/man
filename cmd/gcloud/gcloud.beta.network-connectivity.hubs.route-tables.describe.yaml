name: describe
description: '*(BETA)*  Describe a route table.'
flags:
    --hub=: Id of the hub.
documentation:
    command: |
        *(BETA)*  Retrieve and display details about a route table.
    flag:
        hub: |-
            Id of the hub.
            +
            To set the `hub` attribute:
            * provide the argument `route_table` on the command line with a fully specified name;
            * provide the argument `--hub` on the command line.
