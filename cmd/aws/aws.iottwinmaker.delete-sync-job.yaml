name: delete-sync-job
description: Delete the SyncJob.
flags:
    --sync-source=!: The sync source.
    --workspace-id=!: The workspace ID.
documentation:
    command: Delete the SyncJob.
    flag:
        sync-source: |-
            The sync source.

            Currently the only supported syncSource is `SITEWISE`.
        workspace-id: The workspace ID.
