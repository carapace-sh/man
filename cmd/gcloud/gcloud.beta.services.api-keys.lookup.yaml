name: lookup
description: '*(BETA)*  Look up resource name of a key string.'
documentation:
    command: |-
        *(BETA)*  Look up resource name of a key string.

        ## EXAMPLES

         Look up resource name of a key string named my-key-string:

          $ gcloud beta services api-keys lookup my-key-string
