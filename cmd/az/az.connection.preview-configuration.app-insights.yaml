# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: app-insights
description: Preview the expected configurations of local connection to app-insights.
flags:
    --client-type=: The client type used on the connection
    --secret=:
        description: The secret auth info
        nargs: -1
    --subscription=: Name or ID of subscription.
completion:
    flag:
        client-type:
            - dotnet
            - dotnet-internal
            - java
            - python
            - nodejs
            - go
            - none
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        client-type: |-
            The client type used on the connection.

            Accepted values: dotnet, dotnet-internal, go, java, nodejs, none, python
        secret: The secret auth info.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
