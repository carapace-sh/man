# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List feature flags.
flags:
    --all=: List all feature flags.
    --auth-mode=: This parameter can be used for indicating how a data operation is to be authorized.
    --connection-string=: Combination of access key and endpoint of the App Configuration store.
    --endpoint=: If auth mode is "login", provide endpoint URL of the App Configuration store.
    --feature=: Name of the feature to be listed.
    --fields=:
        description: Customize output fields for Feature Flags.
        nargs: -1
    --key=: Key of the feature flag.
    --label=: If no label specified, list all labels.
    --subscription=: Name or ID of subscription.
    -n, --name=: Name of the App Configuration store.
    -t, --top=: Maximum number of items to return.
completion:
    flag:
        auth-mode:
            - login
            - key
        fields:
            - name
            - key
            - label
            - locked
            - last_modified
            - state
            - description
            - conditions
documentation:
    flag:
        all: |-
            List all feature flags.

            Default value: False
        auth-mode: This parameter can be used for indicating how a data operation is to be authorized. If the auth mode is "key", provide connection string or store name and your account access keys will be retrieved for authorization. If the auth mode is "login", provide the `--endpoint` or `--name` and your "az login" credentials will be used for authorization. You can configure the default auth mode using `az configure --defaults appconfig_auth_mode=<auth_mode>`. For more information, see https://learn.microsoft.com/azure/azure-app-configuration/concept-enable-rbac
        connection-string: Combination of access key and endpoint of the App Configuration store. Can be found using 'az appconfig credential list'. Users can preset it using ``` az configure --defaults appconfig_connection_string=<connection_string> ``` or environment variable with the name AZURE_APPCONFIG_CONNECTION_STRING.
        endpoint: If auth mode is "login" or "anonymous", provide endpoint URL of the App Configuration store. The endpoint can be retrieved using "az appconfig show" command. You can configure the default endpoint using ``` az configure --defaults appconfig_endpoint=<endpoint> ``` .
        feature: Name of the feature to be listed. If the feature flag key is different from the default key, provide the `--key` argument instead. Support star sign as filters, for instance * means all features and abc* means features with abc as prefix. Comma separated features are not supported. Please provide escaped string if your feature name contains comma.
        fields: |-
            Customize output fields for Feature Flags.

            Accepted values: conditions, description, key, label, last_modified, locked, name, state
        key: Key of the feature flag. Key must start with the ".appconfig.featureflag/" prefix. Key cannot contain the "%" character. If both key and feature arguments are provided, only key will be used. Support star sign as filters, for instance ".appconfig.featureflag/*" means all features and ".appconfig.featureflag/abc*" means features with abc as prefix. Comma separated features are not supported. Please provide escaped string if your feature name contains comma.
        label: If no label specified, list all labels. Support star sign as filters, for instance * means all labels and abc* means labels with abc as prefix. Use '\0' for null label.
        name: |-
            ``` and your "az login" credentials will be used for authorization. If the auth mode is "anonymous", provide the --endpoint that will be used for authorization. Anonymous mode is intended for custom endpoints only, such as the App Configuration emulator. You can configure the default auth mode using ``` az configure --defaults appconfig_auth_mode=<auth_mode> ``` . For more information, see https://learn.microsoft.com/azure/azure-app-configuration/concept-enable-rbac .

            Default value: key

            Accepted values: anonymous, key, login
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        tags: 'If no tags are specified, list all feature flags with any tags. Support space-separated tags: key[=value] [key[=value] ...].'
        top: Maximum number of items to return. Must be a positive integer. Default to 100.
examples:
    List 150 feature flags with any labels.: az appconfig feature list --connection-string Endpoint=https://contoso.azconfig.io;Id=xxx;Secret=xxx  --top 150
    List a specific feature for any label start with v1. using connection string.: az appconfig feature list --feature color --connection-string Endpoint=https://contoso.azconfig.io;Id=xxx;Secret=xxx --label v1.*
    List all feature flags with null labels.: az appconfig feature list -n MyAppConfiguration --label \0
    List all feature flags with specific tags.: az appconfig feature list -n MyAppConfiguration --tags tag1=value1 tag2=value2
    List all feature flags with tag name "tag1" with empty value.: az appconfig feature list -n MyAppConfiguration --tags tag1=
    List all feature flags.: az appconfig feature list -n MyAppConfiguration
    List all features starting with "MyApp1".: az appconfig feature list -n MyAppConfiguration --key .appconfig.featureflag/MyApp1*
    List all features with any labels and query only key, state and conditions.: az appconfig feature list --connection-string Endpoint=https://contoso.azconfig.io;Id=xxx;Secret=xxx --fields key state conditions
    List feature flags with multiple labels.: az appconfig feature list --label test,prod,\0 -n MyAppConfiguration
