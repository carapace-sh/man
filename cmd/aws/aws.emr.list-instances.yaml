name: list-instances
description: Provides information for all active Amazon EC2 instances and Amazon EC2 instances terminated in the last 30 days, up to a maximum of 2,000.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --cluster-id=!: The identifier of the cluster for which to list the instances.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --instance-fleet-id=: The unique identifier of the instance fleet.
    --instance-fleet-type=: The node type of the instance fleet.
    --instance-group-id=: The identifier of the instance group for which to list the instances.
    --instance-group-types=:
        description: The type of instance group for which to list the instances.
        nargs: -1
    --instance-states=:
        description: A list of instance states that will filter the instances returned with this request.
        nargs: -1
    --marker=: The pagination token that indicates the next set of results to retrieve.
    --max-items=: The  total number of items to return in the command's output.
    --starting-token=: A token to specify where to start paginating.
completion:
    flag:
        instance-fleet-type:
            - MASTER
            - CORE
            - TASK
documentation:
    command: 'Provides information for all active Amazon EC2 instances and Amazon EC2 instances terminated in the last 30 days, up to a maximum of 2,000. Amazon EC2 instances in any of the following states are considered active: AWAITING\_FULFILLMENT, PROVISIONING, BOOTSTRAPPING, RUNNING.'
    flag:
        cli-input-json: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        cli-input-yaml: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        cluster-id: The identifier of the cluster for which to list the instances.
        generate-cli-skeleton: |-
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values. It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        instance-fleet-id: The unique identifier of the instance fleet.
        instance-fleet-type: The node type of the instance fleet. For example MASTER, CORE, or TASK.
        instance-group-id: The identifier of the instance group for which to list the instances.
        instance-group-types: The type of instance group for which to list the instances.
        instance-states: A list of instance states that will filter the instances returned with this request.
        marker: The pagination token that indicates the next set of results to retrieve.
        max-items: |-
            The total number of items to return in the command's output.
            If the total number of items available is more than the value specified, a NextToken is provided in the command's output.
            To resume pagination, provide the NextToken value in the starting-token argument of a subsequent  command.
            Do not use the NextToken response element directly outside of the AWS CLI.

            For usage examples, see Pagination in the AWS Command Line Interface User Guide.
        starting-token: |-
            A token to specify where to start paginating.
            This is the NextToken from a previously truncated response.

            For usage examples, see Pagination in the AWS Command Line Interface User Guide.
