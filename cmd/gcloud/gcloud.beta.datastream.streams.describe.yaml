name: describe
description: '*(BETA)*  *(DEPRECATED)*  Show details about a Datastream stream resource.'
flags:
    --location=: The location of the resources.
documentation:
    command: |4-
        *(BETA)*  *(DEPRECATED)*  This command has been deprecated.
        Use `gcloud datastream streams describe` instead.


        Show details about a Datastream stream resource.
    flag:
        location: |-
            The location of the resources.
            +
            To set the `location` attribute:
            * provide the argument `stream` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
