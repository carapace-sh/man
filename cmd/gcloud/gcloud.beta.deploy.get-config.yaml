name: get-config
description: '*(BETA)*  Get the Cloud Deploy config for the provided region and project.'
flags:
    --region=: ID of the location or fully qualified identifier for the location.
documentation:
    command: '*(BETA)*  Get the Cloud Deploy config for the provided region and project.'
    flag:
        region: |-
            ID of the location or fully qualified identifier for the location.
            +
            To set the `region` attribute:
            * provide the argument `--region` on the command line;
            * set the property `deploy/region`.
