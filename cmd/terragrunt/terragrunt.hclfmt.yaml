# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: hclfmt
description: Recursively find hcl files and rewrite them into a canonical format
group: terragrunt
documentation:
    command: '[AI] `terragrunt hclfmt` is a command provided by Terragrunt, a Terraform wrapper tool. This command is used to format HCL (Hashicorp Configuration Language) files following the Terraform style guide, ensuring consistent and readable configurations across your project.'
