name: describe
description: Displays configuration and metadata about a Cloud SQL database.
flags:
    --instance=!: Cloud SQL instance ID.
documentation:
    command: Information such as database name, charset, and collation will be displayed.
    flag:
        instance: Cloud SQL instance ID.
