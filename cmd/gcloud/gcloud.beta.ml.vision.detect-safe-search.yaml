name: detect-safe-search
description: '*(BETA)*  Detect explicit content in an image.'
flags:
    --model-version=: Model version to use for the feature.
completion:
    flag:
        model-version:
            - builtin/latest
            - builtin/stable
documentation:
    command: |
        *(BETA)*  Safe Search Detection detects adult content, violent content, medical
        content and spoof content in an image.
    flag:
        model-version: 'Model version to use for the feature. _MODEL_VERSION_ must be one of: *builtin/latest*, *builtin/stable*.'
