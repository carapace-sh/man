# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Create an Azure security partner provider.
flags:
    --no-wait=:
        description: Do not wait for the long-running operation to finish.
        nargs: -1
    --provider=: The security provider name.
    --subscription=: Name or ID of subscription.
    --tags=:
        description: 'Space-separated tags: key[=value] [key[=value] ...].'
        nargs: -1
    --vhub=: Name or ID of the virtual hub to which the Security Partner Provider belongs.
    -g, --resource-group=!: Name of resource group.
    -l, --location=: Location.
    -n, --name=!: Name of the Security Partner Provider.
completion:
    flag:
        no-wait:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        provider:
            - Checkpoint
            - IBoss
            - ZScaler
documentation:
    command: |-
        Command group 'az network security-partner-provider' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        location: 'Location. Values from: ``` az account list-locations ``` . You can configure the default location using ``` az configure --defaults location=<location> ``` .'
        name: Name of the Security Partner Provider.
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Accepted values: 0, 1, f, false, n, no, t, true, y, yes
        provider: |-
            The security provider name.  Allowed values: Checkpoint, IBoss, ZScaler.

            Accepted values: Checkpoint, IBoss, ZScaler
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        tags: 'Space-separated tags: key[=value] [key[=value] ...]. Use "" to clear existing tags.  Support shorthand-syntax, json-file and yaml-file. Try "??" to show more.'
        vhub: Name or ID of the virtual hub to which the Security Partner Provider belongs.
examples:
    Create a security partner provider.: az network security-partner-provider create -n MyProvider -g MyResourceGroup --vhub MyVirtualHub --provider Checkpoint
