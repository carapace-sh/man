name: describe
description: '*(BETA)*  Describe an `LbEdgeExtension` resource.'
flags:
    --location=: Cloud region in which the resource is located.
documentation:
    command: |
        *(BETA)*  Show details about an `LbEdgeExtension` resource.
    flag:
        location: |-
            Cloud region in which the resource is located.
            +

            To set the `location` attribute:
            * provide the argument `lb_edge_extension` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
