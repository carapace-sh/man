name: get-compatible-elasticsearch-versions
description: Returns a list of upgrade compatible Elastisearch versions.
flags:
    --domain-name=: ""
documentation:
    command: Returns a list of upgrade compatible Elastisearch versions. You can optionally pass a `DomainName` to get all upgrade compatible Elasticsearch versions for that specific domain.
    flag:
        domain-name: ""
