name: add-labels
description: '*(BETA)*  Add labels to Google Compute Engine images.'
flags:
    --labels=!: A list of labels to add.
documentation:
    command: |
        *(BETA)*  *gcloud beta compute images add-labels* adds labels to a Google Compute Engine image.
    flag:
        labels: A list of labels to add.
