name: describe
description: Describe a specified OS patch job.
documentation:
    command: |-
        ## EXAMPLES

        To check the status of the patch job `job1`, run:

              $ gcloud compute os-config patch-jobs describe job1
