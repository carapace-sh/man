name: delete-batch-prediction-job
description: Deletes a batch prediction job.
flags:
    --job-id=!: The ID of the batch prediction job to delete.
documentation:
    command: Deletes a batch prediction job.
    flag:
        job-id: The ID of the batch prediction job to delete.
