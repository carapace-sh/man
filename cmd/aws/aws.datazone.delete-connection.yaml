name: delete-connection
description: Deletes and connection.
flags:
    --domain-identifier=!: The ID of the domain where the connection is deleted.
    --identifier=!: The ID of the connection that is deleted.
documentation:
    command: Deletes and connection. In Amazon DataZone, a connection enables you to connect your resources (domains, projects, and environments) to external resources and services.
    flag:
        domain-identifier: The ID of the domain where the connection is deleted.
        identifier: The ID of the connection that is deleted.
