name: delete
description: Deletes a Cloud SQL database.
flags:
    --instance=!: Cloud SQL instance ID.
documentation:
    command: For MySQL, also deletes all files in the database directory.
    flag:
        instance: Cloud SQL instance ID.
