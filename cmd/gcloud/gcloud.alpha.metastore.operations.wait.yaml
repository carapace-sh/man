name: wait
description: '*(ALPHA)*  Poll a long-running Dataproc Metastore operation until it completes.'
flags:
    --location=: The location of the Dataproc Metastore service.
documentation:
    command: |
        *(ALPHA)*  Poll a long-running Dataproc Metastore operation until it completes. When
        the operation is complete, this command will display the results of the
        operation.
    flag:
        location: |-
            The location of the Dataproc Metastore service.
            +
            If not specified, will use `default` metastore/location.
            +

            To set the `location` attribute:
            * provide the argument `operation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `metastore/location`.
