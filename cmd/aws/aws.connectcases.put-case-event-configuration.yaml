name: put-case-event-configuration
description: Adds case event publishing configuration.
flags:
    --domain-id=!: The unique identifier of the Cases domain.
    --event-bridge=!: Configuration to enable EventBridge case event delivery and determine what data is delivered.
documentation:
    command: Adds case event publishing configuration. For a complete list of fields you can add to the event message, see [Create case fields](https://docs.aws.amazon.com/connect/latest/adminguide/case-fields.html) in the *Amazon Connect Administrator Guide*
    flag:
        domain-id: The unique identifier of the Cases domain.
        event-bridge: Configuration to enable EventBridge case event delivery and determine what data is delivered.
