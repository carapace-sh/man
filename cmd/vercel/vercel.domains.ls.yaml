# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ls
description: Show all domains in a list
documentation:
    command: '[AI] The `vercel domains ls` command is used in the Vercel CLI (Command Line Interface) to list all custom domain aliases associated with your Vercel projects or teams. It helps you view and manage the custom domains configured for your deployed applications easily.'
