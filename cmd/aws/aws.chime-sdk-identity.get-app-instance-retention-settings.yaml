name: get-app-instance-retention-settings
description: Gets the retention settings for an `AppInstance`.
flags:
    --app-instance-arn=!: The ARN of the `AppInstance`.
documentation:
    command: Gets the retention settings for an `AppInstance`.
    flag:
        app-instance-arn: The ARN of the `AppInstance`.
