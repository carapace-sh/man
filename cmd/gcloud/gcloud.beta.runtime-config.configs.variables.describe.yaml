name: describe
description: '*(BETA)*  Describe variable resources.'
flags:
    --config-name=!: The name of the configuration resource to use.
documentation:
    command: |-
        *(BETA)*  This command displays information about the variable resource with the
        specified name.
    flag:
        config-name: The name of the configuration resource to use.
