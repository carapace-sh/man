name: perform-maintenance
description: '*(ALPHA)*  Perform maintenance on nodes in a Compute Engine node group.'
flags:
    --nodes=!: The names of the nodes to perform maintenance on.
    --start-time=: The requested time for the maintenance window to start.
    --zone=: Zone of the node group to operate on.
documentation:
    command: '*(ALPHA)*  Perform maintenance on nodes in a Compute Engine node group.'
    flag:
        nodes: The names of the nodes to perform maintenance on.
        start-time: The requested time for the maintenance window to start. The timestamp must be an RFC3339 valid string.
        zone: |-
            Zone of the node group to operate on. If not specified and the ``compute/zone'' property isn't set, you
            might be prompted to select a zone (interactive mode only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/zone'' property:
            +
              $ gcloud config set compute/zone ZONE
            +
            A list of zones can be fetched by running:
            +
              $ gcloud compute zones list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/zone
            +
            Alternatively, the zone can be stored in the environment variable
            ``CLOUDSDK_COMPUTE_ZONE''.
