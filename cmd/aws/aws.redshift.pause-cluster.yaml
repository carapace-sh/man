name: pause-cluster
description: Pauses a cluster.
flags:
    --cluster-identifier=!: The identifier of the cluster to be paused.
documentation:
    command: Pauses a cluster.
    flag:
        cluster-identifier: The identifier of the cluster to be paused.
