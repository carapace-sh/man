name: describe-connections
description: Displays the specified connection or all connections in this Region.
flags:
    --connection-id=: The ID of the connection.
    --max-results=: The maximum number of results to return with a single call.
    --next-token=: The token for the next page of results.
documentation:
    command: Displays the specified connection or all connections in this Region.
    flag:
        connection-id: The ID of the connection.
        max-results: |-
            The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned `nextToken` value.

            If `MaxResults` is given a value larger than 100, only 100 results are returned.
        next-token: The token for the next page of results.
