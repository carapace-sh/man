name: get-voice-connector-group
description: Retrieves details for the specified Amazon Chime SDK Voice Connector group, such as timestamps,name, and associated `VoiceConnectorItems`.
flags:
    --voice-connector-group-id=!: The Voice Connector group ID.
documentation:
    command: Retrieves details for the specified Amazon Chime SDK Voice Connector group, such as timestamps,name, and associated `VoiceConnectorItems`.
    flag:
        voice-connector-group-id: The Voice Connector group ID.
