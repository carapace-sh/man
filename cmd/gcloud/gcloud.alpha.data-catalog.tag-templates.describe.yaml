name: describe
description: '*(ALPHA)*  *(DEPRECATED)*  Describe a Data Catalog tag template.'
flags:
    --location=: Location of the tag template.
documentation:
    command: |4-
        *(ALPHA)*  *(DEPRECATED)*  This command is deprecated. Please use `gcloud dataplex aspect-types` instead.

        Describe a Data Catalog tag template.
    flag:
        location: |-
            Location of the tag template.
            +

            To set the `location` attribute:
            * provide the argument `tag_template` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
