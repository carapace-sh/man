name: delete
description: '*(ALPHA)*  Delete a Cloud Dataflow snapshot.'
flags:
    --region=!: Region ID of the snapshot regional endpoint.
documentation:
    command: '*(ALPHA)*  Delete a Cloud Dataflow snapshot.'
    flag:
        region: Region ID of the snapshot regional endpoint.
