name: describe
description: '*(ALPHA)*  Describe a Cloud Security Command Center (SCC) posture.'
flags:
    --location=: ID of the location where the resource exists (for example, global).
    --organization=: ID of the organization which is the parent of the resource.
    --revision-id=: ID of the specific posture revision to described.
documentation:
    command: |
        *(ALPHA)*  Describe a Cloud Security Command Center (SCC) posture.

        By default, the latest updated revision of the posture is described. Users must provide revision ID to describe a specific revision.
    flag:
        location: |-
            ID of the location where the resource exists (for example, global).
            +

            To set the `location` attribute:
            * provide the argument `posture` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        organization: |-
            ID of the organization which is the parent of the resource.
            +

            To set the `organization` attribute:
            * provide the argument `posture` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line.
        revision-id: ID of the specific posture revision to described. If not specified, latest revision is described.
