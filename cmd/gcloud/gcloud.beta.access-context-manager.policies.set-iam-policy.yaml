name: set-iam-policy
description: '*(BETA)*  Set IAM policy for an access policy.'
documentation:
    command: |
        *(BETA)*  Sets the IAM policy for a access policy, given access policy ID and a file
        encoded in JSON or YAML that contains the IAM policy.
