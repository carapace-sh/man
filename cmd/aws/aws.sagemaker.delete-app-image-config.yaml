name: delete-app-image-config
description: Deletes an AppImageConfig.
flags:
    --app-image-config-name=!: The name of the AppImageConfig to delete.
documentation:
    command: Deletes an AppImageConfig.
    flag:
        app-image-config-name: The name of the AppImageConfig to delete.
