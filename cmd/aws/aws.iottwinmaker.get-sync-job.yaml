name: get-sync-job
description: Gets the SyncJob.
flags:
    --sync-source=!: The sync source.
    --workspace-id=: The workspace ID.
documentation:
    command: Gets the SyncJob.
    flag:
        sync-source: |-
            The sync source.

            Currently the only supported syncSource is `SITEWISE`.
        workspace-id: The workspace ID.
