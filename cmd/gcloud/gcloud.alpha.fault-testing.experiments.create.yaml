name: create
description: '*(ALPHA)*  Create an experiment.'
hidden: true
flags:
    --description=: Optional user-provided description or notes.
    --display-name=: Human-readable name of the experiment
    --experiment-template=!: ID of the experimentTemplate or fully qualified identifier for the experimentTemplate.
    --location=: For resources [experiment, experiment-template], provides fallback value for resource location attribute.
documentation:
    command: |
        *(ALPHA)*  Create an experiment.
    flag:
        description: Optional user-provided description or notes. Min length 0, max length 2000.
        display-name: Human-readable name of the experiment
        experiment-template: |-
            ID of the experimentTemplate or fully qualified identifier for the experimentTemplate.
            +
            To set the `experiment-template` attribute:
            * provide the argument `--experiment-template` on the command line.
        location: For resources [experiment, experiment-template], provides fallback value for resource location attribute. When the resource's full URI path is not provided, location will fallback to this flag value.
