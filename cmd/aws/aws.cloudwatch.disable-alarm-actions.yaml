name: disable-alarm-actions
description: Disables the actions for the specified alarms.
flags:
    --alarm-names=!: The names of the alarms.
documentation:
    command: Disables the actions for the specified alarms. When an alarm's actions are disabled, the alarm actions do not execute when the alarm state changes.
    flag:
        alarm-names: The names of the alarms.
