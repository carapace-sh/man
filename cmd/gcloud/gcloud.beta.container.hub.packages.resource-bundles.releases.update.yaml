name: update
description: '*(BETA)*  Update Package Rollouts Release.'
flags:
    --lifecycle=: Lifecycle of the Release.
    --location=: Google Cloud zone or region.
    --resource-bundle=!: Resource Bundle name.
documentation:
    command: '*(BETA)*  Update Package Rollouts Release.'
    flag:
        lifecycle: Lifecycle of the Release.
        location: Google Cloud zone or region.
        resource-bundle: Resource Bundle name.
