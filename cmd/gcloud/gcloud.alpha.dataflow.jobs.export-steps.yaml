name: export-steps
description: '*(ALPHA)*  Exports information about the steps for the given job.'
flags:
    --region=: Region ID of the job's regional endpoint.
documentation:
    command: '*(ALPHA)*  The only currently supported format is to a GraphViz dot file.'
    flag:
        region: Region ID of the job's regional endpoint. Defaults to 'us-central1'.
