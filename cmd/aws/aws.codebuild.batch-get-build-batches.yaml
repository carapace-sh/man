name: batch-get-build-batches
description: Retrieves information about one or more batch builds.
flags:
    --ids=!: An array that contains the batch build identifiers to retrieve.
documentation:
    command: Retrieves information about one or more batch builds.
    flag:
        ids: An array that contains the batch build identifiers to retrieve.
