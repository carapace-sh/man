name: describe
description: '*(BETA)*  Describe backupCollections.'
flags:
    --location=: The location id of the backupCollection resource.
documentation:
    command: '*(BETA)*  Describe a backupCollection'
    flag:
        location: |-
            The location id of the backupCollection resource.
            +
            To set the `location` attribute:
            * provide the argument `backup_collection` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
