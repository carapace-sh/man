name: describe-label-group
description: Returns information about the label group.
flags:
    --label-group-name=!: Returns the name of the label group.
documentation:
    command: Returns information about the label group.
    flag:
        label-group-name: Returns the name of the label group.
