name: delete-resource-policy
description: Removes the association of a resource-based policy from an app monitor.
flags:
    --name=!: The app monitor that you want to remove the resource policy from.
    --policy-revision-id=: Specifies a specific policy revision to delete.
documentation:
    command: Removes the association of a resource-based policy from an app monitor.
    flag:
        name: The app monitor that you want to remove the resource policy from.
        policy-revision-id: Specifies a specific policy revision to delete. Provide a `PolicyRevisionId` to ensure an atomic delete operation. If the revision ID that you provide doesn't match the latest policy revision ID, the request will be rejected with an `InvalidPolicyRevisionIdException` error.
