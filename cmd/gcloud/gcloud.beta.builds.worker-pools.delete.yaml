name: delete
description: '*(BETA)*  Delete a worker pool from Google Cloud Build.'
flags:
    --generation=: Generation of the worker pool.
    --region=: The Cloud region where the worker pool is.
documentation:
    command: '*(BETA)*  Delete a worker pool from Google Cloud Build.'
    flag:
        generation: Generation of the worker pool.
        region: The Cloud region where the worker pool is.
