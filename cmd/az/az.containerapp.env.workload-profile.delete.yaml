# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: delete
description: Delete a workload profile from a Container Apps environment
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: The name of the Container App environment
    -w, --workload-profile-name=!: The friendly name for the workload profile
documentation:
    command: Delete a workload profile from a Container Apps environment.
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        name: The name of the Container App environment.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        workload-profile-name: The friendly name for the workload profile.
examples:
    Delete a workload profile from a Container Apps environment: az containerapp env workload-profile delete -g MyResourceGroup -n MyEnvironment --workload-profile-name my-wlp
