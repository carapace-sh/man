name: create
description: Create a Compute Engine interconnect attachment group.
flags:
    --attachments=: Member interconnect attachments to add to the interconnect attachment
    --description=: An optional, textual description for the interconnect attachment group.
    --intended-availability-sla=!: The availability SLA that the user intends this group to support.
documentation:
    command: |
        *gcloud compute interconnects attachments groups create* is used to create interconnect attachment groups. An
        interconnect attachment group connects a set of redundant interconnect
        attachments between Google and the customer.

        For an example, refer to the *EXAMPLES* section below.
    flag:
        attachments: |-
            Member interconnect attachments to add to the interconnect attachment
            group initially.
        description: An optional, textual description for the interconnect attachment group.
        intended-availability-sla: The availability SLA that the user intends this group to support.
