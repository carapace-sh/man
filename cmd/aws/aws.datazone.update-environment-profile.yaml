name: update-environment-profile
description: Updates the specified environment profile in Amazon DataZone.
flags:
    --aws-account-id=: The Amazon Web Services account in which a specified environment profile is to be udpated.
    --aws-account-region=: The Amazon Web Services Region in which a specified environment profile is to be updated.
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --description=: The description to be updated as part of the `UpdateEnvironmentProfile` action.
    --domain-identifier=!: The identifier of the Amazon DataZone domain in which an environment profile is to be updated.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --identifier=!: The identifier of the environment profile that is to be updated.
    --name=: The name to be updated as part of the `UpdateEnvironmentProfile` action.
    --user-parameters=: The user parameters to be updated as part of the `UpdateEnvironmentProfile` action.
documentation:
    command: Updates the specified environment profile in Amazon DataZone.
    flag:
        aws-account-id: The Amazon Web Services account in which a specified environment profile is to be udpated.
        aws-account-region: The Amazon Web Services Region in which a specified environment profile is to be updated.
        cli-input-json: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        cli-input-yaml: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        description: The description to be updated as part of the `UpdateEnvironmentProfile` action.
        domain-identifier: The identifier of the Amazon DataZone domain in which an environment profile is to be updated.
        generate-cli-skeleton: |-
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values. It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        identifier: The identifier of the environment profile that is to be updated.
        name: The name to be updated as part of the `UpdateEnvironmentProfile` action.
        user-parameters: The user parameters to be updated as part of the `UpdateEnvironmentProfile` action.
