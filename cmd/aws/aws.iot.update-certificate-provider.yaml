name: update-certificate-provider
description: Updates a certificate provider.
flags:
    --account-default-for-operations=: A list of the operations that the certificate provider will use to generate certificates.
    --certificate-provider-name=!: The name of the certificate provider.
    --lambda-function-arn=: The Lambda function ARN that's associated with the certificate provider.
documentation:
    command: |-
        Updates a certificate provider.

        Requires permission to access the [UpdateCertificateProvider](https://docs.aws.amazon.com/service-authorization/latest/reference/list_awsiot.html#awsiot-actions-as-permissions) action.
    flag:
        account-default-for-operations: 'A list of the operations that the certificate provider will use to generate certificates. Valid value: `CreateCertificateFromCsr`.'
        certificate-provider-name: The name of the certificate provider.
        lambda-function-arn: The Lambda function ARN that's associated with the certificate provider.
