name: udp
description: '*(ALPHA)*  Create a UDP health check to monitor load balanced instances.'
hidden: true
flags:
    --check-interval=: How often to perform a health check for an instance.
    --description=: An optional string description for the UDP health
    --global: If set, the UDP health check is global.
    --healthy-threshold=: The number of consecutive successful health checks before an
    --no-global&: If set, the UDP health check is global.
    --port-name=: The port name that this health check monitors.
    --port=: The UDP port number that this health check monitors.
    --region=: Region of the UDP health check to create.
    --request=!: Application data to send in payload of an UDP packet.
    --response=!: The bytes to match against the beginning of the response data.
    --timeout=: If Google Compute Engine doesn't receive a healthy response from the
    --unhealthy-threshold=: The number of consecutive health check failures before a healthy
documentation:
    command: |4
          *(ALPHA)*  *gcloud alpha compute health-checks create udp* is used to create a UDP health check. UDP health checks
        monitor instances in a load balancer controlled by a target pool. All
        arguments to the command are optional except for the name of the health
        check, request and response. For more information on load balancing, see
        [](https://cloud.google.com/compute/docs/load-balancing-and-autoscaling/)
    flag:
        check-interval: |-
            How often to perform a health check for an instance. For example,
            specifying ``10s'' will run the check every 10 seconds. The default
            value is ``5s''.
            See $ gcloud topic datetimes for information on duration formats.
        description: |-
            An optional string description for the UDP health
            check.
        global: If set, the UDP health check is global.
        healthy-threshold: |-
            The number of consecutive successful health checks before an
            unhealthy instance is marked as healthy. The default is 2.
        no-global: If set, the UDP health check is global.
        port: The UDP port number that this health check monitors.
        port-name: |-
            The port name that this health check monitors. By default, this is
            empty.
        region: |-
            Region of the UDP health check to create. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/region'' property:
            +
              $ gcloud config set compute/region REGION
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/region
            +
            Alternatively, the region can be stored in the environment
            variable ``CLOUDSDK_COMPUTE_REGION''.
        request: |-
            Application data to send in payload of an UDP packet. It is an error if
            this is empty.
        response: |-
            The bytes to match against the beginning of the response data.
            It is an error if this is empty.
        timeout: |-
            If Google Compute Engine doesn't receive a healthy response from the
            instance by the time specified by the value of this flag, the health
            check request is considered a failure. For example, specifying ``10s''
            will cause the check to wait for 10 seconds before considering the
            request a failure. The default value is ``5s''.
            See $ gcloud topic datetimes for information on duration formats.
        unhealthy-threshold: |-
            The number of consecutive health check failures before a healthy
            instance is marked as unhealthy. The default is 2.
