name: autokey-config
description: '*(ALPHA)*  Update and retrieve the AutokeyConfig.'
documentation:
    command: |-
        *(ALPHA)*  Autokey config is a folder level resource which configures the key project
        for the folder and the resources inside it.
