name: update
description: '*(BETA)*  Update a hub.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --clear-labels: Remove all labels.
    --description=: New description of the hub.
    --export-psc: Whether Private Service Connect propagation is enabled for the hub.
    --export-psc-global-google-apis&: Whether propagation for Private Service Connect for global Google APIs is enabled for the hub.
    --export-psc-published-services-and-regional-google-apis&: Whether propagation for Private Service Connect for published services and regional Google APIs is enabled for the hub.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-clear-labels&: Remove all labels.
    --no-export-psc&: Whether Private Service Connect propagation is enabled for the hub.
    --no-export-psc-global-google-apis&: Whether propagation for Private Service Connect for global Google APIs is enabled for the hub.
    --no-export-psc-published-services-and-regional-google-apis&: Whether propagation for Private Service Connect for published services and regional Google APIs is enabled for the hub.
    --remove-labels=: List of label keys to remove.
    --update-labels=: List of label KEY=VALUE pairs to update.
documentation:
    command: '*(BETA)*  Update the details of a hub.'
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        clear-labels: |-
            Remove all labels. If `--update-labels` is also specified then
            `--clear-labels` is applied first.
            +
            For example, to remove all labels:
            +
                $ {command} --clear-labels
            +
            To remove all existing labels and create two new labels,
            ``foo'' and ``baz'':
            +
                $ {command} --clear-labels --update-labels foo=bar,baz=qux
        description: New description of the hub.
        export-psc: Whether Private Service Connect propagation is enabled for the hub. Use *--export-psc* to enable and *--no-export-psc* to disable.
        export-psc-global-google-apis: Whether propagation for Private Service Connect for global Google APIs is enabled for the hub.
        export-psc-published-services-and-regional-google-apis: Whether propagation for Private Service Connect for published services and regional Google APIs is enabled for the hub.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-clear-labels: |
            Remove all labels. If `--update-labels` is also specified then
            `--clear-labels` is applied first.

            For example, to remove all labels:

                $ {command} --clear-labels

            To remove all existing labels and create two new labels,
            ``foo'' and ``baz'':

                $ {command} --clear-labels --update-labels foo=bar,baz=qux
        no-export-psc: Whether Private Service Connect propagation is enabled for the hub.
        no-export-psc-global-google-apis: Whether propagation for Private Service Connect for global Google APIs is enabled for the hub.
        no-export-psc-published-services-and-regional-google-apis: Whether propagation for Private Service Connect for published services and regional Google APIs is enabled for the hub.
        remove-labels: |-
            List of label keys to remove. If a label does not exist it is
            silently ignored. If `--update-labels` is also specified then
            `--update-labels` is applied first.
        update-labels: |-
            List of label KEY=VALUE pairs to update. If a label exists, its value is modified. Otherwise, a new label is created.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
