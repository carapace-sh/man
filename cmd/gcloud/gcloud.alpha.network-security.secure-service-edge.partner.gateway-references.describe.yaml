name: describe
description: '*(ALPHA)*  Describe a SSE gateway reference.'
hidden: true
flags:
    --location=: The location Id.
documentation:
    command: |
        *(ALPHA)*  Show details for the specified SSE gateway reference.
    flag:
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `sse_gateway_reference` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
