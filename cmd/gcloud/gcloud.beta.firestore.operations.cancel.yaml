name: cancel
description: '*(BETA)*  Cancel a currently-running Cloud Firestore admin operation.'
flags:
    --database=: The database to operate on.
documentation:
    command: '*(BETA)*  Cancel a currently-running Cloud Firestore admin operation.'
    flag:
        database: |-
            The database to operate on. The default value is `(default)`.
            +
            For example, to operate on database `foo`:
            +
              $ {command} --database='foo'
