name: delete
description: '*(BETA)*  Delete runtime-config resources.'
documentation:
    command: |-
        *(BETA)*  This command deletes the runtime-config resource with the specified name and
        all of its variable and waiter children.
