# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all the consumer groups in a Namespace.
flags:
    --eventhub-name=!: The Event Hub name
    --max-items=: Total number of items to return in the command's output.
    --namespace-name=!: The Namespace name
    --next-token=: Token to specify where to start paginating.
    --skip=: Skip is only used if a previous operation returned a partial result.
    --subscription=: Name or ID of subscription.
    --top=: May be used to limit the number of results to the most recent N usageDetails.
    -g, --resource-group=!: Name of resource group.
documentation:
    command: List all the consumer groups in a Namespace. An empty feed is returned if no consumer group exists in the Namespace.
    flag:
        eventhub-name: The Event Hub name.
        max-items: Total number of items to return in the command's output. If the total number of items available is more than the value specified, a token is provided in the command's output. To resume pagination, provide the token value in `--next-token` argument of a subsequent command.
        namespace-name: The Namespace name.
        next-token: Token to specify where to start paginating. This is the token value from a previously truncated response.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        skip: Skip is only used if a previous operation returned a partial result. If a previous response contains a nextLink element, the value of the nextLink element will include a skip parameter that specifies a starting point to use for subsequent calls.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        top: May be used to limit the number of results to the most recent N usageDetails.
