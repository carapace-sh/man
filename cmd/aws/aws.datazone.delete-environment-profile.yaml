name: delete-environment-profile
description: Deletes an environment profile in Amazon DataZone.
flags:
    --domain-identifier=!: The ID of the Amazon DataZone domain in which the environment profile is deleted.
    --identifier=!: The ID of the environment profile that is deleted.
documentation:
    command: Deletes an environment profile in Amazon DataZone.
    flag:
        domain-identifier: The ID of the Amazon DataZone domain in which the environment profile is deleted.
        identifier: The ID of the environment profile that is deleted.
