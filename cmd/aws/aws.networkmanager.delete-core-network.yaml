name: delete-core-network
description: Deletes a core network along with all core network policies.
flags:
    --core-network-id=!: The network ID of the deleted core network.
documentation:
    command: Deletes a core network along with all core network policies. This can only be done if there are no attachments on a core network.
    flag:
        core-network-id: The network ID of the deleted core network.
