name: get-repository-catalog-data
description: Retrieve catalog metadata for a repository in a public registry.
flags:
    --registry-id=: The Amazon Web Services account ID that's associated with the registry that contains the repositories to be described.
    --repository-name=!: The name of the repository to retrieve the catalog metadata for.
documentation:
    command: Retrieve catalog metadata for a repository in a public registry. This metadata is displayed publicly in the Amazon ECR Public Gallery.
    flag:
        registry-id: The Amazon Web Services account ID that's associated with the registry that contains the repositories to be described. If you do not specify a registry, the default public registry is assumed.
        repository-name: The name of the repository to retrieve the catalog metadata for.
