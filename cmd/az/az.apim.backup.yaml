# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: backup
description: back up an API Management service to the configured storage account
flags:
    --backup-name=!: The name of the backup file to create.
    --no-wait=: Do not wait for the long-running operation to finish.
    --storage-account-container=!: The name of the storage account container used to place the backup.
    --storage-account-key=!: The access key of the storage account used to place the backup.
    --storage-account-name=!: The name of the storage account used to place the backup.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: The name of the api management service instance
documentation:
    command: Creates a backup of the API Management service to the given Azure Storage Account. This is long running operation and could take several minutes to complete.
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        backup-name: The name of the backup file to create. --container-name --storage-account-container The name of the storage account container used to place the backup.
        change-reference: The related change reference ID for this resource operation.
        name: The name of the api management service instance.
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Default value: False
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        storage-account-key: The access key of the storage account used to place the backup.
        storage-account-name: The name of the storage account used to place the backup.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Create a backup of the API Management service instance: az apim backup --name MyApim -g MyResourceGroup --backup-name myBackup              --storage-account-name mystorageaccount --storage-account-container backups              --storage-account-key Ay2ZbdxLnD4OJPT29F6jLPkB6KynOzx85YCObhrw==
