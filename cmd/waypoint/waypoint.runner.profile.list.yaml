# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all registered runner profiles
documentation:
    command: '[AI] The "waypoint runner profile list" command is used in HERE Location Services Platform to retrieve a list of available Waypoint Runner profiles. A Waypoint Runner profile defines settings for a specific use case, such as vehicle type, sensor configuration, and data reporting frequency. Each profile impacts the performance and cost of the service. By running this command, you can identify which profiles are currently offered by HERE, allowing you to make informed decisions when selecting the most suitable one for your application.'
