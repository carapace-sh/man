name: describe-workflow
description: Describes the specified workflow.
flags:
    --workflow-id=!: A unique identifier for the workflow.
documentation:
    command: Describes the specified workflow.
    flag:
        workflow-id: A unique identifier for the workflow.
