name: delete
description: '*(ALPHA)*  Delete a translation.'
hidden: true
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --force: If set to true, any SourcePolicies and TranslatedPolicies from this
    --location=: The location id of the translation resource.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-force&: If set to true, any SourcePolicies and TranslatedPolicies from this
    --policy-porter=: The policyPorter id of the translation resource.
    --request-id=: An optional request ID to identify requests.
documentation:
    command: |
        *(ALPHA)*  Deletes PolicyPorter translation resource.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        force: |-
            If set to true, any SourcePolicies and TranslatedPolicies from this
            Translation will also be deleted. (Otherwise, the request will only work if
            the Translation has no SourcePolicies and TranslatedPolicies.)
        location: |-
            The location id of the translation resource.
            +
            To set the `location` attribute:
            * provide the argument `translation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-force: |-
            If set to true, any SourcePolicies and TranslatedPolicies from this
            Translation will also be deleted. (Otherwise, the request will only work if
            the Translation has no SourcePolicies and TranslatedPolicies.)
        policy-porter: |-
            The policyPorter id of the translation resource.
            +
            To set the `policy-porter` attribute:
            * provide the argument `translation` on the command line with a fully specified name;
            * provide the argument `--policy-porter` on the command line.
        request-id: |-
            An optional request ID to identify requests. Specify a unique request ID
            so that if you must retry your request, the server will know to ignore
            the request if it has already been completed. The server will guarantee
            that for at least 60 minutes after the first request.
            +
            For example, consider a situation where you make an initial request and the
            request times out. If you make the request again with the same request
            ID, the server can check if original operation with the same request ID
            was received, and if so, will ignore the second request. This prevents
            clients from accidentally creating duplicate commitments.
            +
            The request ID must be a valid UUID with the exception that zero UUID is
            not supported (00000000-0000-0000-0000-000000000000).
