# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: work
description: workspace maintenance
documentation:
    command: '[AI] The `go work` command is not a standard Go command. It seems like you may have misunderstood the intended command. In Go programming language, there are several commands related to building and running Go projects, such as `go build`, `go run`, `go test`, etc. Each of these commands serves a specific purpose in the Go development workflow. If you meant one of these commands, please provide more context or correct the command name for an accurate explanation.'
