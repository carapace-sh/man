# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-installed
description: list already-installed verbs
documentation:
    command: '[AI] `winetricks list-installed` is a command used in Wine, a compatibility layer for running Windows applications on Linux and other operating systems. This specific command lists all the installed packages or software components managed by Winetricks. When executed, it displays a table with columns showing the package name, version number, and installation date. It helps users keep track of what they have installed using Winetricks within their Wine prefix.'
