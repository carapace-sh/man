name: delete-user-profile
description: Deletes a user profile.
flags:
    --domain-id=!: The domain ID.
    --user-profile-name=!: The user profile name.
documentation:
    command: Deletes a user profile. When a user profile is deleted, the user loses access to their EFS volume, including data, notebooks, and other artifacts.
    flag:
        domain-id: The domain ID.
        user-profile-name: The user profile name.
