name: create
description: '*(BETA)*  Create a certificate map.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --description=: Text description of a certificate map.
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: The Cloud location for the certificate map.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --tags=&: List of tags KEY=VALUE pairs to bind.
documentation:
    command: |-
        *(BETA)*  This command creates a certificate map.

        ## EXAMPLES

        To create a certificate map with name simple-map, run:

          $ gcloud beta certificate-manager maps create simple-map
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        description: Text description of a certificate map.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            The Cloud location for the certificate map.
            +
            To set the `location` attribute:
            * provide the argument `map` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * default value of location is [global].
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        tags: |
            List of tags KEY=VALUE pairs to bind.
            Each item must be expressed as
            `<tag-key-namespaced-name>=<tag-value-short-name>`.

            Example: `123/environment=production,123/costCenter=marketing`
