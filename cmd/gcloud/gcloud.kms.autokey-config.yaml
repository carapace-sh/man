name: autokey-config
description: Update and retrieve the AutokeyConfig.
documentation:
    command: |-
        Autokey config is a folder level resource which configures the key project
        for the folder and the resources inside it.
