name: describe
description: '*(ALPHA)*  Describe a Mirroring Endpoint Group Association.'
flags:
    --location=: Location of the mirroring endpoint group association.
documentation:
    command: |+
        *(ALPHA)*  Describe a mirroring endpoint group association.

        For more examples, refer to the EXAMPLES section below.

    flag:
        location: |-
            Location of the mirroring endpoint group association.
            +
            To set the `location` attribute:
            * provide the argument `MIRRORING_ENDPOINT_GROUP_ASSOCIATION` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
