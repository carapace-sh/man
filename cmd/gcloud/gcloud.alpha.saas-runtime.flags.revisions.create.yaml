name: create
description: '*(ALPHA)*  Create Flag Revision.'
hidden: true
flags:
    --annotations=: Annotations is an unstructured key-value map stored with a resource that
    --flag=!: ID of the flag or fully qualified identifier for the flag.
    --labels=: Labels on the resource, which can be used for categorization.
    --location=: For resources [flag, flag_revision], provides fallback value for resource location attribute.
documentation:
    command: '*(ALPHA)*  Create a Flag Revision.'
    flag:
        annotations: |-
            Annotations is an unstructured key-value map stored with a resource that
            may be set by external tools to store and retrieve arbitrary metadata.
            They are not queryable and should be preserved when modifying objects.
            +
            *KEY*:::
            Sets `KEY` value.
            +
            *VALUE*:::
            Sets `VALUE` value.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --annotations=string=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --annotations='{"string": "string"}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --annotations=path_to_file.(yaml|json)
            +
            ```
        flag: |-
            ID of the flag or fully qualified identifier for the flag.
            +
            To set the `flag` attribute:
            * provide the argument `--flag` on the command line.
        labels: |-
            Labels on the resource, which can be used for categorization.
            +
            *KEY*:::
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
            +
            *VALUE*:::
            Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --labels=string=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --labels='{"string": "string"}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --labels=path_to_file.(yaml|json)
            +
            ```
        location: For resources [flag, flag_revision], provides fallback value for resource location attribute. When the resource's full URI path is not provided, location will fallback to this flag value.
