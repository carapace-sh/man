name: remove-mapping
description: '*(ALPHA)*  Remove vlan to ip mapping rule to an L2-forwarding attachment.'
flags:
    --region=: Region of the interconnect attachment to create.
    --vlan-key=!: Desired VLAN key for L2 forwarding mapping for the attachment.
documentation:
    command: '*(ALPHA)*  *gcloud alpha compute interconnects attachments l2-forwarding remove-mapping* remove vlan to ip mapping rule to an L2-forwarding attachment.'
    flag:
        region: |-
            Region of the interconnect attachment to create. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/region'' property:
            +
              $ gcloud config set compute/region REGION
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/region
            +
            Alternatively, the region can be stored in the environment
            variable ``CLOUDSDK_COMPUTE_REGION''.
        vlan-key: |-
            Desired VLAN key for L2 forwarding mapping for the attachment. If not
            supplied, all mappings will be displayed.
