name: delete
description: Delete a Compute Engine packet mirroring policy.
flags:
    --region=: Region of the packet mirrorings to delete.
documentation:
    command: Delete a Compute Engine Packet Mirroring policy.
    flag:
        region: Region of the packet mirrorings to delete. Overrides the default *compute/region* property value for this command invocation.
