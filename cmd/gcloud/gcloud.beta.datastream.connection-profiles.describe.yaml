name: describe
description: '*(BETA)*  *(DEPRECATED)*  Show details about a Datastream connection profile.'
flags:
    --location=: The location of the resources.
documentation:
    command: |4-
        *(BETA)*  *(DEPRECATED)*  This command has been deprecated.
        Use `gcloud datastream connection_profiles describe` instead.


        Show details about a connection profile.
    flag:
        location: |-
            The location of the resources.
            +
            To set the `location` attribute:
            * provide the argument `connection_profile` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
