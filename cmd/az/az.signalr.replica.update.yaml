# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: Update a replica of SignalR Service.
flags:
    --add=:
        description: Add an object to a list of objects by specifying a path and key value pairs.
        nargs: -1
    --force-string=: When using 'set' or 'add', preserve string literals instead of attempting to convert to JSON.
    --region-endpoint-enabled=:
        description: Enable or disable region endpoint for a SignalR Service
        nargs: -1
    --remove=:
        description: Remove a property or an element from a list.
        nargs: -1
    --replica-name=!: Name of the replica.
    --set=:
        description: Update an object by specifying a property path and value to set.
        nargs: -1
    --signalr-name=!: Name of the SignalR.
    --subscription=: Name or ID of subscription.
    --unit-count=: The number of signalr service unit count
    -g, --resource-group=!: Name of resource group.
completion:
    flag:
        region-endpoint-enabled:
            - "true"
            - "false"
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        add: |-
            Add an object to a list of objects by specifying a path and key value pairs.  Example: ``` --add property.listProperty <key=value, string or JSON string> ``` .

            Default value: []
        change-reference: The related change reference ID for this resource operation.
        force-string: |-
            When using 'set' or 'add', preserve string literals instead of attempting to convert to JSON.

            Default value: False
        region-endpoint-enabled: |-
            Enable or disable region endpoint for a SignalR Service.

            Accepted values: false, true
        remove: |-
            Remove a property or an element from a list.  Example: ``` --remove property.list <indexToRemove> ``` OR ``` --remove propertyToRemove ``` .

            Default value: []
        replica-name: Name of the replica.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        set: |-
            Update an object by specifying a property path and value to set.  Example: ``` --set property1.property2=<value> ``` .

            Default value: []
        signalr-name: Name of the SignalR.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        unit-count: The number of signalr service unit count.
examples:
    Enable or disable region endpoint for the replica of SignalR Service: az signalr replica update --replica-name MyReplica --signalr-name MySignalR -g MyResourceGroup --region-endpoint-enabled true
    Update the unit count of the replica of SignalR Service: az signalr replica update --replica-name MyReplica --signalr-name MySignalR -g MyResourceGroup --unit-count 2
