name: delete
description: '*(ALPHA)*  Delete Compute Engine firewall rules.'
documentation:
    command: |-
        *(ALPHA)*  *gcloud alpha compute firewall-rules delete* deletes one or more Compute Engine firewall
        rules.

        ## EXAMPLES

        To delete a firewall rule, run:

          $ gcloud alpha compute firewall-rules delete my-firewall-rule
