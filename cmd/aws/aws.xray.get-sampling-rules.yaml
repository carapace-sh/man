name: get-sampling-rules
description: Retrieves all sampling rules.
flags:
    --next-token=: Pagination token.
documentation:
    command: Retrieves all sampling rules.
    flag:
        next-token: Pagination token.
