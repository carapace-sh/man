name: export-rules
description: '*(BETA)*  Export Compute Engine organization firewall policy rules.'
flags:
    --destination=: Path to a YAML file where the configuration will be exported.
    --organization=: Organization in which the organization firewall policy rules export from.
documentation:
    command: |
        *(BETA)*  Exports Firewall Policy rules configuration to a file.
    flag:
        destination: |-
            Path to a YAML file where the configuration will be exported.
                      Alternatively, you may omit this flag to write to standard output. For a schema describing the export/import format, see:
                      $CLOUDSDKROOT/lib/googlecloudsdk/schemas/compute/beta/FirewallPolicy.yaml.
        organization: Organization in which the organization firewall policy rules export from. Must be set if FIREWALL_POLICY is short name.
