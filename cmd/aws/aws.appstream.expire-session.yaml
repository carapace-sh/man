name: expire-session
description: Immediately stops the specified streaming session.
flags:
    --session-id=!: The identifier of the streaming session.
documentation:
    command: Immediately stops the specified streaming session.
    flag:
        session-id: The identifier of the streaming session.
