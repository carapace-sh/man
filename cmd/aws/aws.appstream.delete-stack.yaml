name: delete-stack
description: Deletes the specified stack.
flags:
    --name=!: The name of the stack.
documentation:
    command: Deletes the specified stack. After the stack is deleted, the application streaming environment provided by the stack is no longer available to users. Also, any reservations made for application streaming sessions for the stack are released.
    flag:
        name: The name of the stack.
