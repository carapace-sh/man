# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Get an individual provider's operations.
flags:
    --subscription=: Name or ID of subscription.
    -n, --namespace=!: the resource namespace, aka 'provider'
documentation:
    flag:
        namespace: The resource namespace, aka 'provider'.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Get an individual provider's operations. (autogenerated): az provider operation show --namespace Microsoft.Storage
