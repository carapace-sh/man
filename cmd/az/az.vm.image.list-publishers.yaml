# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-publishers
description: List the VM image publishers available in the Azure Marketplace.
flags:
    --edge-zone=: The name of edge zone.
    --subscription=: Name or ID of subscription.
    -l, --location=!: Location.
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        edge-zone: The name of edge zone.
        location: 'Location. Values from: ``` az account list-locations ``` . You can configure the default location using ``` az configure --defaults location=<location> ``` .'
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List all publishers in the West US region.: az vm image list-publishers -l westus
    List all publishers with names starting with "Open" in westus.: az vm image list-publishers -l westus --query "[?starts_with(name, 'Open')]"
