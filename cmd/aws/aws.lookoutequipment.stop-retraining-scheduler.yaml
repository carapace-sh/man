name: stop-retraining-scheduler
description: Stops a retraining scheduler.
flags:
    --model-name=!: The name of the model whose retraining scheduler you want to stop.
documentation:
    command: Stops a retraining scheduler.
    flag:
        model-name: The name of the model whose retraining scheduler you want to stop.
