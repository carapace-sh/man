# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: kube
description: Run Kube benchmark tests
hidden: true
flags:
    --kube-namespace=: 'Selects the '
documentation:
    command: '[AI] The `tsh bench kube` command is a part of the TelescopeSH (Tsh) tool, which is a CLI (Command Line Interface) tool for Kubernetes management and observation. This specific command is used to run benchmarks against a given Kubernetes cluster using TelescopeSH. It provides metrics related to the performance and health of various components within the cluster such as API server, etcd, kubelet, and more. The result includes statistics on latency, throughput, and other relevant aspects for capacity planning and troubleshooting purposes.'
    flag:
        kube-namespace: '[AI] `tsh bench kube` is a command from the Temporal Systems'' Toolkit for Kubernetes (TSH), which is used to run performance tests on Kubernetes clusters. The specific command `tsh bench kube --kube-namespace` is used to run these tests within a specific namespace in your Kubernetes cluster. The `--kube-namespace` flag specifies the name of the namespace where the performance test workloads and resources will be deployed.'
