# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-shared
description: List all shared galleries shared directly to your subscription or tenant
flags:
    --max-items=: Total number of items to return in the command's output.
    --next-token=: Token to specify where to start paginating.
    --shared-to=: The query parameter to decide what shared galleries to fetch when doing listing operations.
    --subscription=: Name or ID of subscription.
    -l, --location=!: Location.
completion:
    flag:
        shared-to:
            - tenant
documentation:
    command: List all shared galleries shared directly to your subscription or tenant.
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        location: 'Location. Values from: ``` az account list-locations ``` . You can configure the default location using ``` az configure --defaults location=<location> ``` .'
        max-items: Total number of items to return in the command's output. If the total number of items available is more than the value specified, a token is provided in the command's output. To resume pagination, provide the token value in `--next-token` argument of a subsequent command.
        next-token: Token to specify where to start paginating. This is the token value from a previously truncated response.
        shared-to: |-
            The query parameter to decide what shared galleries to fetch when doing listing operations. If not specified, list by subscription id.

            Accepted values: tenant
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List shared galleries shared directly to your subscription in a given location: az sig list-shared --location myLocation
    List shared galleries shared directly to your tenant in a given location: az sig list-shared --location myLocation --shared-to tenant
