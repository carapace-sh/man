name: stop-bgp-failover-test
description: Stops the virtual interface failover test.
flags:
    --virtual-interface-id=!: The ID of the virtual interface you no longer want to test.
documentation:
    command: Stops the virtual interface failover test.
    flag:
        virtual-interface-id: The ID of the virtual interface you no longer want to test.
