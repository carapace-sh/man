name: create
description: '*(BETA)*  Create the IAP TCP Destination Group resource.'
flags:
    --fqdn-list=: List of FQDNs in the Destination Group.
    --ip-range-list=: List of ip-ranges in the Destination Group.
    --region=!: Region of the Destination Group.
documentation:
    command: '*(BETA)*  Create the IAP TCP Destination Group resource.'
    flag:
        fqdn-list: List of FQDNs in the Destination Group.
        ip-range-list: List of ip-ranges in the Destination Group.
        region: Region of the Destination Group.
