# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: assign
description: Enable managed service identity on a VM.
flags:
    --identities=:
        description: Space-separated identities to assign.
        nargs: -1
    --ids=:
        description: One or more resource IDs (space-delimited).
        nargs: -1
    --role=: Role name or id the system assigned identity will have.
    --scope=: Scope that the system assigned identity can access.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --name=: The name of the Virtual Machine.
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        identities: 'Space-separated identities to assign. Use ''[system]'' to refer to the system assigned identity. Default: ''[system]''.'
        ids: One or more resource IDs (space-delimited). It should be a complete resource ID containing all information of 'Resource Id' arguments. You should provide either --ids or other 'Resource Id' arguments.
        name: The name of the Virtual Machine. You can configure the default using ``` az configure --defaults vm=<name> ``` .
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        role: Role name or id the system assigned identity will have.
        scope: 'Scope that the system assigned identity can access. '
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Enable the system assigned identity and a user assigned identity on a VM with the 'Reader' role.: az vm identity assign -g MyResourceGroup -n MyVm --role Reader --identities [system] myAssignedId --scope /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/MyResourceGroup
    Enable the system assigned identity on a VM with the 'Reader' role.: az vm identity assign -g MyResourceGroup -n MyVm --role Reader --scope /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/MyResourceGroup
