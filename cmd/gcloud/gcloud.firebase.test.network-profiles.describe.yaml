name: describe
description: Describe a network profile.
documentation:
    command: |
        Describe a network profile.

        Run `$ gcloud firebase test network-profiles --help` for descriptions of the network profile
        parameters.
