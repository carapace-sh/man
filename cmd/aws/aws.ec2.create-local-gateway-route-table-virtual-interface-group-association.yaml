name: create-local-gateway-route-table-virtual-interface-group-association
description: Creates a local gateway route table virtual interface group association.
flags:
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --local-gateway-route-table-id=!: The ID of the local gateway route table.
    --local-gateway-virtual-interface-group-id=!: The ID of the local gateway route table virtual interface group association.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --tag-specifications=: The tags assigned to the local gateway route table virtual interface group association.
documentation:
    command: Creates a local gateway route table virtual interface group association.
    flag:
        dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is `DryRunOperation`. Otherwise, it is `UnauthorizedOperation`.
        local-gateway-route-table-id: The ID of the local gateway route table.
        local-gateway-virtual-interface-group-id: The ID of the local gateway route table virtual interface group association.
        tag-specifications: The tags assigned to the local gateway route table virtual interface group association.
