name: list-stages
description: Gets summary information about all stages in your account, in the AWS region where the API request is processed.
flags:
    --max-results=: Maximum number of results to return.
    --next-token=: The first stage to retrieve.
documentation:
    command: Gets summary information about all stages in your account, in the AWS region where the API request is processed.
    flag:
        max-results: 'Maximum number of results to return. Default: 50.'
        next-token: The first stage to retrieve. This is used for pagination; see the `nextToken` response field.
