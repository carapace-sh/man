# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List managed applications.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: the resource group of the managed application
documentation:
    flag:
        resource-group: The resource group of the managed application.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List managed applications. (autogenerated): az managedapp list --resource-group MyResourceGroup
