# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: Update content-trust policy for an Azure Container Registry.
flags:
    --status=: Indicates whether content-trust is enabled.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -r, --registry=!: The name of the container registry.
completion:
    flag:
        status:
            - enabled
            - disabled
documentation:
    command: This command is implicitly deprecated because command group 'acr config content-trust' is deprecated and will be removed in a future release.
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        registry: The name of the container registry. It should be specified in lower case. You can configure the default registry name using ``` az configure --defaults acr=<registry name> ``` .
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        status: |-
            Indicates whether content-trust is enabled. Only 'disabled' is allowed.

            Accepted values: disabled
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        "yes": |-
            Do not prompt for confirmation.

            Default value: False
examples:
    Update content-trust policy for an Azure Container Registry: az acr config content-trust update -r myregistry --status Enabled
