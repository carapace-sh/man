name: update-resolver-dnssec-config
description: Updates an existing DNSSEC validation configuration.
flags:
    --resource-id=!: The ID of the virtual private cloud (VPC) that you're updating the DNSSEC validation status for.
    --validation=!: The new value that you are specifying for DNSSEC validation for the VPC.
completion:
    flag:
        validation:
            - ENABLE
            - DISABLE
            - USE_LOCAL_RESOURCE_SETTING
documentation:
    command: Updates an existing DNSSEC validation configuration. If there is no existing DNSSEC validation configuration, one is created.
    flag:
        resource-id: The ID of the virtual private cloud (VPC) that you're updating the DNSSEC validation status for.
        validation: The new value that you are specifying for DNSSEC validation for the VPC. The value can be `ENABLE` or `DISABLE`. Be aware that it can take time for a validation status change to be completed.
