name: add-enable-rules
description: '*(BETA)*  Add service(s) to a consumer policy for a project, folder or organization.'
hidden: true
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --folder=: The Google Cloud Platform folder ID to use for this invocation.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-validate-only&: Validate the add enable rule action, but don't actually perform it
    --organization=: The Google Cloud Platform organization ID to use for this invocation.
    --policy-name=: Name of the consumer policy.
    --validate-only: Validate the add enable rule action, but don't actually perform it
documentation:
    command: |
        *(BETA)*  Add service(s) to a consumer policy for a project, folder or organization.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        folder: The Google Cloud Platform folder ID to use for this invocation.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-validate-only: Validate the add enable rule action, but don't actually perform it
        organization: The Google Cloud Platform organization ID to use for this invocation.
        policy-name: Name of the consumer policy. Currently only "default" is supported.
        validate-only: Validate the add enable rule action, but don't actually perform it
