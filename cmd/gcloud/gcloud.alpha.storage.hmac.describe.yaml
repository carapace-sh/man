name: describe
description: '*(ALPHA)*  Describes a service account HMAC key.'
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha storage hmac describe* retrieves the specified HMAC key's metadata. Note that there
        is no option to retrieve a key's secret material after it has
        been created.
