name: get-domain
description: Returns information about a specific domain.
flags:
    --domain-name=!: The unique name of the domain.
documentation:
    command: Returns information about a specific domain.
    flag:
        domain-name: The unique name of the domain.
