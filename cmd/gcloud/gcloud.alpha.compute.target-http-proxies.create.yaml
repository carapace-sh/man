name: create
description: '*(ALPHA)*  Create a target HTTP proxy.'
flags:
    --description=: An optional, textual description for the target HTTP proxy.
    --global: If set, the target HTTP proxy is global.
    --global-url-map: If set, the URL map is global.
    --http-keep-alive-timeout-sec=: Represents the maximum amount of time that a TCP connection can be idle
    --no-global&: If set, the target HTTP proxy is global.
    --no-global-url-map&: If set, the URL map is global.
    --no-proxy-bind&: This flag applies when the load_balancing_scheme of the associated
    --proxy-bind: This flag applies when the load_balancing_scheme of the associated
    --region=: Region of the target HTTP proxy to create.
    --url-map-region=: Region of the URL map to operate on.
    --url-map=!: A reference to a URL map resource.
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute target-http-proxies create* is used to create target HTTP proxies. A target
        HTTP proxy is referenced by one or more forwarding rules which
        specify the network traffic that the proxy is responsible for
        routing. The target HTTP proxy points to a URL map that defines
        the rules for routing the requests. The URL map's job is to map
        URLs to backend services which handle the actual requests.
    flag:
        description: An optional, textual description for the target HTTP proxy.
        global: If set, the target HTTP proxy is global.
        global-url-map: If set, the URL map is global.
        http-keep-alive-timeout-sec: |-
            Represents the maximum amount of time that a TCP connection can be idle
            between the (downstream) client and the target HTTP proxy. If an HTTP
            keepalive  timeout is not specified, the default value is 610 seconds.
            For global external Application Load Balancers, the minimum allowed
            value is 5 seconds and the maximum allowed value is 1200 seconds.
        no-global: If set, the target HTTP proxy is global.
        no-global-url-map: If set, the URL map is global.
        no-proxy-bind: |
            This flag applies when the load_balancing_scheme of the associated
            backend service is INTERNAL_SELF_MANAGED. When specified, the envoy binds
            to the forwarding rule's IP address and port. By default,
            this flag is off.
        proxy-bind: |-
            This flag applies when the load_balancing_scheme of the associated
            backend service is INTERNAL_SELF_MANAGED. When specified, the envoy binds
            to the forwarding rule's IP address and port. By default,
            this flag is off.
        region: |-
            Region of the target HTTP proxy to create. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/region'' property:
            +
              $ gcloud config set compute/region REGION
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/region
            +
            Alternatively, the region can be stored in the environment
            variable ``CLOUDSDK_COMPUTE_REGION''.
        url-map: |-
            A reference to a URL map resource. A URL map defines the mapping of URLs
            to backend services. Before you can refer to a URL map, you must
            create the URL map. To delete a URL map that a target proxy is referring
            to, you must first delete the target HTTP proxy.
        url-map-region: Region of the URL map to operate on. Overrides the default *compute/region* property value for this command invocation.
