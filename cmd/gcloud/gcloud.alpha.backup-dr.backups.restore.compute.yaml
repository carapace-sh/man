name: compute
description: '*(ALPHA)*  Restores a Compute Engine VM Backup.'
flags:
    --accelerator=: Attaches accelerators (e.g. GPUs) to the instances.
    --async: Return immediately, without waiting for the operation in progress to
    --backup-vault=: The ID of the Backup Vault.
    --can-ip-forward: If provided, allows the restored instances to send and receive packets
    --confidential-compute: The restored instance boots with Confidential Computing enabled.
    --create-disk=: Creates and attaches persistent disks to the instances.
    --data-source=: The ID of the Data Source.
    --deletion-protection: Enables deletion protection for the restored instance.
    --description=: Specifies a textual description of the restored instance.
    --enable-display-device: Enable a display device on the restored VM instances.
    --enable-uefi-networking: If set to true, enables UEFI networking for the instance creation.
    --hostname=: Specify the hostname of the restore instance to be created.
    --instance-kms-key=: The Cloud KMS (Key Management Service) cryptokey that will be used to
    --instance-termination-action=: Specifies the termination action that will be taken upon VM preemption
    --key-revocation-action-type=: Specifies the behavior of the instance when the KMS key of one of its
    --labels=: List of label KEY=VALUE pairs to add.
    --local-ssd-recovery-timeout=: Specifies the maximum amount of time a Local SSD VM should wait while
    --location=: The location of the Backup.
    --machine-type=: Specifies the machine type used for the restored instance.
    --maintenance-policy=: Specifies the behavior of the VMs when their host machines undergo
    --metadata=: Metadata to be made available to the guest operating system
    --min-cpu-platform=: When specified, the VM will be scheduled on host with specified CPU
    --min-node-cpu=: Minimum number of virtual CPUs this instance will consume when running on
    --name=!: Name of the restored Compute Instance.
    --network-interface=: Adds a network interface to the instance.
    --network-performance-configs=: Configures network performance settings for the restored instance.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-can-ip-forward&: If provided, allows the restored instances to send and receive packets
    --no-confidential-compute&: The restored instance boots with Confidential Computing enabled.
    --no-deletion-protection&: Enables deletion protection for the restored instance.
    --no-enable-display-device&: Enable a display device on the restored VM instances.
    --no-enable-uefi-networking&: If set to true, enables UEFI networking for the instance creation.
    --no-preemptible&: If provided, instances will be preemptible and time-limited.
    --no-restart-on-failure&: The instances will be restarted if they are terminated by Compute Engine.
    --no-scopes: Create the instance with no scopes.
    --node-affinity-file=&: The JSON/YAML file containing the configuration of desired nodes onto
    --preemptible: If provided, instances will be preemptible and time-limited.
    --private-ipv6-google-access-type=: The private IPv6 Google access type for the restored VM.
    --provisioning-model=: Specifies provisioning model, which determines price, obtainability,
    --reservation-affinity=: Specifies the reservation affinity of the instance.
    --reservation=: The name of the reservation, required when `--reservation-affinity=specific`.
    --resource-manager-tags=: Specifies a list of resource manager tags to apply to the instance.
    --resource-policies=: A list of resource policy names to be added to the instance.
    --restart-on-failure: The instances will be restarted if they are terminated by Compute Engine.
    --scopes=: If not provided, the instance will be assigned the default scopes, described below.
    --service-account=: A service account is an identity attached to the instance.
    --tags=: Specifies a list of tags to apply to the instance.
    --target-project=!: Project where the restore should happen.
    --target-zone=!: Zone where the target instance is restored.
    --threads-per-core=: The number of visible threads per physical core.
    --visible-core-count=: The number of physical cores to expose to the instance's guest operating
completion:
    flag:
        instance-termination-action:
            - DELETE
            - STOP
        maintenance-policy:
            - MIGRATE
            - TERMINATE
        provisioning-model:
            - SPOT
            - STANDARD
        reservation-affinity:
            - any
            - none
            - specific
documentation:
    command: '*(ALPHA)*  Restores a Compute Engine VM Backup.'
    flag:
        accelerator: |-
            Attaches accelerators (e.g. GPUs) to the instances.
            +
            *type*::: The specific type (e.g. nvidia-tesla-k80 for nVidia Tesla K80)
            of accelerator to attach to the instances. Use 'gcloud compute
            accelerator-types list' to learn about all available accelerator types.
            +
            *count*::: Number of accelerators to attach to each
            instance. The default value is 1.
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete. The default is *True*. Enabled by default, use *--no-async* to disable.
        backup-vault: |-
            The ID of the Backup Vault.
            +
            To set the `backup-vault` attribute:
            * provide the argument `backup` on the command line with a fully specified name;
            * provide the argument `--backup-vault` on the command line.
        can-ip-forward: |-
            If provided, allows the restored instances to send and receive packets
            with non-matching destination or source IP addresses. Use *--can-ip-forward* to enable and *--no-can-ip-forward* to disable.
        confidential-compute: |-
            The restored instance boots with Confidential Computing enabled.
            Confidential Computing is based on Secure Encrypted Virtualization (SEV),
            an AMD virtualization feature for running confidential instances.
        create-disk: |-
            Creates and attaches persistent disks to the instances.
            +
            *name*: Specifies the name of the disk.
            +
            *replica-zones*: Required for each regional disk associated with the
            instance. Specify the URLs of the zones where the disk should be
            replicated to. You must provide exactly two replica zones, and one
            zone must be the same as the instance zone.
            +
            *device-name*: Device name of the disk from the source instance.
        data-source: |-
            The ID of the Data Source.
            +
            To set the `data-source` attribute:
            * provide the argument `backup` on the command line with a fully specified name;
            * provide the argument `--data-source` on the command line.
        deletion-protection: Enables deletion protection for the restored instance. Use *--deletion-protection* to enable and *--no-deletion-protection* to disable.
        description: Specifies a textual description of the restored instance.
        enable-display-device: Enable a display device on the restored VM instances. Disabled by default. Use *--enable-display-device* to enable and *--no-enable-display-device* to disable.
        enable-uefi-networking: If set to true, enables UEFI networking for the instance creation. Use *--enable-uefi-networking* to enable and *--no-enable-uefi-networking* to disable.
        hostname: |-
            Specify the hostname of the restore instance to be created. The specified
            hostname must be RFC1035 compliant. If hostname is not specified, the
            default hostname is [INSTANCE_NAME].c.[TARGET_PROJECT_ID].internal when using the
            global DNS, and [INSTANCE_NAME].[ZONE].c.[TARGET_PROJECT_ID].internal when using
            zonal DNS.
        instance-kms-key: |-
            The Cloud KMS (Key Management Service) cryptokey that will be used to
            protect the restored instance.
            +
            Provide the full resource name of the cryptokey in the format:
            `projects/<project>/locations/<location>/keyRings/<key-ring>/cryptoKeys/<key>`
        instance-termination-action: |-
            Specifies the termination action that will be taken upon VM preemption
              (--provisioning-model=SPOT) or automatic instance
              termination (--max-run-duration or --termination-time).
            +
            _INSTANCE_TERMINATION_ACTION_ must be one of:
            +
            *DELETE*::: Permanently delete the VM.
            *STOP*::: Default only for Spot VMs. Stop the VM without preserving memory. The VM can be restarted later.
            :::
            +
        key-revocation-action-type: |-
            Specifies the behavior of the instance when the KMS key of one of its
            attached disks is revoked. The default is none. POLICY must be one of:
            * none
            No operation is performed.
            * stop
            The instance is stopped when the KMS key of one of its attached disks is
            revoked.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (-),
            underscores (_), lowercase characters, and numbers. Values must contain
            only hyphens (-), underscores (_), lowercase characters, and numbers.
        local-ssd-recovery-timeout: |-
            Specifies the maximum amount of time a Local SSD VM should wait while
            recovery of the Local SSD state is attempted. Its value should be in
            between 0 and 168 hours with hour granularity and the default value being 1
            hour.
        location: |-
            The location of the Backup.
            +
            To set the `location` attribute:
            * provide the argument `backup` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        machine-type: |-
            Specifies the machine type used for the restored instance. To get a list
            of available machine types, run 'gcloud compute machine-types list'. If
            unspecified, the default type will be based on the source instance.
            +
            This can either be the fully qualified path or the name.
            For example:
            * ``--machine-type=projects/my-project/zones/us-central1-a/machineTypes/n1-standard-1''
            * ``--machine-type=n1-standard-1''
        maintenance-policy: |-
            Specifies the behavior of the VMs when their host machines undergo
            maintenance. The default is MIGRATE.
            For more information, see
            https://cloud.google.com/compute/docs/instances/host-maintenance-options. _MAINTENANCE_POLICY_ must be one of:
            +
            *MIGRATE*::: The instances should be migrated to a new host. This will temporarily impact the performance of instances during a migration event.
            *TERMINATE*::: The instances should be terminated.
            :::
            +
        metadata: |-
            Metadata to be made available to the guest operating system
            running on the instances. Each metadata entry is a key/value
            pair separated by an equals sign. Each metadata key must be unique
            and have a max of 128 bytes in length. Each value must have a max of
            256 KB in length. Multiple arguments can be
            passed to this flag, e.g.,
            ``--metadata key-1=value-1,key-2=value-2,key-3=value-3''.
            The combined total size for all metadata entries is 512 KB.
            +
            In images that have Compute Engine tools installed on them,
            such as the
            link:https://cloud.google.com/compute/docs/images[official images],
            the following metadata keys have special meanings:
            +
            *startup-script*::: Specifies a script that will be executed
            by the instances once they start running.
            +
            *startup-script-url*::: Same as ``startup-script'' except that
            the script contents are pulled from a publicly-accessible
            location on the web.
            +

            For startup scripts on Windows instances, the following metadata keys
            have special meanings:
            ``windows-startup-script-url'',
            ``windows-startup-script-cmd'', ``windows-startup-script-bat'',
            ``windows-startup-script-ps1'', ``sysprep-specialize-script-url'',
            ``sysprep-specialize-script-cmd'', ``sysprep-specialize-script-bat'',
            and ``sysprep-specialize-script-ps1''. For more information, see
            [Running startup scripts](https://cloud.google.com/compute/docs/startupscript).
        min-cpu-platform: |-
            When specified, the VM will be scheduled on host with specified CPU
            architecture or a newer one. To list available CPU platforms in given
            zone, run:
            +
                $ gcloud compute zones describe ZONE --format="value(availableCpuPlatforms)"
            +
            Default setting is "AUTOMATIC".
            +
            CPU platform selection is available only in selected zones.
            +
            You can find more information on-line:
            [](https://cloud.google.com/compute/docs/instances/specify-min-cpu-platform)
        min-node-cpu: |-
            Minimum number of virtual CPUs this instance will consume when running on
            a sole-tenant node.
        name: Name of the restored Compute Instance.
        network-interface: 'Adds a network interface to the instance. This flag can be repeated to specify multiple network interfaces. The following keys are allowed: network, subnet, private-network-ip, internal-ipv6-address, internal-ipv6-prefix-length, address, external-ipv6-address, external-ipv6-prefix-length, network-tier, aliases, stack-type, queue-count, nic-type, network-attachment'
        network-performance-configs: |-
            Configures network performance settings for the restored instance.
            If this flag is not specified, the restored instance will be created
            with its source instance's network performance configuration.
            +
            *total-egress-bandwidth-tier*::: Total egress bandwidth is the available
            outbound bandwidth from a VM, regardless of whether the traffic
            is going to internal IP or external IP destinations.
            The following tier values are allowed: [DEFAULT, TIER_1]
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-can-ip-forward: |
            If provided, allows the restored instances to send and receive packets
            with non-matching destination or source IP addresses.
        no-confidential-compute: |
            The restored instance boots with Confidential Computing enabled.
            Confidential Computing is based on Secure Encrypted Virtualization (SEV),
            an AMD virtualization feature for running confidential instances.
        no-deletion-protection: |
            Enables deletion protection for the restored instance.
        no-enable-display-device: |
            Enable a display device on the restored VM instances. Disabled by default.
        no-enable-uefi-networking: |
            If set to true, enables UEFI networking for the instance creation.
        no-preemptible: |
            If provided, instances will be preemptible and time-limited. Instances
            might be preempted to free up resources for standard VM instances,
            and will only be able to run for a limited amount of time. Preemptible
            instances can not be restarted and will not migrate.
        no-restart-on-failure: |
            The instances will be restarted if they are terminated by Compute Engine.
            This does not affect terminations performed by the user.
        no-scopes: Create the instance with no scopes.
        node-affinity-file: |
            The JSON/YAML file containing the configuration of desired nodes onto
            which this instance could be scheduled. These rules filter the nodes
            according to their node affinity labels. A node's affinity labels come
            from the node template of the group the node is in.

            The file should contain a list of a JSON/YAML objects. For an example,
            see https://cloud.google.com/compute/docs/nodes/provisioning-sole-tenant-vms#configure_node_affinity_labels.
            The following list describes the fields:

            *key*::: Corresponds to the node affinity label keys of
            the Node resource.
            *operator*::: Specifies the node selection type. Must be one of:
              `IN`: Requires Compute Engine to seek for matched nodes.
              `NOT_IN`: Requires Compute Engine to avoid certain nodes.
            *values*::: Optional. A list of values which correspond to the node
            affinity label values of the Node resource.
        preemptible: |-
            If provided, instances will be preemptible and time-limited. Instances
            might be preempted to free up resources for standard VM instances,
            and will only be able to run for a limited amount of time. Preemptible
            instances can not be restarted and will not migrate. Use *--preemptible* to enable and *--no-preemptible* to disable.
        private-ipv6-google-access-type: |-
            The private IPv6 Google access type for the restored VM.
            PRIVATE_IPV6_GOOGLE_ACCESS_TYPE must be one of: inherit-subnetwork, enable-bidirectional-access, enable-outbound-vm-access
        provisioning-model: |-
            Specifies provisioning model, which determines price, obtainability,
            and runtime for the restored VM instance. _PROVISIONING_MODEL_ must be one of:
            +
            *SPOT*::: Spot VMs are spare capacity; Spot VMs are discounted to have much lower prices than standard VMs but have no guaranteed runtime. Spot VMs are the new version of preemptible VM instances, except Spot VMs do not have a 24-hour maximum runtime.
            *STANDARD*::: Default. Standard provisioning model for VM instances, which has user-controlled runtime but no Spot discounts.
            :::
            +
        reservation: The name of the reservation, required when `--reservation-affinity=specific`.
        reservation-affinity: |-
            Specifies the reservation affinity of the instance. _RESERVATION_AFFINITY_ must be one of:
            +
            *any*::: Consume any available, matching reservation.
            *none*::: Do not consume from any reserved capacity.
            *specific*::: Must consume from a specific reservation.
            :::
            +
        resource-manager-tags: Specifies a list of resource manager tags to apply to the instance.
        resource-policies: |-
            A list of resource policy names to be added to the instance.
            The policies must exist in the same region as the instance.
        restart-on-failure: |-
            The instances will be restarted if they are terminated by Compute Engine.
            This does not affect terminations performed by the user. Use *--restart-on-failure* to enable and *--no-restart-on-failure* to disable.
        scopes: 'If not provided, the instance will be assigned the default scopes, described below. However, if neither --scopes nor --no-scopes are specified and the project has no default service account, then the instance will be created with no scopes. Note that the level of access that a service account has is determined by a combination of access scopes and IAM roles so you must configure both access scopes and IAM roles for the service account to work properly. SCOPE can be either the full URI of the scope or an alias. Default scopes are assigned to all instances. Available aliases are: https://cloud.google.com/sdk/gcloud/reference/compute/instances/create#--scopes'
        service-account: A service account is an identity attached to the instance. Its access tokens can be accessed through the instance metadata server and are used to authenticate applications on the instance. The account can be set using an email address corresponding to the required service account. If not provided, the instance will use the project's default service account.
        tags: |-
            Specifies a list of tags to apply to the instance. These tags allow
            network firewall rules and routes to be applied to specified VM instances.
            See gcloud_compute_firewall-rules_create(1) for more details.
        target-project: Project where the restore should happen.
        target-zone: Zone where the target instance is restored.
        threads-per-core: |-
            The number of visible threads per physical core. To disable simultaneous
            multithreading (SMT) set this to 1. Valid values are: 1 or 2.
            +
            For more information about configuring SMT, see:
            https://cloud.google.com/compute/docs/instances/configuring-simultaneous-multithreading.
        visible-core-count: |-
            The number of physical cores to expose to the instance's guest operating
            system. The number of virtual CPUs visible to the instance's guest
            operating system is this number of cores multiplied by the instance's
            count of visible threads per physical core.
