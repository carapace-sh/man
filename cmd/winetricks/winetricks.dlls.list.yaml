# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: list verbs
documentation:
    command: '[AI] `winetricks dlls list` is a command used in Wine, a compatibility layer for running Windows applications on Linux and macOS. This command lists all the dynamic link libraries (DLLs) that Wine has installed or is currently using. It helps users identify which DLLs might be causing conflicts or issues when running specific Windows applications under Wine.'
