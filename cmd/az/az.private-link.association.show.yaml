# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Get a private link association.
flags:
    --subscription=: Name or ID of subscription.
    -m, --management-group-id=!: The management group id.
    -n, --name=!: The name of the private link association
documentation:
    flag:
        management-group-id: The management group id.
        name: The name of the private link association.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Get a single private link association.: az private-link association show --management-group-id TestMG --name testPLA
