name: delete
description: Remove a service account HMAC.
documentation:
    command: |
        *gcloud storage hmac delete* permanently deletes the specified HMAC key. Note that keys
        must be updated to be in the ``INACTIVE'' state before they can be
        deleted.
