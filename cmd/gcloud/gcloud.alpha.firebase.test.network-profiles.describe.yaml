name: describe
description: '*(ALPHA)*  Describe a network profile.'
documentation:
    command: |
        *(ALPHA)*  Describe a network profile.

        Run `$ gcloud alpha firebase test network-profiles --help` for descriptions of the network profile
        parameters.
