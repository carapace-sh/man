# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: get
description: get profile value
documentation:
    command: '[AI] The `npm profile get` command is used in Node.js package management with npm (Node Package Manager) to retrieve the details of an authenticated npm user profile. This includes the username, email address, and other relevant account information. The output is usually displayed in the terminal or command prompt.'
