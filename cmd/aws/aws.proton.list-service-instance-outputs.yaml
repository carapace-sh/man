name: list-service-instance-outputs
description: Get a list service of instance Infrastructure as Code (IaC) outputs.
flags:
    --deployment-id=: The ID of the deployment whose outputs you want.
    --next-token=: A token that indicates the location of the next output in the array of outputs, after the list of outputs that was previously requested.
    --service-instance-name=!: The name of the service instance whose outputs you want.
    --service-name=!: The name of the service that `serviceInstanceName` is associated to.
documentation:
    command: Get a list service of instance Infrastructure as Code (IaC) outputs.
    flag:
        deployment-id: The ID of the deployment whose outputs you want.
        next-token: A token that indicates the location of the next output in the array of outputs, after the list of outputs that was previously requested.
        service-instance-name: The name of the service instance whose outputs you want.
        service-name: The name of the service that `serviceInstanceName` is associated to.
