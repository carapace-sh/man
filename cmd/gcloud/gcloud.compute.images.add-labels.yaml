name: add-labels
description: Add labels to Google Compute Engine images.
flags:
    --labels=!: A list of labels to add.
documentation:
    command: |
        *gcloud compute images add-labels* adds labels to a Google Compute Engine image.
    flag:
        labels: A list of labels to add.
