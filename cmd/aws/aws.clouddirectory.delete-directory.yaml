name: delete-directory
description: Deletes a directory.
flags:
    --directory-arn=!: The ARN of the directory to delete.
documentation:
    command: Deletes a directory. Only disabled directories can be deleted. A deleted directory cannot be undone. Exercise extreme caution when deleting directories.
    flag:
        directory-arn: The ARN of the directory to delete.
