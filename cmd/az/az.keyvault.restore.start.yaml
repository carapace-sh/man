# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: start
description: Restore a full backup of a HSM.
flags:
    --backup-folder=!: Name of the blob container which contains the backup
    --blob-container-name=: Name of Blob Container.
    --hsm-name=: Name of the HSM.
    --id=: Full URI of the HSM.
    --key-name=: Name of a single key in the backup.
    --storage-account-name=: Name of Azure Storage Account.
    --storage-resource-uri=: Azure Blob storage container Uri.
    --subscription=: Name or ID of subscription.
    --use-managed-identity=:
        description: If True, Managed HSM will use the configured user-assigned managed identity to authenticate with Azure Storage.
        nargs: -1
    -t, --storage-container-SAS-token=: The SAS token pointing to an Azure Blob storage container
completion:
    flag:
        use-managed-identity:
            - "true"
            - "false"
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        backup-folder: Name of the blob container which contains the backup.
        blob-container-name: Name of Blob Container.
        change-reference: The related change reference ID for this resource operation.
        hsm-name: Name of the HSM. Can be omitted if --id is specified.
        id: Full URI of the HSM.
        key-name: Name of a single key in the backup. When set, only this key will be restored
        storage-account-name: Name of Azure Storage Account.
        storage-container-SAS-token: The SAS token pointing to an Azure Blob storage container.
        storage-resource-uri: Azure Blob storage container Uri. If specified all other 'Storage Id' arguments should be omitted.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        use-managed-identity: |-
            If True, Managed HSM will use the configured user-assigned managed identity to authenticate with Azure Storage. Otherwise, a ``` sas_token ``` has to be specified.

            Accepted values: false, true
