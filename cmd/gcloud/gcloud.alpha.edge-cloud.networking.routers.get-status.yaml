name: get-status
description: '*(ALPHA)*  Get the status of a specified Distributed Cloud Edge Network router.'
flags:
    --location=: The Cloud location for the router.
    --zone=: The zone of the router.
documentation:
    command: '*(ALPHA)*  Get the status of a specified Distributed Cloud Edge Network router.'
    flag:
        location: |-
            The Cloud location for the router.
            +
            To set the `location` attribute:
            * provide the argument `router` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        zone: |-
            The zone of the router.
            +
            To set the `zone` attribute:
            * provide the argument `router` on the command line with a fully specified name;
            * provide the argument `--zone` on the command line.
