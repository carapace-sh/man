# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: list the virtual network integrations on a functionapp
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: name of the function app.
    -s, --slot=: The name of the slot.
documentation:
    command: List the virtual network integrations on a functionapp.
    flag:
        name: Name of the function app.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        slot: The name of the slot. Default to the productions slot if not specified.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    list the virtual networks integrations on a functionapp: az functionapp vnet-integration list -g MyResourceGroup -n MyFunctionapp -s [slot]
