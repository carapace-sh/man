name: cancel-replay
description: Cancels the specified replay.
flags:
    --replay-name=!: The name of the replay to cancel.
documentation:
    command: Cancels the specified replay.
    flag:
        replay-name: The name of the replay to cancel.
