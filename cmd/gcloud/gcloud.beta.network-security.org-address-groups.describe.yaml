name: describe
description: '*(BETA)*  Describe an address group.'
flags:
    --location=: The location Id.
    --organization=: Organization number.
documentation:
    command: '*(BETA)*  Show details of an address group.'
    flag:
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `address_group` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        organization: |-
            Organization number.
            +

            To set the `organization` attribute:
            * provide the argument `address_group` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line.
