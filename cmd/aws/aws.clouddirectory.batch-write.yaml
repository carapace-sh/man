name: batch-write
description: Performs all the write operations in a batch.
flags:
    --directory-arn=!: The Amazon Resource Name (ARN) that is associated with the [Directory]().
    --operations=!: A list of operations that are part of the batch.
documentation:
    command: Performs all the write operations in a batch. Either all the operations succeed or none.
    flag:
        directory-arn: The Amazon Resource Name (ARN) that is associated with the [Directory](). For more information, see [arns]().
        operations: A list of operations that are part of the batch.
