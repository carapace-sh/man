# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Create a replica of SignalR Service.
flags:
    --replica-name=!: Name of the replica.
    --signalr-name=!: Name of the SignalR.
    --sku=!: The sku name of the replica.
    --subscription=: Name or ID of subscription.
    --tags=:
        description: 'space-separated tags: key[=value] [key[=value] ...].'
        nargs: -1
    --unit-count=: The number of signalr service unit count
    -g, --resource-group=!: Name of resource group.
    -l, --location=: Location.
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        location: 'Location. Values from: ``` az account list-locations ``` . You can configure the default location using ``` az configure --defaults location=<location> ``` .'
        replica-name: Name of the replica.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        signalr-name: Name of the SignalR.
        sku: 'The sku name of the replica. Currently allowed values: Premium_P1.'
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        tags: 'space-separated tags: key[=value] [key[=value] ...]. Use '''' to clear existing tags.'
        unit-count: |-
            The number of signalr service unit count.

            Default value: 1
examples:
    Get the detail of a replica: az signalr replica create --sku Premium_P1 -l eastus --replica-name MyReplica --signalr-name MySignalR -g MyResourceGroup
