name: get-schema-mapping
description: Returns the SchemaMapping of a given name.
flags:
    --schema-name=!: The name of the schema to be retrieved.
documentation:
    command: Returns the SchemaMapping of a given name.
    flag:
        schema-name: The name of the schema to be retrieved.
