# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Creates or Updates a Api Version Set.
flags:
    --description=: Description of API Version Set.
    --display-name=!: Required.
    --if-match=: ETag of the Entity.
    --subscription=: Name or ID of subscription.
    --version-header-name=: Name of HTTP header parameter that indicates the API Version if versioningScheme is set to `header`.
    --version-query-name=: Name of query parameter that indicates the API Version if versioningScheme is set to `query`.
    --version-set-id=: A resource identifier for the related ApiVersionSet.
    --versioning-scheme=!: Required.
    -g, --resource-group=!: Name of resource group.
    -n, --service-name=!: The name of the API Management service instance.
documentation:
    command: Creates a Api Version Set.
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        description: Description of API Version Set.
        display-name: Required. Name of API Version Set.
        if-match: ETag of the Entity.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        service-name: The name of the API Management service instance.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        version-header-name: Name of HTTP header parameter that indicates the API Version if versioningScheme is set to ``` header ``` .
        version-query-name: Name of query parameter that indicates the API Version if versioningScheme is set to ``` query ``` .
        version-set-id: A resource identifier for the related ApiVersionSet.
        versioning-scheme: 'Required. An value that determines where the API Version identifer will be located in a HTTP request. Possible values include: ''Segment'', ''Query'', ''Header''.'
examples:
    Creates a Api Version Set with version schema as header.: az apim api versionset create --resource-group MyResourceGroup --service-name MyServiceName --version-set-id MyVersionSetId --display-name MyDisplayName --versioning-scheme "Header" --description MyDescription --version-header-name MyHeaderName
    Creates a Api Version Set with version schema as query.: az apim api versionset create --resource-group MyResourceGroup --service-name MyServiceName --version-set-id MyVersionSetId --display-name MyDisplayName --versioning-scheme "Query" --description MyDescription --version-query-name MyQueryName
