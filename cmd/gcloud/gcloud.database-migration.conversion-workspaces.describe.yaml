name: describe
description: Show details about a database migration conversion workspace.
flags:
    --region=: The name of the region.
documentation:
    command: Show details about a conversion workspace.
    flag:
        region: |-
            The name of the region.
            +
            To set the `region` attribute:
            * provide the argument `conversion_workspace` on the command line with a fully specified name;
            * provide the argument `--region` on the command line.
