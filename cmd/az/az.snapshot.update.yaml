# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: Update a snapshot.
flags:
    --accelerated-network=:
        description: Customers can set on Managed Disks or Snapshots to enable the accelerated networking if the OS disk image support.
        nargs: -1
    --add=:
        description: Add an object to a list of objects by specifying a path and key value pairs.
        nargs: -1
    --architecture=: CPU architecture.
    --disk-access=: Name or ID of the disk access resource for using private endpoints on disks.
    --disk-encryption-set=: Name or ID of disk encryption set that is used to encrypt the disk.
    --encryption-type=: Encryption type.
    --force-string=:
        description: When using 'set' or 'add', preserve string literals instead of attempting to convert to JSON.
        nargs: -1
    --ids=:
        description: One or more resource IDs (space-delimited).
        nargs: -1
    --network-access-policy=: Policy for accessing the disk via network.
    --no-wait=:
        description: Do not wait for the long-running operation to finish.
        nargs: -1
    --public-network-access=: Policy for controlling export on the disk.
    --remove=:
        description: Remove a property or an element from a list.
        nargs: -1
    --set=:
        description: Update an object by specifying a property path and value to set.
        nargs: -1
    --sku=: The sku name.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --name=: The name of the snapshot that is being created.
completion:
    flag:
        accelerated-network:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        architecture:
            - Arm64
            - x64
        encryption-type:
            - EncryptionAtRestWithCustomerKey
            - EncryptionAtRestWithPlatformAndCustomerKeys
            - EncryptionAtRestWithPlatformKey
        force-string:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        network-access-policy:
            - AllowAll
            - AllowPrivate
            - DenyAll
        no-wait:
            - "true"
            - t
            - "yes"
            - "y"
            - "1"
            - "false"
            - f
            - "no"
            - "n"
            - "0"
        public-network-access:
            - Disabled
            - Enabled
        sku:
            - Premium_LRS
            - Standard_LRS
            - Standard_ZRS
documentation:
    flag:
        accelerated-network: |-
            Customers can set on Managed Disks or Snapshots to enable the accelerated networking if the OS disk image support.

            Accepted values: 0, 1, f, false, n, no, t, true, y, yes
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        add: 'Add an object to a list of objects by specifying a path and key value pairs.  Example: ``` --add property.listProperty <key=value, string or JSON string> ``` .'
        architecture: |-
            CPU architecture.

            Accepted values: Arm64, x64
        change-reference: The related change reference ID for this resource operation.
        disk-access: Name or ID of the disk access resource for using private endpoints on disks.
        disk-encryption-set: Name or ID of disk encryption set that is used to encrypt the disk.
        encryption-type: |-
            Encryption type.

            Accepted values: EncryptionAtRestWithCustomerKey, EncryptionAtRestWithPlatformAndCustomerKeys, EncryptionAtRestWithPlatformKey
        force-string: |-
            When using 'set' or 'add', preserve string literals instead of attempting to convert to JSON.

            Accepted values: 0, 1, f, false, n, no, t, true, y, yes
        ids: One or more resource IDs (space-delimited). It should be a complete resource ID containing all information of 'Resource Id' arguments. You should provide either --ids or other 'Resource Id' arguments.
        name: The name of the snapshot that is being created. The name can't be changed after the snapshot is created. Supported characters for the name are a-z, A-Z, 0-9, _ and -. The max name length is 80 characters.
        network-access-policy: |-
            Policy for accessing the disk via network.

            Accepted values: AllowAll, AllowPrivate, DenyAll
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Accepted values: 0, 1, f, false, n, no, t, true, y, yes
        public-network-access: |-
            Policy for controlling export on the disk.

            Accepted values: Disabled, Enabled
        remove: 'Remove a property or an element from a list.  Example: ``` --remove property.list <indexToRemove> ``` OR ``` --remove propertyToRemove ``` .'
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        set: 'Update an object by specifying a property path and value to set.  Example: ``` --set property1.property2=<value> ``` .'
        sku: |-
            The sku name.

            Accepted values: Premium_LRS, Standard_LRS, Standard_ZRS
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Update a snapshot and associate it with a disk access resource.: az snapshot update --name MySnapshot --resource-group MyResourceGroup --network-access-policy AllowPrivate --disk-access MyDiskAccessID
    Update a snapshot.: az snapshot update --name MySnapshot --resource-group MyResourceGroup --subscription MySubscription
