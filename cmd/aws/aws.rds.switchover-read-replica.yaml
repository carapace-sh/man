name: switchover-read-replica
description: Switches over an Oracle standby database in an Oracle Data Guard environment, making it the new primary database.
flags:
    --dbinstance-identifier=!: The DB instance identifier of the current standby database.
documentation:
    command: Switches over an Oracle standby database in an Oracle Data Guard environment, making it the new primary database. Issue this command in the Region that hosts the current standby database.
    flag:
        dbinstance-identifier: |-
            The DB instance identifier of the current standby database. This value is stored as a lowercase string.

            Constraints:

            - Must match the identiﬁer of an existing Oracle read replica DB instance.
