name: delete
description: '*(BETA)*  Delete Compute Engine firewall rules.'
documentation:
    command: |-
        *(BETA)*  *gcloud beta compute firewall-rules delete* deletes one or more Compute Engine firewall
        rules.

        ## EXAMPLES

        To delete a firewall rule, run:

          $ gcloud beta compute firewall-rules delete my-firewall-rule
