name: delete-service-sync-config
description: Delete the Proton Ops file.
flags:
    --service-name=!: The name of the service that you want to delete the service sync configuration for.
documentation:
    command: Delete the Proton Ops file.
    flag:
        service-name: The name of the service that you want to delete the service sync configuration for.
