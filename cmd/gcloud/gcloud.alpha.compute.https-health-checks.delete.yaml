name: delete
description: '*(ALPHA)*  Delete HTTPS health checks.'
documentation:
    command: |-
        *(ALPHA)*  *gcloud alpha compute https-health-checks delete* deletes one or more Compute Engine
        HTTPS health checks.
