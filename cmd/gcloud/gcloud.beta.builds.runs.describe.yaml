name: describe
description: '*(BETA)*  Get a PipelineRun/TaskRun.'
hidden: true
flags:
    --region=: ID of the region or fully qualified identifier for the region.
    --type=: Type of Run.
completion:
    flag:
        type:
            - pipelinerun
            - taskrun
documentation:
    command: '*(BETA)*  Get a PipelineRun/TaskRun.'
    flag:
        region: |-
            ID of the region or fully qualified identifier for the region.
            +
            To set the `region` attribute:
            * provide the argument `--region` on the command line;
            * set the property `builds/region`.
        type: 'Type of Run. _TYPE_ must be one of: *pipelinerun*, *taskrun*.'
