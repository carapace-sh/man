name: describe
description: '*(ALPHA)*  Describe a multicast domain group.'
flags:
    --location=: The location Id.
documentation:
    command: |
        *(ALPHA)*  Show details of a multicast domain group in the specified location of the current project.
    flag:
        location: |-
            The location Id.
            +
            To set the `location` attribute:
            * provide the argument `multicast_domain_group` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
