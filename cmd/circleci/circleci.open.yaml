# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: open
description: Open the current project in the browser.
documentation:
    command: '[AI] `circleci open` is not a valid command for CircleCI. CircleCI is a continuous integration and delivery platform that provides several commands for interacting with its services, such as `circle init`, `circle build`, `circle artifacts`, etc. The "open" command is not part of the official CircleCI CLI toolset.'
