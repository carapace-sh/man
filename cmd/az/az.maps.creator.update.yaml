# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: Updates the Maps Creator resource.
documentation:
    flag:
        ids: One or more resource IDs (space-delimited). It should be a complete resource ID containing all information of 'Resource Id' arguments. You should provide either --ids or other 'Resource Id' arguments.
        storage-units: The storage units to be allocated. Integer values from 1 to 100, inclusive.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        tags: 'space-separated tags: key[=value] [key[=value] ...]. Use '''' to clear existing tags.'
flags:
    --creator-name=: The name of the Maps Creator instance.
    --ids=:
        description: One or more resource IDs (space-delimited).
        nargs: -1
    --storage-units=: The storage units to be allocated.
    --subscription=: Name or ID of subscription.
    --tags=:
        description: 'space-separated tags: key[=value] [key[=value] ...].'
        nargs: -1
    -g, --resource-group=: Resource group name
    -n, --name=: The name of the maps account
