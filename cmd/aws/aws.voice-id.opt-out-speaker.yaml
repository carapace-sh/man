name: opt-out-speaker
description: Opts out a speaker from Voice ID.
flags:
    --domain-id=!: The identifier of the domain that contains the speaker.
    --speaker-id=!: The identifier of the speaker you want opted-out.
documentation:
    command: Opts out a speaker from Voice ID. A speaker can be opted out regardless of whether or not they already exist in Voice ID. If they don't yet exist, a new speaker is created in an opted out state. If they already exist, their existing status is overridden and they are opted out. Enrollment and evaluation authentication requests are rejected for opted out speakers, and opted out speakers have no voice embeddings stored in Voice ID.
    flag:
        domain-id: The identifier of the domain that contains the speaker.
        speaker-id: The identifier of the speaker you want opted-out.
