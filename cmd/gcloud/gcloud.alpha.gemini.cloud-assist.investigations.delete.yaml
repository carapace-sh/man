name: delete
description: '*(ALPHA)*  Delete an Investigation.'
hidden: true
flags:
    --location=: ID of the location of the Gemini Cloud Assist resource.
documentation:
    command: '*(ALPHA)*  Delete a Gemini Cloud Assist Investigation.'
    flag:
        location: |-
            ID of the location of the Gemini Cloud Assist resource.
            +

            To set the `location` attribute:
            * provide the argument `investigation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * The default is `global`.
