name: delete-classifier
description: Removes a classifier from the Data Catalog.
flags:
    --name=!: Name of the classifier to remove.
documentation:
    command: Removes a classifier from the Data Catalog.
    flag:
        name: Name of the classifier to remove.
