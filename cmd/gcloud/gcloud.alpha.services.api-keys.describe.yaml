name: describe
description: '*(ALPHA)*  Describe an API key''s metadata.'
flags:
    --location=: Location of the key.
documentation:
    command: '*(ALPHA)*  Describe an API key''s metadata.'
    flag:
        location: |-
            Location of the key.
            +
            To set the `location` attribute:
            * provide the argument `key` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * location will default to global.
