name: describe-access-entry
description: Describes an access entry.
flags:
    --cluster-name=!: The name of your cluster.
    --principal-arn=!: The ARN of the IAM principal for the `AccessEntry`.
documentation:
    command: Describes an access entry.
    flag:
        cluster-name: The name of your cluster.
        principal-arn: The ARN of the IAM principal for the `AccessEntry`.
