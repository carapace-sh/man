name: credentials
description: '*(ALPHA)*  Create and manage OAuth client credentials.'
documentation:
    command: |-
        *(ALPHA)*  The gcloud alpha iam oauth-clients credentials group lets you create and manage OAuth client credentials for
        projects on the Google Cloud Platform.
