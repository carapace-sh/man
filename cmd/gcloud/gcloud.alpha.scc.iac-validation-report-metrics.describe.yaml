name: describe
description: '*(ALPHA)*  Describe Cloud Security Command Center (SCC) IaC validation report metrics.'
hidden: true
flags:
    --duration-days=: Duration for which the metrics are to be fetched which must be in integer format.
documentation:
    command: |
        *(ALPHA)*  Describe Cloud Security Command Center (SCC) IaC validation report metrics.
        By default, the latest created revision of the IaC validation report metrics is described.
    flag:
        duration-days: |-
            Duration for which the metrics are to be fetched which must be in integer format.
            Duration must be greater than 0 and less than 30 days.
