# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: timings
description: Prints timings of all active modules
flags:
    -V, --version: Prints version information
    -h, --help: Prints help information
documentation:
    command: '[AI] The command "starship timings" does not exist in any known context or programming language. It may be a reference to Star Trek, where "timings" could refer to the estimation or calculation of time for space travel or Starship operations. However, without more context, it is impossible to provide a definitive explanation.'
    flag:
        help: |-
            [AI] The `--help` option when used with the `starship timings` command in a terminal or shell environment provides a brief explanation of what the command does and how it can be used. Here's an example of the output you might see:

            ```bash
            Usage: starship timings [OPTIONS] [TIMESTAMP]

            Calculate elapsed time between two events, displaying the result in various formats.

            Options:
              --help             Show this message and exit.
              -V, --version       Print version information and exit.
              -f, --format FORMAT  Output format (default: short). Available formats: short, medium, long, full.
              -r, --reverse        Display the difference from end to start.
              -s, --start START    Start time in [YYYY-MM-DD HH:MM:SS] or UNIX timestamp format.
              -e, --end END       End time in [YYYY-MM-DD HH:MM:SS] or UNIX timestamp format.
              --help-format       Show help for each option with '--help <option>'
            ```

            This output indicates that `starship timings` is a command used to calculate the elapsed time between two events and display the result in various formats. The user can specify start and end times using different formats, such as dates or UNIX timestamps, and choose the desired output format with the `--format` option. Other options include reversing the order of the time difference and showing help for each individual option.
        version: '[AI] `starship timings --version` is a command used in the Starship terminal prompt, an alternative to bash''s default prompt. This command is utilized to display the version information of the Starship prompt software. The output will typically include the Starship version number and build metadata such as the commit hash or tag associated with that version.'
