name: survey
description: '*(BETA)*  Invoke a customer satisfaction survey for Google Cloud CLI.'
documentation:
    command: |-
        *(BETA)*  To permanently disable the survey prompt, run:

           $ gcloud config set survey/disable_prompts True

        ## EXAMPLES

        To launch the survey, run:

          $ gcloud beta survey
