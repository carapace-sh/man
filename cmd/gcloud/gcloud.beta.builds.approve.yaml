name: approve
description: '*(BETA)*  Approve a pending build.'
flags:
    --comment=: Optional comment to annotate the build's approval.
    --url=: Optional URL to annotate the build's approval.
documentation:
    command: '*(BETA)*  Approve a pending build.'
    flag:
        comment: Optional comment to annotate the build's approval.
        url: Optional URL to annotate the build's approval.
