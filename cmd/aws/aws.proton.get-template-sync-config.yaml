name: get-template-sync-config
description: Get detail data for a template sync configuration.
flags:
    --template-name=!: The template name.
    --template-type=!: The template type.
completion:
    flag:
        template-type:
            - ENVIRONMENT
            - SERVICE
documentation:
    command: Get detail data for a template sync configuration.
    flag:
        template-name: The template name.
        template-type: The template type.
