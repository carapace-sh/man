name: update-kms-key
description: '*(ALPHA)*  Update the KMS key of a Compute Engine snapshot.'
flags:
    --region=: The region of the snapshot to update.
documentation:
    command: |
        *(ALPHA)*  * gcloud alpha compute snapshots update-kms-key* updates the KMS key of a Compute Engine persistent snapshot.
    flag:
        region: The region of the snapshot to update.
