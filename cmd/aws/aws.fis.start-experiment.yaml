name: start-experiment
description: Starts running an experiment from the specified experiment template.
flags:
    --client-token=!: Unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --experiment-options=: The experiment options for running the experiment.
    --experiment-template-id=!: The ID of the experiment template.
    --tags=: The tags to apply to the experiment.
documentation:
    command: Starts running an experiment from the specified experiment template.
    flag:
        client-token: Unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
        experiment-options: The experiment options for running the experiment.
        experiment-template-id: The ID of the experiment template.
        tags: The tags to apply to the experiment.
