# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: crypt
description: Use Charm encryption.
documentation:
    command: '[AI] "charm crypt" is not a standard Unix or Linux command. It seems to be specific to the Charmshell scripting language used in OpenStack charms. This command could be defined in a custom charm to implement encryption and decryption of data using a specific cryptographic algorithm. The exact implementation and behavior would depend on how it''s defined in the corresponding charm.'
