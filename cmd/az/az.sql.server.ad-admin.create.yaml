# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Sets a server's AD admin.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -i, --object-id=!: The unique ID of the Azure AD administrator.
    -s, --server-name=!: Name of the Azure SQL Server.
    -u, --display-name=!: Display name of the Azure AD administrator user or group.
documentation:
    command: Create a new server Active Directory administrator.
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        display-name: Display name of the Azure AD administrator user or group.
        object-id: The unique ID of the Azure AD administrator.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` . --server --server-name -s Name of the Azure SQL Server. You can configure the default using ``` az configure --defaults sql-server=<name> ``` .
        server-name: Name of the Azure SQL Server. You can configure the default using `az configure --defaults sql-server=<name>`
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
