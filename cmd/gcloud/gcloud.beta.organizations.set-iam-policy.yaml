name: set-iam-policy
description: '*(BETA)*  Set IAM policy for an organization.'
documentation:
    command: |-
        *(BETA)*  Given an organization ID and a file encoded in JSON or YAML that contains the
        IAM policy, this command will set the IAM policy for that organization.
