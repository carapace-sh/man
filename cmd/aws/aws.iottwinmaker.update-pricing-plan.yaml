name: update-pricing-plan
description: Update the pricing plan.
flags:
    --bundle-names=: The bundle names.
    --pricing-mode=!: The pricing mode.
completion:
    flag:
        pricing-mode:
            - BASIC
            - STANDARD
            - TIERED_BUNDLE
documentation:
    command: Update the pricing plan.
    flag:
        bundle-names: The bundle names.
        pricing-mode: The pricing mode.
