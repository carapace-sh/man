name: operations
description: '*(BETA)*  Manage IAM workload identity pool provider long running operations.'
documentation:
    command: |-
        *(BETA)*  Commands for managing IAM workload identity pool provider long running
        operations.
