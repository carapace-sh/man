# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Returns the properties for the specified encryption scope.
flags:
    --account-name=!: The storage account name.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --name=!: The name of the encryption scope within the specified storage account.
documentation:
    command: Show properties for specified encryption scope within storage account.
    flag:
        account-name: The storage account name.
        name: The name of the encryption scope within the specified storage account.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Show properties for specified encryption scope within storage account.: az storage account encryption-scope show --name myencryption --account-name mystorageaccount -g MyResourceGroup
