# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: version
description: Version
flags:
    --debug: Add build information
    --format=: 'The version''s format can be: ''short'', ''json'''
documentation:
    command: '[AI] The `golangci-lint version` command is used to check the current version of GolangCI-Lint, a popular static analyzer tool for Go programming language. By executing this command in your terminal or command prompt, you''ll get output displaying the version number of the installed instance of the tool, ensuring that you have the latest features and bug fixes.'
    flag:
        debug: '[AI] `golangci-lint` is a static analyzer tool for Go programming language. The command `golangci-lint version --debug` is used to display detailed debug information about the installed version of Golangci Lint. This can be helpful in troubleshooting any issues related to the tool''s configuration or usage.'
        format: '[AI] `golangci-lint` is a static code analysis tool for Go languages. The command `golangci-lint version --format` is used to display the version information of `golangci-lint` in a specific format. The `--format` option defines how the output should be formatted, which can be set to "json", "table", or "human". By default, it prints the output as human-readable text.'
