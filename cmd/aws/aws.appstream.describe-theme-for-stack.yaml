name: describe-theme-for-stack
description: Retrieves a list that describes the theme for a specified stack.
flags:
    --stack-name=!: The name of the stack for the theme.
documentation:
    command: Retrieves a list that describes the theme for a specified stack. A theme is custom branding that customizes the appearance of the streaming application catalog page.
    flag:
        stack-name: The name of the stack for the theme.
