name: start-address-list-import-job
description: Starts an import job for an address list.
flags:
    --job-id=!: The identifier of the import job that needs to be started.
documentation:
    command: Starts an import job for an address list.
    flag:
        job-id: The identifier of the import job that needs to be started.
