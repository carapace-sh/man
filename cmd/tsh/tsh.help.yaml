# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: help
description: Show help.
documentation:
    command: '[AI] `tsh help` is a command used in the Tcl Shell (tsh), which is a Tcl interpreter and shell. This command displays a list of available commands and their usage information within the tsh environment. It helps users to quickly find out how to use various built-in tsh commands correctly.'
