name: snapshots
description: Save and load snapshots of environment.
documentation:
    command: |-
        The gcloud composer environments snapshots command group saves and loads snapshots of Cloud Composer
        environments.
