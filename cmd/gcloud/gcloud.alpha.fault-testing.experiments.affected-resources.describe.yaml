name: describe
description: '*(ALPHA)*  Describe an affected resource.'
hidden: true
flags:
    --experiment=: The experiment id of the affectedResource resource.
    --location=: The location id of the affectedResource resource.
documentation:
    command: |
        *(ALPHA)*  Describe an affected resource.
    flag:
        experiment: |-
            The experiment id of the affectedResource resource.
            +
            To set the `experiment` attribute:
            * provide the argument `affected_resource` on the command line with a fully specified name;
            * provide the argument `--experiment` on the command line.
        location: |-
            The location id of the affectedResource resource.
            +
            To set the `location` attribute:
            * provide the argument `affected_resource` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
