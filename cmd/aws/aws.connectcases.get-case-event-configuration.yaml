name: get-case-event-configuration
description: Returns the case event publishing configuration.
flags:
    --domain-id=!: The unique identifier of the Cases domain.
documentation:
    command: Returns the case event publishing configuration.
    flag:
        domain-id: The unique identifier of the Cases domain.
