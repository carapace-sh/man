name: cancel
description: '*(ALPHA)*  Cancel a Filestore operation.'
flags:
    --location=: Location of the Cloud Filestore instance/operation.
    --zone=: The zone of the operation.
documentation:
    command: |
        *(ALPHA)*  Cancels a Filestore operation. The server makes a best effort to cancel
        the operation, but success is not guaranteed. Clients can use the
        `filestore operations describe` command to check whether the
        cancellation succeeded or not.
    flag:
        location: Location of the Cloud Filestore instance/operation.
        zone: |-
            The zone of the operation.
            +
            To set the `zone` attribute:
            * provide the argument `operation` on the command line with a fully specified name;
            * provide the argument `--zone` on the command line;
            * provide the argument `region` on the command line;
            * provide the argument `location` on the command line;
            * set the property `filestore/zone`;
            * set the property `filestore/region`;
            * set the property `filestore/location`.
