name: create-model-bias-job-definition
description: Creates the definition for a model bias job.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --job-definition-name=!: The name of the bias job definition.
    --job-resources=!: ""
    --model-bias-app-specification=!: Configures the model bias job to run a specified Docker container image.
    --model-bias-baseline-config=: The baseline configuration for a model bias job.
    --model-bias-job-input=!: Inputs for the model bias job.
    --model-bias-job-output-config=!: ""
    --network-config=: Networking options for a model bias job.
    --role-arn=!: The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker AI can assume to perform tasks on your behalf.
    --stopping-condition=: ""
    --tags=: (Optional) An array of key-value pairs.
documentation:
    command: Creates the definition for a model bias job.
    flag:
        cli-input-json: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        cli-input-yaml: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        generate-cli-skeleton: |-
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values. It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        job-definition-name: The name of the bias job definition. The name must be unique within an Amazon Web Services Region in the Amazon Web Services account.
        job-resources: ""
        model-bias-app-specification: Configures the model bias job to run a specified Docker container image.
        model-bias-baseline-config: The baseline configuration for a model bias job.
        model-bias-job-input: Inputs for the model bias job.
        model-bias-job-output-config: ""
        network-config: Networking options for a model bias job.
        role-arn: The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker AI can assume to perform tasks on your behalf.
        stopping-condition: ""
        tags: (Optional) An array of key-value pairs. For more information, see [Using Cost Allocation Tags](https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/cost-alloc-tags.html#allocation-whatURL) in the *Amazon Web Services Billing and Cost Management User Guide*.
