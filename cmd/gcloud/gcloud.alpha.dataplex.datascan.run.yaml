name: run
description: '*(ALPHA)*  *(DEPRECATED)*  Run a Dataplex DataScan resource.'
hidden: true
flags:
    --location=: Location of the Dataplex resource.
documentation:
    command: |4
        *(ALPHA)*  *(DEPRECATED)*  This command is deprecated. Please use `gcloud alpha dataplex datascans` instead.

        Run a Dataplex Datascan resource given a valid Datascan ID.
    flag:
        location: |-
            Location of the Dataplex resource.
            +

            To set the `location` attribute:
            * provide the argument `datascan` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `dataplex/location`.
