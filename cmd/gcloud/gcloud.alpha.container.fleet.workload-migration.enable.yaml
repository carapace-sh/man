name: enable
description: '*(ALPHA)*  Enable Workload Migration Feature.'
hidden: true
documentation:
    command: |-
        *(ALPHA)*  Enable the Workload Migration Feature in a fleet.

        ## Examples

        To enable Workload Migration Feature, run:

          $ gcloud alpha container fleet workload-migration enable
