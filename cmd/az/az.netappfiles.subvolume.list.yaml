# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List a list of the subvolumes in the volume
flags:
    --max-items=: Total number of items to return in the command's output.
    --next-token=: Token to specify where to start paginating.
    --subscription=: Name or ID of subscription.
    -a, --account-name=!: The name of the NetApp account
    -g, --resource-group=!: Name of resource group.
    -p, --pool-name=!: The name of the capacity pool
    -v, --volume-name=!: The name of the volume
documentation:
    command: List a list of the subvolumes in the volume.
    flag:
        account-name: The name of the NetApp account.
        max-items: Total number of items to return in the command's output. If the total number of items available is more than the value specified, a token is provided in the command's output. To resume pagination, provide the token value in `--next-token` argument of a subsequent command.
        next-token: Token to specify where to start paginating. This is the token value from a previously truncated response.
        pool-name: The name of the capacity pool.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        volume-name: The name of the volume.
examples:
    List all subvolumes of a ANF volume: az netappfiles subvolume list -g mygroup --account-name myaccountname  --pool-name mypoolname --volume-name myvolumename
