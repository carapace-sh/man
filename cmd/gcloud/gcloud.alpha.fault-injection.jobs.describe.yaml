name: describe
description: '*(ALPHA)*  Describe a Fault injection testing job.'
hidden: true
flags:
    --location=: The Cloud location for the job.
documentation:
    command: '*(ALPHA)*  Describe a Fault injection testing job.'
    flag:
        location: |-
            The Cloud location for the job.
            +
            To set the `location` attribute:
            * provide the argument `job` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
