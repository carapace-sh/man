name: delete-segment
description: Deletes a segment.
flags:
    --segment=!: Specifies the segment to delete.
documentation:
    command: Deletes a segment. You can't delete a segment that is being used in a launch or experiment, even if that launch or experiment is not currently running.
    flag:
        segment: Specifies the segment to delete.
