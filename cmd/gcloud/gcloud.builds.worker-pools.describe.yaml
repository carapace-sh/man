name: describe
description: Describe a worker pool used by Cloud Build.
flags:
    --region=: The Cloud region where the worker pool is.
documentation:
    command: Describe a worker pool used by Cloud Build.
    flag:
        region: The Cloud region where the worker pool is.
