name: delete-apps-list
description: Permanently deletes an Firewall Manager applications list.
flags:
    --list-id=!: The ID of the applications list that you want to delete.
documentation:
    command: Permanently deletes an Firewall Manager applications list.
    flag:
        list-id: The ID of the applications list that you want to delete. You can retrieve this ID from `PutAppsList`, `ListAppsLists`, and `GetAppsList`.
