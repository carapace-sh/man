name: undelete
description: '*(BETA)*  Undelete a service account for a project.'
documentation:
    command: |
        *(BETA)*  Undelete a service account for a project.

        If the service account does not exist, this command returns a `PERMISSION_DENIED` error.
