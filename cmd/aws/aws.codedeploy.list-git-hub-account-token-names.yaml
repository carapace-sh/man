name: list-git-hub-account-token-names
description: Lists the names of stored connections to GitHub accounts.
flags:
    --next-token=: An identifier returned from the previous `ListGitHubAccountTokenNames` call.
documentation:
    command: Lists the names of stored connections to GitHub accounts.
    flag:
        next-token: An identifier returned from the previous `ListGitHubAccountTokenNames` call. It can be used to return the next set of names in the list.
