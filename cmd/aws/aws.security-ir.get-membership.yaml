name: get-membership
description: Returns the attributes of a membership.
flags:
    --membership-id=!: Required element for GetMembership to identify the membership ID to query.
documentation:
    command: Returns the attributes of a membership.
    flag:
        membership-id: Required element for GetMembership to identify the membership ID to query.
