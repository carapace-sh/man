name: create
description: Create a Compute Engine instant snapshot.
flags:
    --labels=: List of label KEY=VALUE pairs to add.
    --region=: Region of the instant snapshot to create.
    --source-disk=!: Source disk used to create the instant snapshot.
    --zone=: Zone of the instant snapshot to create.
documentation:
    command: |
        *gcloud compute instant-snapshots create* creates an instant snapshot of a disk. Instant snapshots are useful for
        backing up the disk data.
    flag:
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        region: |-
            Region of the instant snapshot to create. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/region'' property:
            +
              $ gcloud config set compute/region REGION
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/region
            +
            Alternatively, the region can be stored in the environment
            variable ``CLOUDSDK_COMPUTE_REGION''.
        source-disk: Source disk used to create the instant snapshot.
        zone: |-
            Zone of the instant snapshot to create. If not specified and the ``compute/zone'' property isn't set, you
            might be prompted to select a zone (interactive mode only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/zone'' property:
            +
              $ gcloud config set compute/zone ZONE
            +
            A list of zones can be fetched by running:
            +
              $ gcloud compute zones list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/zone
            +
            Alternatively, the zone can be stored in the environment variable
            ``CLOUDSDK_COMPUTE_ZONE''.
