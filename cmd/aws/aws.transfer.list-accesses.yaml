name: list-accesses
description: Lists the details for all the accesses you have on your server.
flags:
    --max-results=: The maximum number of items to return.
    --next-token=: When you can get additional results from the `ListAccesses` call, a `NextToken` parameter is returned in the output.
    --server-id=!: A system-assigned unique identifier for a server that has users assigned to it.
documentation:
    command: Lists the details for all the accesses you have on your server.
    flag:
        max-results: The maximum number of items to return.
        next-token: When you can get additional results from the `ListAccesses` call, a `NextToken` parameter is returned in the output. You can then pass in a subsequent command to the `NextToken` parameter to continue listing additional accesses.
        server-id: A system-assigned unique identifier for a server that has users assigned to it.
