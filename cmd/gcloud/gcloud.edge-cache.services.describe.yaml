name: describe
description: Show details about an EdgeCacheService resource.
flags:
    --location=: The location Id.
documentation:
    command: Show details about an EdgeCacheService resource.
    flag:
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `service` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * use global location.
