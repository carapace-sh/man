# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Get an user assigned managed identity from the server.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --identity=!: Name or ID of identity to show.
    -s, --server-name=!: Name of the server.
documentation:
    flag:
        identity: Name or ID of identity to show.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        server-name: Name of the server. The name can contain only lowercase letters, numbers, and the hyphen (-) character. Minimum 3 characters and maximum 63 characters.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Get identity 'test-identity' from server 'testsvr'.: az mysql flexible-server identity show -g testgroup -s testsvr --identity test-identity
