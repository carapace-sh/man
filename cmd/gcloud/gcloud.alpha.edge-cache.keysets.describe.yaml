name: describe
description: '*(ALPHA)*  Show details about an EdgeCacheKeyset resource.'
flags:
    --location=: The location Id.
documentation:
    command: '*(ALPHA)*  Show details about an EdgeCacheKeyset resource.'
    flag:
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `keyset` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * use global location.
