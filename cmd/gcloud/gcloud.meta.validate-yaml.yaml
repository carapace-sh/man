name: validate-yaml
description: Validate a YAML file against a JSON Schema.
hidden: true
documentation:
    command: |-
        gcloud meta validate-yaml validates YAML / JSON files against
        [JSON Schemas](https://json-schema.org/).
