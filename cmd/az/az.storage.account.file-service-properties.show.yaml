# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Gets the properties of file services in storage accounts, including CORS (Cross-Origin Resource Sharing) rules.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --account-name=!: The storage account name.
documentation:
    command: Show the properties of file service in storage account.
    flag:
        account-name: The storage account name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Show the properties of file service in storage account.: az storage account file-service-properties show -n mystorageaccount -g MyResourceGroup
