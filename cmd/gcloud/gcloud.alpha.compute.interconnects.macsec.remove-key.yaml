name: remove-key
description: '*(ALPHA)*  Remove pre-shared key from a Compute Engine interconnect MACsec configuration.'
flags:
    --key-name=!: The name of pre-shared key being removed from MACsec configuration of the
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute interconnects macsec remove-key* is used to remove pre-shared key from MACsec configuration of
        interconnect.

        For an example, refer to the *EXAMPLES* section below.
    flag:
        key-name: |-
            The name of pre-shared key being removed from MACsec configuration of the
            interconnect.
