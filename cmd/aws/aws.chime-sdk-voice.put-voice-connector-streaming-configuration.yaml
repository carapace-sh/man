name: put-voice-connector-streaming-configuration
description: Updates a Voice Connector's streaming configuration settings.
flags:
    --streaming-configuration=!: The streaming settings being updated.
    --voice-connector-id=!: The Voice Connector ID.
documentation:
    command: Updates a Voice Connector's streaming configuration settings.
    flag:
        streaming-configuration: The streaming settings being updated.
        voice-connector-id: The Voice Connector ID.
