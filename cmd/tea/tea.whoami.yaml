# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: whoami
description: Show current logged in user
group: MISCELLANEOUS
documentation:
    command: '[AI] The `tea whoami` command is not a valid or recognized command in any common operating system or programming language that I''m aware of. It seems like a typo as it should be the Unix/Linux command `whoami`, which returns the username of the current user executing the command.'
