name: update-gateway
description: Updates a gateway's name.
flags:
    --gateway-id=!: The ID of the gateway to update.
    --gateway-name=!: A unique name for the gateway.
documentation:
    command: Updates a gateway's name.
    flag:
        gateway-id: The ID of the gateway to update.
        gateway-name: A unique name for the gateway.
