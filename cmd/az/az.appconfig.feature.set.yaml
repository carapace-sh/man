# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: set
description: Set a feature flag.
flags:
    --auth-mode=: This parameter can be used for indicating how a data operation is to be authorized.
    --connection-string=: Combination of access key and endpoint of the App Configuration store.
    --description=: Description of the feature flag to be set.
    --endpoint=: If auth mode is "login", provide endpoint URL of the App Configuration store.
    --feature=: Name of the feature flag to be set.
    --key=: Key of the feature flag.
    --label=: If no label specified, set the feature flag with null label by default
    --requirement-type=: Requirement type determines if filters should use "Any" or "All" logic when evaluating the state of a feature.
    --subscription=: Name or ID of subscription.
    -n, --name=: Name of the App Configuration store.
    -y, --yes=: Do not prompt for confirmation.
completion:
    flag:
        auth-mode:
            - login
            - key
        requirement-type:
            - all
            - any
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        auth-mode: This parameter can be used for indicating how a data operation is to be authorized. If the auth mode is "key", provide connection string or store name and your account access keys will be retrieved for authorization. If the auth mode is "login", provide the `--endpoint` or `--name` and your "az login" credentials will be used for authorization. You can configure the default auth mode using `az configure --defaults appconfig_auth_mode=<auth_mode>`. For more information, see https://learn.microsoft.com/azure/azure-app-configuration/concept-enable-rbac
        change-reference: The related change reference ID for this resource operation.
        connection-string: Combination of access key and endpoint of the App Configuration store. Can be found using 'az appconfig credential list'. Users can preset it using ``` az configure --defaults appconfig_connection_string=<connection_string> ``` or environment variable with the name AZURE_APPCONFIG_CONNECTION_STRING.
        description: Description of the feature flag to be set.
        endpoint: If auth mode is "login" or "anonymous", provide endpoint URL of the App Configuration store. The endpoint can be retrieved using "az appconfig show" command. You can configure the default endpoint using ``` az configure --defaults appconfig_endpoint=<endpoint> ``` .
        feature: Name of the feature flag to be set. Feature name cannot contain the '%' or ':' characters.
        key: Key of the feature flag. Key must start with the ".appconfig.featureflag/" prefix. Key cannot contain the "%" character. Default key is the reserved prefix ".appconfig.featureflag/" + feature name.
        label: If no label specified, set the feature flag with null label by default.
        name: |-
            ``` and your "az login" credentials will be used for authorization. If the auth mode is "anonymous", provide the --endpoint that will be used for authorization. Anonymous mode is intended for custom endpoints only, such as the App Configuration emulator. You can configure the default auth mode using ``` az configure --defaults appconfig_auth_mode=<auth_mode> ``` . For more information, see https://learn.microsoft.com/azure/azure-app-configuration/concept-enable-rbac .

            Default value: key

            Accepted values: anonymous, key, login
        requirement-type: |-
            Requirement type determines if filters should use "Any" or "All" logic when evaluating the state of a feature.

            Accepted values: All, Any
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        tags: 'Space-separated tags: key[=value] [key[=value] ...]. Use "" to clear existing tags.'
        telemetry-enabled: |-
            Enable or disable telemetry for the feature flag.

            Accepted values: false, true
        "yes": |-
            Do not prompt for confirmation.

            Default value: False
examples:
    Disable telemetry on a feature flag.: az appconfig feature set -n MyAppConfiguration --feature color --telemetry-enabled false
    Set a feature flag using your 'az login' credentials.: az appconfig feature set --endpoint https://myappconfiguration.azconfig.io --feature color --label MyLabel --auth-mode login
    Set a feature flag with label MyLabel.: az appconfig feature set -n MyAppConfiguration --feature color --label MyLabel
    ? Set a feature flag with name "Beta" and custom key ".appconfig.featureflag/MyApp1:Beta" with tags "tag1=value1" and "tag2=value2".
    : az appconfig feature set -n MyAppConfiguration --feature Beta --key .appconfig.featureflag/MyApp1:Beta --tags tag1=value1 tag2=value2
    Set a feature flag with name "Beta" and custom key ".appconfig.featureflag/MyApp1:Beta".: az appconfig feature set -n MyAppConfiguration --feature Beta --key .appconfig.featureflag/MyApp1:Beta
    Set a feature flag with null label using connection string and set a description.: az appconfig feature set --connection-string Endpoint=https://contoso.azconfig.io;Id=xxx;Secret=xxx --feature color --description "This is a colorful feature"
    Set a feature flag with telemetry enabled.: az appconfig feature set -n MyAppConfiguration --feature color --telemetry-enabled
