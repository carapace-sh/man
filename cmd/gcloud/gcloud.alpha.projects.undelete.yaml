name: undelete
description: '*(ALPHA)*  Undelete a project.'
documentation:
    command: |-
        *(ALPHA)*  Undeletes the project with the given project ID.

        This command can fail for the following reasons:
        * There is no project with the given ID.
        * The active account does not have Owner or Editor permissions for the
          given project.

        ## EXAMPLES

        The following command undeletes the project with the ID `example-foo-bar-1`:

          $ gcloud alpha projects undelete example-foo-bar-1

        ## SEE ALSO

        {see_also}
