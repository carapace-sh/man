name: delete
description: '*(ALPHA)*  Delete one or more target gRPC proxy.'
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute target-grpc-proxies delete* deletes one or more target gRPC proxies.
