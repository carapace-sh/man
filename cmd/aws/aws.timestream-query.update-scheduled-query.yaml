name: update-scheduled-query
description: Update a scheduled query.
flags:
    --scheduled-query-arn=!: ARN of the scheuled query.
    --state=!: State of the scheduled query.
completion:
    flag:
        state:
            - ENABLED
            - DISABLED
documentation:
    command: Update a scheduled query.
    flag:
        scheduled-query-arn: ARN of the scheuled query.
        state: State of the scheduled query.
