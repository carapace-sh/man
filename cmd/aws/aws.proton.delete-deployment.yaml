name: delete-deployment
description: Delete the deployment.
flags:
    --id=!: The ID of the deployment to delete.
documentation:
    command: Delete the deployment.
    flag:
        id: The ID of the deployment to delete.
