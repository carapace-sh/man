name: update-connectivity
description: Updates the cluster's connectivity configuration.
flags:
    --cluster-arn=!: The Amazon Resource Name (ARN) of the configuration.
    --connectivity-info=!: Information about the broker access configuration.
    --current-version=!: The version of the MSK cluster to update.
documentation:
    command: Updates the cluster's connectivity configuration.
    flag:
        cluster-arn: The Amazon Resource Name (ARN) of the configuration.
        connectivity-info: Information about the broker access configuration.
        current-version: The version of the MSK cluster to update. Cluster versions aren't simple numbers. You can describe an MSK cluster to find its version. When this update operation is successful, it generates a new cluster version.
