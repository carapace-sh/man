# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all hostname bindings for a function app.
flags:
    --subscription=: Name or ID of subscription.
    --webapp-name=!: name of the web app.
    -g, --resource-group=!: Name of resource group.
    -s, --slot=: the name of the slot.
documentation:
    flag:
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        slot: The name of the slot. Default to the productions slot if not specified.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        webapp-name: Name of the web app. If left unspecified, a name will be randomly generated. You can configure the default using ``` az configure --defaults web=<name> ``` .
examples:
    List all hostname bindings for a function app. (autogenerated): az functionapp config hostname list --resource-group MyResourceGroup --webapp-name MyWebapp
