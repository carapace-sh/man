name: create
description: '*(ALPHA)*  Command to create a fault in the Project/Location.'
hidden: true
flags:
    --file=!: The file containing the fault to be created.
    --location=: The Cloud location for the fault.
documentation:
    command: '*(ALPHA)*  Command to create a fault in the Project/Location.'
    flag:
        file: The file containing the fault to be created.
        location: |-
            The Cloud location for the fault.
            +
            To set the `location` attribute:
            * provide the argument `fault` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
