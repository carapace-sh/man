name: describe
description: '*(BETA)*  Gets details about a Cloud Data Fusion instance.'
flags:
    --location=: Compute Engine region in which to create the instance.
documentation:
    command: |-
        ## EXAMPLES

        To describe instance `my-instance` in project `my-project` and location
        `my-location`, run:

          $ gcloud beta data-fusion instances describe my-instance --project=my-project --location=my-location
    flag:
        location: |-
            Compute Engine region in which to create the instance.
            +
            To set the `location` attribute:
            * provide the argument `instance` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `datafusion/location`.
