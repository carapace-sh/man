name: docker-helper
description: A Docker credential helper to provide access to GCR repositories.
hidden: true
documentation:
    command: A Docker credential helper to provide access to GCR repositories.
