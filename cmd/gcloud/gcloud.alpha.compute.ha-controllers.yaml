name: ha-controllers
description: '*(ALPHA)*  Manage HA Controllers.'
hidden: true
documentation:
    command: |-
        *(ALPHA)*  Manage High Availability (HA) Controllers. An HA Controller is a regional
        resource that provides high availability for a virtual machine (VM) instance
        in case of a zonal outage by offering a failover path to a VM in another zone.
