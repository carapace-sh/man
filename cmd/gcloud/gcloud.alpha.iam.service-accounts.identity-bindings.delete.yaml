name: delete
description: '*(ALPHA)*  Delete a service account identity binding.'
hidden: true
flags:
    --service-account=!: The service account with the identity binding.
documentation:
    command: '*(ALPHA)*  Delete a service account identity binding.'
    flag:
        service-account: The service account with the identity binding.
