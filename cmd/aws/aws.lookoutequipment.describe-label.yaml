name: describe-label
description: Returns the name of the label.
flags:
    --label-group-name=!: Returns the name of the group containing the label.
    --label-id=!: Returns the ID of the label.
documentation:
    command: Returns the name of the label.
    flag:
        label-group-name: Returns the name of the group containing the label.
        label-id: Returns the ID of the label.
