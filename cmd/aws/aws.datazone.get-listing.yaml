name: get-listing
description: Gets a listing (a record of an asset at a given time).
flags:
    --domain-identifier=!: The ID of the Amazon DataZone domain.
    --identifier=!: The ID of the listing.
    --listing-revision=: The revision of the listing.
documentation:
    command: Gets a listing (a record of an asset at a given time). If you specify a listing version, only details that are specific to that version are returned.
    flag:
        domain-identifier: The ID of the Amazon DataZone domain.
        identifier: The ID of the listing.
        listing-revision: The revision of the listing.
