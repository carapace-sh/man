name: create-capacity-reservation-by-splitting
description: Create a new Capacity Reservation by splitting the capacity of the source Capacity Reservation.
flags:
    --client-token=: Unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --instance-count=!: The number of instances to split from the source Capacity Reservation.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --source-capacity-reservation-id=!: The ID of the Capacity Reservation from which you want to split the capacity.
    --tag-specifications=: The tags to apply to the new Capacity Reservation.
documentation:
    command: Create a new Capacity Reservation by splitting the capacity of the source Capacity Reservation. The new Capacity Reservation will have the same attributes as the source Capacity Reservation except for tags. The source Capacity Reservation must be `active` and owned by your Amazon Web Services account.
    flag:
        client-token: Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see [Ensure Idempotency](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/Run_Instance_Idempotency.html).
        dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is `DryRunOperation`. Otherwise, it is `UnauthorizedOperation`.
        instance-count: The number of instances to split from the source Capacity Reservation.
        source-capacity-reservation-id: The ID of the Capacity Reservation from which you want to split the capacity.
        tag-specifications: The tags to apply to the new Capacity Reservation.
