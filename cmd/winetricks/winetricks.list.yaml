# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: list categories
documentation:
    command: '[AI] `winetricks list` is a command used in Wine, a compatibility layer for running Windows applications on Unix-based systems. This specific command displays a list of all installed and available Windows components that can be managed with Winetricks. It helps users to check which DLLs or other Windows features are currently configured for use with their Wine-run applications.'
