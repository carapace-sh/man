name: detect-image-properties
description: '*(BETA)*  Detect general attributes of an image.'
flags:
    --model-version=: Model version to use for the feature.
completion:
    flag:
        model-version:
            - builtin/latest
            - builtin/stable
documentation:
    command: |
        *(BETA)*  Detect general attributes of an image, such as dominant color.
    flag:
        model-version: 'Model version to use for the feature. _MODEL_VERSION_ must be one of: *builtin/latest*, *builtin/stable*.'
