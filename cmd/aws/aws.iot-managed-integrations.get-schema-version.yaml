name: get-schema-version
description: Gets a schema version with the provided information.
flags:
    --format=: The format of the schema version.
    --schema-versioned-id=!: Schema id with a version specified.
    --type=!: The type of schema version.
completion:
    flag:
        format:
            - AWS
            - ZCL
            - CONNECTOR
        type:
            - capability
            - definition
documentation:
    command: Gets a schema version with the provided information.
    flag:
        format: The format of the schema version.
        schema-versioned-id: Schema id with a version specified. If the version is missing, it defaults to latest version.
        type: The type of schema version.
