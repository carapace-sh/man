# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: prune
description: removes old allowed files
documentation:
    command: '[AI] `direnv prune` is a command used in the Direnv environment manager for managing active directories and configurations. It removes any saved directory contexts that are no longer present in the filesystem, helping to keep your configuration clean and up-to-date.'
