name: describe-capacity-block-extension-history
description: Describes the events for the specified Capacity Block extension during the specified time.
flags:
    --capacity-reservation-ids=: The IDs of Capacity Block reservations that you want to display the history for.
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --filters=: One or more filters
    --max-results=: The maximum number of items to return for this request.
    --next-token=: The token to use to retrieve the next page of results.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
documentation:
    command: Describes the events for the specified Capacity Block extension during the specified time.
    flag:
        capacity-reservation-ids: The IDs of Capacity Block reservations that you want to display the history for.
        dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is `DryRunOperation`. Otherwise, it is `UnauthorizedOperation`.
        filters: |-
            One or more filters

            - `availability-zone` - The Availability Zone of the extension.
            - `availability-zone-id` - The Availability Zone ID of the extension.
            - `capacity-block-extension-offering-id` - The ID of the extension offering.
            - `capacity-block-extension-status` - The status of the extension (`payment-pending` | `payment-failed` | `payment-succeeded`).
            - `capacity-reservation-id` - The reservation ID of the extension.
            - `instance-type` - The instance type of the extension.
        max-results: The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see [Pagination](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/Query-Requests.html#api-pagination).
        next-token: The token to use to retrieve the next page of results.
