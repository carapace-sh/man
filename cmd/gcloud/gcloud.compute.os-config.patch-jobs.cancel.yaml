name: cancel
description: Cancel a specific OS patch job which must currently be active.
documentation:
    command: |-
        ## EXAMPLES

        To cancel the patch job `job1`, run:

              $ gcloud compute os-config patch-jobs cancel job1
