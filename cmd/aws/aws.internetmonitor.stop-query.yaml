name: stop-query
description: Stop a query that is progress for a specific monitor.
flags:
    --monitor-name=!: The name of the monitor.
    --query-id=!: The ID of the query that you want to stop.
documentation:
    command: Stop a query that is progress for a specific monitor.
    flag:
        monitor-name: The name of the monitor.
        query-id: The ID of the query that you want to stop. A `QueryId` is an internally-generated identifier for a specific query.
