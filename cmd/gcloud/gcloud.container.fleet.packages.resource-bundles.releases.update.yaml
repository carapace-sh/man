name: update
description: Update Package Rollouts Release.
flags:
    --lifecycle=: Lifecycle of the Release.
    --location=: Google Cloud zone or region.
    --resource-bundle=!: Resource Bundle name.
documentation:
    command: Update Package Rollouts Release.
    flag:
        lifecycle: Lifecycle of the Release.
        location: Google Cloud zone or region.
        resource-bundle: Resource Bundle name.
