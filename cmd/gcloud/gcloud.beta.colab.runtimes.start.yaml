name: start
description: '*(BETA)*  Start a stopped runtime.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --no-async&: Return immediately, without waiting for the operation in progress to
    --region=: Cloud region for the runtime.
documentation:
    command: |
        *(BETA)*  Start a stopped Colab Enterprise notebook runtime.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        region: |-
            Cloud region for the runtime.
            +
            To set the `region` attribute:
            * provide the argument `runtime` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `colab/region`.
