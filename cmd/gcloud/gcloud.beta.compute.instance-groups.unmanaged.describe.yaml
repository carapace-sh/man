name: describe
description: '*(BETA)*  Describe an instance group.'
flags:
    --zone=: Zone of the instance group to describe.
documentation:
    command: |
        *(BETA)*  *gcloud beta compute instance-groups unmanaged describe* displays detailed information about a Google Compute
        Engine instance group.
    flag:
        zone: |-
            Zone of the instance group to describe. If not specified and the ``compute/zone'' property isn't set, you
            might be prompted to select a zone (interactive mode only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/zone'' property:
            +
              $ gcloud config set compute/zone ZONE
            +
            A list of zones can be fetched by running:
            +
              $ gcloud compute zones list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/zone
            +
            Alternatively, the zone can be stored in the environment variable
            ``CLOUDSDK_COMPUTE_ZONE''.
