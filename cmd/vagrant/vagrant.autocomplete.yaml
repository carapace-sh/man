# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: autocomplete
description: manages autocomplete installation on host
documentation:
    command: '[AI] `vagrant autocomplete` is a command provided by Vagrant, a tool for building and managing virtual development environments. This command is used to install or update file completions for various shells (like Bash or Zsh), which enables the terminal to suggest possible completions as you type. These suggestions can include Vagrant commands, box names, or machine names, making it easier to use Vagrant in your terminal sessions.'
