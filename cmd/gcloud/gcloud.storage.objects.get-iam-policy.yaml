name: get-iam-policy
description: Get the access policy for an object.
hidden: true
documentation:
    command: |
        *gcloud storage objects get-iam-policy* behaves similarly to *gcloud storage objects get-object-acl*, but
        uses the IAM policy binding syntax in the output.
