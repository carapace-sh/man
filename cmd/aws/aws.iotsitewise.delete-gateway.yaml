name: delete-gateway
description: Deletes a gateway from IoT SiteWise.
flags:
    --gateway-id=!: The ID of the gateway to delete.
documentation:
    command: Deletes a gateway from IoT SiteWise. When you delete a gateway, some of the gateway's files remain in your gateway's file system.
    flag:
        gateway-id: The ID of the gateway to delete.
