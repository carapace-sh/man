name: describe-explicit
description: '*(ALPHA)*  Display settings of Security Command Center(SCC).'
flags:
    --organization=: ORGANIZATION_ID
documentation:
    command: |
        *(ALPHA)*  Describe settings of Security Command Center(SCC).
    flag:
        organization: ORGANIZATION_ID
