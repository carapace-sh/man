name: batch-create-variable
description: Creates a batch of variables.
flags:
    --tags=: A collection of key and value pairs.
    --variable-entries=!: The list of variables for the batch create variable request.
documentation:
    command: Creates a batch of variables.
    flag:
        tags: A collection of key and value pairs.
        variable-entries: The list of variables for the batch create variable request.
