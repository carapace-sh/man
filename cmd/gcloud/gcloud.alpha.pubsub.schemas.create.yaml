name: create
description: '*(ALPHA)*  Create a Pub/Sub schema.'
flags:
    --definition-file=: File containing schema definition.
    --definition=: Inline schema definition.
    --tags=&: List of tag KEY=VALUE pairs to add.
    --type=!: Type of the schema.
completion:
    flag:
        type:
            - avro
            - protocol-buffer
documentation:
    command: |
        *(ALPHA)*  Create a new Pub/Sub schema.
    flag:
        definition: Inline schema definition.
        definition-file: File containing schema definition. Use a full or relative path to a local file containing the value of definition_file.
        tags: |
            List of tag KEY=VALUE pairs to add.
        type: 'Type of the schema. _TYPE_ must be one of: *avro*, *protocol-buffer*.'
