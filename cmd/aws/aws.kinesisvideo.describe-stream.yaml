name: describe-stream
description: Returns the most current information about the specified stream.
flags:
    --stream-arn=: The Amazon Resource Name (ARN) of the stream.
    --stream-name=: The name of the stream.
documentation:
    command: Returns the most current information about the specified stream. You must specify either the `StreamName` or the `StreamARN`.
    flag:
        stream-arn: The Amazon Resource Name (ARN) of the stream.
        stream-name: The name of the stream.
