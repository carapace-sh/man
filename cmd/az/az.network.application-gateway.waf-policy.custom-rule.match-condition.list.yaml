# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List application gateway WAF policy custom rule match conditions.
flags:
    --policy-name=!: Name of the application gateway WAF policy.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: Name of the WAF policy rule.
documentation:
    flag:
        name: Name of the WAF policy rule.
        policy-name: Name of the application gateway WAF policy.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List application gateway WAF policy custom rule match conditions.: az network application-gateway waf-policy custom-rule match-condition list --name MyWAFPolicyRule --policy-name MyPolicy --resource-group MyResourceGroup --subscription MySubscription
