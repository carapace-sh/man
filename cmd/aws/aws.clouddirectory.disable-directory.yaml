name: disable-directory
description: Disables the specified directory.
flags:
    --directory-arn=!: The ARN of the directory to disable.
documentation:
    command: Disables the specified directory. Disabled directories cannot be read or written to. Only enabled directories can be disabled. Disabled directories may be reenabled.
    flag:
        directory-arn: The ARN of the directory to disable.
