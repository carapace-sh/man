name: describe-user-import-job
description: Describes a user import job.
flags:
    --job-id=!: The Id of the user import job that you want to describe.
    --user-pool-id=!: The ID of the user pool that's associated with the import job.
documentation:
    command: Describes a user import job. For more information about user CSV import, see [Importing users from a CSV file](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-using-import-tool.html).
    flag:
        job-id: The Id of the user import job that you want to describe.
        user-pool-id: The ID of the user pool that's associated with the import job.
