name: describe-release-label
description: Provides Amazon EMR release label details, such as the releases available the Region where the API request is run, and the available applications for a specific Amazon EMR release label.
flags:
    --max-results=: Reserved for future use.
    --next-token=: The pagination token.
    --release-label=: The target release label to be described.
documentation:
    command: Provides Amazon EMR release label details, such as the releases available the Region where the API request is run, and the available applications for a specific Amazon EMR release label. Can also list Amazon EMR releases that support a specified version of Spark.
    flag:
        max-results: Reserved for future use. Currently set to null.
        next-token: The pagination token. Reserved for future use. Currently set to null.
        release-label: The target release label to be described.
