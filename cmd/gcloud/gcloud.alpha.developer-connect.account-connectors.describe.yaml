name: describe
description: '*(ALPHA)*  Describe accountConnectors.'
flags:
    --location=: The location id of the accountConnector resource.
documentation:
    command: '*(ALPHA)*  Describe an accountConnector'
    flag:
        location: |-
            The location id of the accountConnector resource.
            +
            To set the `location` attribute:
            * provide the argument `account_connector` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
