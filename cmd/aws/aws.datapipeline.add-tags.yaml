name: add-tags
description: Adds or modifies tags for the specified pipeline.
flags:
    --pipeline-id=!: The ID of the pipeline.
    --tags=!: The tags to add, as key/value pairs.
documentation:
    command: Adds or modifies tags for the specified pipeline.
    flag:
        pipeline-id: The ID of the pipeline.
        tags: The tags to add, as key/value pairs.
