name: remove-named-set-element
description: '*(ALPHA)*  Remove an element from a named set of a Compute Engine router.'
hidden: true
flags:
    --region=: Region of the router to update.
    --set-element=!: CEL expression for the element.
    --set-name=!: Name of the match set.
documentation:
    command: '*(ALPHA)*  Remove an element from a named set of a Compute Engine router.'
    flag:
        region: |-
            Region of the router to update. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/region'' property:
            +
              $ gcloud config set compute/region REGION
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/region
            +
            Alternatively, the region can be stored in the environment
            variable ``CLOUDSDK_COMPUTE_REGION''.
        set-element: CEL expression for the element.
        set-name: Name of the match set.
