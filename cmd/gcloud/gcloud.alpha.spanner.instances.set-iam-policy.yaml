name: set-iam-policy
description: '*(ALPHA)*  Set the IAM policy for a Cloud Spanner instance.'
documentation:
    command: |
        *(ALPHA)*  Set the IAM policy for a Cloud Spanner instance given a instance ID and
        a file encoded in JSON or YAML that contains the IAM policy.
