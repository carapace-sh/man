name: locations
description: '*(BETA)*  Manage Artifact Registry resource locations.'
documentation:
    command: |-
        ## EXAMPLES
        To list all supported locations, run:

            $ gcloud beta artifacts locations list
