name: describe
description: '*(ALPHA)*  Describe validationResources.'
hidden: true
flags:
    --location=: The location id of the validationResource resource.
    --validation=: The validation id of the validationResource resource.
documentation:
    command: '*(ALPHA)*  Describe a validationResource'
    flag:
        location: |-
            The location id of the validationResource resource.
            +
            To set the `location` attribute:
            * provide the argument `validation_resource` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        validation: |-
            The validation id of the validationResource resource.
            +
            To set the `validation` attribute:
            * provide the argument `validation_resource` on the command line with a fully specified name;
            * provide the argument `--validation` on the command line.
