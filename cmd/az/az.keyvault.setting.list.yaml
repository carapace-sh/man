# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: Lists all account settings.
flags:
    --hsm-name=: Name of the HSM.
    --id=: Full URI of the HSM.
    --subscription=: Name or ID of subscription.
documentation:
    command: Get all settings associated with the managed HSM.
    flag:
        hsm-name: Name of the HSM.
        id: Full URI of the HSM. If specified all other 'Id' arguments should be omitted.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
