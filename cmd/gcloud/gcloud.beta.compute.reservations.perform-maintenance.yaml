name: perform-maintenance
description: '*(BETA)*  Perform maintenance on a reservation, only applicable to reservations with reservation blocks.'
flags:
    --scope=: The maintenance scope to set for the reservation.
    --zone=: Zone of the reservation to perform-maintenance.
completion:
    flag:
        scope:
            - all
            - running
            - unused
documentation:
    command: '*(BETA)*  Perform maintenance on a reservation, only applicable to reservations with reservation blocks.'
    flag:
        scope: |-
            The maintenance scope to set for the reservation. _SCOPE_ must be one of:
            +
            *all*::: Perform maintenance on all hosts in the reservation.
            *running*::: Perform maintenance only on the hosts in the reservation that have running VMs.
            *unused*::: Perform maintenance only on the hosts in the reservation that don't have running VMs.
            :::
            +
        zone: |-
            Zone of the reservation to perform-maintenance. If not specified and the ``compute/zone'' property isn't set, you
            might be prompted to select a zone (interactive mode only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/zone'' property:
            +
              $ gcloud config set compute/zone ZONE
            +
            A list of zones can be fetched by running:
            +
              $ gcloud compute zones list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/zone
            +
            Alternatively, the zone can be stored in the environment variable
            ``CLOUDSDK_COMPUTE_ZONE''.
