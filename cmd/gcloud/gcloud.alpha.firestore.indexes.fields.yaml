name: fields
description: '*(ALPHA)*  Manage single-field indexes for Cloud Firestore.'
documentation:
    command: |-
        *(ALPHA)*  Changes here apply to index settings for individual fields, and won't affect
        any composite indexes using those fields.
