name: set-iam-policy
description: '*(ALPHA)*  Set IAM policy for an access policy.'
documentation:
    command: |
        *(ALPHA)*  Sets the IAM policy for a access policy, given access policy ID and a file
        encoded in JSON or YAML that contains the IAM policy.
