name: create
description: '*(BETA)*  Create a restore plan.'
flags:
    --all-namespaces: If true, restore all namespaced resources in the backup.
    --async: Return immediately, without waiting for the operation in progress to
    --backup-plan=!: The BackupPlan from which Backups may be used as the source for Restores created via this
    --cluster-resource-conflict-policy=: Define how to handle restore-time conflicts for cluster-scoped resources.
    --cluster-resource-scope-all-group-kinds: If true, all valid cluster-scoped resources will be restored.
    --cluster-resource-scope-excluded-group-kinds=: List of cluster-scoped resource group kinds to NOT restore from the backup.
    --cluster-resource-scope-no-group-kinds: If true, no cluster-scoped resources will be restored.
    --cluster-resource-scope-selected-group-kinds=: List of cluster-scoped resource group kinds to restore from the backup.
    --cluster=!: The target cluster into which Restores created via this RestorePlan will restore data.
    --description=: The description of the restore plan.
    --excluded-namespaces=: List of selected namespaces to skip.
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: Google Cloud location.
    --namespaced-resource-restore-mode=: Define how to handle restore-time conflicts for namespaced resources.
    --no-all-namespaces&: If true, restore all namespaced resources in the backup.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-cluster-resource-scope-all-group-kinds&: If true, all valid cluster-scoped resources will be restored.
    --no-cluster-resource-scope-no-group-kinds&: If true, no cluster-scoped resources will be restored.
    --no-namespaces: If true, do not restore any namespaced resources in the backup.
    --restore-order-file=: If provided, defines a custom order that can be used in a restore, which includes dependencies
    --selected-applications=: List of selected applications to restore.
    --selected-namespaces=: List of selected namespaces to restore.
    --substitution-rules-file=: (DEPRECATED) If provided, defines a set of resource transformations that will be applied to resources from
    --tags=&: List of tag KEY=VALUE pairs to add.
    --transformation-rules-file=: If provided, defines a set of resource transformations that will be applied to resources from
    --volume-data-restore-policy-bindings=: Dict that binds a supported volume type to a volume data restore policy.
    --volume-data-restore-policy=: Define how data is populated for restored volumes.
completion:
    flag:
        cluster-resource-conflict-policy:
            - use-backup-version
            - use-existing-version
        namespaced-resource-restore-mode:
            - delete-and-restore
            - fail-on-conflict
            - merge-replace-on-conflict
            - merge-replace-volume-on-conflict
            - merge-skip-on-conflict
        volume-data-restore-policy:
            - no-volume-data-restoration
            - restore-volume-data-from-backup
            - reuse-volume-handle-from-backup
documentation:
    command: |
        *(BETA)*  Create a Backup for GKE restore plan.

        ## EXAMPLES

        To create a restore plan ``my-restore-plan'' in project ``my-project'' in location ``us-central1'', run:

          $ gcloud beta container backup-restore restore-plans create my-restore-plan --project=my-project --location=us-central1 --cluster=projects/my-project/locations/us-central1/clusters/my-cluster --backup-plan=projects/my-project/locations/us-central1/backupPlans/my-backup-plan --all-namespaces --namespaced-resource-restore-mode=merge-skip-on-conflict
    flag:
        all-namespaces: If true, restore all namespaced resources in the backup.
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        backup-plan: |-
            The BackupPlan from which Backups may be used as the source for Restores created via this
            RestorePlan.
            +
            Value must be entered as a relative name, e.g.
            `projects/<project>/locations/<location>/backupPlans/<backupPlans>`.
        cluster: |-
            The target cluster into which Restores created via this RestorePlan will restore data.
            NOTE: the cluster's region must be the same as the RestorePlan.
            +
            Value must be entered as a relative name, e.g.:
            `projects/<project>/locations/<location>/clusters/<cluster>` for a regional cluster
            or
            `projects/<project>/zones/<zone>/clusters/<cluster>` for a zonal cluster.
        cluster-resource-conflict-policy: |-
            Define how to handle restore-time conflicts for cluster-scoped resources. _CLUSTER_RESOURCE_CONFLICT_POLICY_ must be one of:
            +
            *use-backup-version*::: Selecting 'use-existing-version' means deleting the existing version of the conflicting
            resources before re-creating them from the Backup.
            +
            Note that this is a dangerous option which
            could cause unintentional data loss if used inappropriately. For example, deleting a CRD will
            cause Kubernetes to delete all CRs of that type.
            +
            *use-existing-version*::: Selecting 'use-existing-version' means no conflicting resources will be restored.
            +
            :::
            +
        cluster-resource-scope-all-group-kinds: If true, all valid cluster-scoped resources will be restored.
        cluster-resource-scope-excluded-group-kinds: |-
            List of cluster-scoped resource group kinds to NOT restore from the backup. If specified,
            all valid cluster-scoped resources will be restored except for those specified in the list.
            The format of a resource is "<group>/<kind>", e.g. `storage.k8s.io/StorageClass` for
            StorageClass. Use an empty string for core API group.
        cluster-resource-scope-no-group-kinds: If true, no cluster-scoped resources will be restored.
        cluster-resource-scope-selected-group-kinds: |-
            List of cluster-scoped resource group kinds to restore from the backup. If specified,
            only the selected resources will be restored. The format of a resource is "<group>/<kind>", e.g.
            `storage.k8s.io/StorageClass` for StorageClass. Use an empty string for core API group.
        description: The description of the restore plan.
        excluded-namespaces: |-
            List of selected namespaces to skip. All namespaced resources in all namespaces excluding
            selected namespaces are restored.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            Google Cloud location.
            +
            To set the `location` attribute:
            * provide the argument `restore_plan` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `gkebackup/location`.
        namespaced-resource-restore-mode: |-
            Define how to handle restore-time conflicts for namespaced resources. _NAMESPACED_RESOURCE_RESTORE_MODE_ must be one of:
            +
            *delete-and-restore*::: When conflicting top-level resources (either Namespaces or ProtectedApplications, depending
            upon the scope) are encountered, this will first trigger a delete of the conflicting resource
            AND ALL OF ITS REFERENCED RESOURCES (e.g., all resources in the Namespace or all resources
            referenced by the ProtectedApplication) before restoring the resources from the Backup. This
            mode should only be used when you are intending to revert some portion of a cluster to an
            earlier state.
            +
            *fail-on-conflict*::: If conflicting top-level resources (either Namespaces or ProtectedApplications, depending upon
            the scope) are encountered at the beginning of a restore process, the Restore will fail. If a
            conflict occurs during the restore process itself (e.g., because an out of band process
            creates conflicting resources), a conflict will be reported.
            +
            *merge-replace-on-conflict*::: This mode merges the backup and the target cluster and replaces the conflicting resources with
            the ones in the backup. If a single resource to restore exists in the cluster before
            restoration, the resource will be replaced with the one from the backup. To replace an
            existing resource, the first attempt is to update the resource to match the one from the
            backup; if the update fails, the second attempt is to delete the resource and restore it from
            the backup. Note that this mode could cause data loss as it replaces the existing resources
            in the target cluster, and the original PV can be retained or deleted depending on its reclaim
            policy.
            +
            *merge-replace-volume-on-conflict*::: This mode merges the backup and the target cluster and skips the conflicting resources except
            volume data. If a PVC to restore already exists, this mode will restore/reconnect the volume
            without overwriting the PVC. It is similar to MERGE_SKIP_ON_CONFLICT except that it will apply
            the volume data policy for the conflicting PVCs
            "RESTORE_VOLUME_DATA_FROM_BACKUP:" restore data only and respect the reclaim policy of the
            original PV;
            "REUSE_VOLUME_HANDLE_FROM_BACKUP:" reconnect and respect the reclaim policy of the original
            PV;
            "NO_VOLUME_DATA_RESTORATION:" new provision and respect the reclaim policy of the original PV.
            Note that this mode could cause data loss as the original PV can be retained or deleted
            depending on its reclaim policy.
            +
            *merge-skip-on-conflict*::: This mode merges the backup and the target cluster and skips the conflicting resources. If a
            single resource to restore exists in the cluster before restoration, the resource will be
            skipped, otherwise it will be restored.
            +
            :::
            +
        no-all-namespaces: |
            If true, restore all namespaced resources in the backup.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-cluster-resource-scope-all-group-kinds: |
            If true, all valid cluster-scoped resources will be restored.
        no-cluster-resource-scope-no-group-kinds: |
            If true, no cluster-scoped resources will be restored.
        no-namespaces: If true, do not restore any namespaced resources in the backup.
        restore-order-file: |-
            If provided, defines a custom order that can be used in a restore, which includes dependencies
            between two group kinds that must be honored during a restore, a satisfying group kind and a
            requiring group kind. The satisfying group kind must be restored before the requiring group
            kind.
        selected-applications: |-
            List of selected applications to restore. Only those namespaced resources which belong to one of
            the selected applications are restored.
        selected-namespaces: |-
            List of selected namespaces to restore. Only those namespaced resources belonging to a selected
            namespace are restored.
        substitution-rules-file: |-
            (DEPRECATED) If provided, defines a set of resource transformations that will be applied to resources from
            the source backup before they are created in the target cluster.
            +
            Flag substitution-rules-file is deprecated.
              Please use --transformation-rules-file instead.
        tags: |
            List of tag KEY=VALUE pairs to add.
        transformation-rules-file: |-
            If provided, defines a set of resource transformations that will be applied to resources from
            the source backup before they are created in the target cluster.
        volume-data-restore-policy: |-
            Define how data is populated for restored volumes. If this flag is not specified,
            'no-volume-data-restoration' will be used. _VOLUME_DATA_RESTORE_POLICY_ must be one of:
            +
            *no-volume-data-restoration*::: Selecting 'no-volume-data-restoration' means PV will not be restored. The restoration will
            ONLY restore selected PVCs and expects corresponding Kubernetes controllers to either
            dynamically provision blank PVs or bind them to pre-provisioned PVs created out-of-band.
            +
            *restore-volume-data-from-backup*::: Selecting 'restore-volume-data-from-backup' means a new PV will be restored using the
            corresponding volume backup data in the Backup.
            +
            *reuse-volume-handle-from-backup*::: Selecting 'reuse-volume-handle-from-backup' means a PV will be pre-provisioned using the
            volume handle of the original PV in the Backup.
            +
            :::
            +
        volume-data-restore-policy-bindings: |-
            Dict that binds a supported volume type to a volume data restore policy.
            e.g. `gce-persistent-disk=restore-volume-data-from-backup` for
            a binding that results in Persistent Disk volumes being restored
            using the "restore volume data from backup" policy.
