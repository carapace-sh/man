# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: thaw
description: Resume execution of a frozen unit
group: unit
documentation:
    command: '[AI] The `systemctl thaw` command is not a valid command in systemd, which is a system and service manager for Linux operating systems. There seems to be no such command defined in the systemd documentation or implemented in any known distribution. It''s essential to double-check commands and their correct syntax before executing them on your system to avoid potential issues.'
