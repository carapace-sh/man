# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ffmpeg
description: Hyper fast Audio and Video encoder
documentation:
    command: '[AI] `ffmpeg` is a powerful open-source multimedia framework with a CLI (Command Line Interface) tool that handles various tasks related to audio, video, and media streams processing. It''s widely used for transcoding, encoding, decoding, streaming media, and creating media formats. Users provide input files or streams, along with the desired output format or settings, which `ffmpeg` processes accordingly. Its flexibility and capabilities make it a popular choice for multimedia manipulation and production in various industries.'
