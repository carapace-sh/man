name: get-vpc-endpoint-service-name
description: Retrieves the VPC endpoint service name.
flags:
    --identifier=!: The ID of the cluster to retrieve.
documentation:
    command: Retrieves the VPC endpoint service name.
    flag:
        identifier: The ID of the cluster to retrieve.
