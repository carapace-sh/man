name: index-documents
description: Tells the search domain to start indexing its documents using the latest indexing options.
flags:
    --domain-name=!: ""
documentation:
    command: Tells the search domain to start indexing its documents using the latest indexing options. This operation must be invoked to activate options whose [OptionStatus]() is `RequiresIndexDocuments`.
    flag:
        domain-name: ""
