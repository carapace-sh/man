name: tag-resource
description: Tags a resource with user-supplied tags.
flags:
    --resource-arn=!: The `GraphqlApi` Amazon Resource Name (ARN).
    --tags=!: A `TagMap` object.
documentation:
    command: Tags a resource with user-supplied tags.
    flag:
        resource-arn: The `GraphqlApi` Amazon Resource Name (ARN).
        tags: A `TagMap` object.
