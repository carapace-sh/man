# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: preview
description: Preview a given theme
flags:
    -h, --help: Prints help information
documentation:
    command: '[AI] "Vivid Preview" is not a standard command or term in computing or graphic design. It could potentially be a custom command specific to certain software applications, particularly those related to video editing or image processing, that allows users to view high-resolution previews of their media files with enhanced detail and clarity. The exact functionality may vary depending on the specific application.'
    flag:
        help: |-
            [AI] `vivid preview` is a command-line tool used in Blender for interactive rendering of 3D scenes. The `--help` option is used to display detailed information about how to use this command. Here's what you can expect when using `vivid preview --help`:

            * A brief description of what `vivid preview` does and its usage.
            * Information on required arguments, such as the path to a Blender .blend file.
            * Options that can be used with `vivid preview`, including `--resolution` for specifying output resolution and `--viewport` for rendering from a specific viewpoint.
            * Details about various settings that can be adjusted, like quality presets or sampling methods.
            * Information on the environment variables that can be set to customize the behavior of `vivid preview`.
            * Examples of how to use `vivid preview` with different arguments and options.
