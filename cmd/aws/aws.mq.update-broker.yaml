name: update-broker
description: Adds a pending configuration change to a broker.
flags:
    --authentication-strategy=: Optional.
    --auto-minor-version-upgrade: Enables automatic upgrades to new patch versions for brokers as new versions are released and supported by Amazon MQ.
    --broker-id=!: The unique ID that Amazon MQ generates for the broker.
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --configuration=: A list of information about the configuration.
    --data-replication-mode=: Defines whether this broker is a part of a data replication pair.
    --engine-version=: The broker engine version.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --host-instance-type=: The broker's host instance type to upgrade to.
    --ldap-server-metadata=: Optional.
    --logs=: Enables Amazon CloudWatch logging for brokers.
    --maintenance-window-start-time=: The parameters that determine the WeeklyStartTime.
    --no-auto-minor-version-upgrade: Enables automatic upgrades to new patch versions for brokers as new versions are released and supported by Amazon MQ.
    --security-groups=: The list of security groups (1 minimum, 5 maximum) that authorizes connections to brokers.
completion:
    flag:
        authentication-strategy:
            - SIMPLE
            - LDAP
            - CONFIG_MANAGED
        data-replication-mode:
            - NONE
            - CRDR
documentation:
    command: Adds a pending configuration change to a broker.
    flag:
        authentication-strategy: Optional. The authentication strategy used to secure the broker. The default is SIMPLE.
        auto-minor-version-upgrade: |-
            Enables automatic upgrades to new patch versions for brokers as new versions are released and supported by Amazon MQ. Automatic upgrades occur during the scheduled maintenance window or after a manual broker reboot.

            Must be set to true for ActiveMQ brokers version 5.18 and above and for RabbitMQ brokers version 3.13 and above.
        broker-id: The unique ID that Amazon MQ generates for the broker.
        cli-input-json: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        cli-input-yaml: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        configuration: A list of information about the configuration.
        data-replication-mode: Defines whether this broker is a part of a data replication pair.
        engine-version: |-
            The broker engine version. For more information, see the [ActiveMQ version management](https://docs.aws.amazon.com//amazon-mq/latest/developer-guide/activemq-version-management.html) and the [RabbitMQ version management](https://docs.aws.amazon.com//amazon-mq/latest/developer-guide/rabbitmq-version-management.html) sections in the Amazon MQ Developer Guide.

            When upgrading to ActiveMQ version 5.18 and above or RabbitMQ version 3.13 and above, you must have autoMinorVersionUpgrade set to true for the broker.
        generate-cli-skeleton: |-
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values. It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        host-instance-type: The broker's host instance type to upgrade to. For a list of supported instance types, see [Broker instance types](https://docs.aws.amazon.com//amazon-mq/latest/developer-guide/broker.html#broker-instance-types).
        ldap-server-metadata: Optional. The metadata of the LDAP server used to authenticate and authorize connections to the broker. Does not apply to RabbitMQ brokers.
        logs: Enables Amazon CloudWatch logging for brokers.
        maintenance-window-start-time: The parameters that determine the WeeklyStartTime.
        security-groups: The list of security groups (1 minimum, 5 maximum) that authorizes connections to brokers.
