name: disable-ldaps
description: Deactivates LDAP secure calls for the specified directory.
flags:
    --directory-id=!: The identifier of the directory.
    --type=!: The type of LDAP security to enable.
completion:
    flag:
        type:
            - Client
documentation:
    command: Deactivates LDAP secure calls for the specified directory.
    flag:
        directory-id: The identifier of the directory.
        type: The type of LDAP security to enable. Currently only the value `Client` is supported.
