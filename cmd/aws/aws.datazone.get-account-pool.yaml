name: get-account-pool
description: Gets the details of the account pool.
flags:
    --domain-identifier=!: The ID of the domain in which the account pool lives whose details are to be displayed.
    --identifier=!: The ID of the account pool whose details are to be displayed.
documentation:
    command: Gets the details of the account pool.
    flag:
        domain-identifier: The ID of the domain in which the account pool lives whose details are to be displayed.
        identifier: The ID of the account pool whose details are to be displayed.
