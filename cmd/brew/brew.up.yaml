# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: up
description: Fetch the newest version of Homebrew and all formulae from GitHub using `git`(1) and perform any necessary migrations
flags:
    --auto-update: Run on auto-updates (e.g. before `brew install`). Skips some slower steps.
    --debug: Display a trace of all shell commands as they are executed.
    --force: Always do a slower, full update check (even if unnecessary).
    --help: Show this message.
    --merge: Use `git merge` to apply updates (rather than `git rebase`).
    --quiet: Make some output more quiet.
    --verbose: Print the directories checked and `git` operations performed.
documentation:
    command: '[AI] The `brew up` command is not a valid command in Homebrew, which is a popular package manager for macOS. There''s no such command defined in Homebrew''s documentation or functionality. To use Homebrew effectively, please refer to the available commands such as `brew install`, `brew update`, and `brew uninstall`.'
    flag:
        auto-update: '[AI] The `brew upgrade --auto-update` command is used with Homebrew, a popular package manager for macOS. It automatically checks for updates to all installed formulas (packages) and their dependencies, and installs any available upgrades. This helps keep the system secure and up-to-date with the latest software versions. The `--auto-update` option enables the automatic updating feature, allowing Homebrew to check for and install new updates in the background without requiring manual intervention.'
        debug: '[AI] `brew up --debug` is a command used in Homebrew, a popular package manager for macOS. This command initiates the installation or updating of a formula with additional debug output to help diagnose potential issues. The `--debug` flag enables more detailed logging, displaying verbose messages that provide information on each step taken during the process. This can be useful when troubleshooting complex installations or encountering errors.'
        force: |-
            [AI] `brew up --force` is a command used in Homebrew, a popular package manager for macOS. The `--force` option is also known as the `-f` flag. It's used to override any user or system settings that would prevent Homebrew from installing or updating a particular package. When you use `brew up --force`, Homebrew will attempt to forcefully update the given formula even if there are dependencies with conflicting versions, or if the new version might break existing configurations.

            Use this command carefully as it can potentially cause unintended issues and instability in your system if used incorrectly. Always ensure you have a good backup before using `brew up --force`.
        help: '[AI] The `--help` option in the command `brew up` is used to display help information about the `brew up` command in Homebrew, a popular package manager for macOS. This help output explains what `brew up` does, its usage syntax, and provides examples. It also lists any available options or flags that can be passed to `brew up`. In summary, it is used to obtain detailed information on how to use `brew up` effectively in managing Homebrew installations.'
        merge: '[AI] `brew upgrade --merge` is a command used in Homebrew, a popular package manager for macOS. The command''s purpose is to merge any pending changes in the Homebrew repository with your currently installed versions of packages. This command ensures that your local Homebrew installation is updated to the latest stable version without losing any customizations or modifications you may have made to the installed packages. In essence, it combines the benefits of upgrading and keeping your customizations.'
        quiet: '[AI] The command `brew upgrade --quiet` is used with Homebrew, a popular package manager for macOS. This command performs an automatic update of all installed Homebrew packages without displaying any progress or informational messages during the process. By using the `--quiet` flag, the output is minimized, making it useful for running the upgrade in the background or in scripts where you don''t want the extra noise.'
        verbose: '[AI] The command `brew up --verbose` is used in Homebrew, a popular package manager for macOS, to update and install software from its repository. This specific command with the `--verbose` option enables detailed output during the process, providing more information about each step taken. It''s useful for troubleshooting or understanding what actions Homebrew is performing behind the scenes.'
