name: delete
description: '*(ALPHA)*  Mark the Service Perimeter as deleted in the dry-run mode.'
flags:
    --async: Return immediately, without waiting for the operation in
    --no-async&: Return immediately, without waiting for the operation in
    --policy=: The ID of the access policy.
documentation:
    command: '*(ALPHA)*  When this command completed successfully, the affected Service Perimeter will be considered to have been deleted in the dry-run mode, but the enforcement mode configuration will be left untouched.'
    flag:
        async: |-
            Return immediately, without waiting for the operation in
                            progress to complete.
        no-async: |-
            Return immediately, without waiting for the operation in
                            progress to complete.
        policy: |-
            The ID of the access policy.
            +
            To set the `policy` attribute:
            * provide the argument `perimeter` on the command line with a fully specified name;
            * provide the argument `--policy` on the command line;
            * set the property `access_context_manager/policy`.
