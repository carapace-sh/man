name: delete-cluster-scheduler-config
description: Deletes the cluster policy of the cluster.
flags:
    --cluster-scheduler-config-id=!: ID of the cluster policy.
documentation:
    command: Deletes the cluster policy of the cluster.
    flag:
        cluster-scheduler-config-id: ID of the cluster policy.
