# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: Retrieves the list of all Azure Cosmos DB Mongo User Definition.
flags:
    --subscription=: Name or ID of subscription.
    -a, --account-name=!: Cosmosdb account name.
    -g, --resource-group=!: Name of resource group.
documentation:
    command: List all MongoDb user definitions under an Azure Cosmos DB account.
    flag:
        account-name: Cosmosdb account name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List all Mongodb user definitions under an Azure Cosmos DB account.: az cosmosdb mongodb user definition list --account-name MyAccount --resource-group MyResourceGroup
