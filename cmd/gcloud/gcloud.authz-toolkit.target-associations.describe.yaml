name: describe
description: Describe a target association.
hidden: true
flags:
    --location=: The location Id.
documentation:
    command: |
        Show details of the specified target association.
    flag:
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `target_association` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
