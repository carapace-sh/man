name: stop-application
description: Stops a specified application and releases initial capacity if configured.
flags:
    --application-id=!: The ID of the application to stop.
documentation:
    command: Stops a specified application and releases initial capacity if configured. All scheduled and running jobs must be completed or cancelled before stopping an application.
    flag:
        application-id: The ID of the application to stop.
