# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: unmark
description: Removes all marks from the workspace
group: branching and committing
flags:
    -h, --help: Print help
documentation:
    command: This command removes all auto-assign and auto-commit rules (marks) from the workspace.

