name: describe
description: '*(BETA)*  Show details about a Compute Engine reservation.'
flags:
    --zone=: The name of the Google Compute Engine zone.
documentation:
    command: '*(BETA)*  Show details about a Compute Engine reservation.'
    flag:
        zone: |-
            The name of the Google Compute Engine zone.
            +
            To set the `zone` attribute:
            * provide the argument `reservation` on the command line with a fully specified name;
            * provide the argument `--zone` on the command line;
            * set the property `compute/zone`.
