name: delete-compute-quota
description: Deletes the compute allocation from the cluster.
flags:
    --compute-quota-id=!: ID of the compute allocation definition.
documentation:
    command: Deletes the compute allocation from the cluster.
    flag:
        compute-quota-id: ID of the compute allocation definition.
