name: import
description: '*(BETA)*  Import a forwarding rule.'
flags:
    --global: If set, the forwarding rule is global.
    --no-global&: If set, the forwarding rule is global.
    --region=: Region of the forwarding rule to import.
    --source=: Path to a YAML file containing configuration export data.
documentation:
    command: |
        *(BETA)*  Imports a forwarding rule's configuration from a file.
    flag:
        global: If set, the forwarding rule is global.
        no-global: If set, the forwarding rule is global.
        region: |-
            Region of the forwarding rule to import. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/region'' property:
            +
              $ gcloud config set compute/region REGION
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/region
            +
            Alternatively, the region can be stored in the environment
            variable ``CLOUDSDK_COMPUTE_REGION''.
        source: |-
            Path to a YAML file containing configuration export data.
                      Alternatively, you may omit this flag to read from standard input.For a schema describing the export/import format, see:
                      $CLOUDSDKROOT/lib/googlecloudsdk/schemas/compute/beta/ForwardingRule.yaml.
            +

                  Note: $CLOUDSDKROOT represents the Google Cloud CLI's installation directory.
