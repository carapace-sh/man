# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: init
description: Initialize this project as a Hugo Module.
documentation:
    command: '[AI] `hugo mod init` is a Hugo command used to create a new Go module in the Hugo project structure. It initializes a new Go module with the specified name and sets up the necessary directories, files, and configuration for managing dependencies using the Go Mod system.'
