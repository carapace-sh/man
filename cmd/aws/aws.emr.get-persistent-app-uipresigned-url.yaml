name: get-persistent-app-uipresigned-url
description: The presigned URL properties for the cluster's application user interface.
flags:
    --application-id=: The application ID associated with the presigned URL.
    --auth-proxy-call=: A boolean that represents if the caller is an authentication proxy call.
    --execution-role-arn=: The execution role ARN associated with the presigned URL.
    --persistent-app-uiid=!: The persistent application user interface ID associated with the presigned URL.
    --persistent-app-uitype=: The persistent application user interface type associated with the presigned URL.
completion:
    flag:
        persistent-app-uitype:
            - SHS
            - TEZ
            - YTS
documentation:
    command: The presigned URL properties for the cluster's application user interface.
    flag:
        application-id: The application ID associated with the presigned URL.
        auth-proxy-call: A boolean that represents if the caller is an authentication proxy call.
        execution-role-arn: The execution role ARN associated with the presigned URL.
        persistent-app-uiid: The persistent application user interface ID associated with the presigned URL.
        persistent-app-uitype: The persistent application user interface type associated with the presigned URL.
