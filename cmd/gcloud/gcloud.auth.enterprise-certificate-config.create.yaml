name: create
description: Create enterprise certificate configurations.
documentation:
    command: |-
        The gcloud auth enterprise-certificate-config create group lets you create enterprise certificate configurations.
        This configuration will be used by gcloud to communicate with the
        enterprise-certificate-proxy.

        See more details at `gcloud topic client-certificate`.
