name: set-iam-policy
description: Set the IAM policy for a Compute Engine instance template.
documentation:
    command: |
        Sets the IAM policy for the given instance template as defined in a JSON or YAML file.
