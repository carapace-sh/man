name: delete
description: '*(BETA)*  Delete Compute Engine wire groups.'
flags:
    --cross-site-network=!: Name of the crossSiteNetwork to operate on.
documentation:
    command: |
        *(BETA)*  *gcloud beta compute interconnects wire-groups delete* is used to delete wire groups.

        For an example, refer to the *EXAMPLES* section below.
    flag:
        cross-site-network: Name of the crossSiteNetwork to operate on.
