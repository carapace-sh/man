name: start-associations-once
description: Runs an association immediately and only one time.
flags:
    --association-ids=!: The association IDs that you want to run immediately and only one time.
documentation:
    command: Runs an association immediately and only one time. This operation can be helpful when troubleshooting associations.
    flag:
        association-ids: The association IDs that you want to run immediately and only one time.
