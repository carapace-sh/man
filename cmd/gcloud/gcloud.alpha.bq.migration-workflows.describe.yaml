name: describe
description: '*(ALPHA)*  Describe migration workflows.'
flags:
    --location=: The location id of the workflow resource.
documentation:
    command: '*(ALPHA)*  Describe a migration workflow'
    flag:
        location: |-
            The location id of the workflow resource.
            +
            To set the `location` attribute:
            * provide the argument `workflow` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
