name: set-iam-policy
description: '*(BETA)*  Set the IAM policy of a hub resource.'
documentation:
    command: |
        *(BETA)*  Replace the existing IAM policy of a hub resource with a policy
        encoded in a JSON or YAML file.
