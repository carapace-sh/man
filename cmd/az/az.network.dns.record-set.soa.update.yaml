# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: Update properties of an SOA record.
flags:
    --if-none-match=: Create the record set only if it does not already exist.
    --subscription=: Name or ID of subscription.
    -e, --email=: Email address.
    -f, --refresh-time=: Refresh value (seconds).
    -g, --resource-group=!: Name of resource group.
    -m, --minimum-ttl=: Minimum TTL (time-to-live, seconds).
    -r, --retry-time=: Retry time (seconds).
    -s, --serial-number=: Serial number.
    -t, --host=: Host name.
    -x, --expire-time=: Expire time (seconds).
    -z, --zone-name=!: The name of the zone.
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        email: Email address.
        expire-time: Expire time (seconds).
        host: Host name.
        if-none-match: Create the record set only if it does not already exist.
        minimum-ttl: |-
            Minimum TTL (time-to-live, seconds).

            Default value: 3600
        refresh-time: Refresh value (seconds).
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        retry-time: Retry time (seconds).
        serial-number: Serial number.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        zone-name: The name of the zone.
examples:
    Update properties of an SOA record.: |-
        az network dns record-set soa update -g MyResourceGroup -z www.mysite.com \
            -e myhostmaster.mysite.com
    Update properties of an SOA record. (autogenerated): az network dns record-set soa update --email myhostmaster.mysite.com --only-show-errors --resource-group MyResourceGroup --subscription MySubscription --zone-name www.mysite.com
