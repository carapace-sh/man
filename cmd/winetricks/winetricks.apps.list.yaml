# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: list verbs
documentation:
    command: '[AI] `winetricks apps list` is a command used in Wine, a compatibility layer for running Windows applications on Unix-based systems. This command displays a list of installed Windows applications managed by Winetricks, a graphical user interface for configuring and installing various Windows software and runtime components using Wine. The output provides the name and version number for each application.'
