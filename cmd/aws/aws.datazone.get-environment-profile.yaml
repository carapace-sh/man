name: get-environment-profile
description: Gets an evinronment profile in Amazon DataZone.
flags:
    --domain-identifier=!: The ID of the Amazon DataZone domain in which this environment profile exists.
    --identifier=!: The ID of the environment profile.
documentation:
    command: Gets an evinronment profile in Amazon DataZone.
    flag:
        domain-identifier: The ID of the Amazon DataZone domain in which this environment profile exists.
        identifier: The ID of the environment profile.
