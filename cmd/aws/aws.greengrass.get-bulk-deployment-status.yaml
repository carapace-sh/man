name: get-bulk-deployment-status
description: Returns the status of a bulk deployment.
flags:
    --bulk-deployment-id=!: The ID of the bulk deployment.
documentation:
    command: Returns the status of a bulk deployment.
    flag:
        bulk-deployment-id: The ID of the bulk deployment.
