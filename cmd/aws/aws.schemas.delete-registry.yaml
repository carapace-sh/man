name: delete-registry
description: Deletes a Registry.
flags:
    --registry-name=!: The name of the registry.
documentation:
    command: Deletes a Registry.
    flag:
        registry-name: The name of the registry.
