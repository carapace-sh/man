name: describe
description: '*(ALPHA)*  Describe a multicast group definition.'
flags:
    --location=: The location Id.
documentation:
    command: |
        *(ALPHA)*  Show details of a multicast group definition in the specified location of the current project.
    flag:
        location: |-
            The location Id.
            +
            To set the `location` attribute:
            * provide the argument `multicast_group_definition` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
