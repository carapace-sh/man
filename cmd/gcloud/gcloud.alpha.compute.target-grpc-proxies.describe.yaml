name: describe
description: '*(ALPHA)*  Display detailed information about a target gRPC proxy.'
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute target-grpc-proxies describe* displays all data associated with a Compute Engine
        target gRPC proxy.
