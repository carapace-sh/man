name: get-subscription
description: Gets a subscription in Amazon DataZone.
flags:
    --domain-identifier=!: The ID of the Amazon DataZone domain in which the subscription exists.
    --identifier=!: The ID of the subscription.
documentation:
    command: Gets a subscription in Amazon DataZone.
    flag:
        domain-identifier: The ID of the Amazon DataZone domain in which the subscription exists.
        identifier: The ID of the subscription.
