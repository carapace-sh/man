# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: close-handle
description: Close file handles of a file share.
flags:
    --account-key=: Storage account key.
    --account-name=: Storage account name.
    --auth-mode=: The mode in which to run the command.
    --close-all=:
        description: Whether or not to close all the file handles.
        nargs: -1
    --connection-string=: Storage account connection string.
    --disallow-trailing-dot=:
        description: If true, the trailing dot will be trimmed from the target URI.
        nargs: -1
    --enable-file-backup-request-intent=: Required parameter to use with OAuth (Azure AD) Authentication for Files.
    --file-endpoint=: Storage data service endpoint.
    --handle-id=: Specifies handle ID opened on the file or directory to be closed.
    --recursive=:
        description: Boolean that specifies if operation should apply to the directory specified in the URI, its files, with its subdirectories and their files.
        nargs: -1
    --sas-token=: A Shared Access Signature (SAS).
    --snapshot=: A string that represents the snapshot version, if applicable.
    --subscription=: Name or ID of subscription.
    --timeout=: Request timeout in seconds.
    -n, --name=!: The file share name.
    -p, --path=: The path to the file/directory within the file share.
completion:
    flag:
        auth-mode:
            - login
            - key
        close-all:
            - "true"
            - "false"
        disallow-trailing-dot:
            - "true"
            - "false"
        recursive:
            - "true"
            - "false"
documentation:
    flag:
        account-key: 'Storage account key. Must be used in conjunction with storage account name or service endpoint. Environment variable: AZURE_STORAGE_KEY.'
        account-name: 'Storage account name. Related environment variable: AZURE_STORAGE_ACCOUNT. Must be used in conjunction with either storage account key or a SAS token. If neither are present, the command will try to query the storage account key using the authenticated Azure account. If a large number of storage commands are executed the API quota may be hit.'
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        auth-mode: |-
            The mode in which to run the command. "login" mode will directly use your login credentials for the authentication. The legacy "key" mode will attempt to query for an account key if no authentication parameters for the account are provided. Environment variable: AZURE_STORAGE_AUTH_MODE.

            Accepted values: key, login

            Default value: False
        change-reference: The related change reference ID for this resource operation.
        close-all: |-
            Whether or not to close all the file handles. Specify close-all or a specific handle-id.

            Accepted values: false, true
        connection-string: 'Storage account connection string. Environment variable: AZURE_STORAGE_CONNECTION_STRING.'
        disallow-trailing-dot: |-
            If true, the trailing dot will be trimmed from the target URI. Default to False.

            Default value: False

            Accepted values: false, true
        enable-file-backup-request-intent: Required parameter to use with OAuth (Azure AD) Authentication for Files. This will bypass any file/directory level permission checks and allow access, based on the allowed data actions, even if there are ACLs in place for those files/directories.
        file-endpoint: 'Storage data service endpoint. Must be used in conjunction with either storage account key or a SAS token. You can find each service primary endpoint with ``` az storage account show ``` . Environment variable: AZURE_STORAGE_SERVICE_ENDPOINT.'
        handle-id: Specifies handle ID opened on the file or directory to be closed. Astrix (‘*’) is a wildcard that specifies all handles.
        name: The file share name.
        path: The path to the file/directory within the file share.
        recursive: |-
            Boolean that specifies if operation should apply to the directory specified in the URI, its files, with its subdirectories and their files.

            Accepted values: false, true
        sas-token: 'A Shared Access Signature (SAS). Must be used in conjunction with storage account name or service endpoint. Environment variable: AZURE_STORAGE_SAS_TOKEN.'
        snapshot: A string that represents the snapshot version, if applicable.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        timeout: Request timeout in seconds. Applies to each call to the service.
examples:
    Close all file handles of a file directory recursively.: az storage share close-handle --account-name MyAccount --name MyFileShare --path 'dir1' --close-all --recursive
    Close all file handles of a file share recursively.: |-
        az storage share close-handle --account-name MyAccount --name MyFileShare --close-all --recursive
        az storage share close-handle --account-name MyAccount --name MyFileShare --handle-id "*" --recursive
    Close all file handles of a file.: az storage share close-handle --account-name MyAccount --name MyFileShare --path 'dir1/test.txt' --close-all
    Close file handle with a specific handle-id of a file.: az storage share close-handle --account-name MyAccount --name MyFileShare --path 'dir1/test.txt' --handle-id "id"
