name: list-tracks
description: List the Amazon Redshift Serverless versions.
flags:
    --max-results=: The maximum number of response records to return in each call.
    --next-token=: If your initial `ListTracksRequest` operation returns a `nextToken`, you can include the returned `nextToken` in following `ListTracksRequest` operations, which returns results in the next page.
documentation:
    command: List the Amazon Redshift Serverless versions.
    flag:
        max-results: The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified MaxRecords value, a value is returned in a marker field of the response. You can retrieve the next set of records by retrying the command with the returned marker value.
        next-token: If your initial `ListTracksRequest` operation returns a `nextToken`, you can include the returned `nextToken` in following `ListTracksRequest` operations, which returns results in the next page.
