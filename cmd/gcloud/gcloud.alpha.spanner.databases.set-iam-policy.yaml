name: set-iam-policy
description: '*(ALPHA)*  Set the IAM policy for a Cloud Spanner database.'
flags:
    --instance=: The Cloud Spanner instance for the database.
documentation:
    command: '*(ALPHA)*  Set the IAM policy for a Cloud Spanner database.'
    flag:
        instance: |-
            The Cloud Spanner instance for the database.
            +
            To set the `instance` attribute:
            * provide the argument `database` on the command line with a fully specified name;
            * provide the argument `--instance` on the command line;
            * set the property `spanner/instance`.
