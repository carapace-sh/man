name: describe
description: '*(BETA)*  Describe the specified guest policy.'
documentation:
    command: |-
        ## EXAMPLES

          To describe the guest policy `policy1` in the current project, run:

                $ gcloud beta compute os-config guest-policies describe policy1
