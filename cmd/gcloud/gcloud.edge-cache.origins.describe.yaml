name: describe
description: Show details about an EdgeCacheOrigin resource.
flags:
    --location=: The location Id.
documentation:
    command: Show details about an EdgeCacheOrigin resource.
    flag:
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `origin` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * use global location.
