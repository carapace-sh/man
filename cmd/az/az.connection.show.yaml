# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Get the details of a Service Connector local connection.
flags:
    --connection=: Name of the connection.
    --id=: The id of connection.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -l, --location=: Location.
documentation:
    flag:
        connection: Name of the connection.
        id: The id of connection.
        location: 'Location. Values from: ``` az account list-locations ``` . You can configure the default location using ``` az configure --defaults location=<location> ``` .'
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Get a connection by connection id: az connection show --id /subscriptions/{subscriptionId}/resourcegroups/{resourceGroupName}/providers/Microsoft.ServiceLinker/locations/{location}/connectors/{connectorName}
    Get a connection by connection name: az connection show -g resourceGroup --connection MyConnection
    Get a connection interactively: az connection show
