name: commit
description: '*(BETA)*  Commit a Pub/Sub schema revision.'
flags:
    --definition-file=: File containing the new schema definition.
    --definition=: The new definition of the schema.
    --type=!: The type of the schema.
documentation:
    command: '*(BETA)*  Commit a Pub/Sub schema revision.'
    flag:
        definition: The new definition of the schema.
        definition-file: File containing the new schema definition. Use a full or relative path to a local file containing the value of definition_file.
        type: The type of the schema.
