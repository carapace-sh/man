name: delete
description: '*(ALPHA)*  Delete the IAP TCP Destination Group resource.'
flags:
    --region=!: Region of the Destination Group.
documentation:
    command: '*(ALPHA)*  Delete the IAP TCP Destination Group resource.'
    flag:
        region: Region of the Destination Group.
