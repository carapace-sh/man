name: register-targets
description: Registers the targets with the target group.
flags:
    --target-group-identifier=!: The ID or ARN of the target group.
    --targets=!: The targets.
documentation:
    command: Registers the targets with the target group. If it's a Lambda target, you can only have one target in a target group.
    flag:
        target-group-identifier: The ID or ARN of the target group.
        targets: The targets.
