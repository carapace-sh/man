name: set-iam-policy
description: '*(BETA)*  Set the IAM policy for a Compute Engine instance template.'
documentation:
    command: |
        *(BETA)*  Sets the IAM policy for the given instance template as defined in a JSON or YAML file.
