name: tag-resource
description: Associates the list of tags in the input `Tags` parameter with the resource identified by the `ResourceArn` input parameter.
flags:
    --resource-arn=!: The ARN of a resource, such as a CodeDeploy application or deployment group.
    --tags=!: A list of tags that `TagResource` associates with a resource.
documentation:
    command: Associates the list of tags in the input `Tags` parameter with the resource identified by the `ResourceArn` input parameter.
    flag:
        resource-arn: The ARN of a resource, such as a CodeDeploy application or deployment group.
        tags: A list of tags that `TagResource` associates with a resource. The resource is identified by the `ResourceArn` input parameter.
