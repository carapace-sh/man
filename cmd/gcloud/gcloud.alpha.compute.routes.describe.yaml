name: describe
description: '*(ALPHA)*  Describe a route.'
documentation:
    command: |-
        *(ALPHA)*  *gcloud alpha compute routes describe* displays all data associated with a Compute
        Engine route in a project.

        ## EXAMPLES

        To describe a route with the name 'route-name', run:

          $ gcloud alpha compute routes describe route-name
