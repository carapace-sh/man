name: detect-landmarks
description: '*(BETA)*  Detect popular natural and man-made structures within an image.'
flags:
    --max-results=: Maximum number of results to be provided.
    --model-version=: Model version to use for the feature.
completion:
    flag:
        model-version:
            - builtin/latest
            - builtin/stable
documentation:
    command: |
        *(BETA)*  Google Cloud Vision will recognize landmarks in an image, such as "Palace
        of Fine Arts."
    flag:
        max-results: Maximum number of results to be provided.
        model-version: 'Model version to use for the feature. _MODEL_VERSION_ must be one of: *builtin/latest*, *builtin/stable*.'
