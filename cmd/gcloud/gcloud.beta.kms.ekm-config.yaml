name: ekm-config
description: '*(BETA)*  Update and retrieve the EkmConfig.'
documentation:
    command: |-
        *(BETA)*  EkmConfig is a singleton resource that represents configuration
        parameters that apply to all CryptoKeys and CryptoKeyVersions of EXTERNAL_VPC
        protecion level.
