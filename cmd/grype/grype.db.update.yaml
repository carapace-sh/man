# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: download the latest vulnerability database
documentation:
    command: '[AI] The `grype db update` command is used in Grype, a vulnerability scanning tool for container images, to fetch the latest vulnerability database and update the local database. This ensures that your scans will include the most recent security findings.'
