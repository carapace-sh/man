# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all the databases in a given server.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -s, --server-name=!: Name of the server.
documentation:
    command: List the databases for a flexible server.
    flag:
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        server-name: Name of the server. The name can contain only lowercase letters, numbers, and the hyphen (-) character. Minimum 3 characters and maximum 63 characters.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List databases in the flexible server 'testserver' in table format.: az mysql flexible-server db list --resource-group testGroup --server-name testserver --output table
    List databases in the flexible server 'testserver'.: az mysql flexible-server db list --resource-group testGroup --server-name testserver
