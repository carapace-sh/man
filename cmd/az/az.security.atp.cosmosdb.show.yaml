# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Display Advanced Threat Protection settings for an Azure Cosmos DB account.
flags:
    --cosmosdb-account=!: Name of an existing Cosmos DB account.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
documentation:
    flag:
        cosmosdb-account: Name of an existing Cosmos DB account.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Retrieve Advanced Threat Protection settings for an Azure Cosmos DB account on a subscription scope.: az security atp cosmosdb show --resource-group MyResourceGroup --cosmosdb-account MyCosmosDbAccount
