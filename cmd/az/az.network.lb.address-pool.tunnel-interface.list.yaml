# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all tunnel interfacees of the load balance tunnel interface pool.
flags:
    --address-pool=!: The name of the backend address pool.
    --lb-name=!: The name of the load balancer.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
documentation:
    command: |-
        Command group 'az network lb address-pool tunnel-interface' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        lb-name: The name of the load balancer.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List all tunnel interfacees of the load balance tunnel interface pool.: az network lb address-pool tunnel-interface list -g MyResourceGroup --lb-name MyLb --address-pool MyAddressPool
