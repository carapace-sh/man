name: update
description: '*(ALPHA)*  Update a certificate.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --certificate-file=!: The certificate data in PEM-encoded form.
    --clear-labels: Remove all labels.
    --description=: Text description of a certificate.
    --location=: The Cloud location for the certificate.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-clear-labels&: Remove all labels.
    --private-key-file=!: The private key data in PEM-encoded form.
    --remove-labels=: List of label keys to remove.
    --update-labels=: List of label KEY=VALUE pairs to update.
documentation:
    command: |-
        *(ALPHA)*  This command updates existing certificate.

        ## EXAMPLES

        To update a certificate with name simple-cert, run:

          $ gcloud alpha certificate-manager certificates update simple-cert --description="desc" --update-labels="key=value"
              --certificate-file=cert.pem --private-key-file=key.pem
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        certificate-file: The certificate data in PEM-encoded form. Use a full or relative path to a local file containing the value of certificate_file.
        clear-labels: |-
            Remove all labels. If `--update-labels` is also specified then
            `--clear-labels` is applied first.
            +
            For example, to remove all labels:
            +
                $ {command} --clear-labels
            +
            To remove all existing labels and create two new labels,
            ``foo'' and ``baz'':
            +
                $ {command} --clear-labels --update-labels foo=bar,baz=qux
        description: Text description of a certificate.
        location: |-
            The Cloud location for the certificate.
            +
            To set the `location` attribute:
            * provide the argument `certificate` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * default value of location is [global].
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-clear-labels: |
            Remove all labels. If `--update-labels` is also specified then
            `--clear-labels` is applied first.

            For example, to remove all labels:

                $ {command} --clear-labels

            To remove all existing labels and create two new labels,
            ``foo'' and ``baz'':

                $ {command} --clear-labels --update-labels foo=bar,baz=qux
        private-key-file: The private key data in PEM-encoded form. Use a full or relative path to a local file containing the value of private_key_file.
        remove-labels: |-
            List of label keys to remove. If a label does not exist it is
            silently ignored. If `--update-labels` is also specified then
            `--update-labels` is applied first.
        update-labels: |-
            List of label KEY=VALUE pairs to update. If a label exists, its value is modified. Otherwise, a new label is created.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
