name: inject-fault
description: Inject fault on an AlloyDB instance within a given cluster.
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --cluster=!: AlloyDB cluster ID
    --fault-type=!: Specifies fault type.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --region=!: Regional location (e.g. `asia-east1`, `us-east1`).
completion:
    flag:
        fault-type:
            - stop-vm
documentation:
    command: Inject fault on an AlloyDB instance within a given cluster.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        cluster: AlloyDB cluster ID
        fault-type: |-
            Specifies fault type. _FAULT_TYPE_ must be (only one value is supported):
            +
            *stop-vm*::: stop-vm fault type supports stopping the VM.
            :::
            +
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        region: Regional location (e.g. `asia-east1`, `us-east1`). See the full list of regions at https://cloud.google.com/sql/docs/instance-locations.
