name: describe-user-profile
description: Describes a user profile.
flags:
    --domain-id=!: The domain ID.
    --user-profile-name=!: The user profile name.
documentation:
    command: Describes a user profile. For more information, see `CreateUserProfile`.
    flag:
        domain-id: The domain ID.
        user-profile-name: The user profile name. This value is not case sensitive.
