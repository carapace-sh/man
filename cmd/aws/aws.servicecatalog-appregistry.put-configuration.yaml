name: put-configuration
description: Associates a `TagKey` configuration to an account.
flags:
    --configuration=!: Associates a `TagKey` configuration to an account.
documentation:
    command: Associates a `TagKey` configuration to an account.
    flag:
        configuration: Associates a `TagKey` configuration to an account.
