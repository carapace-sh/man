# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: list verbs
documentation:
    command: '[AI] `winetricks games list` is a command used in Wine, a compatibility layer for running Windows applications on Linux and macOS. This specific command displays a list of installed Windows games managed by Winetricks, a graphical user interface for managing Wine configurations. The output shows the game titles along with their installation directories.'
