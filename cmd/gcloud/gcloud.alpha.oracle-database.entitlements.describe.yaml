name: describe
description: '*(ALPHA)*  Describe entitlements.'
hidden: true
flags:
    --location=: The location id of the entitlement resource.
documentation:
    command: '*(ALPHA)*  Describe an entitlement'
    flag:
        location: |-
            The location id of the entitlement resource.
            +
            To set the `location` attribute:
            * provide the argument `entitlement` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
