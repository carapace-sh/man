name: get-access-token
description: Returns a credentials set to be used with just-in-time node access.
flags:
    --access-request-id=!: The ID of a just-in-time node access request.
documentation:
    command: Returns a credentials set to be used with just-in-time node access.
    flag:
        access-request-id: The ID of a just-in-time node access request.
