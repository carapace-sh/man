name: update-pipeline
description: Updates the settings of a pipeline.
flags:
    --pipeline-activities=!: A list of `PipelineActivity` objects.
    --pipeline-name=!: The name of the pipeline to update.
documentation:
    command: Updates the settings of a pipeline. You must specify both a `channel` and a `datastore` activity and, optionally, as many as 23 additional activities in the `pipelineActivities` array.
    flag:
        pipeline-activities: |-
            A list of `PipelineActivity` objects. Activities perform transformations on your messages, such as removing, renaming or adding message attributes; filtering messages based on attribute values; invoking your Lambda functions on messages for advanced processing; or performing mathematical transformations to normalize device data.

            The list can be 2-25 `PipelineActivity` objects and must contain both a `channel` and a `datastore` activity. Each entry in the list must contain only one activity. For example:

            `pipelineActivities = [ { "channel": { ... } }, { "lambda": { ... } }, ... ]`
        pipeline-name: The name of the pipeline to update.
