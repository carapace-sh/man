name: restore
description: '*(ALPHA)*  Restore an AlloyDB cluster from a given backup or a source cluster and a timestamp.'
flags:
    --allocated-ip-range-name=: 'Name of the allocated IP range for the private IP AlloyDB cluster, for example: "google-managed-services-default".'
    --async: Return immediately, without waiting for the operation in progress to
    --backup=: AlloyDB backup to restore from.
    --backupdr-backup=: Backup DR backup to restore from.
    --backupdr-data-source=: Backup DR data source to restore from.
    --enable-private-service-connect: Enable Private Service Connect (PSC) connectivity for the cluster.
    --kms-key=!: ID of the key or fully qualified identifier for the key.
    --kms-keyring=: The KMS keyring of the key.
    --kms-location=: The Google Cloud location for the key.
    --kms-project=: The Google Cloud project for the key.
    --network=: Network in the current project that the instance will be part of.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-enable-private-service-connect&: Enable Private Service Connect (PSC) connectivity for the cluster.
    --point-in-time=!: Point in time to restore to, in RFC 3339 format.
    --region=!: Regional location (e.g. `asia-east1`, `us-east1`).
    --source-cluster=: AlloyDB source cluster to restore from.
    --tags=: List of tags KEY=VALUE pairs to bind.
documentation:
    command: '*(ALPHA)*  Restore an AlloyDB cluster from a given backup or a source cluster and a timestamp.'
    flag:
        allocated-ip-range-name: 'Name of the allocated IP range for the private IP AlloyDB cluster, for example: "google-managed-services-default". If set, the instance IPs for this cluster will be created in the allocated range. The range name must comply with RFC 1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?`.'
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        backup: AlloyDB backup to restore from. This must either be the full backup name (projects/myProject/locations/us-central1/backups/myBackup) or the backup ID (myBackup). In the second case, the project and location are assumed to be the same as the restored cluster that is being created.
        backupdr-backup: Backup DR backup to restore from. This is a resource path of the form projects/myProject/locations/us-central1/backupVaults/myBackupVault/dataSources/myDataSource/backups/myBackup.
        backupdr-data-source: Backup DR data source to restore from. This is a resource path of the form projects/myProject/locations/us-central1/backupVaults/myBackupVault/dataSources/myDataSource.
        enable-private-service-connect: Enable Private Service Connect (PSC) connectivity for the cluster.
        kms-key: |-
            ID of the key or fully qualified identifier for the key.
            +
            To set the `kms-key` attribute:
            * provide the argument `--kms-key` on the command line.
        kms-keyring: |-
            The KMS keyring of the key.
            +
            To set the `kms-keyring` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-keyring` on the command line.
        kms-location: |-
            The Google Cloud location for the key.
            +
            To set the `kms-location` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-location` on the command line.
        kms-project: |-
            The Google Cloud project for the key.
            +
            To set the `kms-project` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-project` on the command line;
            * set the property `core/project`.
        network: Network in the current project that the instance will be part of. To specify using a network with a shared VPC, use the full URL of the network. For an example host project, `testproject`, and shared network, `testsharednetwork`, this would be of the form:`--network=projects/testproject/global/networks/testsharednetwork`
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-enable-private-service-connect: Enable Private Service Connect (PSC) connectivity for the cluster.
        point-in-time: Point in time to restore to, in RFC 3339 format. For example, 2012-11-15T16:19:00.094Z.
        region: Regional location (e.g. `asia-east1`, `us-east1`). See the full list of regions at https://cloud.google.com/sql/docs/instance-locations.
        source-cluster: AlloyDB source cluster to restore from. This must either be the full cluster name (projects/myProject/locations/us-central1/backups/myCluster) or the cluster ID (myCluster). In the second case, the project and location are assumed to be the same as the restored cluster that is being created.
        tags: |-
            List of tags KEY=VALUE pairs to bind.
            Each item must be expressed as
            `<tag-key-namespaced-name>=<tag-value-short-name>`.
            +
            Example: `123/environment=production,123/costCenter=marketing`
