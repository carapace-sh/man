# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: Lists the Cassandra table under an existing Azure Cosmos DB database account.
flags:
    --subscription=: Name or ID of subscription.
    -a, --account-name=!: Cosmosdb account name.
    -g, --resource-group=!: Name of resource group.
    -k, --keyspace-name=!: Keyspace name
documentation:
    command: List the Cassandra tables under an Azure Cosmos DB Cassandra keyspace.
    flag:
        account-name: Cosmosdb account name.
        keyspace-name: Keyspace name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List the Cassandra tables under an Azure Cosmos DB Cassandra keyspace. (autogenerated): az cosmosdb cassandra table list --account-name MyAccount --keyspace-name MyKeyspace --resource-group MyResourceGroup
