name: update-service-settings
description: Updates settings configured for Quick Setup.
flags:
    --explorer-enabling-role-arn=: The IAM role used to enable Explorer.
documentation:
    command: Updates settings configured for Quick Setup.
    flag:
        explorer-enabling-role-arn: The IAM role used to enable Explorer.
