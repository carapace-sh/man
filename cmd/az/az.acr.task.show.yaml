# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Get the properties of a named task for an Azure Container Registry.
flags:
    --subscription=: Name or ID of subscription.
    --with-secure-properties=: Indicates whether the secure properties of a task should be returned.
    -g, --resource-group=: Name of resource group.
    -n, --name=!: The name of the task.
    -r, --registry=!: The name of the container registry.
documentation:
    flag:
        name: The name of the task.
        registry: The name of the container registry. It should be specified in lower case. You can configure the default registry name using ``` az configure --defaults acr=<registry name> ``` .
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        with-secure-properties: |-
            Indicates whether the secure properties of a task should be returned.

            Default value: False
examples:
    Get the properties of a task, displaying the results in a table.: az acr task show -n MyTask -r myregistry -o table
    Get the properties of a task, including secure properties.: az acr task show -n MyTask -r myregistry --with-secure-properties
