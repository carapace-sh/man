name: describe
description: Returns details of Anywhere Cache instance of a bucket.
flags:
    --no-raw&: Shows metadata in the format returned by the API instead of standardizing it.
    --raw: Shows metadata in the format returned by the API instead of standardizing it.
documentation:
    command: |4
        Desribes a single Anywhere Cache instance if it exists.
    flag:
        no-raw: Shows metadata in the format returned by the API instead of standardizing it.
        raw: Shows metadata in the format returned by the API instead of standardizing it.
