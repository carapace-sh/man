name: delete
description: '*(ALPHA)*  Delete a disk migration resource.'
flags:
    --location=: Resource location.
    --source=: The client source environment object.
documentation:
    command: |
        *(ALPHA)*  gcloud alpha compute migration disk-migrations delete deletes a disk migration resource.
        To use this command, you must enable VM Migration API in your project.

        This command does not delete any disks on Google Compute Engine.
    flag:
        location: |-
            Resource location.
            +
            To set the `location` attribute:
            * provide the argument `disk_migration` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `compute/region`.
        source: |-
            The client source environment object.
            +
            To set the `source` attribute:
            * provide the argument `disk_migration` on the command line with a fully specified name;
            * provide the argument `--source` on the command line.
