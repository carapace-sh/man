name: disassociate-applications
description: Disassociate applications from wave.
flags:
    --account-id=: Account ID.
    --application-ids=!: Application IDs list.
    --wave-id=!: Wave ID.
documentation:
    command: Disassociate applications from wave.
    flag:
        account-id: Account ID.
        application-ids: Application IDs list.
        wave-id: Wave ID.
