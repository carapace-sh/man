# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: auth
description: Inspect authorization
documentation:
    command: '[AI] `kubectl alpha auth` is a command used with the Kubernetes command-line interface (CLI) tool, `kubectl`. It''s part of the `alpha` features in `kubectl`, which are experimental and subject to change. This command is intended for managing RBAC authorization resources in a Kubernetes cluster, such as creating, updating, or deleting RoleBindings and ClusterRoleBindings. The exact syntax and usage may vary depending on the specific subcommand used (like `create`, `get`, etc.), so it''s best to consult the official Kubernetes documentation for the most up-to-date information.'
