name: describe
description: '*(BETA)*  Describe a route.'
documentation:
    command: |-
        *(BETA)*  *gcloud beta compute routes describe* displays all data associated with a Compute
        Engine route in a project.

        ## EXAMPLES

        To describe a route with the name 'route-name', run:

          $ gcloud beta compute routes describe route-name
