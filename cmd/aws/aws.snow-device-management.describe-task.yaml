name: describe-task
description: Checks the metadata for a given task on a device.
flags:
    --task-id=!: The ID of the task to be described.
documentation:
    command: Checks the metadata for a given task on a device.
    flag:
        task-id: The ID of the task to be described.
