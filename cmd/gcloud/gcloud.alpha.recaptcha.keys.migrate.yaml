name: migrate
description: '*(ALPHA)*  Migrate a key to reCAPTCHA Enterprise.'
flags:
    --no-skip-billing-check&: If true, skips the billing check.
    --skip-billing-check: If true, skips the billing check.
documentation:
    command: '*(ALPHA)*  Migrate a key from reCAPTCHA to reCAPTCHA Enterprise.'
    flag:
        no-skip-billing-check: |
            If true, skips the billing check. If your usage of reCAPTCHA is under
            the free quota, you can safely skip the billing check.
        skip-billing-check: |-
            If true, skips the billing check. If your usage of reCAPTCHA is under
            the free quota, you can safely skip the billing check.
