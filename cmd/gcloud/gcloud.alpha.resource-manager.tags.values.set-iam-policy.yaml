name: set-iam-policy
description: '*(ALPHA)*  Sets IAM policy for a TagValue resource.'
documentation:
    command: |-
        *(ALPHA)*  Sets the IAM policy for a TagValue resource given the TagValue's short name
        name and parent or the TagValue's numeric id and a file encoded in
        JSON or YAML that contains the IAM policy.
