# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List rewrite rule conditions.
flags:
    --gateway-name=!: Name of the application gateway.
    --rule-name=!: Name of the rewrite rule.
    --rule-set-name=!: Name of the rewrite rule set.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
documentation:
    flag:
        gateway-name: Name of the application gateway.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        rule-name: Name of the rewrite rule.
        rule-set-name: Name of the rewrite rule set.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List rewrite rule conditions.: az network application-gateway rewrite-rule condition list --gateway-name MyGateway --resource-group MyResourceGroup --rule-name MyRule --rule-set-name MyRuleSet
