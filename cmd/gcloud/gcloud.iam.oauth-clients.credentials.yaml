name: credentials
description: Create and manage OAuth client credentials.
documentation:
    command: |-
        The gcloud iam oauth-clients credentials group lets you create and manage OAuth client credentials for
        projects on the Google Cloud Platform.
