name: describe
description: '*(ALPHA)*  Describe a Policy Tag Taxonomy.'
flags:
    --location=: Location of the taxonomy.
documentation:
    command: '*(ALPHA)*  Describe a Policy Tag Taxonomy.'
    flag:
        location: |-
            Location of the taxonomy.
            +

            To set the `location` attribute:
            * provide the argument `taxonomy` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
