name: describe
description: Display all data about an existing instance.
flags:
    --service=!: The service ID.
documentation:
    command: Display all data about an existing instance.
    flag:
        service: The service ID.
