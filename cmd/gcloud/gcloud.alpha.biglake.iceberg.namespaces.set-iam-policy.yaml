name: set-iam-policy
description: '*(ALPHA)*  Set the IAM policy for a BigLake Iceberg REST catalog namespace.'
flags:
    --catalog=: The Iceberg Catalog for the resource.
documentation:
    command: '*(ALPHA)*  Sets the IAM policy for a BigLake Iceberg REST catalog namespace.'
    flag:
        catalog: |-
            The Iceberg Catalog for the resource.
            +
            To set the `catalog` attribute:
            * provide the argument `namespace` on the command line with a fully specified name;
            * provide the argument `--catalog` on the command line.
