name: describe-resource-server
description: Describes a resource server.
flags:
    --identifier=!: A unique resource server identifier for the resource server.
    --user-pool-id=!: The ID of the user pool that hosts the resource server.
documentation:
    command: Describes a resource server. For more information about resource servers, see [Access control with resource servers](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-define-resource-servers.html).
    flag:
        identifier: |-
            A unique resource server identifier for the resource server. The identifier can be an API friendly name like `solar-system-data`. You can also set an API URL like `https://solar-system-data-api.example.com` as your identifier.

            Amazon Cognito represents scopes in the access token in the format `$resource-server-identifier/$scope`. Longer scope-identifier strings increase the size of your access tokens.
        user-pool-id: The ID of the user pool that hosts the resource server.
