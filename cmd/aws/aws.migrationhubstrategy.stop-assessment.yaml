name: stop-assessment
description: Stops the assessment of an on-premises environment.
flags:
    --assessment-id=!: The `assessmentId` returned by [StartAssessment]().
documentation:
    command: Stops the assessment of an on-premises environment.
    flag:
        assessment-id: The `assessmentId` returned by [StartAssessment]().
