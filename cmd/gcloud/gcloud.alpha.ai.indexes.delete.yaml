name: delete
description: '*(ALPHA)*  Delete an existing Vertex AI index.'
flags:
    --region=: Cloud region for the index.
documentation:
    command: |-
        ## EXAMPLES

        To delete an index `123` of project `example` in region `us-central1`, run:

          $ gcloud alpha ai indexes delete 123 --project=example --region=us-central1
    flag:
        region: |-
            Cloud region for the index.
            +
            To set the `region` attribute:
            * provide the argument `index` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `ai/region`;
            * choose one from the prompted list of available regions.
