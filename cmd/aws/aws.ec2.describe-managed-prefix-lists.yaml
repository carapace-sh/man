name: describe-managed-prefix-lists
description: Describes your managed prefix lists and any Amazon Web Services-managed prefix lists.
flags:
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --filters=: One or more filters.
    --max-results=: The maximum number of results to return with a single call.
    --next-token=: The token for the next page of results.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --prefix-list-ids=: One or more prefix list IDs.
documentation:
    command: Describes your managed prefix lists and any Amazon Web Services-managed prefix lists.
    flag:
        dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is `DryRunOperation`. Otherwise, it is `UnauthorizedOperation`.
        filters: |-
            One or more filters.

            - `owner-id` - The ID of the prefix list owner.
            - `prefix-list-id` - The ID of the prefix list.
            - `prefix-list-name` - The name of the prefix list.
        max-results: The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned `nextToken` value.
        next-token: The token for the next page of results.
        prefix-list-ids: One or more prefix list IDs.
