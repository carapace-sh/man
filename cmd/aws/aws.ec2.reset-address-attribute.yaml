name: reset-address-attribute
description: Resets the attribute of the specified IP address.
flags:
    --allocation-id=!: \[EC2-VPC] The allocation ID.
    --attribute=!: The attribute of the IP address.
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --no-dry-run&: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
completion:
    flag:
        attribute:
            - domain-name
documentation:
    command: Resets the attribute of the specified IP address. For requirements, see [Using reverse DNS for email applications](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html#Using_Elastic_Addressing_Reverse_DNS).
    flag:
        allocation-id: \[EC2-VPC] The allocation ID.
        attribute: The attribute of the IP address.
        dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is `DryRunOperation`. Otherwise, it is `UnauthorizedOperation`.
