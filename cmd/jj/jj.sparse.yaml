# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: sparse
description: Manage which paths from the working-copy commit are present in the working copy
flags:
    -h, --help: Print help (see more with '--help')
documentation:
    command: '[AI] The `jj sparse` command is a shortcut in Vim or JetBrains'' IntelliJ IDEA editor for navigating through your code with ease. It moves the text cursor to the previous or next sparse comment, which is a type of comment that is only rendered when printed or displayed in a narrow context, such as in an outline or jump list. This command helps you quickly navigate between related parts of your code, especially when dealing with large files or complex projects.'
    flag:
        help: '[AI] `jj sparse --help` is a command used in Jupyter Notebooks or Jupyter Jupyter''s "jj" tool, which provides shortcuts for common notebook actions. The `--help` option displays detailed information about the "sparse" command. It likely modifies or creates sparse data structures, such as sparse matrices or arrays in Python, to save storage space by storing only non-zero values and their locations. However, without accessing the specific implementation of "jj sparse", this explanation is based on general knowledge about Jupyter Notebooks and sparse data structures.'
