name: enable-user
description: Enables a user in the user pool.
flags:
    --authentication-type=!: The authentication type for the user.
    --user-name=!: The email address of the user.
completion:
    flag:
        authentication-type:
            - API
            - SAML
            - USERPOOL
            - AWS_AD
documentation:
    command: Enables a user in the user pool. After being enabled, users can sign in to AppStream 2.0 and open applications from the stacks to which they are assigned.
    flag:
        authentication-type: The authentication type for the user. You must specify USERPOOL.
        user-name: |-
            The email address of the user.

            Users' email addresses are case-sensitive. During login, if they specify an email address that doesn't use the same capitalization as the email address specified when their user pool account was created, a "user does not exist" error message displays.
