name: describe-authentication-profiles
description: Describes an authentication profile.
flags:
    --authentication-profile-name=: The name of the authentication profile to describe.
documentation:
    command: Describes an authentication profile.
    flag:
        authentication-profile-name: The name of the authentication profile to describe. If not specified then all authentication profiles owned by the account are listed.
