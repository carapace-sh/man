name: describe
description: '*(ALPHA)*  Describe a BigQuery Engine for Apache Flink job.'
flags:
    --location=: ID of the location of the Apache Flink for BigQuery resource.
documentation:
    command: |
        *(ALPHA)*  Describe a BigQuery Engine for Apache Flink job.
    flag:
        location: |-
            ID of the location of the Apache Flink for BigQuery resource. See
            https://cloud.google.com/managed-flink/docs/locations for a list of supported
            locations.
            +

            To set the `location` attribute:
            * provide the argument `job` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
