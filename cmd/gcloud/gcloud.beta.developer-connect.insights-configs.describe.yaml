name: describe
description: '*(BETA)*  Describe insightsConfigs.'
flags:
    --location=: The location id of the insightsConfig resource.
documentation:
    command: '*(BETA)*  Describe an insightsConfig'
    flag:
        location: |-
            The location id of the insightsConfig resource.
            +
            To set the `location` attribute:
            * provide the argument `insights_config` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
