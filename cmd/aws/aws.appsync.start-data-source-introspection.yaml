name: start-data-source-introspection
description: Creates a new introspection.
flags:
    --rds-data-api-config=: The `rdsDataApiConfig` object data.
documentation:
    command: Creates a new introspection. Returns the `introspectionId` of the new introspection after its creation.
    flag:
        rds-data-api-config: The `rdsDataApiConfig` object data.
