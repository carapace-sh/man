name: delete-domain-name
description: Deletes a custom `DomainName` object.
flags:
    --domain-name=!: The domain name.
documentation:
    command: Deletes a custom `DomainName` object.
    flag:
        domain-name: The domain name.
