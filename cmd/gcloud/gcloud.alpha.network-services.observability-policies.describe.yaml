name: describe
description: '*(ALPHA)*  Describe an observability policy.'
flags:
    --location=: The location Id.
documentation:
    command: '*(ALPHA)*  Show details of an observability policy.'
    flag:
        location: |-
            The location Id.
            +
            To set the `location` attribute:
            * provide the argument `observability_policy` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
