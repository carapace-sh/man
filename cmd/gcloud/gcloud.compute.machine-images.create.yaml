name: create
description: Create a Compute Engine machine image.
flags:
    --csek-key-file=: Path to a Customer-Supplied Encryption Key (CSEK) key file that maps
    --description=: Specifies a text description of the machine image.
    --guest-flush: Create an application-consistent machine image by informing the OS
    --kms-key=!: ID of the key or fully qualified identifier for the key.
    --kms-keyring=: The KMS keyring of the key.
    --kms-location=: The Google Cloud location for the key.
    --kms-project=: The Google Cloud project for the key.
    --no-guest-flush&: ""
    --no-require-csek-key-create&: Refuse to create machine images not protected by a user managed key in
    --require-csek-key-create: Refuse to create machine images not protected by a user managed key in
    --source-disk-csek-key=: Customer-supplied encryption key of the disk attached to the
    --source-instance-zone=: Zone of the instance to operate on.
    --source-instance=!: The source instance to create a machine image from.
    --storage-location=: Google Cloud Storage location, either regional or multi-regional, where
documentation:
    command: Create a Compute Engine machine image.
    flag:
        csek-key-file: |-
            Path to a Customer-Supplied Encryption Key (CSEK) key file that maps
            Compute Engine machine images to user managed keys to be used when
            creating, mounting, or taking snapshots of disks.
            +
            If you pass `-` as value of the flag, the CSEK is read from stdin.
            See https://cloud.google.com/compute/docs/disks/customer-supplied-encryption for more details.
        description: Specifies a text description of the machine image.
        guest-flush: |-
            Create an application-consistent machine image by informing the OS
            to prepare for the snapshot process.
        kms-key: |-
            ID of the key or fully qualified identifier for the key.
            +
            To set the `kms-key` attribute:
            * provide the argument `--kms-key` on the command line.
        kms-keyring: |-
            The KMS keyring of the key.
            +
            To set the `kms-keyring` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-keyring` on the command line.
        kms-location: |-
            The Google Cloud location for the key.
            +
            To set the `kms-location` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-location` on the command line.
        kms-project: |-
            The Google Cloud project for the key.
            +
            To set the `kms-project` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-project` on the command line;
            * set the property `core/project`.
        no-guest-flush: |
            Create an application-consistent machine image by informing the OS
            to prepare for the snapshot process.
        no-require-csek-key-create: |
            Refuse to create machine images not protected by a user managed key in
            the key file when --csek-key-file is given. This behavior is enabled
            by default to prevent incorrect gcloud invocations from accidentally
            creating machine images with no user managed key. Disabling the check
            allows creation of some machine images without a matching
            Customer-Supplied Encryption Key in the supplied --csek-key-file.
            See https://cloud.google.com/compute/docs/disks/customer-supplied-encryption for more details.
        require-csek-key-create: |-
            Refuse to create machine images not protected by a user managed key in
            the key file when --csek-key-file is given. This behavior is enabled
            by default to prevent incorrect gcloud invocations from accidentally
            creating machine images with no user managed key. Disabling the check
            allows creation of some machine images without a matching
            Customer-Supplied Encryption Key in the supplied --csek-key-file.
            See https://cloud.google.com/compute/docs/disks/customer-supplied-encryption for more details. Enabled by default, use *--no-require-csek-key-create* to disable.
        source-disk-csek-key: |-
            Customer-supplied encryption key of the disk attached to the
            source instance. Required if the source disk is protected by
            a customer-supplied encryption key. This flag can be repeated to
            specify multiple attached disks.
            +
            *disk*::: URL of the disk attached to the source instance.
            This can be a full or   valid partial URL
            +
            *csek-key-file*::: path to customer-supplied encryption key.
        source-instance: The source instance to create a machine image from.
        source-instance-zone: |-
            Zone of the instance to operate on. If not specified and the ``compute/zone'' property isn't set, you
            might be prompted to select a zone (interactive mode only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/zone'' property:
            +
              $ gcloud config set compute/zone ZONE
            +
            A list of zones can be fetched by running:
            +
              $ gcloud compute zones list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/zone
            +
            Alternatively, the zone can be stored in the environment variable
            ``CLOUDSDK_COMPUTE_ZONE''.
        storage-location: |-
            Google Cloud Storage location, either regional or multi-regional, where
            machine image's content is to be stored. If absent, a nearby regional or
            multi-regional location is chosen automatically.
