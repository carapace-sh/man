name: wait
description: '*(BETA)*  Wait for all operations specified to complete before returning.'
documentation:
    command: |-
        *(BETA)*  Polls until all operations have finished, then prints the resulting operations
        along with any operation errors.
