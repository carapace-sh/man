name: untag
description: '*(ALPHA)*  Remove existing image tags.'
documentation:
    command: |
        *(ALPHA)*  The container images untag command removes the specified tag
        from the image.
