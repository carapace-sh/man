name: describe
description: '*(ALPHA)*  Describe an operation.'
flags:
    --location=: Google Cloud location.
documentation:
    command: |
        *(ALPHA)*  Describe a Backup for GKE operation.

        ## EXAMPLES

        To describe an operation ``OPERATION'' in project ``my-project'', run:

          $ gcloud alpha container backup-restore operations describe OPERATION --project=my-project
    flag:
        location: |-
            Google Cloud location.
            +
            To set the `location` attribute:
            * provide the argument `operation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `gkebackup/location`.
