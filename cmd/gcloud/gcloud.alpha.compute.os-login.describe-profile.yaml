name: describe-profile
description: '*(ALPHA)*  Describe the OS Login profile for the current user.'
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute os-login describe-profile* displays the OS Login profile for the currently
        authenticated user, including Posix accounts and SSH keys
        associated with the user.
