name: get-read-set-export-job
description: Retrieves status information about a read set export job and returns the data in JSON format.
flags:
    --id=!: The job's ID.
    --sequence-store-id=!: The job's sequence store ID.
documentation:
    command: Retrieves status information about a read set export job and returns the data in JSON format. Use this operation to actively monitor the progress of an export job.
    flag:
        id: The job's ID.
        sequence-store-id: The job's sequence store ID.
