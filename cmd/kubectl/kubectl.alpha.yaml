# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: alpha
description: Commands for features in alpha
documentation:
    command: '[AI] `kubectl alpha` is not a standalone command in Kubernetes, but rather a prefix used for commands that enable interacting with alpha (experimental) features in Kubernetes using the `kubectl` command-line interface. These commands allow users to try new functionality before it''s officially released. Always ensure that you have a good understanding of the risks involved and follow the specific instructions provided by the Kubernetes documentation when using these commands.'
