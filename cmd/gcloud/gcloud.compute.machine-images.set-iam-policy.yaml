name: set-iam-policy
description: Set the IAM policy for a Compute Engine machine image.
documentation:
    command: |
        Sets the IAM policy for the given machine image as defined in a JSON or YAML file.
