name: list-hits
description: The `ListHITs` operation returns all of a Requester's HITs.
flags:
    --max-results=: ""
    --next-token=: Pagination token
documentation:
    command: The `ListHITs` operation returns all of a Requester's HITs. The operation returns HITs of any status, except for HITs that have been deleted of with the DeleteHIT operation or that have been auto-deleted.
    flag:
        max-results: ""
        next-token: Pagination token
