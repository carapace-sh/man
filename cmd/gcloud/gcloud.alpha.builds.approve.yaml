name: approve
description: '*(ALPHA)*  Approve a pending build.'
flags:
    --comment=: Optional comment to annotate the build's approval.
    --location=: Cloud Region
    --url=: Optional URL to annotate the build's approval.
documentation:
    command: '*(ALPHA)*  Approve a pending build.'
    flag:
        comment: Optional comment to annotate the build's approval.
        location: |-
            Cloud Region
            +
            To set the `location` attribute:
            * provide the argument `build` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        url: Optional URL to annotate the build's approval.
