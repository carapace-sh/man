# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: headless
description: Headless authentication commands.
documentation:
    command: '[AI] The `tsh headless` command is used in the TeamSupport software environment to start a new instance of the TSH (TeamSupport Hub) agent without a graphical user interface (GUI). This command is typically run on remote machines, allowing for unattended and automated support sessions. It''s important to note that you should have administrative access or appropriate permissions on the target machine to execute this command effectively.'
