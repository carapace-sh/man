# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List the virtual network links to the specified private DNS zone.
flags:
    --max-items=: Total number of items to return in the command's output.
    --next-token=: Token to specify where to start paginating.
    --subscription=: Name or ID of subscription.
    --top=: Maximum number of virtual network links to return.
    -g, --resource-group=!: Name of resource group.
    -z, --zone-name=!: Name of the private DNS zone.
documentation:
    flag:
        max-items: Total number of items to return in the command's output. If the total number of items available is more than the value specified, a token is provided in the command's output. To resume pagination, provide the token value in `--next-token` argument of a subsequent command.
        next-token: Token to specify where to start paginating. This is the token value from a previously truncated response.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        top: Maximum number of virtual network links to return. If not specified, returns up to 100 virtual network links.
        zone-name: Name of the private DNS zone.
examples:
    List virtual network links to the specified private DNS zone in a resource group.: az network private-dns link vnet list -g MyResourceGroup -z www.mysite.com
