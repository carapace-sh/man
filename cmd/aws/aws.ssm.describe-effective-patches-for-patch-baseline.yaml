name: describe-effective-patches-for-patch-baseline
description: Retrieves the current effective patches (the patch and the approval state) for the specified patch baseline.
flags:
    --baseline-id=!: The ID of the patch baseline to retrieve the effective patches for.
    --max-results=: The maximum number of patches to return (per page).
    --next-token=: The token for the next set of items to return.
documentation:
    command: Retrieves the current effective patches (the patch and the approval state) for the specified patch baseline. Applies to patch baselines for Windows only.
    flag:
        baseline-id: The ID of the patch baseline to retrieve the effective patches for.
        max-results: The maximum number of patches to return (per page).
        next-token: The token for the next set of items to return. (You received this token from a previous call.)
