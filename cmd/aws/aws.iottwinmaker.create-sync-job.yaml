name: create-sync-job
description: This action creates a SyncJob.
flags:
    --sync-role=!: The SyncJob IAM role.
    --sync-source=!: The sync source.
    --tags=: The SyncJob tags.
    --workspace-id=!: The workspace ID.
documentation:
    command: This action creates a SyncJob.
    flag:
        sync-role: The SyncJob IAM role. This IAM role is used by the SyncJob to read from the syncSource, and create, update, or delete the corresponding resources.
        sync-source: |-
            The sync source.

            Currently the only supported syncSoource is `SITEWISE`.
        tags: The SyncJob tags.
        workspace-id: The workspace ID.
