name: get-case
description: Returns the attributes of a case.
flags:
    --case-id=!: Required element for GetCase to identify the requested case ID.
documentation:
    command: Returns the attributes of a case.
    flag:
        case-id: Required element for GetCase to identify the requested case ID.
