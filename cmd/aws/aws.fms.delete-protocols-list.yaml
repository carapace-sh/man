name: delete-protocols-list
description: Permanently deletes an Firewall Manager protocols list.
flags:
    --list-id=!: The ID of the protocols list that you want to delete.
documentation:
    command: Permanently deletes an Firewall Manager protocols list.
    flag:
        list-id: The ID of the protocols list that you want to delete. You can retrieve this ID from `PutProtocolsList`, `ListProtocolsLists`, and `GetProtocolsLost`.
