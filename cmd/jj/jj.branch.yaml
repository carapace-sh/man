# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: branch
description: Manage branches
flags:
    -h, --help: Print help (see more with '--help')
documentation:
    command: '[AI] The `jj branch` command is not a standard Git command. It seems to be specific to the Jump Lines tool, which is an extension for Vim or other terminal editors. This command allows creating, listing, or deleting Git branches using keyboard shortcuts within the text editor interface. For more information on this command and its usage, please refer to the documentation provided with the Jump Lines plugin.'
    flag:
        help: '[AI] The `jj` command is a Git extension for interactive rebase by Janis Johnsons. The `--help` option when used with this command displays detailed information about how to use the `jj` tool for git rebasing, including available commands and options. It provides usage instructions and examples to help users effectively manage their Git repositories.'
