name: create-ruleset
description: Creates a new ruleset that can be used in a profile job to validate the data quality of a dataset.
flags:
    --description=: The description of the ruleset.
    --name=!: The name of the ruleset to be created.
    --rules=!: A list of rules that are defined with the ruleset.
    --tags=: Metadata tags to apply to the ruleset.
    --target-arn=!: The Amazon Resource Name (ARN) of a resource (dataset) that the ruleset is associated with.
documentation:
    command: Creates a new ruleset that can be used in a profile job to validate the data quality of a dataset.
    flag:
        description: The description of the ruleset.
        name: The name of the ruleset to be created. Valid characters are alphanumeric (A-Z, a-z, 0-9), hyphen (-), period (.), and space.
        rules: A list of rules that are defined with the ruleset. A rule includes one or more checks to be validated on a DataBrew dataset.
        tags: Metadata tags to apply to the ruleset.
        target-arn: The Amazon Resource Name (ARN) of a resource (dataset) that the ruleset is associated with.
