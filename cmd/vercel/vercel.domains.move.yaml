# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: move
description: Move a domain to another user or team
documentation:
    command: '[AI] The `vercel domains move` command is not a standard Vercel CLI (Command Line Interface) command. It seems to be misspelled or incorrect as there''s no such command in the official Vercel CLI documentation. The Vercel CLI offers various commands for managing your projects, like `vercel`, `vercel deploy`, `vercel open`, etc. For more information on these commands and their usage, refer to the Vercel CLI documentation: https://nextjs.org/docs/cli/vercel.'
