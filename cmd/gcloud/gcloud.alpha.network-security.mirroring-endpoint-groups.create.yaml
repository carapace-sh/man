name: create
description: '*(ALPHA)*  Create a Mirroring Endpoint Group.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --description=: Description of the endpoint
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: Location of the mirroring endpoint group.
    --max-wait=: Time to synchronously wait for the operation to complete, after which the operation continues asynchronously.
    --mirroring-deployment-group-location=: Location of the mirroring deployment group.
    --mirroring-deployment-group-project=: Project of the mirroring deployment group.
    --mirroring-deployment-group=!: ID of the mirroring deployment group or fully qualified identifier for the mirroring deployment group.
    --mirroring-deployment-groups=: A comma-separated list of Mirroring Deployment Groups to associate with the Endpoint Group.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --type=: The type of the endpoint group.
completion:
    flag:
        type:
            - DIRECT
            - BROKER
documentation:
    command: |+
        *(ALPHA)*  Create a mirroring endpoint group. Successful creation of an endpoint group results
        in an endpoint group in ACTIVE state. Check the progress of endpoint group creation
        by using `gcloud network-security mirroring-endpoint-groups list`.

        For more examples, refer to the EXAMPLES section below.

    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete. The default is *True*. Enabled by default, use *--no-async* to disable.
        description: Description of the endpoint
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            Location of the mirroring endpoint group.
            +
            To set the `location` attribute:
            * provide the argument `MIRRORING_ENDPOINT_GROUP` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        max-wait: Time to synchronously wait for the operation to complete, after which the operation continues asynchronously. Ignored if --no-async isn't specified. See $ gcloud topic datetimes for information on time formats.
        mirroring-deployment-group: |-
            ID of the mirroring deployment group or fully qualified identifier for the mirroring deployment group.
            +
            To set the `id` attribute:
            * provide the argument `--mirroring-deployment-group` on the command line.
        mirroring-deployment-group-location: |-
            Location of the mirroring deployment group.
            +
            To set the `location` attribute:
            * provide the argument `--mirroring-deployment-group` on the command line with a fully specified name;
            * provide the argument `--mirroring-deployment-group-location` on the command line;
            * provide the argument `--location` on the command line;
            * provide the argument `MIRRORING_ENDPOINT_GROUP` on the command line with a fully specified name.
        mirroring-deployment-group-project: |-
            Project of the mirroring deployment group.
            +
            To set the `project` attribute:
            * provide the argument `--mirroring-deployment-group` on the command line with a fully specified name;
            * provide the argument `--mirroring-deployment-group-project` on the command line;
            * provide the argument `--project` on the command line;
            * set the property `core/project`;
            * provide the argument `MIRRORING_ENDPOINT_GROUP` on the command line with a fully specified name.
        mirroring-deployment-groups: A comma-separated list of Mirroring Deployment Groups to associate with the Endpoint Group. Each deployment group must be specified by its full resource name. e.g. --mirroring-deployment-groups=projects/123/locations/global/mirroringDeploymentGroups/my-dg1,projects/456/locations/global/mirroringDeploymentGroups/my-dg2
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        type: 'The type of the endpoint group. If not specified, the default type is DIRECT. _TYPE_ must be one of: *DIRECT*, *BROKER*.'
