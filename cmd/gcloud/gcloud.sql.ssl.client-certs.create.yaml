name: create
description: Create a client certificate for a Cloud SQL instance.
flags:
    --instance=!: Cloud SQL instance ID.
documentation:
    command: Create a client certificate for a Cloud SQL instance.
    flag:
        instance: Cloud SQL instance ID.
