# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List link authorizations of an ExpressRoute circuit.
flags:
    --circuit-name=!: ExpressRoute circuit name.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
documentation:
    flag:
        circuit-name: ExpressRoute circuit name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List link authorizations of an ExpressRoute circuit.: az network express-route auth list -g MyResourceGroup --circuit-name MyCircuit
    List link authorizations of an ExpressRoute circuit. (autogenerated): az network express-route auth list --circuit-name MyCircuit --resource-group MyResourceGroup --subscription MySubscription
