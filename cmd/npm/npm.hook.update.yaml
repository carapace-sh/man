# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: update
description: Update registry hook
documentation:
    command: '[AI] The `npm` `hook update` is a custom script registered in an `npm` project''s `package.json` file under the `"scripts": { "postupdate": "..." }` section. It gets executed automatically after an `npm install` or `npm update` command, allowing developers to automate tasks related to updating dependencies, such as rebuilding assets, testing, or publishing.'
