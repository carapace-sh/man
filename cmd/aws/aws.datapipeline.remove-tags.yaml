name: remove-tags
description: Removes existing tags from the specified pipeline.
flags:
    --pipeline-id=!: The ID of the pipeline.
    --tag-keys=!: The keys of the tags to remove.
documentation:
    command: Removes existing tags from the specified pipeline.
    flag:
        pipeline-id: The ID of the pipeline.
        tag-keys: The keys of the tags to remove.
