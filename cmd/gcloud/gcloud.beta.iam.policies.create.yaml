name: create
description: '*(BETA)*  Create a policy on the given attachment point with the given name.'
flags:
    --attachment-point=!: Resource to which the policy is attached.
    --kind=!: Policy type.
    --policy-file=!: Path to the file that contains the policy, in JSON or YAML format.
documentation:
    command: '*(BETA)*  Create a policy on the given attachment point with the given name.'
    flag:
        attachment-point: Resource to which the policy is attached. For valid formats, see https://cloud.google.com/iam/help/deny/attachment-point.
        kind: Policy type. Use `denypolicies` for deny policies.
        policy-file: Path to the file that contains the policy, in JSON or YAML format. For valid syntax, see https://cloud.google.com/iam/help/deny/policy-syntax.
