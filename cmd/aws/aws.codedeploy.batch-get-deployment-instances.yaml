name: batch-get-deployment-instances
description: This method works, but is deprecated.
flags:
    --deployment-id=!: The unique ID of a deployment.
    --instance-ids=!: The unique IDs of instances used in the deployment.
documentation:
    command: |-
        This method works, but is deprecated. Use `BatchGetDeploymentTargets` instead.

        Returns an array of one or more instances associated with a deployment. This method works with EC2/On-premises and Lambda compute platforms. The newer `BatchGetDeploymentTargets` works with all compute platforms. The maximum number of instances that can be returned is 25.
    flag:
        deployment-id: The unique ID of a deployment.
        instance-ids: The unique IDs of instances used in the deployment. The maximum number of instance IDs you can specify is 25.
