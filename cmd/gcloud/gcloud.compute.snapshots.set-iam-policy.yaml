name: set-iam-policy
description: Set the IAM policy for a Compute Engine snapshot.
documentation:
    command: |
        Sets the IAM policy for the given snapshot as defined in a JSON or YAML file.
