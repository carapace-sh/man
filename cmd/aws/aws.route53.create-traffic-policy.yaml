name: create-traffic-policy
description: Creates a traffic policy, which you use to create multiple DNS resource record sets for one domain name (such as example.com) or one subdomain name (such as www.example.com).
flags:
    --comment=: (Optional) Any comments that you want to include about the traffic policy.
    --document=!: The definition of this traffic policy in JSON format.
    --name=!: The name of the traffic policy.
documentation:
    command: Creates a traffic policy, which you use to create multiple DNS resource record sets for one domain name (such as example.com) or one subdomain name (such as www.example.com).
    flag:
        comment: (Optional) Any comments that you want to include about the traffic policy.
        document: The definition of this traffic policy in JSON format. For more information, see [Traffic Policy Document Format](https://docs.aws.amazon.com/Route53/latest/APIReference/api-policies-traffic-policy-document-format.html).
        name: The name of the traffic policy.
