name: describe
description: '*(ALPHA)*  describe zonal project.'
flags:
    --location=: The location name.
    --organization=: The organization name.
    --zone=: The name of the Google Distributed Cloud Edge zone.
documentation:
    command: '*(ALPHA)*  Describe a zonal project.'
    flag:
        location: |-
            The location name.
            +
            To set the `location` attribute:
            * provide the argument `zonal_project` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        organization: |-
            The organization name.
            +
            To set the `organization` attribute:
            * provide the argument `zonal_project` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line.
        zone: |-
            The name of the Google Distributed Cloud Edge zone.
            +
            To set the `zone` attribute:
            * provide the argument `zonal_project` on the command line with a fully specified name;
            * provide the argument `--zone` on the command line.
