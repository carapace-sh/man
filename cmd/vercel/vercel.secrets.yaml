# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: secrets
description: Manages your global Secrets, for use in Environment Variables
documentation:
    command: '[AI] `vercel secrets` is a command used in Vercel, a cloud platform for static sites and serverless functions. This command allows you to manage and configure environment variables securely for your projects. The specific usage of the command varies between setting up new secrets, listing existing ones, or deleting them. For more detailed information on how to use this command, refer to the Vercel documentation.'
