# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List managed clusters.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Specify the resource group name.
documentation:
    flag:
        resource-group: Specify the resource group name. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List clusters by resource group.: az sf managed-cluster list -g testRG
    List clusters by subscription.: az sf managed-cluster list
