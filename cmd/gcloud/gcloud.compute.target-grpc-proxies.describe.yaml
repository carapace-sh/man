name: describe
description: Display detailed information about a target gRPC proxy.
documentation:
    command: |
        *gcloud compute target-grpc-proxies describe* displays all data associated with a Compute Engine
        target gRPC proxy.
