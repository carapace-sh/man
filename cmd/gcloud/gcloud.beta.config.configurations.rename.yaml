name: rename
description: '*(BETA)*  Renames a named configuration.'
flags:
    --new-name=!: Specifies the new name of the configuration.
documentation:
    command: |
        *(BETA)*  Renames a named configuration.

        See `gcloud topic configurations` for an overview of named
        configurations.
    flag:
        new-name: Specifies the new name of the configuration.
