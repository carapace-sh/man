name: batch-get-stream-key
description: Performs [GetStreamKey]() on multiple ARNs simultaneously.
flags:
    --arns=!: Array of ARNs, one per stream key.
documentation:
    command: Performs [GetStreamKey]() on multiple ARNs simultaneously.
    flag:
        arns: Array of ARNs, one per stream key.
