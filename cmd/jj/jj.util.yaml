# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: util
description: Infrequently used commands such as for generating shell completions
flags:
    -h, --help: Print help (see more with '--help')
documentation:
    command: '[AI] The command "jj util" is likely related to Jupyter Notebook''s Jupyter-JavaScript tools. It doesn''t have a universally accepted meaning on its own, but in the context of these tools, it might be used to invoke specific utility functions or features. For instance, "jj util open" could open a new file or "jj util import" might import some external libraries. However, without more context, it''s hard to provide an exact explanation. Always refer to the official documentation for the most accurate information.'
    flag:
        help: '[AI] `jj util` is a command-line utility provided by Jira JIRA Software and Jira Service Management. The `--help` option is used to display detailed help information about a specific `jj` sub-command or the entire `jj` tool itself. When you run `jj util --help`, it will show you a comprehensive description of what the `util` sub-command does, its usage syntax, and available options with their descriptions. This way, users can understand how to use the utility effectively for managing various Jira-related tasks.'
