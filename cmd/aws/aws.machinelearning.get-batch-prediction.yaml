name: get-batch-prediction
description: Returns a `BatchPrediction` that includes detailed metadata, status, and data file information for a `Batch Prediction` request.
flags:
    --batch-prediction-id=!: An ID assigned to the `BatchPrediction` at creation.
documentation:
    command: Returns a `BatchPrediction` that includes detailed metadata, status, and data file information for a `Batch Prediction` request.
    flag:
        batch-prediction-id: An ID assigned to the `BatchPrediction` at creation.
