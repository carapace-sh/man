name: put-voice-connector-termination-credentials
description: Updates a Voice Connector's termination credentials.
flags:
    --credentials=: The termination credentials being updated.
    --voice-connector-id=!: The Voice Connector ID.
documentation:
    command: Updates a Voice Connector's termination credentials.
    flag:
        credentials: The termination credentials being updated.
        voice-connector-id: The Voice Connector ID.
