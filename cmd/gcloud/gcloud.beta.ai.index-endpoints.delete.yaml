name: delete
description: '*(BETA)*  Delete an existing Vertex AI index endpoint.'
flags:
    --region=: Cloud region for the index_endpoint.
documentation:
    command: |-
        ## EXAMPLES

        To delete an index endpoint `123` of project `example` in region
        `us-central1`, run:

          $ gcloud beta ai index-endpoints delete 123 --project=example --region=us-central1
    flag:
        region: |-
            Cloud region for the index_endpoint.
            +
            To set the `region` attribute:
            * provide the argument `index_endpoint` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `ai/region`;
            * choose one from the prompted list of available regions.
