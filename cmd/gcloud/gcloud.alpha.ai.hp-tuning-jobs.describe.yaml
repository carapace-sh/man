name: describe
description: '*(ALPHA)*  Get detail information about the hyperparameter tuning job by given id.'
flags:
    --region=: Cloud region for the hyperparameter tuning job.
documentation:
    command: |-
        ## EXAMPLES

        To get a job ``123'' under project ``example'' in region
        ``us-central1'', run:

          $ gcloud alpha ai hp-tuning-jobs describe 123 --project=example --region=us-central1
    flag:
        region: |-
            Cloud region for the hyperparameter tuning job.
            +
            To set the `region` attribute:
            * provide the argument `hptuning_job` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `ai/region`;
            * choose one from the prompted list of available regions.
