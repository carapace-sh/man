# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: stop
description: Power off (stop) VMs within a VMSS.
flags:
    --instance-ids=:
        description: 'Space-separated list of IDs (ex: 1 2 3 ...) or * for all instances.'
        nargs: -1
    --no-wait=: Do not wait for the long-running operation to finish.
    --skip-shutdown=: Skip shutdown and power-off immediately.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: Scale set name.
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        instance-ids: 'Space-separated list of IDs (ex: 1 2 3 ...) or * for all instances. If not provided, the action will be applied on the scaleset itself.'
        name: Scale set name. You can configure the default using ``` az configure --defaults vmss=<name> ``` .
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Default value: False
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        skip-shutdown: |-
            Skip shutdown and power-off immediately.

            Default value: False
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Power off (stop) VMs within a VMSS. (autogenerated): az vmss stop --instance-ids 1 --name MyScaleSet --resource-group MyResourceGroup
    Power off VMs within a VMSS without shutting down.: az vmss stop --name MyScaleSet --resource-group MyResourceGroup --skip-shutdown
