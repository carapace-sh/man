name: start-inference-scheduler
description: Starts an inference scheduler.
flags:
    --inference-scheduler-name=!: The name of the inference scheduler to be started.
documentation:
    command: Starts an inference scheduler.
    flag:
        inference-scheduler-name: The name of the inference scheduler to be started.
