name: set-iam-policy
description: '*(ALPHA)*  Set IAM policy for a project.'
documentation:
    command: |
        *(ALPHA)*  Sets the IAM policy for a project, given a project ID and a file encoded
        in JSON or YAML that contains the IAM policy.
