name: disconnect-user
description: Disconnects all connections using a specified user ID from a room.
flags:
    --reason=: Reason for disconnecting the user.
    --room-identifier=!: Identifier of the room from which the user's clients should be disconnected.
    --user-id=!: ID of the user (connection) to disconnect from the room.
documentation:
    command: Disconnects all connections using a specified user ID from a room. This replicates the [DisconnectUser](https://docs.aws.amazon.com/ivs/latest/chatmsgapireference/actions-disconnectuser-publish.html) WebSocket operation in the Amazon IVS Chat Messaging API.
    flag:
        reason: Reason for disconnecting the user.
        room-identifier: Identifier of the room from which the user's clients should be disconnected. Currently this must be an ARN.
        user-id: ID of the user (connection) to disconnect from the room.
