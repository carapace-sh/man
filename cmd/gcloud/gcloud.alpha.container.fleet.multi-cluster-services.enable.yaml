name: enable
description: '*(ALPHA)*  Enable the Multi-cluster Services Feature.'
documentation:
    command: |-
        *(ALPHA)*  This command enables the Multi-cluster Services Feature in a fleet.

        ## EXAMPLES

        To enable the Multi-cluster Services Feature, run:

          $ gcloud alpha container fleet multi-cluster-services enable
