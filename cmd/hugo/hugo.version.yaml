# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: version
description: Print the version number of Hugo
documentation:
    command: '[AI] The `hugo version` command is used to check and display the currently installed version of Hugo, a popular open-source static site generator written in Go. It retrieves this information from the Hugo executable itself, ensuring accuracy and reliability.'
