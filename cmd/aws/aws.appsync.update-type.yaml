name: update-type
description: Updates a `Type` object.
flags:
    --api-id=!: The API ID.
    --definition=: The new definition.
    --format=!: 'The new type format: SDL or JSON.'
    --type-name=!: The new type name.
completion:
    flag:
        format:
            - SDL
            - JSON
documentation:
    command: Updates a `Type` object.
    flag:
        api-id: The API ID.
        definition: The new definition.
        format: 'The new type format: SDL or JSON.'
        type-name: The new type name.
