# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: version
hidden: true
documentation:
    command: '[AI] `gh version` is a GitHub Desktop command used to display the current version of GitHub Desktop and the installed Git version. It provides information about the application''s major, minor, and patch versions, as well as the Git version and its commit SHA.'
