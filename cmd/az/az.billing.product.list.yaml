# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List the products for a billing account.
flags:
    --account-name=!: The ID that uniquely identifies a billing account.
    --customer-name=: The ID that uniquely identifies a customer.
    --filter=: May be used to filter by product type.
    --invoice-section-name=: The ID that uniquely identifies an invoice section.
    --profile-name=: The ID that uniquely identifies a billing profile.
    --subscription=: Name or ID of subscription.
documentation:
    command: |-
        Command group 'billing product' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        account-name: The ID that uniquely identifies a billing account.
        customer-name: The ID that uniquely identifies a customer.
        filter: May be used to filter by product type. The filter supports 'eq', 'lt', 'gt', 'le', 'ge', and 'and'. It does not currently support 'ne', 'or', or 'not'. Tag filter is a key value pair string where key and value are separated by a colon (:).
        invoice-section-name: The ID that uniquely identifies an invoice section.
        profile-name: The ID that uniquely identifies a billing profile.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List products by invoice name: az billing product list --account-name "{billingAccountName}" --profile-name "{billingProfileName}" --invoice-section-name "{invoiceSectionName}"
