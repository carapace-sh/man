# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: delete BRANCH_NAME
description: Deletes a branch from the workspace
flags:
    -f, --force: Force deletion without confirmation
    -h, --help: Print help
