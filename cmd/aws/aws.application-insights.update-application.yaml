name: update-application
description: Updates the application.
flags:
    --attach-missing-permission=: If set to true, the managed policies for SSM and CW will be attached to the instance roles if they are missing.
    --auto-config-enabled=: Turns auto-configuration on or off.
    --cwemonitor-enabled=: Indicates whether Application Insights can listen to CloudWatch events for the application resources, such as `instance terminated`, `failed deployment`, and others.
    --ops-center-enabled=: When set to `true`, creates opsItems for any problems detected on an application.
    --ops-item-snstopic-arn=: The SNS topic provided to Application Insights that is associated to the created opsItem.
    --remove-snstopic=: Disassociates the SNS topic from the opsItem created for detected problems.
    --resource-group-name=!: The name of the resource group.
    --snsnotification-arn=: The SNS topic ARN.
documentation:
    command: Updates the application.
    flag:
        attach-missing-permission: If set to true, the managed policies for SSM and CW will be attached to the instance roles if they are missing.
        auto-config-enabled: Turns auto-configuration on or off.
        cwemonitor-enabled: Indicates whether Application Insights can listen to CloudWatch events for the application resources, such as `instance terminated`, `failed deployment`, and others.
        ops-center-enabled: When set to `true`, creates opsItems for any problems detected on an application.
        ops-item-snstopic-arn: The SNS topic provided to Application Insights that is associated to the created opsItem. Allows you to receive notifications for updates to the opsItem.
        remove-snstopic: Disassociates the SNS topic from the opsItem created for detected problems.
        resource-group-name: The name of the resource group.
        snsnotification-arn: The SNS topic ARN. Allows you to receive SNS notifications for updates and issues with an application.
