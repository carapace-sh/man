# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: add
description: Add regions for the managed HSM Pool.
flags:
    --hsm-name=!: Name of the HSM.
    --no-wait=: Do not wait for the long-running operation to finish.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Proceed only if Key Vault belongs to the specified resource group.
    -r, --region-name=!: The region name.
documentation:
    command: |-
        Command group 'keyvault region' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        hsm-name: Name of the HSM. --region --region-name -r The region name.
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Default value: False
        resource-group: Proceed only if Key Vault belongs to the specified resource group.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Add regions for the managed HSM.: az keyvault region add --region-name westus2 --hsm-name myhsm --resource-group myrg
