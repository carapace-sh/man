name: delete-dbproxy
description: Deletes an existing DB proxy.
flags:
    --dbproxy-name=!: The name of the DB proxy to delete.
documentation:
    command: Deletes an existing DB proxy.
    flag:
        dbproxy-name: The name of the DB proxy to delete.
