name: get-operation
description: Returns information about a specific operation.
flags:
    --operation-id=!: A GUID used to identify the operation.
documentation:
    command: Returns information about a specific operation. Operations include events such as when you create an instance, allocate a static IP, attach a static IP, and so on.
    flag:
        operation-id: A GUID used to identify the operation.
