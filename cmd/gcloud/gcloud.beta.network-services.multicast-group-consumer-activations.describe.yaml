name: describe
description: '*(BETA)*  Describe a multicast group consumer activation.'
flags:
    --location=: The location Id.
documentation:
    command: |
        *(BETA)*  Show details of a multicast group consumer activation in the specified location of the current project.
    flag:
        location: |-
            The location Id.
            +
            To set the `location` attribute:
            * provide the argument `multicast_group_consumer_activation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
