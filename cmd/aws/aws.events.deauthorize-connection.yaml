name: deauthorize-connection
description: Removes all authorization parameters from the connection.
flags:
    --name=!: The name of the connection to remove authorization from.
documentation:
    command: Removes all authorization parameters from the connection. This lets you remove the secret from the connection so you can reuse it without having to create a new connection.
    flag:
        name: The name of the connection to remove authorization from.
