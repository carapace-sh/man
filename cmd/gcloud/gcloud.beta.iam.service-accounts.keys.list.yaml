name: list
description: '*(BETA)*  List the keys for a service account.'
flags:
    --created-before=: Return only keys created before the specified time.
    --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
    --iam-account=!: A textual name to display for the account.
    --limit=: Maximum number of resources to list.
    --managed-by=: The types of keys to list.
    --page-size=: Some services group resource list output into pages.
    --sort-by=: Comma-separated list of resource field key names to sort by.
completion:
    flag:
        managed-by:
            - user
            - system
            - any
documentation:
    command: |-
        *(BETA)*  If the service account does not exist, this command returns a
        `PERMISSION_DENIED` error.
    flag:
        created-before: Return only keys created before the specified time. Common time formats are accepted. This is equivalent to --filter="validAfterTime<DATE_TIME". See $ gcloud topic datetimes for information on time formats.
        filter: |-
            Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            If the expression evaluates `True`, then that item is listed. For more
            details and examples of filter expressions, run $ gcloud topic filters. This
            flag interacts with other flags that are applied in this order: *--flatten*,
            *--sort-by*, *--filter*, *--limit*.
        iam-account: A textual name to display for the account.
        limit: |-
            Maximum number of resources to list. The default is *unlimited*.
            This flag interacts with other flags that are applied in this order:
            *--flatten*, *--sort-by*, *--filter*, *--limit*.
        managed-by: 'The types of keys to list. _MANAGED_BY_ must be one of: *user*, *system*, *any*.'
        page-size: |-
            Some services group resource list output into pages. This flag specifies
            the maximum number of resources per page. The default is determined by the
            service if it supports paging, otherwise it is *unlimited* (no paging).
            Paging may be applied before or after *--filter* and *--limit* depending
            on the service.
        sort-by: |-
            Comma-separated list of resource field key names to sort by. The
            default order is ascending. Prefix a field with ``~'' for descending
            order on that field. This flag interacts with other flags that are applied
            in this order: *--flatten*, *--sort-by*, *--filter*, *--limit*.
