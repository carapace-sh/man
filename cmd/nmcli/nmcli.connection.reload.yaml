# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: reload
description: Reload all connection files from disk
documentation:
    command: '[AI] The `nmcli connection reload` command is used to update NetworkManager''s connection configuration. It reloads the network connections from their respective configuration files and applies any changes, allowing you to quickly adopt new or modified settings without restarting NetworkManager or your system.'
