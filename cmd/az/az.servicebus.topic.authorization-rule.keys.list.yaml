# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: Gets the primary and secondary connection strings for the topic.
flags:
    --namespace-name=!: The namespace name
    --subscription=: Name or ID of subscription.
    --topic-name=!: The topic name.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: The authorization rule name.
documentation:
    flag:
        namespace-name: The namespace name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        topic-name: The topic name.
examples:
    List the keys and connection strings of Authorization Rule for Service Bus Topic.: az servicebus topic authorization-rule keys list --resource-group myresourcegroup --namespace-name mynamespace --topic-name mytopic --name myauthorule
