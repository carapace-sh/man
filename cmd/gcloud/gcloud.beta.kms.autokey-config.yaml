name: autokey-config
description: '*(BETA)*  Update and retrieve the AutokeyConfig.'
documentation:
    command: |-
        *(BETA)*  Autokey config is a folder level resource which configures the key project
        for the folder and the resources inside it.
