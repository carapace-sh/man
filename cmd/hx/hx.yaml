# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: hx
description: A post-modern text editor
documentation:
    command: '[AI] The command "hx" does not exist in common use for version control systems like Git or Mercurial. It may refer to specific tools or configurations depending on the context, but without additional information it is impossible to provide a precise explanation.'
