# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: bind
description: Bind-mount a path from the host into a unit's namespace
group: unit
documentation:
    command: |-
        [AI] The `systemctl` command is a system management utility in Linux systems, primarily used to control the state of various services and targets. The specific command you've mentioned, `systemctl bind`, is not a valid or recognized command for this utility. It seems like there might be a typo or misunderstanding about the purpose of this command.

        The correct usage would involve commands such as `systemctl start`, `stop`, `restart`, `status`, and `is-enabled` to manage services. For binding sockets, you may want to consider using `netstat`, `ss`, or `socat` commands instead.
