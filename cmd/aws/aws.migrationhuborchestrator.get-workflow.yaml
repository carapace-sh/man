name: get-workflow
description: Get migration workflow.
flags:
    --id=!: The ID of the migration workflow.
documentation:
    command: Get migration workflow.
    flag:
        id: The ID of the migration workflow.
