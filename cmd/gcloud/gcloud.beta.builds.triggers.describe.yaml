name: describe
description: '*(BETA)*  Get information about a particular trigger.'
flags:
    --region=: The Cloud location for the trigger.
documentation:
    command: '*(BETA)*  Get information about the specified build trigger.'
    flag:
        region: |-
            The Cloud location for the trigger.
            +
            To set the `region` attribute:
            * provide the argument `TRIGGER` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `builds/region`.
