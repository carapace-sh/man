name: delete-template-sync-config
description: Delete a template sync configuration.
flags:
    --template-name=!: The template name.
    --template-type=!: The template type.
completion:
    flag:
        template-type:
            - ENVIRONMENT
            - SERVICE
documentation:
    command: Delete a template sync configuration.
    flag:
        template-name: The template name.
        template-type: The template type.
