name: describe
description: '*(BETA)*  Describe BackendAuthenticationConfig.'
flags:
    --location=: The location Id.
documentation:
    command: |
        *(BETA)*  Describe the specified BackendAuthenticationConfig.
    flag:
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `backend_authentication_config` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
