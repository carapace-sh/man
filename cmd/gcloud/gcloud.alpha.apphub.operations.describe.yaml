name: describe
description: '*(ALPHA)*  Describe an Apphub operation (long-running operation).'
flags:
    --location=: The Cloud location for the operation.
documentation:
    command: '*(ALPHA)*  Describe an Apphub operation (long-running operation).'
    flag:
        location: |-
            The Cloud location for the operation.
            +
            To set the `location` attribute:
            * provide the argument `operation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
