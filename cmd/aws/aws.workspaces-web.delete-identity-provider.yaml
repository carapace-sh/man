name: delete-identity-provider
description: Deletes the identity provider.
flags:
    --identity-provider-arn=!: The ARN of the identity provider.
documentation:
    command: Deletes the identity provider.
    flag:
        identity-provider-arn: The ARN of the identity provider.
