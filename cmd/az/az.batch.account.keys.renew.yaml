# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: renew
description: Renew keys for a Batch account.
flags:
    --key-name=: Name of the batch account key.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of the resource group.
    -n, --name=: Name of the batch account to show.
completion:
    flag:
        key-name:
            - Primary
            - Secondary
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        key-name: |-
            Name of the batch account key.

            Accepted values: Primary, Secondary
        name: Name of the batch account to show. If not specified will display currently set account.
        resource-group: Name of the resource group. If not specified will display currently set account.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Renew keys for a Batch account.: az batch account keys renew --name MyBatchAccount --resource-group MyResourceGroup --key-name primary
