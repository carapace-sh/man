name: set-iam-policy
description: '*(ALPHA)*  Set an IAM policy binding for a Dataplex Environment as defined in a JSON or YAML file.'
flags:
    --lake=: The identifier of the Dataplex lake resource.
    --location=: The location of the Dataplex resource.
documentation:
    command: |-
        *(ALPHA)*  See https://cloud.google.com/iam/docs/managing-policies for details of
          the policy file format and contents.
    flag:
        lake: |-
            The identifier of the Dataplex lake resource.
            +
            To set the `lake` attribute:
            * provide the argument `environment` on the command line with a fully specified name;
            * provide the argument `--lake` on the command line.
        location: |-
            The location of the Dataplex resource.
            +
            To set the `location` attribute:
            * provide the argument `environment` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `dataplex/location`.
