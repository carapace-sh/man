# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: reread
description: Reload the daemon’s configuration files, without add/remove (no restarts)
documentation:
    command: '[AI] `supervisorctl reread` is a command used in Supervisor, a popular process monitoring and control system for UNIX and Windows. This command reloads the Supervisor configuration files without restarting any processes, allowing changes made to the config files to take effect without interrupting running processes.'
