name: describe
description: '*(ALPHA)*  Show details of the backup plan association.'
flags:
    --location=: Location ID of the resource.
documentation:
    command: '*(ALPHA)*  Show all configuration data associated with the specified backup plan association.'
    flag:
        location: |-
            Location ID of the resource.
            +

            To set the `location` attribute:
            * provide the argument `backup_plan_association` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
