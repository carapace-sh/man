name: create-package-group
description: Creates a package group.
flags:
    --contact-info=: The contact information for the created package group.
    --description=: A description of the package group.
    --domain-owner=: The 12-digit account number of the Amazon Web Services account that owns the domain.
    --domain=!: The name of the domain in which you want to create a package group.
    --package-group=!: The pattern of the package group to create.
    --tags=: One or more tag key-value pairs for the package group.
documentation:
    command: Creates a package group. For more information about creating package groups, including example CLI commands, see [Create a package group](https://docs.aws.amazon.com/codeartifact/latest/ug/create-package-group.html) in the *CodeArtifact User Guide*.
    flag:
        contact-info: The contact information for the created package group.
        description: A description of the package group.
        domain: The name of the domain in which you want to create a package group.
        domain-owner: The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include dashes or spaces.
        package-group: The pattern of the package group to create. The pattern is also the identifier of the package group.
        tags: One or more tag key-value pairs for the package group.
