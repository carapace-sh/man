name: get-workflow-step-execution
description: Get the runtime information that was logged for a specific runtime instance of the workflow step.
flags:
    --step-execution-id=!: Use the unique identifier for a specific runtime instance of the workflow step to get runtime details for that step.
documentation:
    command: Get the runtime information that was logged for a specific runtime instance of the workflow step.
    flag:
        step-execution-id: Use the unique identifier for a specific runtime instance of the workflow step to get runtime details for that step.
