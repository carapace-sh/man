name: delete-listing
description: Deletes a listing (a record of an asset at a given time).
flags:
    --domain-identifier=!: The ID of the Amazon DataZone domain.
    --identifier=!: The ID of the listing to be deleted.
documentation:
    command: Deletes a listing (a record of an asset at a given time).
    flag:
        domain-identifier: The ID of the Amazon DataZone domain.
        identifier: The ID of the listing to be deleted.
