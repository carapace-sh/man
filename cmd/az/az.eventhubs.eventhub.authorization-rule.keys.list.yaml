# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: Gets the ACS and SAS connection strings for the Event Hub.
flags:
    --eventhub-name=!: The Event Hub name
    --name=!: The authorization rule name.
    --namespace-name=!: The Namespace name
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
documentation:
    flag:
        eventhub-name: The Event Hub name.
        namespace-name: The Namespace name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Shows the connection strings of Authorizationrule for the eventhub.: az eventhubs eventhub authorization-rule keys list --resource-group myresourcegroup --namespace-name mynamespace --eventhub-name myeventhub --name myauthorule
