name: create-event-bridge-rule-template
description: Creates an eventbridge rule template to monitor events and send notifications to your targeted resources.
flags:
    --description=: A resource's optional description.
    --event-targets=: ""
    --event-type=!: ""
    --group-identifier=!: An eventbridge rule template group's identifier.
    --name=!: A resource's name.
    --request-id=: An ID that you assign to a create request.
    --tags=: ""
completion:
    flag:
        event-type:
            - MEDIALIVE_MULTIPLEX_ALERT
            - MEDIALIVE_MULTIPLEX_STATE_CHANGE
            - MEDIALIVE_CHANNEL_ALERT
            - MEDIALIVE_CHANNEL_INPUT_CHANGE
            - MEDIALIVE_CHANNEL_STATE_CHANGE
            - MEDIAPACKAGE_INPUT_NOTIFICATION
            - MEDIAPACKAGE_KEY_PROVIDER_NOTIFICATION
            - MEDIAPACKAGE_HARVEST_JOB_NOTIFICATION
            - SIGNAL_MAP_ACTIVE_ALARM
            - MEDIACONNECT_ALERT
            - MEDIACONNECT_SOURCE_HEALTH
            - MEDIACONNECT_OUTPUT_HEALTH
            - MEDIACONNECT_FLOW_STATUS_CHANGE
documentation:
    command: Creates an eventbridge rule template to monitor events and send notifications to your targeted resources.
    flag:
        description: A resource's optional description.
        event-targets: ""
        event-type: ""
        group-identifier: An eventbridge rule template group's identifier. Can be either be its id or current name.
        name: A resource's name. Names must be unique within the scope of a resource type in a specific region.
        request-id: An ID that you assign to a create request. This ID ensures idempotency when creating resources.
        tags: ""
