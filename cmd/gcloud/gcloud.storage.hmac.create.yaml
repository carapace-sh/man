name: create
description: Add a service account HMAC.
documentation:
    command: |
        *gcloud storage hmac create* command creates an HMAC key for the specified service
        account. The secret key material is only available upon creation, so be
        sure to store the returned secret along with the access_id.
