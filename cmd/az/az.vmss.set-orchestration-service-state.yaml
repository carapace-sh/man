# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: set-orchestration-service-state
description: Change ServiceState property for a given service within a VMSS.
flags:
    --action=!: The action to be performed.
    --ids=:
        description: One or more resource IDs (space-delimited).
        nargs: -1
    --no-wait=: Do not wait for the long-running operation to finish.
    --service-name=!: The name of the orchestration service.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --name=: Scale set name.
completion:
    flag:
        action:
            - Resume
            - Suspend
        service-name:
            - AutomaticRepairs
            - DummyOrchestrationServiceName
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        action: |-
            The action to be performed.

            Accepted values: Resume, Suspend
        change-reference: The related change reference ID for this resource operation.
        ids: One or more resource IDs (space-delimited). It should be a complete resource ID containing all information of 'Resource Id' arguments. You should provide either --ids or other 'Resource Id' arguments.
        name: Scale set name. You can configure the default using ``` az configure --defaults vmss=<name> ``` .
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Default value: False
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        service-name: |-
            The name of the orchestration service.

            Accepted values: AutomaticRepairs
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Change ServiceState property for AutomaticRepairs: az vmss set-orchestration-service-state --service-name AutomaticRepairs --action Resume --name MyScaleSet --resource-group MyResourceGroup
