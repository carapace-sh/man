name: wait
description: '*(ALPHA)*  Wait for all operations specified to complete before returning.'
documentation:
    command: |-
        *(ALPHA)*  Polls until all operations have finished, then prints the resulting operations
        along with any operation errors.
