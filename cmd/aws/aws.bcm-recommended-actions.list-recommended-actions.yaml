name: list-recommended-actions
description: Returns a list of recommended actions that match the filter criteria.
flags:
    --filter=: The criteria that you want all returned recommended actions to match.
    --max-results=: The maximum number of results to return in the response.
    --next-token=: The pagination token that indicates the next set of results that you want to retrieve.
documentation:
    command: Returns a list of recommended actions that match the filter criteria.
    flag:
        filter: The criteria that you want all returned recommended actions to match.
        max-results: The maximum number of results to return in the response.
        next-token: The pagination token that indicates the next set of results that you want to retrieve.
