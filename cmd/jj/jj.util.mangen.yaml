# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: mangen
description: Print a ROFF (manpage)
flags:
    -h, --help: Print help (see more with '--help')
documentation:
    command: |-
        [AI] The command `jj util mangen` is not a standard Linux or Unix command. It appears to be specific to JJ-EDB, a Japanese database management system.

        This command is likely to invoke a utility function named "mangen" within the JJ-EDB environment. The exact functionality of this utility isn't clear without additional context.
    flag:
        help: '[AI] `jj` is a command-line tool for managing and deploying Java projects, developed by ZeroTurnaround. The flag `util mangen --help` is a sub-command of `jj` that specifically refers to the utility named `mangen`. This utility is used for managing and configuring the environments (manifests) where your Java applications will be deployed. The `--help` flag is provided to display detailed information about how to use this specific utility, including available options and their usage.'
