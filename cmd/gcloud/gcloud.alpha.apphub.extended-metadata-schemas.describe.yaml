name: describe
description: '*(ALPHA)*  Describe an App Hub extended metadata schema.'
hidden: true
flags:
    --location=: The Cloud location for the extendedMetadataSchema.
documentation:
    command: '*(ALPHA)*  Describe an App Hub extended metadata schema.'
    flag:
        location: |-
            The Cloud location for the extendedMetadataSchema.
            +
            To set the `location` attribute:
            * provide the argument `extended_metadata_schema` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
