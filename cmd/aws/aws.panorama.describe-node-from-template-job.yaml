name: describe-node-from-template-job
description: Returns information about a job to create a camera stream node.
flags:
    --job-id=!: The job's ID.
documentation:
    command: Returns information about a job to create a camera stream node.
    flag:
        job-id: The job's ID.
