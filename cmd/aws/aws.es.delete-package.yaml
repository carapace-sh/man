name: delete-package
description: Delete the package.
flags:
    --package-id=!: Internal ID of the package that you want to delete.
documentation:
    command: Delete the package.
    flag:
        package-id: Internal ID of the package that you want to delete. Use `DescribePackages` to find this value.
