name: get-repository
description: Get detail data for a linked repository.
flags:
    --name=!: The repository name, for example `myrepos/myrepo`.
    --provider=!: The repository provider.
completion:
    flag:
        provider:
            - GITHUB
            - GITHUB_ENTERPRISE
            - BITBUCKET
documentation:
    command: Get detail data for a linked repository.
    flag:
        name: The repository name, for example `myrepos/myrepo`.
        provider: The repository provider.
