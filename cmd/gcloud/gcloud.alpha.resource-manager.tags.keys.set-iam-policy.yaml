name: set-iam-policy
description: '*(ALPHA)*  Sets IAM policy for a TagKey resource.'
documentation:
    command: |-
        *(ALPHA)*  Sets the IAM policy for a TagKey resource given the TagKey's display
        name and parent or the TagKey's numeric id and a file encoded in
        JSON or YAML that contains the IAM policy.
