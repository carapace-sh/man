name: get
description: '*(ALPHA)*  Get consumer policy for a project, folder or organization.'
flags:
    --folder=: The Google Cloud Platform folder ID to use for this invocation.
    --organization=: The Google Cloud Platform organization ID to use for this invocation.
    --output-file=: Path to the file to write policy contents to.
    --policy-name=: Name of the consumer policy.
documentation:
    command: |-
        *(ALPHA)*  Get consumer policy for a project, folder or
        organization.

        ## EXAMPLES

         Get consumer policy for default policy on current project:

         $ gcloud alpha services policies get
            OR
         $ gcloud alpha services policies get --policy-name=default

         Get consumer policy for default policy on current project and save the
         content in an output file:

         $ gcloud alpha services policies get --output-file=/path/to/the/file.yaml
             OR
         $ gcloud alpha services policies get --output-file=/path/to/the/file.json
    flag:
        folder: The Google Cloud Platform folder ID to use for this invocation.
        organization: The Google Cloud Platform organization ID to use for this invocation.
        output-file: Path to the file to write policy contents to. Supported format:.yaml or .json.
        policy-name: Name of the consumer policy. Currently only "default" is supported.
