# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: wait
description: Place the CLI in a waiting state until a condition of a integration runtime is met.
flags:
    --created=: wait until created with 'provisioningState' at 'Succeeded'
    --custom=: Wait until the condition satisfies a custom JMESPath query.
    --deleted=: wait until deleted
    --exists=: wait until the resource exists
    --if-none-match=: ETag of the integration runtime entity.
    --interval=: polling interval in seconds
    --subscription=: Name or ID of subscription.
    --timeout=: maximum wait in seconds
    --updated=: wait until updated with provisioningState at 'Succeeded'
    --workspace-name=!: The workspace name.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: The integration runtime name.
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        created: |-
            Wait until created with 'provisioningState' at 'Succeeded'.

            Default value: False
        custom: Wait until the condition satisfies a custom JMESPath query. E.g. provisioningState!='InProgress', instanceView.statuses[?code=='PowerState/running'].
        deleted: |-
            Wait until deleted.

            Default value: False
        exists: |-
            Wait until the resource exists.

            Default value: False
        if-none-match: ETag of the integration runtime entity. Should only be specified for get. If the ETag matches the existing entity tag, or if * was provided, then no content will be returned. Default value is None.
        interval: |-
            Polling interval in seconds.

            Default value: 30
        name: The integration runtime name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        timeout: |-
            Maximum wait in seconds.

            Default value: 3600
        updated: |-
            Wait until updated with provisioningState at 'Succeeded'.

            Default value: False
        workspace-name: The workspace name.
