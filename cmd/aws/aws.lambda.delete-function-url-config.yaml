name: delete-function-url-config
description: Deletes a Lambda function URL.
flags:
    --function-name=!: The name or ARN of the Lambda function.
    --qualifier=: The alias name.
documentation:
    command: Deletes a Lambda function URL. When you delete a function URL, you can't recover it. Creating a new function URL results in a different URL address.
    flag:
        function-name: |-
            The name or ARN of the Lambda function.

            **Name formats**

            - **Function name** – `my-function`.
            - **Function ARN** – `arn:aws:lambda:us-west-2:123456789012:function:my-function`.
            - **Partial ARN** – `123456789012:function:my-function`.

            The length constraint applies only to the full ARN. If you specify only the function name, it is limited to 64 characters in length.
        qualifier: The alias name.
