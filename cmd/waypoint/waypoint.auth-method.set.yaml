# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: set
description: Create or update an auth method
documentation:
    command: '[AI] The `waypoint auth-method set` command is used in Hashicorp''s Waypoint configuration management tool to configure the authentication method for a specific resource or module. This command sets the chosen authentication provider and its associated configuration. For instance, you may use this command to set up an SSH or token-based authentication for a remote infrastructure. The usage of this command typically involves specifying the resource or module context and the desired authentication method and configuration.'
