name: list-stage-sessions
description: Gets all sessions for a specified stage.
flags:
    --max-results=: Maximum number of results to return.
    --next-token=: The first stage session to retrieve.
    --stage-arn=!: Stage ARN.
documentation:
    command: Gets all sessions for a specified stage.
    flag:
        max-results: 'Maximum number of results to return. Default: 50.'
        next-token: The first stage session to retrieve. This is used for pagination; see the `nextToken` response field.
        stage-arn: Stage ARN.
