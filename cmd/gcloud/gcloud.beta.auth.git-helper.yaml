name: git-helper
description: '*(BETA)*  A git credential helper to provide access to Google git repositories.'
hidden: true
flags:
    --ignore-unknown: Produce no output and exit with 0 when given an unknown method (e.g. store) or host.
    --no-ignore-unknown&: Produce no output and exit with 0 when given an unknown method (e.g. store) or host.
documentation:
    command: '*(BETA)*  A git credential helper to provide access to Google git repositories.'
    flag:
        ignore-unknown: Produce no output and exit with 0 when given an unknown method (e.g. store) or host.
        no-ignore-unknown: Produce no output and exit with 0 when given an unknown method (e.g. store) or host.
