name: describe
description: '*(ALPHA)*  Describe an Apphub discovered service.'
flags:
    --location=: The Cloud location for the discoveredService.
documentation:
    command: '*(ALPHA)*  Describe an Apphub discovered service.'
    flag:
        location: |-
            The Cloud location for the discoveredService.
            +
            To set the `location` attribute:
            * provide the argument `discovered_service` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
