name: open-console
description: '*(BETA)*  Open the App Engine dashboard, or log viewer, in a web browser.'
flags:
    --logs: Open the log viewer instead of the App Engine dashboard.
    --no-logs&: Open the log viewer instead of the App Engine dashboard.
    --service=: The service to consider.
documentation:
    command: |
        *(BETA)*  Open the App Engine dashboard, or log viewer, in a web browser.
    flag:
        logs: Open the log viewer instead of the App Engine dashboard.
        no-logs: Open the log viewer instead of the App Engine dashboard.
        service: The service to consider. If not specified, use the default service.
