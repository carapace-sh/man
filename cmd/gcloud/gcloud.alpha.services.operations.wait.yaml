name: wait
description: '*(ALPHA)*  Waits for an operation to complete for a given operation name.'
documentation:
    command: |-
        *(ALPHA)*  This command will block until an operation has been marked as complete.

        ## EXAMPLES
        To wait on an operation named `operations/abc`
        to complete, run:

          $ gcloud alpha services operations wait operations/abc
