name: lookup
description: '*(ALPHA)*  Look up resource name of a key string.'
documentation:
    command: |-
        *(ALPHA)*  Look up resource name of a key string.

        ## EXAMPLES

         Look up resource name of a key string named my-key-string:

          $ gcloud alpha services api-keys lookup my-key-string
