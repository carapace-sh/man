name: describe
description: '*(ALPHA)*  Describe a deploymentEvent.'
hidden: true
flags:
    --insights-config=: The insightsConfig id of the deploymentEvent resource.
    --location=: The location id of the deploymentEvent resource.
    --no-show-previous-diff&: If true, shows the diff between this deployment and the previous one.
    --show-previous-diff: If true, shows the diff between this deployment and the previous one.
documentation:
    command: '*(ALPHA)*  Describe a deploymentEvent'
    flag:
        insights-config: |-
            The insightsConfig id of the deploymentEvent resource.
            +
            To set the `insights-config` attribute:
            * provide the argument `deployment_event` on the command line with a fully specified name;
            * provide the argument `--insights-config` on the command line.
        location: |-
            The location id of the deploymentEvent resource.
            +
            To set the `location` attribute:
            * provide the argument `deployment_event` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        no-show-previous-diff: If true, shows the diff between this deployment and the previous one.
        show-previous-diff: If true, shows the diff between this deployment and the previous one.
