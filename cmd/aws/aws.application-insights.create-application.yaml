name: create-application
description: Adds an application that is created from a resource group.
flags:
    --attach-missing-permission=: If set to true, the managed policies for SSM and CW will be attached to the instance roles if they are missing.
    --auto-config-enabled=: Indicates whether Application Insights automatically configures unmonitored resources in the resource group.
    --auto-create=: Configures all of the resources in the resource group by applying the recommended configurations.
    --cwemonitor-enabled=: Indicates whether Application Insights can listen to CloudWatch events for the application resources, such as `instance terminated`, `failed deployment`, and others.
    --grouping-type=: Application Insights can create applications based on a resource group or on an account.
    --ops-center-enabled=: When set to `true`, creates opsItems for any problems detected on an application.
    --ops-item-snstopic-arn=: The SNS topic provided to Application Insights that is associated to the created opsItem.
    --resource-group-name=: The name of the resource group.
    --snsnotification-arn=: The SNS notification topic ARN.
    --tags=: List of tags to add to the application.
completion:
    flag:
        grouping-type:
            - ACCOUNT_BASED
documentation:
    command: Adds an application that is created from a resource group.
    flag:
        attach-missing-permission: If set to true, the managed policies for SSM and CW will be attached to the instance roles if they are missing.
        auto-config-enabled: Indicates whether Application Insights automatically configures unmonitored resources in the resource group.
        auto-create: Configures all of the resources in the resource group by applying the recommended configurations.
        cwemonitor-enabled: Indicates whether Application Insights can listen to CloudWatch events for the application resources, such as `instance terminated`, `failed deployment`, and others.
        grouping-type: Application Insights can create applications based on a resource group or on an account. To create an account-based application using all of the resources in the account, set this parameter to `ACCOUNT_BASED`.
        ops-center-enabled: When set to `true`, creates opsItems for any problems detected on an application.
        ops-item-snstopic-arn: The SNS topic provided to Application Insights that is associated to the created opsItem. Allows you to receive notifications for updates to the opsItem.
        resource-group-name: The name of the resource group.
        snsnotification-arn: The SNS notification topic ARN.
        tags: List of tags to add to the application. tag key (`Key`) and an associated tag value (`Value`). The maximum length of a tag key is 128 characters. The maximum length of a tag value is 256 characters.
