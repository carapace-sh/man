name: describe-pull-request-events
description: Returns information about one or more pull request events.
flags:
    --actor-arn=: The Amazon Resource Name (ARN) of the user whose actions resulted in the event.
    --max-results=: A non-zero, non-negative integer used to limit the number of returned results.
    --next-token=: An enumeration token that, when provided in a request, returns the next batch of the results.
    --pull-request-event-type=: Optional.
    --pull-request-id=!: The system-generated ID of the pull request.
completion:
    flag:
        pull-request-event-type:
            - PULL_REQUEST_CREATED
            - PULL_REQUEST_STATUS_CHANGED
            - PULL_REQUEST_SOURCE_REFERENCE_UPDATED
            - PULL_REQUEST_MERGE_STATE_CHANGED
            - PULL_REQUEST_APPROVAL_RULE_CREATED
            - PULL_REQUEST_APPROVAL_RULE_UPDATED
            - PULL_REQUEST_APPROVAL_RULE_DELETED
            - PULL_REQUEST_APPROVAL_RULE_OVERRIDDEN
            - PULL_REQUEST_APPROVAL_STATE_CHANGED
documentation:
    command: Returns information about one or more pull request events.
    flag:
        actor-arn: The Amazon Resource Name (ARN) of the user whose actions resulted in the event. Examples include updating the pull request with more commits or changing the status of a pull request.
        max-results: A non-zero, non-negative integer used to limit the number of returned results. The default is 100 events, which is also the maximum number of events that can be returned in a result.
        next-token: An enumeration token that, when provided in a request, returns the next batch of the results.
        pull-request-event-type: Optional. The pull request event type about which you want to return information.
        pull-request-id: The system-generated ID of the pull request. To get this ID, use [ListPullRequests]().
