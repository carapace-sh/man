name: describe
description: '*(ALPHA)*  Show details about an operation.'
flags:
    --location=: Location of the blockchain node resource.
documentation:
    command: '*(ALPHA)*  Show details about an operation.'
    flag:
        location: |-
            Location of the blockchain node resource.
            +

            To set the `location` attribute:
            * provide the argument `operation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `web3/location`.
