name: search-flow-templates
description: Searches for summary information about workflows.
flags:
    --filters=: An array of objects that limit the result set.
    --max-results=: The maximum number of results to return in the response.
    --next-token=: The string that specifies the next page of results.
documentation:
    command: Searches for summary information about workflows.
    flag:
        filters: An array of objects that limit the result set. The only valid filter is `DEVICE_MODEL_ID`.
        max-results: The maximum number of results to return in the response.
        next-token: The string that specifies the next page of results. Use this when you're paginating results.
