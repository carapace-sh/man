name: get-connection
description: Gets a connection.
flags:
    --domain-identifier=!: The ID of the domain where we get the connection.
    --identifier=!: The connection ID.
    --no-with-secret: Specifies whether a connection has a secret.
    --with-secret: Specifies whether a connection has a secret.
documentation:
    command: Gets a connection. In Amazon DataZone, a connection enables you to connect your resources (domains, projects, and environments) to external resources and services.
    flag:
        domain-identifier: The ID of the domain where we get the connection.
        identifier: The connection ID.
        with-secret: Specifies whether a connection has a secret.
