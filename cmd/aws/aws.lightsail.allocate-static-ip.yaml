name: allocate-static-ip
description: Allocates a static IP address.
flags:
    --static-ip-name=!: The name of the static IP address.
documentation:
    command: Allocates a static IP address.
    flag:
        static-ip-name: The name of the static IP address.
