name: set-standby-policy
description: '*(ALPHA)*  Set the standby policy for a managed instance group.'
flags:
    --initial-delay=: Initialization delay before stopping or suspending instances
    --region=: Region of the managed instance group to operate on.
    --zone=: Zone of the managed instance group to operate on.
documentation:
    command: |-
        *(ALPHA)*  *gcloud alpha compute instance-groups managed set-standby-policy* sets the fields in the standby policy for a managed instance
        group. The standby policy dictates the behaviour of standby (stopped and
        suspended) instances
    flag:
        initial-delay: |-
            Initialization delay before stopping or suspending instances
            in this managed instance group. For example: 5m or 300s.
            See `gcloud topic datetimes` for information on duration formats.
        region: |-
            Region of the managed instance group to operate on. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            Overrides the default *compute/region* property value for this command invocation.
        zone: |-
            Zone of the managed instance group to operate on. If not specified, you might be prompted to select a zone (interactive mode
            only).
            +
            A list of zones can be fetched by running:
            +
              $ gcloud compute zones list
            +
            Overrides the default *compute/zone* property value for this command invocation.
