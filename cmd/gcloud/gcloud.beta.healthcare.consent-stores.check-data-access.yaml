name: check-data-access
description: '*(BETA)*  Check the consent for a particular data ID.'
flags:
    --data-id=!: The unique identifier of the data to check access for.
    --dataset=: Cloud Healthcare dataset.
    --location=: Google Cloud location.
    --request-attributes=: Comma-separated list of request attributes associated with this access request.
documentation:
    command: '*(BETA)*  Check if a particular data ID of a user data mapping in the given Cloud Healthcare API consent store is consented for a given use.'
    flag:
        data-id: The unique identifier of the data to check access for.
        dataset: |-
            Cloud Healthcare dataset.
            +
            To set the `dataset` attribute:
            * provide the argument `consent_store` on the command line with a fully specified name;
            * provide the argument `--dataset` on the command line.
        location: |-
            Google Cloud location.
            +
            To set the `location` attribute:
            * provide the argument `consent_store` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `healthcare/location`.
        request-attributes: |-
            Comma-separated list of request attributes associated with this access request. Each
            attribute has the form "KEY=VALUE".
