name: delete
description: '*(BETA)*  *(DEPRECATED)*  Delete a Datastream stream.'
flags:
    --location=: The location of the resources.
documentation:
    command: |4
        *(BETA)*  *(DEPRECATED)*  This command has been deprecated.
        Use `gcloud datastream streams delete` instead.


        Deletes a stream.
    flag:
        location: |-
            The location of the resources.
            +
            To set the `location` attribute:
            * provide the argument `stream` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
