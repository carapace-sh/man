name: describe
description: '*(ALPHA)*  Get details of a Database.'
hidden: true
flags:
    --location=: The location id of the database resource.
documentation:
    command: '*(ALPHA)*  Get details of a Database.'
    flag:
        location: |-
            The location id of the database resource.
            +
            To set the `location` attribute:
            * provide the argument `database` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
