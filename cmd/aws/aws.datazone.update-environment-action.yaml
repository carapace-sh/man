name: update-environment-action
description: Updates an environment action.
flags:
    --description=: The description of the environment action.
    --domain-identifier=!: The domain ID of the environment action.
    --environment-identifier=!: The environment ID of the environment action.
    --identifier=!: The ID of the environment action.
    --name=: The name of the environment action.
    --parameters=: The parameters of the environment action.
documentation:
    command: Updates an environment action.
    flag:
        description: The description of the environment action.
        domain-identifier: The domain ID of the environment action.
        environment-identifier: The environment ID of the environment action.
        identifier: The ID of the environment action.
        name: The name of the environment action.
        parameters: The parameters of the environment action.
