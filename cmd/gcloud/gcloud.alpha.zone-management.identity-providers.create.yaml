name: create
description: '*(ALPHA)*  create identity provider.'
flags:
    --config=!: The identity provider configuration file.
    --location=: The global location name.
    --organization=: The organization name.
documentation:
    command: '*(ALPHA)*  Creates an identity provider.'
    flag:
        config: The identity provider configuration file.
        location: |-
            The global location name.
            +
            To set the `location` attribute:
            * provide the argument `identity_provider` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        organization: |-
            The organization name.
            +
            To set the `organization` attribute:
            * provide the argument `identity_provider` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line.
