name: update
description: '*(ALPHA)*  Updates the specified TagValue resource''s description.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --description=: User-assigned description of the TagKey or TagValue.
    --no-async&: Return immediately, without waiting for the operation in progress to
documentation:
    command: |-
        *(ALPHA)*  Updates the TagValue's description given the TagValue's
        namespaced name <parent_namespace>/<tagkey_short_name>/<short_name>
        or the TagValue's numeric id tagValues/<id>
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        description: User-assigned description of the TagKey or TagValue. Must not exceed 256 characters.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
