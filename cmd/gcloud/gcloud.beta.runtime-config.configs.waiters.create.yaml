name: create
description: '*(BETA)*  Create waiter resources.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --config-name=!: The name of the configuration resource to use.
    --failure-cardinality-number=: The minimum required number of failure variables.
    --failure-cardinality-path=: The path where failure variables are written.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --success-cardinality-number=: The minimum required number of success variables.
    --success-cardinality-path=!: The path where success variables are written.
    --timeout=!: The amount of time to wait before failing with DEADLINE_EXCEEDED.
documentation:
    command: |-
        *(BETA)*  This command creates a new waiter resource with the specified name and
        parameters.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        config-name: The name of the configuration resource to use.
        failure-cardinality-number: The minimum required number of failure variables.
        failure-cardinality-path: The path where failure variables are written.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        success-cardinality-number: The minimum required number of success variables.
        success-cardinality-path: The path where success variables are written.
        timeout: |-
            The amount of time to wait before failing with DEADLINE_EXCEEDED.
            See $ gcloud topic datetimes for information on duration formats.
