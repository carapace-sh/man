name: delete
description: Delete a Database Migration private connection.
flags:
    --no-async: Waits for the operation in progress to complete before returning.
    --region=: The Cloud region for the private_connection.
documentation:
    command: Delete a Database Migration private connection
    flag:
        no-async: Waits for the operation in progress to complete before returning.
        region: |-
            The Cloud region for the private_connection.
            +
            To set the `region` attribute:
            * provide the argument `private_connection` on the command line with a fully specified name;
            * provide the argument `--region` on the command line.
