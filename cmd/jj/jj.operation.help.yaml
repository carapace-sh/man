# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: help
description: Print this message or the help of the given subcommand(s)
documentation:
    command: '[AI] The `jj` command is a text editor for JSON files, similar to `vim` or `emacs`. The `operation help` subcommand displays a list of available operations that can be used with `jj`. These operations include navigating and modifying JSON data, formatting, and validating. For more detailed information on each operation, use the `operation <name> help` command.'
