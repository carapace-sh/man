name: delete-alias
description: Deletes a Lambda function [alias](https://docs.aws.amazon.com/lambda/latest/dg/configuration-aliases.html).
flags:
    --function-name=!: The name or ARN of the Lambda function.
    --name=!: The name of the alias.
documentation:
    command: Deletes a Lambda function [alias](https://docs.aws.amazon.com/lambda/latest/dg/configuration-aliases.html).
    flag:
        function-name: |-
            The name or ARN of the Lambda function.

            **Name formats**

            - **Function name** - `MyFunction`.
            - **Function ARN** - `arn:aws:lambda:us-west-2:123456789012:function:MyFunction`.
            - **Partial ARN** - `123456789012:function:MyFunction`.

            The length constraint applies only to the full ARN. If you specify only the function name, it is limited to 64 characters in length.
        name: The name of the alias.
