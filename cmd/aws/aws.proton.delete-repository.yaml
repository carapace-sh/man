name: delete-repository
description: De-register and unlink your repository.
flags:
    --name=!: The repository name.
    --provider=!: The repository provider.
completion:
    flag:
        provider:
            - GITHUB
            - GITHUB_ENTERPRISE
            - BITBUCKET
documentation:
    command: De-register and unlink your repository.
    flag:
        name: The repository name.
        provider: The repository provider.
