name: get-maintenance-window
description: Retrieves a maintenance window.
flags:
    --window-id=!: The ID of the maintenance window for which you want to retrieve information.
documentation:
    command: Retrieves a maintenance window.
    flag:
        window-id: The ID of the maintenance window for which you want to retrieve information.
