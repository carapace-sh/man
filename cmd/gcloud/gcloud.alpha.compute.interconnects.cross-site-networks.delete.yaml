name: delete
description: '*(ALPHA)*  Delete Compute Engine cross site networks.'
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute interconnects cross-site-networks delete* is used to delete cross site networks.

        For an example, refer to the *EXAMPLES* section below.
