# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: logout
description: Clear CLI session credentials
documentation:
    command: |-
        [AI] The command "fury logout" is not a standard or recognized command in common operating systems, applications, or programming languages. It seems to be specific to a game or application named Fury, but without further context, it's impossible to determine its exact meaning and function.

        Assuming that "Fury logout" is a command used within the Fury game environment to signify user disconnection from the game, then it can be described as follows:

        The "fury logout" command instructs the Fury application or game client to terminate the current user session and disconnect the player from the game world. This allows users to gracefully exit the game and may save any progress made during the session. The specific effects of the command depend on the game design and implementation.
