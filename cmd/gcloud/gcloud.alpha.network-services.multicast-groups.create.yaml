name: create
description: '*(ALPHA)*  Create a multicast group.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --description=: The description for the multicast group.
    --enable-logging: Whether to enable logging for this multicast group.
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: The location Id.
    --multicast-domain-activation=!: The multicast domain activation to be used.
    --multicast-group-definition=!: The multicast group definition to be used.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-enable-logging&: Whether to enable logging for this multicast group.
documentation:
    command: |
        *(ALPHA)*  Create a multicast group in the specified location of the current project.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        description: The description for the multicast group.
        enable-logging: Whether to enable logging for this multicast group. Use *--enable-logging* to enable and *--no-enable-logging* to disable.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            The location Id.
            +
            To set the `location` attribute:
            * provide the argument `multicast_group` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        multicast-domain-activation: The multicast domain activation to be used.
        multicast-group-definition: The multicast group definition to be used.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-enable-logging: Whether to enable logging for this multicast group.
