name: delete
description: '*(BETA)*  Delete one or more target gRPC proxy.'
documentation:
    command: |
        *(BETA)*  *gcloud beta compute target-grpc-proxies delete* deletes one or more target gRPC proxies.
