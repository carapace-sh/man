# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: confluent-cloud
description: Create a local connection to confluent-cloud.
flags:
    --bootstrap-server=!: Kafka bootstrap server url
    --client-type=: The client type used on the connection
    --connection=: Name of the connection
    --customized-keys=:
        description: The customized keys used to change default configuration names.
        nargs: -1
    --kafka-key=!: Kafka API-Key (key)
    --kafka-secret=!: Kafka API-Key (secret)
    --no-wait=: Do not wait for the long-running operation to finish.
    --schema-key=!: Schema registry API-Key (key)
    --schema-registry=!: Schema registry url
    --schema-secret=!: Schema registry API-Key (secret)
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -l, --location=: Location.
completion:
    flag:
        client-type:
            - dotnet
            - dotnet-internal
            - java
            - python
            - go
            - springBoot
            - none
documentation:
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        bootstrap-server: Kafka bootstrap server url.
        change-reference: The related change reference ID for this resource operation.
        client-type: |-
            The client type used on the connection.

            Accepted values: dotnet, dotnet-internal, go, java, none, python, springBoot
        connection: Name of the connection.
        customized-keys: The customized keys used to change default configuration names. Key is the original name, value is the customized name.
        kafka-key: Kafka API-Key (key).
        kafka-secret: Kafka API-Key (secret).
        location: 'Location. Values from: ``` az account list-locations ``` . You can configure the default location using ``` az configure --defaults location=<location> ``` .'
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Default value: False
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        schema-key: Schema registry API-Key (key).
        schema-registry: Schema registry url.
        schema-secret: Schema registry API-Key (secret).
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Create a connection form local to confluent-cloud: az connection create confluent-cloud -g resourceGroup --connection myConnection --bootstrap-server xxx.eastus.azure.confluent.cloud:9092 --kafka-key Name --kafka-secret Secret --schema-registry https://xxx.eastus.azure.confluent.cloud --schema-key Name --schema-secret Secret
