name: start-sandbox-connection
description: Starts a sandbox connection.
flags:
    --sandbox-id=!: A `sandboxId` or `sandboxArn`.
documentation:
    command: Starts a sandbox connection.
    flag:
        sandbox-id: A `sandboxId` or `sandboxArn`.
