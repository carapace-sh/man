name: get-template-sync-status
description: Get the status of a template sync.
flags:
    --template-name=!: The template name.
    --template-type=!: The template type.
    --template-version=!: The template major version.
completion:
    flag:
        template-type:
            - ENVIRONMENT
            - SERVICE
documentation:
    command: Get the status of a template sync.
    flag:
        template-name: The template name.
        template-type: The template type.
        template-version: The template major version.
