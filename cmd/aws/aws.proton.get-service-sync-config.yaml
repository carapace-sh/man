name: get-service-sync-config
description: Get detailed information for the service sync configuration.
flags:
    --service-name=!: The name of the service that you want to get the service sync configuration for.
documentation:
    command: Get detailed information for the service sync configuration.
    flag:
        service-name: The name of the service that you want to get the service sync configuration for.
