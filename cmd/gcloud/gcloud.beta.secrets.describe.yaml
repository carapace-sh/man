name: describe
description: '*(BETA)*  Describe a secret''s metadata.'
flags:
    --location=!: ID of the location or fully qualified identifier for the location.
documentation:
    command: |-
        *(BETA)*  Describe a secret's metadata.

        ## EXAMPLES

        Describe metadata of the secret named 'my-secret':

          $ gcloud beta secrets describe my-secret
    flag:
        location: |-
            ID of the location or fully qualified identifier for the location.
            +
            To set the `location` attribute:
            * provide the argument `--location` on the command line.
