# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: list verbs
documentation:
    command: '[AI] `winetricks benchmarks list` is a command used in Wine, a compatibility layer for running Windows applications on Unix-like operating systems. This specific command lists all the available benchmark tests in Winetricks, a graphical interface for configuring Wine. Running this command provides information about the available benchmarks, which can be used to assess the performance and readiness of specific Wine configurations for running Windows applications.'
