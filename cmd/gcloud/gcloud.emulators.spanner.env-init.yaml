name: env-init
description: Print the commands required to export Spanner emulator's env variables.
documentation:
    command: Print the commands required to export Spanner emulator's env variables.
