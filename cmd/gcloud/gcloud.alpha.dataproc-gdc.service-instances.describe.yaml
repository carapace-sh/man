name: describe
description: '*(ALPHA)*  Get dataproc gdc service instance.'
hidden: true
flags:
    --location=: locations TBD
documentation:
    command: |
        *(ALPHA)*  Get dataproc gdc service instance.
    flag:
        location: |-
            locations TBD
            +
            To set the `location` attribute:
            * provide the argument `service_instance` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
