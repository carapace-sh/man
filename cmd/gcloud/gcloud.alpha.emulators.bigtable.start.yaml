name: start
description: '*(ALPHA)*  Start a local Bigtable emulator.'
flags:
    --host-port=: The host:port to which the emulator should be bound.
documentation:
    command: '*(ALPHA)*  This command starts a local Bigtable emulator.'
    flag:
        host-port: The host:port to which the emulator should be bound. The default value is localhost:8086.
