name: describe
description: '*(BETA)*  *(DEPRECATED)*  Show details about a Datastream private connection.'
flags:
    --location=: The location of the resources.
documentation:
    command: |4-
        *(BETA)*  *(DEPRECATED)*  This command has been deprecated.
        Use `gcloud datastream private_connections describe` instead.


        Show details about a private connection.
    flag:
        location: |-
            The location of the resources.
            +
            To set the `location` attribute:
            * provide the argument `private_connection` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
