name: delete-id-mapping-workflow
description: Deletes the `IdMappingWorkflow` with a given name.
flags:
    --workflow-name=!: The name of the workflow to be deleted.
documentation:
    command: Deletes the `IdMappingWorkflow` with a given name. This operation will succeed even if a workflow with the given name does not exist.
    flag:
        workflow-name: The name of the workflow to be deleted.
