name: set-iam-policy
description: Set the IAM policy binding for a Compute Engine backend bucket.
documentation:
    command: |
        Sets the IAM policy for the given backend bucket as defined in a JSON or YAML file.
