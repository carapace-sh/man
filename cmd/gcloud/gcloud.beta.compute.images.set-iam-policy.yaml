name: set-iam-policy
description: '*(BETA)*  Set the IAM policy for a Compute Engine image.'
documentation:
    command: |
        *(BETA)*  Sets the IAM policy for the given image as defined in a JSON or YAML file.
