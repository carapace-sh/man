name: delete
description: '*(BETA)*  Delete Compute Engine snapshots.'
flags:
    --global: If set, the snapshots are global.
    --no-global&: If set, the snapshots are global.
    --region=: Region of the snapshots to delete.
documentation:
    command: '*(BETA)*  Delete Compute Engine snapshots.'
    flag:
        global: If set, the snapshots are global.
        no-global: If set, the snapshots are global.
        region: Region of the snapshots to delete. Overrides the default *compute/region* property value for this command invocation.
