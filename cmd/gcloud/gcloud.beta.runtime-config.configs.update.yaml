name: update
description: '*(BETA)*  Update runtime-config resources.'
flags:
    --description=!: The new description of the configuration.
documentation:
    command: |-
        *(BETA)*  This command updates the runtime-config resource with the specified
        description.
    flag:
        description: The new description of the configuration.
