name: create
description: '*(ALPHA)*  Create Flag Release.'
hidden: true
flags:
    --annotations=: Unstructured key-value map stored with a resource that may be set by
    --flag-revisions=!: IDs of the flagRevisions or fully qualified identifiers for the flagRevisions.
    --labels=: Labels on the resource, which can be used for categorization.
    --location=: For resources [flag-revisions, flag_release, obsolete-flags, unit-kind], provides fallback value for resource location attribute.
    --obsolete-flags=!: IDs of the flags or fully qualified identifiers for the flags.
    --unit-kind=!: ID of the unitKind or fully qualified identifier for the unitKind.
documentation:
    command: '*(ALPHA)*  Create a Flag Release.'
    flag:
        annotations: |-
            Unstructured key-value map stored with a resource that may be set by
            external tools to store and retrieve arbitrary metadata. Annotations
            are not queryable and should be preserved when modifying objects.
            +
            *KEY*:::
            Sets `KEY` value.
            +
            *VALUE*:::
            Sets `VALUE` value.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --annotations=string=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --annotations='{"string": "string"}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --annotations=path_to_file.(yaml|json)
            +
            ```
        flag-revisions: |-
            IDs of the flagRevisions or fully qualified identifiers for the flagRevisions.
            +
            To set the `flag-revision` attribute:
            * provide the argument `--flag-revisions` on the command line.
        labels: |-
            Labels on the resource, which can be used for categorization.
            +
            *KEY*:::
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
            +
            *VALUE*:::
            Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --labels=string=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --labels='{"string": "string"}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --labels=path_to_file.(yaml|json)
            +
            ```
        location: For resources [flag-revisions, flag_release, obsolete-flags, unit-kind], provides fallback value for resource location attribute. When the resource's full URI path is not provided, location will fallback to this flag value.
        obsolete-flags: |-
            IDs of the flags or fully qualified identifiers for the flags.
            +
            To set the `flag` attribute:
            * provide the argument `--obsolete-flags` on the command line.
        unit-kind: |-
            ID of the unitKind or fully qualified identifier for the unitKind.
            +
            To set the `unit-kind` attribute:
            * provide the argument `--unit-kind` on the command line.
