name: describe
description: Get details about an asynchronous operation.
flags:
    --location=: Compute Engine region in which to create the operation.
documentation:
    command: Get details about an asynchronous operation.
    flag:
        location: |-
            Compute Engine region in which to create the operation.
            +
            To set the `location` attribute:
            * provide the argument `operation` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `composer/location`.
