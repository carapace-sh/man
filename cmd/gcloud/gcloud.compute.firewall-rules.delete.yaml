name: delete
description: Delete Compute Engine firewall rules.
documentation:
    command: |-
        *gcloud compute firewall-rules delete* deletes one or more Compute Engine firewall
        rules.

        ## EXAMPLES

        To delete a firewall rule, run:

          $ gcloud compute firewall-rules delete my-firewall-rule
