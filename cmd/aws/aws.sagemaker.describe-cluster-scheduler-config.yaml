name: describe-cluster-scheduler-config
description: Description of the cluster policy.
flags:
    --cluster-scheduler-config-id=!: ID of the cluster policy.
    --cluster-scheduler-config-version=: Version of the cluster policy.
documentation:
    command: Description of the cluster policy. This policy is used for task prioritization and fair-share allocation. This helps prioritize critical workloads and distributes idle compute across entities.
    flag:
        cluster-scheduler-config-id: ID of the cluster policy.
        cluster-scheduler-config-version: Version of the cluster policy.
