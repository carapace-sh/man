name: create-directory-registration
description: Creates a directory registration that authorizes communication between Amazon Web Services Private CA and an Active Directory
flags:
    --client-token=: Idempotency token.
    --directory-id=!: The identifier of the Active Directory.
    --tags=: Metadata assigned to a directory registration consisting of a key-value pair.
documentation:
    command: Creates a directory registration that authorizes communication between Amazon Web Services Private CA and an Active Directory
    flag:
        client-token: Idempotency token.
        directory-id: The identifier of the Active Directory.
        tags: Metadata assigned to a directory registration consisting of a key-value pair.
