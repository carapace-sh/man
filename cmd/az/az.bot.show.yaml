# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Retrieves the bot's application's application settings.
flags:
    --msbot=:
        description: Show the output as JSON compatible with a .bot file.
        nargs: -1
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: The resource name of the bot.
completion:
    flag:
        msbot:
            - "true"
            - "false"
documentation:
    command: Get an existing bot.
    flag:
        msbot: |-
            Show the output as JSON compatible with a .bot file.

            Accepted values: false, true
        name: The resource name of the bot. Bot name must be between 4 and 42 characters in length. Bot name can only have the following characters -, a - z, A - Z, 0 - 9, and _.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Get the information needed to connect to an existing bot on Azure: az bot show -n botName -g MyResourceGroup --msbot
