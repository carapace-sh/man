name: start-inference-experiment
description: Starts an inference experiment.
flags:
    --name=!: The name of the inference experiment to start.
documentation:
    command: Starts an inference experiment.
    flag:
        name: The name of the inference experiment to start.
