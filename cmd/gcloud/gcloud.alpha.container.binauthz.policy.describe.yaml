name: describe
description: '*(ALPHA)*  Describe a Binary Authorization platform policy.'
flags:
    --platform=: The platform that the policy belongs to.
documentation:
    command: |-
        ## EXAMPLES

        To describe an existing policy using its resource name:

          $ gcloud alpha container binauthz policy describe projects/my_proj/platforms/gke/policies/policy1

        To describe the same policy using flags:

          $ gcloud alpha container binauthz policy describe policy1 --platform=gke --project=my_proj
    flag:
        platform: |-
            The platform that the policy belongs to. PLATFORM must be one of the following: cloudRun, gke.
            +
            To set the `platform` attribute:
            * provide the argument `policy_resource_name` on the command line with a fully specified name;
            * provide the argument `--platform` on the command line.
