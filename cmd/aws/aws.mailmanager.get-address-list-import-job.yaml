name: get-address-list-import-job
description: Fetch attributes of an import job.
flags:
    --job-id=!: The identifier of the import job that needs to be retrieved.
documentation:
    command: Fetch attributes of an import job.
    flag:
        job-id: The identifier of the import job that needs to be retrieved.
