name: describe-maintenance-window-execution-task-invocations
description: Retrieves the individual task executions (one per target) for a particular task run as part of a maintenance window execution.
flags:
    --filters=: Optional filters used to scope down the returned task invocations.
    --max-results=: The maximum number of items to return for this call.
    --next-token=: The token for the next set of items to return.
    --task-id=!: The ID of the specific task in the maintenance window task that should be retrieved.
    --window-execution-id=!: The ID of the maintenance window execution the task is part of.
documentation:
    command: Retrieves the individual task executions (one per target) for a particular task run as part of a maintenance window execution.
    flag:
        filters: Optional filters used to scope down the returned task invocations. The supported filter key is `STATUS` with the corresponding values `PENDING`, `IN_PROGRESS`, `SUCCESS`, `FAILED`, `TIMED_OUT`, `CANCELLING`, and `CANCELLED`.
        max-results: The maximum number of items to return for this call. The call also returns a token that you can specify in a subsequent call to get the next set of results.
        next-token: The token for the next set of items to return. (You received this token from a previous call.)
        task-id: The ID of the specific task in the maintenance window task that should be retrieved.
        window-execution-id: The ID of the maintenance window execution the task is part of.
