name: delete-encoder-configuration
description: Deletes an EncoderConfiguration resource.
flags:
    --arn=!: ARN of the EncoderConfiguration.
documentation:
    command: Deletes an EncoderConfiguration resource. Ensures that no Compositions are using this template; otherwise, returns an error.
    flag:
        arn: ARN of the EncoderConfiguration.
