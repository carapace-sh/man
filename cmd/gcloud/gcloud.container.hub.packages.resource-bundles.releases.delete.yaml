name: delete
description: Delete Package Rollouts Release.
flags:
    --force: If true, force deletion of any child resources.
    --location=: Google Cloud zone or region.
    --no-force&: If true, force deletion of any child resources.
    --resource-bundle=!: Resource Bundle name.
documentation:
    command: Delete Package Rollouts Release.
    flag:
        force: If true, force deletion of any child resources. Otherwise, attempting to delete a Release with children will fail.
        location: Google Cloud zone or region.
        no-force: If true, force deletion of any child resources. Otherwise, attempting to delete a Release with children will fail.
        resource-bundle: Resource Bundle name.
