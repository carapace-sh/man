name: delete-lag
description: Deletes the specified link aggregation group (LAG).
flags:
    --lag-id=!: The ID of the LAG.
documentation:
    command: Deletes the specified link aggregation group (LAG). You cannot delete a LAG if it has active virtual interfaces or hosted connections.
    flag:
        lag-id: The ID of the LAG.
