name: describe-organization-configuration
description: Returns information about the way your organization is configured in Security Hub.
documentation:
    command: Returns information about the way your organization is configured in Security Hub. Only the Security Hub administrator account can invoke this operation.
