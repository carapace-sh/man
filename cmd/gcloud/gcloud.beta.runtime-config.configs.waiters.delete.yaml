name: delete
description: '*(BETA)*  Delete waiter resources.'
flags:
    --config-name=!: The name of the configuration resource to use.
documentation:
    command: '*(BETA)*  This command deletes the waiter resource with the specified name.'
    flag:
        config-name: The name of the configuration resource to use.
