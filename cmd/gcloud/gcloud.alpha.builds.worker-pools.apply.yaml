name: apply
description: '*(ALPHA)*  Create a private pool for use by Cloud Build.'
flags:
    --file=!: The YAML file to use as the worker pool configuration file.
    --generation=: Generation of the worker pool.
    --region=: Region for Cloud Build.
documentation:
    command: '*(ALPHA)*  Create a private pool for use by Cloud Build.'
    flag:
        file: The YAML file to use as the worker pool configuration file.
        generation: Generation of the worker pool.
        region: Region for Cloud Build.
