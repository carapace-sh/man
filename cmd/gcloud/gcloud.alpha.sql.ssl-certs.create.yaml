name: create
description: '*(ALPHA)*  *(DEPRECATED)*  Creates an SSL certificate for a Cloud SQL instance.'
flags:
    --instance=!: Cloud SQL instance ID.
documentation:
    command: |+
        *(ALPHA)*  *(DEPRECATED)*  Creates an SSL certificate for a Cloud SQL instance.

        `gcloud sql ssl-certs` is deprecated. Use `gcloud sql ssl client-certs` instead.

    flag:
        instance: Cloud SQL instance ID.
