# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Get the status of the ongoing migration for the specified storage account.
flags:
    --account-name=: The name of the storage account within the specified resource group.
    --ids=:
        description: One or more resource IDs (space-delimited).
        nargs: -1
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --name=: The name of the Storage Account Migration.
completion:
    flag:
        name:
            - default
documentation:
    flag:
        account-name: The name of the storage account within the specified resource group. Storage account names must be between 3 and 24 characters in length and use numbers and lower-case letters only.
        ids: |-
            One or more resource IDs (space-delimited). It should be a complete resource ID containing all information of 'Resource Id' arguments. You should provide either --ids or other 'Resource Id' arguments.

            Accepted values: default
        name: The name of the Storage Account Migration. It should always be 'default'
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    migration show: az storage account migration show --account-name "storage_account_name" -g "resource_group_name"
