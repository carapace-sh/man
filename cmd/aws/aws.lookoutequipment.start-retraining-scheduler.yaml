name: start-retraining-scheduler
description: Starts a retraining scheduler.
flags:
    --model-name=!: The name of the model whose retraining scheduler you want to start.
documentation:
    command: Starts a retraining scheduler.
    flag:
        model-name: The name of the model whose retraining scheduler you want to start.
