name: get-network-resource-counts
description: Gets the count of network resources, by resource type, for the specified global network.
flags:
    --global-network-id=!: The ID of the global network.
    --max-results=: The maximum number of results to return.
    --next-token=: The token for the next page of results.
    --resource-type=: The resource type.
documentation:
    command: Gets the count of network resources, by resource type, for the specified global network.
    flag:
        global-network-id: The ID of the global network.
        max-results: The maximum number of results to return.
        next-token: The token for the next page of results.
        resource-type: |-
            The resource type.

            The following are the supported resource types for Direct Connect:

            - `dxcon`
            - `dx-gateway`
            - `dx-vif`

            The following are the supported resource types for Network Manager:

            - `attachment`
            - `connect-peer`
            - `connection`
            - `core-network`
            - `device`
            - `link`
            - `peering`
            - `site`

            The following are the supported resource types for Amazon VPC:

            - `customer-gateway`
            - `transit-gateway`
            - `transit-gateway-attachment`
            - `transit-gateway-connect-peer`
            - `transit-gateway-route-table`
            - `vpn-connection`
