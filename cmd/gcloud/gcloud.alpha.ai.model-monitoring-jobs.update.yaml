name: update
description: '*(ALPHA)*  Update an Vertex AI model deployment monitoring job.'
flags:
    --analysis-instance-schema=: YAML schema file uri(Google Cloud Storage) describing the format of a
    --anomaly-cloud-logging: If true, anomaly will be sent to Cloud Logging.
    --clear-labels: Remove all labels.
    --display-name=: Display name of the model deployment monitoring job.
    --emails=: Comma-separated email address list.
    --feature-attribution-thresholds=: List of feature-attribution score threshold value pairs(Apply for all the
    --feature-thresholds=: List of feature-threshold value pairs(Apply for all the deployed models under
    --log-ttl=: TTL of BigQuery tables in user projects which stores logs(Day-based unit).
    --monitoring-config-from-file=: Path to the model monitoring objective config file.
    --monitoring-frequency=: Monitoring frequency, unit is 1 hour.
    --no-anomaly-cloud-logging&: If true, anomaly will be sent to Cloud Logging.
    --no-clear-labels&: Remove all labels.
    --notification-channels=: Comma-separated notification channel list.
    --prediction-sampling-rate=: Prediction sampling rate.
    --region=: Cloud region for the monitoring_job.
    --remove-labels=: List of label keys to remove.
    --update-labels=: List of label KEY=VALUE pairs to update.
documentation:
    command: '*(ALPHA)*  Update an Vertex AI model deployment monitoring job.'
    flag:
        analysis-instance-schema: |-
            YAML schema file uri(Google Cloud Storage) describing the format of a
            single instance that you want Tensorflow Data Validation (TFDV) to analyze.
        anomaly-cloud-logging: If true, anomaly will be sent to Cloud Logging. Use *--anomaly-cloud-logging* to enable and *--no-anomaly-cloud-logging* to disable.
        clear-labels: |-
            Remove all labels. If `--update-labels` is also specified then
            `--clear-labels` is applied first.
            +
            For example, to remove all labels:
            +
                $ {command} --clear-labels
            +
            To remove all existing labels and create two new labels,
            ``foo'' and ``baz'':
            +
                $ {command} --clear-labels --update-labels foo=bar,baz=qux
        display-name: Display name of the model deployment monitoring job.
        emails: Comma-separated email address list. e.g. --emails=a@gmail.com,b@gmail.com
        feature-attribution-thresholds: |-
            List of feature-attribution score threshold value pairs(Apply for all the
            deployed models under the endpoint, if you want to specify different thresholds
            for different deployed model, please use flag --monitoring-config-from-file or
            call API directly). If only feature name is set, the default threshold value
            would be 0.3.
            +
            For example: `feature-attribution-thresholds=feat1=0.1,feat2,feat3=0.2`
        feature-thresholds: |-
            List of feature-threshold value pairs(Apply for all the deployed models under
            the endpoint, if you want to specify different thresholds for different deployed
            model, please use flag --monitoring-config-from-file or call API directly).
            If only feature name is set, the default threshold value would be 0.3.
            +
            For example: `--feature-thresholds=feat1=0.1,feat2,feat3=0.2`
        log-ttl: TTL of BigQuery tables in user projects which stores logs(Day-based unit).
        monitoring-config-from-file: |-
            Path to the model monitoring objective config file. This file should be a YAML
            document containing a `ModelDeploymentMonitoringJob`(https://cloud.google.com/vertex-ai/docs/reference/rest/v1beta1/projects.locations.modelDeploymentMonitoringJobs#ModelDeploymentMonitoringJob),
            but only the ModelDeploymentMonitoringObjectiveConfig needs to be configured.
            +
            Note: Only one of --monitoring-config-from-file and other objective config set,
            like --feature-thresholds, --feature-attribution-thresholds needs to be set.
            +
            Example(YAML):
            +
              modelDeploymentMonitoringObjectiveConfigs:
              - deployedModelId: '5251549009234886656'
                objectiveConfig:
                  trainingDataset:
                    dataFormat: csv
                    gcsSource:
                      uris:
                      - gs://fake-bucket/training_data.csv
                    targetField: price
                  trainingPredictionSkewDetectionConfig:
                    skewThresholds:
                      feat1:
                        value: 0.9
                      feat2:
                        value: 0.8
              - deployedModelId: '2945706000021192704'
                objectiveConfig:
                  predictionDriftDetectionConfig:
                    driftThresholds:
                      feat1:
                        value: 0.3
                      feat2:
                        value: 0.4
        monitoring-frequency: Monitoring frequency, unit is 1 hour.
        no-anomaly-cloud-logging: If true, anomaly will be sent to Cloud Logging.
        no-clear-labels: |
            Remove all labels. If `--update-labels` is also specified then
            `--clear-labels` is applied first.

            For example, to remove all labels:

                $ {command} --clear-labels

            To remove all existing labels and create two new labels,
            ``foo'' and ``baz'':

                $ {command} --clear-labels --update-labels foo=bar,baz=qux
        notification-channels: Comma-separated notification channel list. e.g. --notification-channels=projects/fake-project/notificationChannels/123,projects/fake-project/notificationChannels/456
        prediction-sampling-rate: Prediction sampling rate.
        region: |-
            Cloud region for the monitoring_job.
            +
            To set the `region` attribute:
            * provide the argument `monitoring_job` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `ai/region`;
            * choose one from the prompted list of available regions.
        remove-labels: |-
            List of label keys to remove. If a label does not exist it is
            silently ignored. If `--update-labels` is also specified then
            `--update-labels` is applied first.
        update-labels: |-
            List of label KEY=VALUE pairs to update. If a label exists, its value is modified. Otherwise, a new label is created.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
