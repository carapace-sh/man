name: describe
description: '*(ALPHA)*  Returns the Account object resulting from the Get API.'
hidden: true
flags:
    --billing-account=: Cloud Billing account for the Procurement account.
documentation:
    command: '*(ALPHA)*  Returns the Account object resulting from the Get API.'
    flag:
        billing-account: |-
            Cloud Billing account for the Procurement account.
            +
            To set the `billing-account` attribute:
            * provide the argument `account` on the command line with a fully specified name;
            * provide the argument `--billing-account` on the command line.
