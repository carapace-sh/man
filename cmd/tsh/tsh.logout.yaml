# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: logout
description: Delete a cluster certificate.
documentation:
    command: '[AI] The `tsh logout` command is used in the Telegraf monitoring agent''s plugin for TSH (Telegraf Plugin for TSH Shell) to terminate a previously started TSH session and disconnect from the remote host or service being monitored. This command ensures that resources are released and no further data is collected from the monitored entity.'
