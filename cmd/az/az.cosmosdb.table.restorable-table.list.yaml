# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: Show the event feed of all mutations done on all the Azure Cosmos DB Tables.
flags:
    --subscription=: Name or ID of subscription.
    -e, --end-time=: End time of restorable tables event feed
    -i, --instance-id=!: InstanceId of the Account
    -l, --location=!: Location
    -s, --start-time=: Start time of restorable tables event feed
documentation:
    command: |-
        Command group 'cosmosdb table restorable-table' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        end-time: End time of restorable tables event feed.
        instance-id: InstanceId of the Account.
        location: Location.
        start-time: Start time of restorable tables event feed.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
