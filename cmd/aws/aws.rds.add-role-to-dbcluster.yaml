name: add-role-to-dbcluster
description: Associates an Identity and Access Management (IAM) role with a DB cluster.
flags:
    --dbcluster-identifier=!: The name of the DB cluster to associate the IAM role with.
    --feature-name=: The name of the feature for the DB cluster that the IAM role is to be associated with.
    --role-arn=!: The Amazon Resource Name (ARN) of the IAM role to associate with the Aurora DB cluster, for example `arn:aws:iam::123456789012:role/AuroraAccessRole`.
documentation:
    command: Associates an Identity and Access Management (IAM) role with a DB cluster.
    flag:
        dbcluster-identifier: The name of the DB cluster to associate the IAM role with.
        feature-name: The name of the feature for the DB cluster that the IAM role is to be associated with. For information about supported feature names, see [DBEngineVersion]().
        role-arn: The Amazon Resource Name (ARN) of the IAM role to associate with the Aurora DB cluster, for example `arn:aws:iam::123456789012:role/AuroraAccessRole`.
