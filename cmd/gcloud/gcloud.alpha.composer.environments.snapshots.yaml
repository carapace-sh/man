name: snapshots
description: '*(ALPHA)*  Save and load snapshots of environment.'
documentation:
    command: |-
        *(ALPHA)*  The gcloud alpha composer environments snapshots command group saves and loads snapshots of Cloud Composer
        environments.
