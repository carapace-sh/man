name: disassociate-distribution-tenant-web-acl
description: Disassociates a distribution tenant from the WAF web ACL.
flags:
    --id=!: The ID of the distribution tenant.
    --if-match=: The current version of the distribution tenant that you're disassociating from the WAF web ACL.
documentation:
    command: Disassociates a distribution tenant from the WAF web ACL.
    flag:
        id: The ID of the distribution tenant.
        if-match: The current version of the distribution tenant that you're disassociating from the WAF web ACL. This is the `ETag` value returned in the response to the `GetDistributionTenant` API operation.
