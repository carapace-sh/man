# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List MX record sets in a zone.
flags:
    --max-items=: Total number of items to return in the command's output.
    --next-token=: Token to specify where to start paginating.
    --recordsetnamesuffix=: The suffix label of the record set name that has to be used to filter the record set enumerations.
    --subscription=: Name or ID of subscription.
    --top=: The maximum number of record sets to return.
    -g, --resource-group=!: Name of resource group.
    -z, --zone-name=!: The name of the DNS zone (without a terminating dot).
documentation:
    flag:
        max-items: Total number of items to return in the command's output. If the total number of items available is more than the value specified, a token is provided in the command's output. To resume pagination, provide the token value in `--next-token` argument of a subsequent command.
        next-token: Token to specify where to start paginating. This is the token value from a previously truncated response.
        recordsetnamesuffix: The suffix label of the record set name that has to be used to filter the record set enumerations. If this parameter is specified, Enumeration will return only records that end with ``` .<recordSetNameSuffix> ``` .
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        top: The maximum number of record sets to return. If not specified, returns up to 100 record sets.
        zone-name: The name of the DNS zone (without a terminating dot).
examples:
    List MX record sets in a zone.: az network dns record-set mx list -g MyResourceGroup -z www.mysite.com
