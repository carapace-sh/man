name: create
description: Create a Cloud Security Command Center (SCC) posture.
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --location=: ID of the location where the resource exists (for example, global).
    --no-async&: Return immediately, without waiting for the operation in progress to
    --organization=: ID of the organization which is the parent of the resource.
    --posture-from-file=!: YAML file containing the body of the posture to be created.
documentation:
    command: |
        Create a Cloud Security Command Center (SCC) posture. First argument consists of the parent
        and name of the posture to be created. The posture details are provided in YAML file. The file
        path is mentioned in `--posture-from-file` flag.

        Created posture is returned as the response of the command. LRO operation ID is printed as the
        standard output.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        location: |-
            ID of the location where the resource exists (for example, global).
            +

            To set the `location` attribute:
            * provide the argument `posture` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        organization: |-
            ID of the organization which is the parent of the resource.
            +

            To set the `organization` attribute:
            * provide the argument `posture` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line.
        posture-from-file: YAML file containing the body of the posture to be created. Use a full or relative path to a local file containing the value of posture.
