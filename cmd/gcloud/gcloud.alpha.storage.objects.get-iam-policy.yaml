name: get-iam-policy
description: '*(ALPHA)*  Get the access policy for an object.'
hidden: true
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha storage objects get-iam-policy* behaves similarly to *gcloud alpha storage objects get-object-acl*, but
        uses the IAM policy binding syntax in the output.
