# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List the invoice sections that a user has access to.
flags:
    --account-name=!: The ID that uniquely identifies a billing account.
    --profile-name=!: The ID that uniquely identifies a billing profile.
    --subscription=: Name or ID of subscription.
documentation:
    command: |-
        Command group 'billing invoice section' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        account-name: The ID that uniquely identifies a billing account.
        profile-name: The ID that uniquely identifies a billing profile.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List invoice sections by billing account and billing profile: az billing invoice section list --account-name "{billingAccountName}" --profile-name "{billingProfileName}"
