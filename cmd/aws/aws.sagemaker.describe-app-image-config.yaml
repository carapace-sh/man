name: describe-app-image-config
description: Describes an AppImageConfig.
flags:
    --app-image-config-name=!: The name of the AppImageConfig to describe.
documentation:
    command: Describes an AppImageConfig.
    flag:
        app-image-config-name: The name of the AppImageConfig to describe.
