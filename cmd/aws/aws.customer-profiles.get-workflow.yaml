name: get-workflow
description: Get details of specified workflow.
flags:
    --domain-name=!: The unique name of the domain.
    --workflow-id=!: Unique identifier for the workflow.
documentation:
    command: Get details of specified workflow.
    flag:
        domain-name: The unique name of the domain.
        workflow-id: Unique identifier for the workflow.
