name: create
description: '*(DEPRECATED)*  Create a Data Catalog entry group.'
flags:
    --description=: Description of the entry group.
    --display-name=: Human-readable name of the entry group.
    --location=: Location of the entry group.
documentation:
    command: |4-
        *(DEPRECATED)*  This command is deprecated. Please use `gcloud dataplex entry-groups` instead.

        Create a Data Catalog entry group.
    flag:
        description: Description of the entry group.
        display-name: Human-readable name of the entry group.
        location: |-
            Location of the entry group.
            +

            To set the `location` attribute:
            * provide the argument `entry_group` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
