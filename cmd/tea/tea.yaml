# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: tea
description: command line tool to interact with Gitea
documentation:
    command: '[AI] The `tea` command is not a standard command in Unix or Linux operating systems. It may refer to various things depending on the context, such as the TeX typesetting system, the TEA encryption algorithm, or even a simple alias or script for making a cup of tea. Without additional context, it''s impossible to determine what `tea` is intended to do in a given environment.'
