name: list-service-pipeline-provisioned-resources
description: List provisioned resources for a service and pipeline with details.
flags:
    --next-token=: A token that indicates the location of the next provisioned resource in the array of provisioned resources, after the list of provisioned resources that was previously requested.
    --service-name=!: The name of the service whose pipeline's provisioned resources you want.
documentation:
    command: List provisioned resources for a service and pipeline with details.
    flag:
        next-token: A token that indicates the location of the next provisioned resource in the array of provisioned resources, after the list of provisioned resources that was previously requested.
        service-name: The name of the service whose pipeline's provisioned resources you want.
