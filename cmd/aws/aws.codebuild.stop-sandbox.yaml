name: stop-sandbox
description: Stops a sandbox.
flags:
    --id=!: Information about the requested sandbox ID.
documentation:
    command: Stops a sandbox.
    flag:
        id: Information about the requested sandbox ID.
