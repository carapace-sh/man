# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all Active Directory administrators.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -s, --server-name=!: Name of the server.
documentation:
    flag:
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        server-name: Name of the server. The name can contain only lowercase letters, numbers, and the hyphen (-) character. Minimum 3 characters and maximum 63 characters.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List Active Directory administrators.: az mysql flexible-server ad-admin list -g testgroup -s testsvr
