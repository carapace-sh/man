name: list-environment-provisioned-resources
description: List the provisioned resources for your environment.
flags:
    --environment-name=!: The environment name.
    --next-token=: A token that indicates the location of the next environment provisioned resource in the array of environment provisioned resources, after the list of environment provisioned resources that was previously requested.
documentation:
    command: List the provisioned resources for your environment.
    flag:
        environment-name: The environment name.
        next-token: A token that indicates the location of the next environment provisioned resource in the array of environment provisioned resources, after the list of environment provisioned resources that was previously requested.
