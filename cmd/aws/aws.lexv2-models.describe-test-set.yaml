name: describe-test-set
description: Gets metadata information about the test set.
flags:
    --test-set-id=!: The test set Id for the test set request.
documentation:
    command: Gets metadata information about the test set.
    flag:
        test-set-id: The test set Id for the test set request.
