# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-cached
description: list cached-and-ready-to-install verbs
documentation:
    command: '[AI] `winetricks list-cached` is a command used in Wine, a compatibility layer for running Windows applications on Linux and macOS. This command displays a list of all installed and cached DLLs (Dynamic Link Libraries) and other system components managed by Winetricks, a frontend for Wine configuration. The output indicates the name, version, and installation date of each component.'
