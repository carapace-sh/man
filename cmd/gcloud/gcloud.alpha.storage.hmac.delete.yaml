name: delete
description: '*(ALPHA)*  Remove a service account HMAC.'
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha storage hmac delete* permanently deletes the specified HMAC key. Note that keys
        must be updated to be in the ``INACTIVE'' state before they can be
        deleted.
