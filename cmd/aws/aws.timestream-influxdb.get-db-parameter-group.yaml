name: get-db-parameter-group
description: Returns a Timestream for InfluxDB DB parameter group.
flags:
    --identifier=!: The id of the DB parameter group.
documentation:
    command: Returns a Timestream for InfluxDB DB parameter group.
    flag:
        identifier: The id of the DB parameter group.
