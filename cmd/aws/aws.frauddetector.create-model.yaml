name: create-model
description: Creates a model using the specified model type.
flags:
    --description=: The model description.
    --event-type-name=!: The name of the event type.
    --model-id=!: The model ID.
    --model-type=!: The model type.
    --tags=: A collection of key and value pairs.
completion:
    flag:
        model-type:
            - ONLINE_FRAUD_INSIGHTS
            - TRANSACTION_FRAUD_INSIGHTS
            - ACCOUNT_TAKEOVER_INSIGHTS
documentation:
    command: Creates a model using the specified model type.
    flag:
        description: The model description.
        event-type-name: The name of the event type.
        model-id: The model ID.
        model-type: The model type.
        tags: A collection of key and value pairs.
