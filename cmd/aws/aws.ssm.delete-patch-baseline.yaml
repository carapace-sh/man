name: delete-patch-baseline
description: Deletes a patch baseline.
flags:
    --baseline-id=!: The ID of the patch baseline to delete.
documentation:
    command: Deletes a patch baseline.
    flag:
        baseline-id: The ID of the patch baseline to delete.
