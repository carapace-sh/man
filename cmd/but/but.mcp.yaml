# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: mcp
description: Starts up the MCP server
flags:
    -h, --help: Print help
documentation:
    command: Starts the Model Context Protocol (MCP) server for GitButler, enabling integration with AI assistants and other tools that support the MCP protocol.
