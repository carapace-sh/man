name: describe
description: '*(BETA)*  Show details about a DNS Authorization.'
flags:
    --location=: Certificate Manager location.
documentation:
    command: '*(BETA)*  Show details about a DNS Authorization.'
    flag:
        location: |-
            Certificate Manager location.
            +
            To set the `location` attribute:
            * provide the argument `dns_authorization` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * default value of location is [global].
