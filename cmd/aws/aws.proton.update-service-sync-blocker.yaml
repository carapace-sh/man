name: update-service-sync-blocker
description: Update the service sync blocker by resolving it.
flags:
    --id=!: The ID of the service sync blocker.
    --resolved-reason=!: The reason the service sync blocker was resolved.
documentation:
    command: Update the service sync blocker by resolving it.
    flag:
        id: The ID of the service sync blocker.
        resolved-reason: The reason the service sync blocker was resolved.
