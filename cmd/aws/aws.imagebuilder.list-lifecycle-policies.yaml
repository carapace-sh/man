name: list-lifecycle-policies
description: Get a list of lifecycle policies in your Amazon Web Services account.
flags:
    --filters=: 'Streamline results based on one of the following values: `Name`, `Status`.'
    --max-results=: Specify the maximum number of items to return in a request.
    --next-token=: A token to specify where to start paginating.
documentation:
    command: Get a list of lifecycle policies in your Amazon Web Services account.
    flag:
        filters: 'Streamline results based on one of the following values: `Name`, `Status`.'
        max-results: Specify the maximum number of items to return in a request.
        next-token: A token to specify where to start paginating. This is the nextToken from a previously truncated response.
