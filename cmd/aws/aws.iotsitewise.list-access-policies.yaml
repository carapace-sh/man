name: list-access-policies
description: Retrieves a paginated list of access policies for an identity (an IAM Identity Center user, an IAM Identity Center group, or an IAM user) or an IoT SiteWise Monitor resource (a portal or project).
flags:
    --iam-arn=: The ARN of the IAM user.
    --identity-id=: The ID of the identity.
    --identity-type=: The type of identity (IAM Identity Center user, IAM Identity Center group, or IAM user).
    --max-results=: The maximum number of results to return for each paginated request.
    --next-token=: The token to be used for the next set of paginated results.
    --resource-id=: The ID of the resource.
    --resource-type=: The type of resource (portal or project).
completion:
    flag:
        identity-type:
            - USER
            - GROUP
            - IAM
        resource-type:
            - PORTAL
            - PROJECT
documentation:
    command: Retrieves a paginated list of access policies for an identity (an IAM Identity Center user, an IAM Identity Center group, or an IAM user) or an IoT SiteWise Monitor resource (a portal or project).
    flag:
        iam-arn: The ARN of the IAM user. For more information, see [IAM ARNs](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html) in the *IAM User Guide*. This parameter is required if you specify `IAM` for `identityType`.
        identity-id: The ID of the identity. This parameter is required if you specify `USER` or `GROUP` for `identityType`.
        identity-type: The type of identity (IAM Identity Center user, IAM Identity Center group, or IAM user). This parameter is required if you specify `identityId`.
        max-results: |-
            The maximum number of results to return for each paginated request.

            Default: 50
        next-token: The token to be used for the next set of paginated results.
        resource-id: The ID of the resource. This parameter is required if you specify `resourceType`.
        resource-type: The type of resource (portal or project). This parameter is required if you specify `resourceId`.
