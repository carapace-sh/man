name: start-batch-delete-configuration-task
description: Takes a list of configurationId as input and starts an asynchronous deletion task to remove the configurationItems.
flags:
    --configuration-ids=!: The list of configuration IDs that will be deleted by the task.
    --configuration-type=!: The type of configuration item to delete.
completion:
    flag:
        configuration-type:
            - SERVER
documentation:
    command: Takes a list of configurationId as input and starts an asynchronous deletion task to remove the configurationItems. Returns a unique deletion task identifier.
    flag:
        configuration-ids: The list of configuration IDs that will be deleted by the task.
        configuration-type: 'The type of configuration item to delete. Supported types are: SERVER.'
