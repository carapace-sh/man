name: describe
description: '*(ALPHA)*  Gets Firebase Data Connect service schema.'
hidden: true
flags:
    --location=: The location id of the schema resource.
    --service=: The service id of the schema resource.
documentation:
    command: |
        *(ALPHA)*  Gets Firebase Data Connect service schema.
    flag:
        location: |-
            The location id of the schema resource.
            +
            To set the `location` attribute:
            * provide the argument `schema` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        service: |-
            The service id of the schema resource.
            +
            To set the `service` attribute:
            * provide the argument `schema` on the command line with a fully specified name;
            * provide the argument `--service` on the command line.
