name: get-lists-metadata
description: Gets the metadata of either all the lists under the account or the specified list.
flags:
    --max-results=: The maximum number of objects to return for the request.
    --name=: The name of the list.
    --next-token=: The next token for the subsequent request.
documentation:
    command: Gets the metadata of either all the lists under the account or the specified list.
    flag:
        max-results: The maximum number of objects to return for the request.
        name: The name of the list.
        next-token: The next token for the subsequent request.
