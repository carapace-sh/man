name: enable
description: '*(BETA)*  Enable an IAM service account.'
documentation:
    command: |
        *(BETA)*  Enable an IAM service account.

        If the service account does not exist, this command returns a `PERMISSION_DENIED` error.
