name: list-ip-owners
description: '*(ALPHA)*  List IP owners in a network.'
flags:
    --filter=: Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
    --ip-cidr-range=: Only return IP owners whose IP ranges overlap with the IP CIDR range.
    --limit=: Maximum number of resources to list.
    --no-uri&: Print a list of resource URIs instead of the default output, and change the
    --owner-projects=: Only return IP owners in the projects.
    --owner-types=: Only return IP owners of the types, which can be any combination of instance, address, forwardingRule, subnetwork, or network.
    --page-size=: Some services group resource list output into pages.
    --sort-by=: Comma-separated list of resource field key names to sort by.
    --subnet-name=: Only return IP owners in the subnets with the name.
    --subnet-region=: Only return IP owners in the subnets of the region.
    --uri: Print a list of resource URIs instead of the default output, and change the
documentation:
    command: '*(ALPHA)*  *gcloud alpha compute networks list-ip-owners* is used to list IP owners in a network.'
    flag:
        filter: |-
            Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.
            If the expression evaluates `True`, then that item is listed. For more
            details and examples of filter expressions, run $ gcloud topic filters. This
            flag interacts with other flags that are applied in this order: *--flatten*,
            *--sort-by*, *--filter*, *--limit*.
        ip-cidr-range: Only return IP owners whose IP ranges overlap with the IP CIDR range.
        limit: |-
            Maximum number of resources to list. The default is *unlimited*.
            This flag interacts with other flags that are applied in this order:
            *--flatten*, *--sort-by*, *--filter*, *--limit*.
        no-uri: |
            Print a list of resource URIs instead of the default output, and change the
            command output to a list of URIs. If this flag is used with *--format*,
            the formatting is applied on this URI list. To display URIs alongside other
            keys instead, use the *uri()* transform.
        owner-projects: Only return IP owners in the projects. Multiple projects are separated by comma, e.g., "project-1,project-2".
        owner-types: Only return IP owners of the types, which can be any combination of instance, address, forwardingRule, subnetwork, or network. Multiple types are separated by comma, e.g., "instance,forwardingRule,address"
        page-size: |-
            Some services group resource list output into pages. This flag specifies
            the maximum number of resources per page. The default is determined by the
            service if it supports paging, otherwise it is *unlimited* (no paging).
            Paging may be applied before or after *--filter* and *--limit* depending
            on the service.
        sort-by: |-
            Comma-separated list of resource field key names to sort by. The
            default order is ascending. Prefix a field with ``~'' for descending
            order on that field. This flag interacts with other flags that are applied
            in this order: *--flatten*, *--sort-by*, *--filter*, *--limit*.
        subnet-name: Only return IP owners in the subnets with the name. Not applicable for legacy networks.
        subnet-region: Only return IP owners in the subnets of the region. Not applicable for legacy networks.
        uri: |-
            Print a list of resource URIs instead of the default output, and change the
            command output to a list of URIs. If this flag is used with *--format*,
            the formatting is applied on this URI list. To display URIs alongside other
            keys instead, use the *uri()* transform.
