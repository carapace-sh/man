name: disable
description: '*(ALPHA)*  Disable the given project as a shared VPC host.'
documentation:
    command: |-
        *(ALPHA)*  That is, after running this command, this project will not be able to share
        VPC networks to other projects.
