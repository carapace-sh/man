name: list-run-tasks
description: Returns a list of tasks and status information within their specified run.
flags:
    --id=!: The run's ID.
    --max-results=: The maximum number of run tasks to return in one page of results.
    --starting-token=: Specify the pagination token from a previous request to retrieve the next page of results.
    --status=: Filter the list by status.
completion:
    flag:
        status:
            - PENDING
            - STARTING
            - RUNNING
            - STOPPING
            - COMPLETED
            - CANCELLED
            - FAILED
documentation:
    command: Returns a list of tasks and status information within their specified run. Use this operation to monitor runs and to identify which specific tasks have failed.
    flag:
        id: The run's ID.
        max-results: The maximum number of run tasks to return in one page of results.
        starting-token: Specify the pagination token from a previous request to retrieve the next page of results.
        status: Filter the list by status.
