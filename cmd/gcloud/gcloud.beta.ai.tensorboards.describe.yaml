name: describe
description: '*(BETA)*  Gets detailed Tensorboard information about the given Tensorboard id.'
flags:
    --region=: Cloud region for the tensorboard.
documentation:
    command: '*(BETA)*  Gets detailed Tensorboard information about the given Tensorboard id.'
    flag:
        region: |-
            Cloud region for the tensorboard.
            +
            To set the `region` attribute:
            * provide the argument `tensorboard` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `ai/region`;
            * choose one from the prompted list of available regions.
