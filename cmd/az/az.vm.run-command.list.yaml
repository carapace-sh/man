# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List run commands from a VM or a location
flags:
    --expand=: The expand expression to apply on the operation.
    --subscription=: Name or ID of subscription.
    --vm-name=: The name of the virtual machine
    -g, --resource-group=: Name of resource group.
    -l, --location=: Location.
documentation:
    command: List run commands from a VM or a location.
    flag:
        expand: The expand expression to apply on the operation.
        location: 'Location. Values from: ``` az account list-locations ``` . You can configure the default location using ``` az configure --defaults location=<location> ``` .'
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        vm-name: The name of the virtual machine.
examples:
    List all available run commands for a subscription in a location.: az vm run-command list --location "SoutheastAsia"
    List run commands in a virtual machine.: az vm run-command list --resource-group "myResourceGroup" --vm-name "myVM"
