name: delete-workflow
description: Deletes the specified workflow.
flags:
    --workflow-id=!: A unique identifier for the workflow.
documentation:
    command: Deletes the specified workflow.
    flag:
        workflow-id: A unique identifier for the workflow.
