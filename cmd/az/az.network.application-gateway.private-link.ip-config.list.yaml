# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all the IP configurations of a private link.
flags:
    --gateway-name=!: Name of the application gateway.
    --private-link=!: Name of private link.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
documentation:
    command: |-
        Command group 'az network application-gateway private-link ip-config' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        gateway-name: Name of the application gateway.
        private-link: Name of private link.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List all the IP configurations of a private link.: az network application-gateway private-link ip-config list -g MyResourceGroup --gateway-name MyGateway --private-link MyPrivateLink
