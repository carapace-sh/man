# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: version
description: Prints the version of this Waypoint CLI
documentation:
    command: '[AI] The `waypoint version` command is used in Waypoint, an open-source multi-cluster Kubernetes management platform, to check and print out the current version of Waypoint that is installed on your system. It helps users ensure they are running the latest or required version for their use case.'
