name: describe
description: Describe an accelerator type available for Cloud TPU VM nodes.
flags:
    --zone=: Zone of the Cloud TPU.
documentation:
    command: Get details on an accelerator type.
    flag:
        zone: |-
            Zone of the Cloud TPU.
            +
            If not specified, will use `default` compute/zone.
            +

            To set the `zone` attribute:
            * provide the argument `accelerator_type` on the command line with a fully specified name;
            * provide the argument `--zone` on the command line;
            * set the property `compute/zone`.
