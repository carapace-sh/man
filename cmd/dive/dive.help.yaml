# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: help
description: Help about any command
documentation:
    command: '[AI] The `dive help` command is used in Docker to display help information about the `dive` tool, which is a Docker utility for accessing files within a container without the need to start it. This command shows the available sub-commands and their usage details, providing guidance on how to effectively use the `dive` tool to manage containers and access their files.'
