name: metrics
description: '*(BETA)*  Get the metrics for a Cloud Healthcare API DICOM store.'
flags:
    --dataset=: Cloud Healthcare dataset.
    --location=: Google Cloud location.
documentation:
    command: '*(BETA)*  Gets the metrics for a Cloud Healthcare API DICOM store.'
    flag:
        dataset: |-
            Cloud Healthcare dataset.
            +
            To set the `dataset` attribute:
            * provide the argument `dicom_store` on the command line with a fully specified name;
            * provide the argument `--dataset` on the command line.
        location: |-
            Google Cloud location.
            +
            To set the `location` attribute:
            * provide the argument `dicom_store` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `healthcare/location`.
