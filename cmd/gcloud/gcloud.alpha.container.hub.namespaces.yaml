name: namespaces
description: '*(ALPHA)*  *(REMOVED)*  Fleet namespaces are the fleet equivalent of k8s cluster namespaces.'
hidden: true
documentation:
    command: |-
        *(ALPHA)*  *(REMOVED)*  Fleet namespaces are the fleet equivalent of k8s cluster namespaces.

        This command has been removed as of version 447.0.0. Use `gcloud fleet scopes namespaces` instead.

        This command group allows for manipulation of fleet namespaces.

        ## EXAMPLES

        Manage fleet namespaces:

          $ gcloud alpha container hub namespaces --help

        Manage RBAC RoleBindings in a fleet namespace:

          $ gcloud alpha container hub namespaces rbacrolebindings --help
