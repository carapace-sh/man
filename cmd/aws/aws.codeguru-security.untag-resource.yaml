name: untag-resource
description: Use to remove one or more tags from an existing scan.
flags:
    --resource-arn=!: The ARN of the `ScanName` object.
    --tag-keys=!: A list of keys for each tag you want to remove from a scan.
documentation:
    command: Use to remove one or more tags from an existing scan.
    flag:
        resource-arn: The ARN of the `ScanName` object. You can retrieve this ARN by calling `CreateScan`, `ListScans`, or `GetScan`.
        tag-keys: A list of keys for each tag you want to remove from a scan.
