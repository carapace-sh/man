# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: unapply BRANCH_NAME
description: Unapply a branch from the workspace
flags:
    -f, --force: Force unapply without confirmation
    -h, --help: Print help
