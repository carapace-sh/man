name: delete-graph-snapshot
description: Deletes the specifed graph snapshot.
flags:
    --snapshot-identifier=!: ID of the graph snapshot to be deleted.
documentation:
    command: Deletes the specifed graph snapshot.
    flag:
        snapshot-identifier: ID of the graph snapshot to be deleted.
