name: create
description: '*(ALPHA)*  Command to create a experiment in the Project/Location.'
hidden: true
flags:
    --file=!: The file containing the experiment to be created.
    --location=: The Cloud location for the experiment.
documentation:
    command: '*(ALPHA)*  Command to create a experiment in the Project/Location.'
    flag:
        file: The file containing the experiment to be created.
        location: |-
            The Cloud location for the experiment.
            +
            To set the `location` attribute:
            * provide the argument `experiment` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
