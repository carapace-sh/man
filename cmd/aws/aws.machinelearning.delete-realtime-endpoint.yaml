name: delete-realtime-endpoint
description: Deletes a real time endpoint of an `MLModel`.
flags:
    --mlmodel-id=!: The ID assigned to the `MLModel` during creation.
documentation:
    command: Deletes a real time endpoint of an `MLModel`.
    flag:
        mlmodel-id: The ID assigned to the `MLModel` during creation.
