name: list-catalog-items
description: Lists the items in the catalog.
flags:
    --ec2-family-filter=: Filters the results by EC2 family (for example, M5).
    --item-class-filter=: Filters the results by item class.
    --max-results=: ""
    --next-token=: ""
    --supported-storage-filter=: Filters the results by storage option.
documentation:
    command: |-
        Lists the items in the catalog.

        Use filters to return specific results. If you specify multiple filters, the results include only the resources that match all of the specified filters. For a filter where you can specify multiple values, the results include items that match any of the values that you specify for the filter.
    flag:
        ec2-family-filter: Filters the results by EC2 family (for example, M5).
        item-class-filter: Filters the results by item class.
        max-results: ""
        next-token: ""
        supported-storage-filter: Filters the results by storage option.
