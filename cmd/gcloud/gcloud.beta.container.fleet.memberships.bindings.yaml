name: bindings
description: '*(BETA)*  Membership Bindings for permissions.'
documentation:
    command: |-
        *(BETA)*  This command group allows for manipulation of bindings associated with
        membership-scope.

        ## EXAMPLES

        Manage Bindings:

          $ gcloud beta container fleet memberships bindings --help
