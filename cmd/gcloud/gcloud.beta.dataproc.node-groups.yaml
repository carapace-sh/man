name: node-groups
description: '*(BETA)*  Manage Dataproc node groups.'
documentation:
    command: |-
        *(BETA)*  Manage and modify Dataproc node groups created with a parent cluster.

        ## EXAMPLES

        To describe a node group:

          $ gcloud beta dataproc node-groups describe NODE_GROUP_ID --cluster cluster_name --region region

        To resize a node group:

          $ gcloud beta dataproc node-groups resize NODE_GROUP_ID --cluster cluster_name --region region
          --size new_size
