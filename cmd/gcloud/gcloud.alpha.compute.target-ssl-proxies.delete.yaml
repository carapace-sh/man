name: delete
description: '*(ALPHA)*  Delete target SSL proxies.'
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute target-ssl-proxies delete* deletes one or more target SSL proxies.
