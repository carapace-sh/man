# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all management groups in the current tenant.
flags:
    --no-register=: Skip registration for resource provider Microsoft.Management
documentation:
    flag:
        no-register: |-
            Skip registration for resource provider Microsoft.Management.

            Default value: False
examples:
    List all management groups: az account management-group list
