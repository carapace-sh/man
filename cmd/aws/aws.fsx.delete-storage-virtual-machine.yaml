name: delete-storage-virtual-machine
description: Deletes an existing Amazon FSx for ONTAP storage virtual machine (SVM).
flags:
    --client-request-token=: ""
    --storage-virtual-machine-id=!: The ID of the SVM that you want to delete.
documentation:
    command: Deletes an existing Amazon FSx for ONTAP storage virtual machine (SVM). Prior to deleting an SVM, you must delete all non-root volumes in the SVM, otherwise the operation will fail.
    flag:
        client-request-token: ""
        storage-virtual-machine-id: The ID of the SVM that you want to delete.
