name: delete
description: '*(BETA)*  Delete an existing Vertex AI deployment resource pool.'
hidden: true
flags:
    --region=: Cloud region for the deployment_resource_pool.
documentation:
    command: |-
        ## EXAMPLES

        To delete a deployment resource pool ``123'' under project ``example'' in
        region ``us-central1'', run:

          $ gcloud beta ai deployment-resource-pools delete 123 --project=example --region=us-central1
    flag:
        region: |-
            Cloud region for the deployment_resource_pool.
            +
            To set the `region` attribute:
            * provide the argument `deployment_resource_pool` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `ai/region`;
            * choose one from the prompted list of available regions.
