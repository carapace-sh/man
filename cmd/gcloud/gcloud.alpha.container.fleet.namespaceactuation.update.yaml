name: update
description: '*(ALPHA)*  Update Namespace Actuation Feature.'
flags:
    --actuation-mode=: The actuation mode that can the feature will use.
completion:
    flag:
        actuation-mode:
            - unspecified
            - create-and-delete-if-created
            - add-and-remove-fleet-labels
documentation:
    command: |-
        *(ALPHA)*  This command updates Namespace Actuation Feature in a fleet.

        ## EXAMPLES

        To update the Namespace Actuation Feature, run:

          $ gcloud alpha container fleet namespaceactuation update
    flag:
        actuation-mode: 'The actuation mode that can the feature will use. _ACTUATION_MODE_ must be one of: *unspecified*, *create-and-delete-if-created*, *add-and-remove-fleet-labels*.'
