name: cancel-contact
description: Cancels a contact with a specified contact ID.
flags:
    --contact-id=!: UUID of a contact.
documentation:
    command: Cancels a contact with a specified contact ID.
    flag:
        contact-id: UUID of a contact.
