name: describe-package-import-job
description: Returns information about a package import job.
flags:
    --job-id=!: The job's ID.
documentation:
    command: Returns information about a package import job.
    flag:
        job-id: The job's ID.
