name: delete
description: '*(BETA)*  Delete the IAP TCP Destination Group resource.'
flags:
    --region=!: Region of the Destination Group.
documentation:
    command: '*(BETA)*  Delete the IAP TCP Destination Group resource.'
    flag:
        region: Region of the Destination Group.
