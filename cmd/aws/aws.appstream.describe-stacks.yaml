name: describe-stacks
description: Retrieves a list that describes one or more specified stacks, if the stack names are provided.
flags:
    --names=: The names of the stacks to describe.
    --next-token=: The pagination token to use to retrieve the next page of results for this operation.
documentation:
    command: Retrieves a list that describes one or more specified stacks, if the stack names are provided. Otherwise, all stacks in the account are described.
    flag:
        names: The names of the stacks to describe.
        next-token: The pagination token to use to retrieve the next page of results for this operation. If this value is null, it retrieves the first page.
