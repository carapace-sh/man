name: enable
description: '*(ALPHA)*  Enable a fleet feature.'
hidden: true
documentation:
    command: |-
        ## EXAMPLES

        To enable the `servicemesh` feature, run:

          $ gcloud alpha container hub features enable servicemesh
