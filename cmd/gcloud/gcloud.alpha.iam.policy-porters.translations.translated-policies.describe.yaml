name: describe
description: '*(ALPHA)*  Describes translated policies.'
hidden: true
flags:
    --location=: The location id of the translatedPolicy resource.
    --policy-porter=: The policyPorter id of the translatedPolicy resource.
    --translation=: The translation id of the translatedPolicy resource.
documentation:
    command: |
        *(ALPHA)*  Describes the translated policy.
    flag:
        location: |-
            The location id of the translatedPolicy resource.
            +
            To set the `location` attribute:
            * provide the argument `translated_policy` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        policy-porter: |-
            The policyPorter id of the translatedPolicy resource.
            +
            To set the `policy-porter` attribute:
            * provide the argument `translated_policy` on the command line with a fully specified name;
            * provide the argument `--policy-porter` on the command line.
        translation: |-
            The translation id of the translatedPolicy resource.
            +
            To set the `translation` attribute:
            * provide the argument `translated_policy` on the command line with a fully specified name;
            * provide the argument `--translation` on the command line.
