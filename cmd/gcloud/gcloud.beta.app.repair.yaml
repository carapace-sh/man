name: repair
description: '*(BETA)*  Restores required App Engine features to the current application.'
documentation:
    command: |-
        *(BETA)*  For example, this command will restore the App Engine staging bucket if it
        has been deleted. It will no longer restore the service account, instead, the
        IAM service account undelete API must be used for the purpose.
