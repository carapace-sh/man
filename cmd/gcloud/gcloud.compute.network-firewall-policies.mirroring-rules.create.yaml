name: create
description: Creates a Compute Engine network firewall policy packet mirroring rule.
flags:
    --action=!: Action to take if the request matches the match condition.
    --description=: An optional, textual description for the rule.
    --dest-ip-ranges=: Destination IP ranges to match for this rule.
    --direction=: Direction of the traffic the rule is applied.
    --disabled: Use this flag to disable the rule.
    --firewall-policy=!: Firewall policy ID with which to create rule.
    --global-firewall-policy!: Use this flag to indicate that firewall policy is global.
    --layer4-configs=!: A list of destination protocols and ports to which the firewall rule will apply.
    --no-disabled&: Use this flag to disable the rule.
    --no-global-firewall-policy!&: Use this flag to indicate that firewall policy is global.
    --security-profile-group=: A security profile group to be used with mirror action.
    --src-ip-ranges=: A list of IP address blocks that are allowed to make inbound connections that match the firewall rule to the instances on the network.
    --target-secure-tags=: An optional, list of target secure tags with a name of the format tagValues/ or full namespaced name
completion:
    flag:
        action:
            - mirror
            - do_not_mirror
            - goto_next
        direction:
            - INGRESS
            - EGRESS
documentation:
    command: '*gcloud compute network-firewall-policies mirroring-rules create* is used to create network firewall policy packet mirroring rules.'
    flag:
        action: 'Action to take if the request matches the match condition. _ACTION_ must be one of: *mirror*, *do_not_mirror*, *goto_next*.'
        description: An optional, textual description for the rule.
        dest-ip-ranges: Destination IP ranges to match for this rule.
        direction: 'Direction of the traffic the rule is applied. The default is to apply on incoming traffic. _DIRECTION_ must be one of: *INGRESS*, *EGRESS*.'
        disabled: Use this flag to disable the rule. Disabled rules will not affect traffic. Use *--disabled* to enable and *--no-disabled* to disable.
        firewall-policy: Firewall policy ID with which to create rule.
        global-firewall-policy: Use this flag to indicate that firewall policy is global.
        layer4-configs: A list of destination protocols and ports to which the firewall rule will apply.
        no-disabled: Use this flag to disable the rule. Disabled rules will not affect traffic.
        no-global-firewall-policy: Use this flag to indicate that firewall policy is global.
        security-profile-group: A security profile group to be used with mirror action.
        src-ip-ranges: 'A list of IP address blocks that are allowed to make inbound connections that match the firewall rule to the instances on the network. The IP address blocks must be specified in CIDR format: http://en.wikipedia.org/wiki/Classless_Inter-Domain_Routing.Either --src-ip-ranges or --src-secure-tags must be specified for INGRESS traffic. If both --src-ip-ranges and --src-secure-tags are specified, the rule matches if either the range of the source matches --src-ip-ranges or the secure tag of the source matches --src-secure-tags.Multiple IP address blocks can be specified if they are separated by commas.'
        target-secure-tags: An optional, list of target secure tags with a name of the format tagValues/ or full namespaced name
