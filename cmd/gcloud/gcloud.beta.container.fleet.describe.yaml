name: describe
description: '*(BETA)*  Show fleet info.'
documentation:
    command: |-
        *(BETA)*  This command can fail for the following reasons:
        * The project specified does not exist.
        * The project specified does not have a fleet.
        * The active account does not have permission to access the given project.

        ## EXAMPLES

        To print metadata for the fleet in project `example-foo-bar-1`, run:

          $ gcloud beta container fleet describe --project=example-foo-bar-1
