# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Create a Managed Certificate for a hostname in a webapp app.
flags:
    --certificate-name=: The name of the certificate.
    --hostname=!: The custom domain name
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: Name of the web app.
    -s, --slot=: the name of the slot.
documentation:
    command: |-
        This command is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        certificate-name: The name of the certificate.
        change-reference: The related change reference ID for this resource operation.
        hostname: The custom domain name.
        name: Name of the web app.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        slot: The name of the slot. Default to the productions slot if not specified.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Create a Managed Certificate for cname.mycustomdomain.com.: az webapp config ssl create --resource-group MyResourceGroup --name MyWebapp --hostname cname.mycustomdomain.com
