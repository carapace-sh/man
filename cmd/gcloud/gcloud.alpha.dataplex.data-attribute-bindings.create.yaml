name: create
description: '*(ALPHA)*  Create a Dataplex Data Attribute Binding.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --description=: Description of the Data Attribute Binding.
    --display-name=: Display Name of the Data Attribute Binding.
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: The location of the Dataplex resource.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-validate-only&: Validate the create action, but don't actually perform it.
    --path-file-format=: The format of the file to create the path.
    --path-file-name=: The name of the JSON or YAML file to define Path config from.
    --paths=: The list of paths for items within the associated resource (eg.
    --resource-attributes=: List of attributes to be associated with the resource.
    --resource=: The resource name of the resource that is binded to Data Attribute.
    --validate-only: Validate the create action, but don't actually perform it.
completion:
    flag:
        path-file-format:
            - json
            - yaml
documentation:
    command: '*(ALPHA)*  Create a Dataplex Data Attribute Binding.'
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        description: Description of the Data Attribute Binding.
        display-name: Display Name of the Data Attribute Binding.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            The location of the Dataplex resource.
            +
            To set the `location` attribute:
            * provide the argument `data_attribute_binding` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `dataplex/location`.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-validate-only: Validate the create action, but don't actually perform it.
        path-file-format: 'The format of the file to create the path. Specify either yaml or json. Defaults to json if not specified. Will be ignored if --file-name is not specified. _PATH_FILE_FORMAT_ must be one of: *json*, *yaml*.'
        path-file-name: The name of the JSON or YAML file to define Path config from.
        paths: The list of paths for items within the associated resource (eg. columns within a table) along with attribute bindings. The args can be passed as key value pair. Supported Keys are --path=name=value1,attributes=value2 ,See https://cloud.google.com/sdk/gcloud/reference/topic/escaping for details on using a delimiter other than a comma. Attribute name should be fully qualified attribute name.
        resource: The resource name of the resource that is binded to Data Attribute.
        resource-attributes: List of attributes to be associated with the resource. It should be fully qualified attribute name
        validate-only: Validate the create action, but don't actually perform it.
