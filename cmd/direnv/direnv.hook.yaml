# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: hook
description: Used to setup the shell hook
documentation:
    command: '[AI] `direnv hook` is a command used with Direnv, a directory-scoped environment variable manager for Unix-based systems. The command `direnv add <dir> hook <script>` is used to define custom hooks that get executed when entering or leaving a specified directory. These hooks can be utilized for various purposes such as setting up complex environments or running additional scripts.'
