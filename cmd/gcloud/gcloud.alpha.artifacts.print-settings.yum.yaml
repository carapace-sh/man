name: yum
description: '*(ALPHA)*  Print settings to add to the yum.repos.d directory.'
flags:
    --location=: Location of the repository.
    --repository=: ID of the repository or fully qualified identifier for the repository.
documentation:
    command: |-
        *(ALPHA)*  Print settings to add to the yum.repos.d directory for connecting to a Yum
        repository.
    flag:
        location: |-
            Location of the repository.
            +
            To set the `location` attribute:
            * provide the argument `--repository` on the command line with a fully specified name;
            * set the property `artifacts/repository` with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `artifacts/location`.
        repository: |-
            ID of the repository or fully qualified identifier for the repository.
            +
            To set the `repository` attribute:
            * provide the argument `--repository` on the command line;
            * set the property `artifacts/repository`.
