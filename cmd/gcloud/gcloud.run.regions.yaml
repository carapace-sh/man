name: regions
description: View available Cloud Run (fully managed) regions.
flags:
    --platform=&: Target platform for running commands.
completion:
    flag:
        platform:
            - gke
            - kubernetes
            - managed
documentation:
    command: View available Cloud Run (fully managed) regions.
    flag:
        platform: 'Target platform for running commands. Alternatively, set the property [run/platform]. '
