name: create-configuration-set
description: Create a new configuration set.
flags:
    --configuration-set-name=: The name that you want to give the configuration set.
documentation:
    command: Create a new configuration set. After you create the configuration set, you can add one or more event destinations to it.
    flag:
        configuration-set-name: The name that you want to give the configuration set.
