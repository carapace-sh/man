name: describe
description: '*(ALPHA)*  Gets a SourcePolicy.'
hidden: true
flags:
    --location=: The location id of the sourcePolicy resource.
    --policy-porter=: The policyPorter id of the sourcePolicy resource.
    --translation=: The translation id of the sourcePolicy resource.
documentation:
    command: |
        *(ALPHA)*  Get the source policies parsed from the input policy json.
        resource.
    flag:
        location: |-
            The location id of the sourcePolicy resource.
            +
            To set the `location` attribute:
            * provide the argument `source_policy` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        policy-porter: |-
            The policyPorter id of the sourcePolicy resource.
            +
            To set the `policy-porter` attribute:
            * provide the argument `source_policy` on the command line with a fully specified name;
            * provide the argument `--policy-porter` on the command line.
        translation: |-
            The translation id of the sourcePolicy resource.
            +
            To set the `translation` attribute:
            * provide the argument `source_policy` on the command line with a fully specified name;
            * provide the argument `--translation` on the command line.
