name: set-iam-policy
description: '*(ALPHA)*  Set the IAM policy for a Compute Engine snapshot.'
documentation:
    command: |
        *(ALPHA)*  Sets the IAM policy for the given snapshot as defined in a JSON or YAML file.
