# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: help
description: Help about any command
documentation:
    command: '[AI] `docker buildx help` is a command used with Docker Buildx, which is an extension of the standard Docker build system. It provides multi-context support for building and testing multiple Docker images in parallel. The `help` command displays detailed usage information about the `docker buildx` subcommand, including available options, commands, and their descriptions.'
