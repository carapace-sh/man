name: create-classifier
description: Creates a classifier in the user's account.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --csv-classifier=: A `CsvClassifier` object specifying the classifier to create.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --grok-classifier=: A `GrokClassifier` object specifying the classifier to create.
    --json-classifier=: A `JsonClassifier` object specifying the classifier to create.
    --xml-classifier=: An `XMLClassifier` object specifying the classifier to create.
documentation:
    command: Creates a classifier in the user's account. This can be a `GrokClassifier`, an `XMLClassifier`, a `JsonClassifier`, or a `CsvClassifier`, depending on which field of the request is present.
    flag:
        cli-input-json: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        cli-input-yaml: |-
            Reads arguments from the JSON string provided.
            The JSON string follows the  format  provided  by --generate-cli-skeleton.
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values.
            It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        csv-classifier: A `CsvClassifier` object specifying the classifier to create.
        generate-cli-skeleton: |-
            If other arguments are provided on the command line,  those  values  will override the JSON-provided values. It is not possible to pass arbitrary binary values using a JSON-provided value as the string will be taken literally.
            This may  not  be  specified  along with --cli-input-yaml.
        grok-classifier: A `GrokClassifier` object specifying the classifier to create.
        json-classifier: A `JsonClassifier` object specifying the classifier to create.
        xml-classifier: An `XMLClassifier` object specifying the classifier to create.
