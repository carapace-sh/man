name: cancel
description: Cancel a running AI Platform job.
documentation:
    command: |
        *gcloud ai-platform jobs cancel* cancels a running AI Platform job. If the job is already
        finished, the command will not perform an operation and exit successfully.
