name: get-connection-status
description: Retrieves the Session Manager connection status for a managed node to determine whether it is running and ready to receive Session Manager connections.
flags:
    --target=!: The managed node ID.
documentation:
    command: Retrieves the Session Manager connection status for a managed node to determine whether it is running and ready to receive Session Manager connections.
    flag:
        target: The managed node ID.
