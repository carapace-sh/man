name: list-directories
description: Lists directories created within an account.
flags:
    --max-results=: The maximum number of results to retrieve.
    --next-token=: The pagination token.
    --state=: The state of the directories in the list.
completion:
    flag:
        state:
            - ENABLED
            - DISABLED
            - DELETED
documentation:
    command: Lists directories created within an account.
    flag:
        max-results: The maximum number of results to retrieve.
        next-token: The pagination token.
        state: The state of the directories in the list. Can be either Enabled, Disabled, or Deleted.
