name: npm
description: Print credential settings to add to the .npmrc file.
flags:
    --json-key=: Path to service account JSON key.
    --location=: Location of the repository.
    --repository=: ID of the repository or fully qualified identifier for the repository.
    --scope=: The scope to associate with the Artifact Registry registry.
documentation:
    command: |-
        Print credential settings to add to the .npmrc file for connecting to an npm
        repository.
    flag:
        json-key: Path to service account JSON key. If not specified, current active service account credentials or a placeholder for gcloud credentials is used.
        location: |-
            Location of the repository.
            +
            To set the `location` attribute:
            * provide the argument `--repository` on the command line with a fully specified name;
            * set the property `artifacts/repository` with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `artifacts/location`.
        repository: |-
            ID of the repository or fully qualified identifier for the repository.
            +
            To set the `repository` attribute:
            * provide the argument `--repository` on the command line;
            * set the property `artifacts/repository`.
        scope: The scope to associate with the Artifact Registry registry. If not specified, Artifact Registry is set as the default registry.
