name: describe-patch-group-state
description: Returns high-level aggregated patch compliance state information for a patch group.
flags:
    --patch-group=!: The name of the patch group whose patch snapshot should be retrieved.
documentation:
    command: Returns high-level aggregated patch compliance state information for a patch group.
    flag:
        patch-group: The name of the patch group whose patch snapshot should be retrieved.
