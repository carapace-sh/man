name: cancel-metadata-transfer-job
description: Cancels the metadata transfer job.
flags:
    --metadata-transfer-job-id=!: The metadata transfer job Id.
documentation:
    command: Cancels the metadata transfer job.
    flag:
        metadata-transfer-job-id: The metadata transfer job Id.
