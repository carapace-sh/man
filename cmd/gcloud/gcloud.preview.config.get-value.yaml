name: get-value
description: '*(PREVIEW)*  Alias for `gcloud config get`, for backwards compatibility.'
hidden: true
documentation:
    command: '*(PREVIEW)*  Alias for `gcloud config get`, for backwards compatibility.'
