name: describe
description: '*(BETA)*  Display detailed information about a target TCP proxy.'
flags:
    --global: If set, the target TCP proxy is global.
    --no-global&: If set, the target TCP proxy is global.
    --region=: Region of the target TCP proxy to describe.
documentation:
    command: |
        *(BETA)*  *gcloud beta compute target-tcp-proxies describe* displays all data associated with a target TCP proxy
        in a project.
    flag:
        global: If set, the target TCP proxy is global.
        no-global: If set, the target TCP proxy is global.
        region: |-
            Region of the target TCP proxy to describe. If not specified, you might be prompted to select a region (interactive mode
            only).
            +
            To avoid prompting when this flag is omitted, you can set the
            ``compute/region'' property:
            +
              $ gcloud config set compute/region REGION
            +
            A list of regions can be fetched by running:
            +
              $ gcloud compute regions list
            +
            To unset the property, run:
            +
              $ gcloud config unset compute/region
            +
            Alternatively, the region can be stored in the environment
            variable ``CLOUDSDK_COMPUTE_REGION''.
