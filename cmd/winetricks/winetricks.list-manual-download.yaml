# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-manual-download
description: list verbs which download with some help from the user
documentation:
    command: '[AI] `winetricks list-manual-download` is a command used in Wine, a compatibility layer for running Windows applications on Linux and macOS. This specific command lists the software packages that can be manually installed using Winetricks, an interface for managing Wine configurations. The packages are not available through standard repositories or automatic download, so users must download them manually from their respective websites to install them with Winetricks.'
