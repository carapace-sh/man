name: remove-metadata
description: '*(BETA)*  Remove project-wide metadata entries.'
flags:
    --all: If provided, all metadata entries are removed.
    --keys=: The keys of the entries to remove.
    --no-all&: If provided, all metadata entries are removed.
documentation:
    command: '*(BETA)*  *gcloud beta compute project-info remove-metadata* can be used to remove project-wide metadata entries.'
    flag:
        all: If provided, all metadata entries are removed.
        keys: The keys of the entries to remove.
        no-all: If provided, all metadata entries are removed.
