name: deregister-dbproxy-targets
description: Remove the association between one or more `DBProxyTarget` data structures and a `DBProxyTargetGroup`.
flags:
    --dbcluster-identifiers=: One or more DB cluster identifiers.
    --dbinstance-identifiers=: One or more DB instance identifiers.
    --dbproxy-name=!: The identifier of the `DBProxy` that is associated with the `DBProxyTargetGroup`.
    --target-group-name=: The identifier of the `DBProxyTargetGroup`.
documentation:
    command: Remove the association between one or more `DBProxyTarget` data structures and a `DBProxyTargetGroup`.
    flag:
        dbcluster-identifiers: One or more DB cluster identifiers.
        dbinstance-identifiers: One or more DB instance identifiers.
        dbproxy-name: The identifier of the `DBProxy` that is associated with the `DBProxyTargetGroup`.
        target-group-name: The identifier of the `DBProxyTargetGroup`.
