name: render-markdown
description: Uses gcloud's markdown renderer to render the given markdown file.
hidden: true
flags:
    --style=: The renderer output format.
completion:
    flag:
        style:
            - devsite
            - html
            - linter
            - man
            - markdown
            - text
documentation:
    command: Uses gcloud's markdown renderer to render the given markdown file.
    flag:
        style: 'The renderer output format. _STYLE_ must be one of: *devsite*, *html*, *linter*, *man*, *markdown*, *text*.'
