# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: init
description: Creates a new package and lock file in the current directory
documentation:
    command: '[AI] `toit pkg init` is a command used in Toit programming language for creating new Toit packages. When you run this command in your terminal or command prompt, it initializes a new package directory with essential files and structures, including a `package.json` file for metadata and dependencies management. This setup helps developers build, test, and distribute their Toit packages efficiently.'
