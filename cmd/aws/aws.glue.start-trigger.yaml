name: start-trigger
description: Starts an existing trigger.
flags:
    --name=!: The name of the trigger to start.
documentation:
    command: Starts an existing trigger. See [Triggering Jobs](https://docs.aws.amazon.com/glue/latest/dg/trigger-job.html) for information about how different types of trigger are started.
    flag:
        name: The name of the trigger to start.
