# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Show the name of the current workspac
documentation:
    command: '[AI] The `tofu workspace show` command is used in the Tofu data versioning system to display details about the currently active workspace, including its name, description, and the versions of data artifacts that are currently staged or published within it.'
