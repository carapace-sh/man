name: list-service-templates
description: List service templates with detail data.
flags:
    --max-results=: The maximum number of service templates to list.
    --next-token=: A token that indicates the location of the next service template in the array of service templates, after the list of service templates previously requested.
documentation:
    command: List service templates with detail data.
    flag:
        max-results: The maximum number of service templates to list.
        next-token: A token that indicates the location of the next service template in the array of service templates, after the list of service templates previously requested.
