# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List the keys and connection strings of Authorization Rule for Relay Service Namespace.
flags:
    --namespace-name=!: Name of Namespace.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: Name of Namespace Authorization Rule.
documentation:
    flag:
        name: Name of Namespace Authorization Rule.
        namespace-name: Name of Namespace.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List the keys and connection strings of Authorization Rule for Relay Service Namespace: az relay namespace authorization-rule keys list --resource-group myresourcegroup --namespace-name mynamespace --name myauthorule
    ? List the keys and connection strings of Authorization Rule for Relay Service Namespace (commonly     used with --output and --query). (autogenerated)
    : az relay namespace authorization-rule keys list --name myauthorule --namespace-name mynamespace --resource-group myresourcegroup --subscription MySubscription
