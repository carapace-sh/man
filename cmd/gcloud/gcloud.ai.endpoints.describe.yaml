name: describe
description: Describe an existing Vertex AI endpoint.
flags:
    --region=: Cloud region for the endpoint.
documentation:
    command: |-
        ## EXAMPLES

        To describe an endpoint ``123'' under project ``example'' in region
        ``us-central1'', run:

          $ gcloud ai endpoints describe 123 --project=example --region=us-central1
    flag:
        region: |-
            Cloud region for the endpoint.
            +
            To set the `region` attribute:
            * provide the argument `endpoint` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `ai/region`;
            * choose one from the prompted list of available regions.
