name: delete
description: '*(ALPHA)*  Delete Compute Engine wire groups.'
flags:
    --cross-site-network=!: Name of the crossSiteNetwork to operate on.
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute interconnects wire-groups delete* is used to delete wire groups.

        For an example, refer to the *EXAMPLES* section below.
    flag:
        cross-site-network: Name of the crossSiteNetwork to operate on.
