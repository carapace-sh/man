name: describe
description: '*(ALPHA)*  Describe a Compute Engine wire group.'
flags:
    --cross-site-network=!: Name of the crossSiteNetwork to operate on.
documentation:
    command: |
        *(ALPHA)*  *gcloud alpha compute interconnects wire-groups describe* is used to describe a wire group.

        For an example, refer to the *EXAMPLES* section below.
    flag:
        cross-site-network: Name of the crossSiteNetwork to operate on.
