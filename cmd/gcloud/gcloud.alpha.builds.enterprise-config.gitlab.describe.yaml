name: describe
description: '*(ALPHA)*  Describe a GitLab Enterprise config in a Google Cloud project.'
flags:
    --region=: The Cloud location for the gitLabConfig.
documentation:
    command: '*(ALPHA)*  Describe a GitLab Enterprise config in a Google Cloud project.'
    flag:
        region: |-
            The Cloud location for the gitLabConfig.
            +
            To set the `region` attribute:
            * provide the argument `CONFIG` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `builds/region`.
