name: get-managed-notification-event
description: Returns a specified `ManagedNotificationEvent`.
flags:
    --arn=!: The Amazon Resource Name (ARN) of the `ManagedNotificationEvent` to return.
    --locale=: The locale code of the language used for the retrieved `ManagedNotificationEvent`.
completion:
    flag:
        locale:
            - de_DE
            - en_CA
            - en_US
            - en_UK
            - es_ES
            - fr_CA
            - fr_FR
            - id_ID
            - it_IT
            - ja_JP
            - ko_KR
            - pt_BR
            - tr_TR
            - zh_CN
            - zh_TW
documentation:
    command: Returns a specified `ManagedNotificationEvent`.
    flag:
        arn: The Amazon Resource Name (ARN) of the `ManagedNotificationEvent` to return.
        locale: The locale code of the language used for the retrieved `ManagedNotificationEvent`. The default locale is English `(en_US)`.
