name: delete
description: '*(BETA)*  Delete a Cloud Dataflow snapshot.'
flags:
    --region=!: Region ID of the snapshot regional endpoint.
documentation:
    command: '*(BETA)*  Delete a Cloud Dataflow snapshot.'
    flag:
        region: Region ID of the snapshot regional endpoint.
