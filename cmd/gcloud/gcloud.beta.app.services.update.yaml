name: update
description: '*(BETA)*  Update service-level settings.'
flags:
    --ingress=!: Control what traffic can reach the app.
completion:
    flag:
        ingress:
            - all
            - internal-only
            - internal-and-cloud-load-balancing
documentation:
    command: '*(BETA)*  Update ingress traffic settings for an app.'
    flag:
        ingress: 'Control what traffic can reach the app. _INGRESS_ must be one of: *all*, *internal-only*, *internal-and-cloud-load-balancing*.'
