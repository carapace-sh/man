name: batch-get-vpc-endpoint
description: Returns attributes for one or more VPC endpoints associated with the current account.
flags:
    --ids=!: A list of VPC endpoint identifiers.
documentation:
    command: Returns attributes for one or more VPC endpoints associated with the current account. For more information, see [Access Amazon OpenSearch Serverless using an interface endpoint](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/serverless-vpc.html).
    flag:
        ids: A list of VPC endpoint identifiers.
