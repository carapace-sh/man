name: test-migration
description: Async API to test connection between source and target replication group.
flags:
    --customer-node-endpoint-list=!: List of endpoints from which data should be migrated.
    --replication-group-id=!: The ID of the replication group to which data is to be migrated.
documentation:
    command: Async API to test connection between source and target replication group.
    flag:
        customer-node-endpoint-list: List of endpoints from which data should be migrated. List should have only one element.
        replication-group-id: The ID of the replication group to which data is to be migrated.
