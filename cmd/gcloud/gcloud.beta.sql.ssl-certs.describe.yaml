name: describe
description: '*(BETA)*  *(DEPRECATED)*  Retrieves information about an SSL cert for a Cloud SQL instance.'
flags:
    --instance=!: Cloud SQL instance ID.
documentation:
    command: |+
        *(BETA)*  *(DEPRECATED)*  Retrieves information about an SSL cert for a Cloud SQL instance.

        `gcloud sql ssl-certs` is deprecated. Use `gcloud sql ssl client-certs` instead.

    flag:
        instance: Cloud SQL instance ID.
