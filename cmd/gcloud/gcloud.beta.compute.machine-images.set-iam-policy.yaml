name: set-iam-policy
description: '*(BETA)*  Set the IAM policy for a Compute Engine machine image.'
documentation:
    command: |
        *(BETA)*  Sets the IAM policy for the given machine image as defined in a JSON or YAML file.
