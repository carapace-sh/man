name: set-iam-policy
description: '*(ALPHA)*  Set the IAM policy for a Cloud Deploy Policy.'
flags:
    --region=: Location of the deploy policy.
documentation:
    command: |
        *(ALPHA)*  Set the IAM policy associated with a Cloud Deploy Policy.

        See https://cloud.google.com/iam/docs/managing-policies for details of
        the policy file format and contents.
    flag:
        region: |-
            Location of the deploy policy.
            +

            To set the `region` attribute:
            * provide the argument `deploy_policy` on the command line with a fully specified name;
            * provide the argument `--region` on the command line;
            * set the property `deploy/region`.
