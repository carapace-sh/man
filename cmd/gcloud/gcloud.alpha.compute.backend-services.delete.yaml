name: delete
description: '*(ALPHA)*  Delete backend services.'
flags:
    --global: If set, the backend services are global.
    --no-global&: If set, the backend services are global.
    --region=: Region of the backend services to delete.
documentation:
    command: '*(ALPHA)*  *gcloud alpha compute backend-services delete* deletes one or more backend services.'
    flag:
        global: If set, the backend services are global.
        no-global: If set, the backend services are global.
        region: Region of the backend services to delete. Overrides the default *compute/region* property value for this command invocation.
