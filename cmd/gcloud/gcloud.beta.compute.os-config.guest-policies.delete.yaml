name: delete
description: '*(BETA)*  Delete the specified guest policy.'
documentation:
    command: |-
        ## EXAMPLES

          To delete the guest policy named `policy1` in the current project, run:

                $ gcloud beta compute os-config guest-policies delete policy1
