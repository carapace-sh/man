name: describe
description: Describes a service account HMAC key.
documentation:
    command: |
        *gcloud storage hmac describe* retrieves the specified HMAC key's metadata. Note that there
        is no option to retrieve a key's secret material after it has
        been created.
