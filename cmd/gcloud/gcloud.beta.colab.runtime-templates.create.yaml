name: create
description: '*(BETA)*  Create a runtime template.'
flags:
    --accelerator-count=: The number of accelerators used by the runtime.
    --accelerator-type=: The type of hardware accelerator used by the runtime.
    --async: Return immediately, without waiting for the operation in progress to
    --description=: The description of the runtime template.
    --disk-size-gb=: The disk size of the runtime in GB.
    --disk-type=: The type of the persistent disk.
    --display-name=!: The display name of the runtime template.
    --enable-euc: Enable end user credential access for the runtime.
    --enable-internet-access: Enable public internet access for the runtime.
    --enable-secure-boot: Enables secure boot for the runtime.
    --idle-shutdown-timeout=: The duration after which the runtime is automatically shut down.
    --kms-key=!: ID of the key or fully qualified identifier for the key.
    --kms-keyring=: KMS keyring id of the key.
    --kms-location=: Cloud location for the key.
    --kms-project=: Cloud project id for the key.
    --labels=: Add labels to identify and group the runtime template.
    --machine-type=: The Compute Engine machine type selected for the runtime.
    --network-tags=: Applies the given Compute Engine tags to the runtime.
    --network=!: ID of the network or fully qualified identifier for the network.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-enable-euc&: Enable end user credential access for the runtime.
    --no-enable-internet-access&: Enable public internet access for the runtime.
    --no-enable-secure-boot&: Enables secure boot for the runtime.
    --post-startup-script-behavior=&: The behavior of the post startup script.
    --post-startup-script-url=&: Post startup script URL to execute on the runtime.
    --post-startup-script=&: Post startup script in raw string to execute on the runtime.
    --region=: ID of the region or fully qualified identifier for the region.
    --runtime-template-id=: The id of the runtime template.
    --set-env-vars=&: Set environment variables used by the runtime.
    --subnetwork-region=: Google Cloud region of this subnetwork https://cloud.google.com/compute/docs/regions-zones/#locations.
    --subnetwork=!: ID of the subnetwork or fully qualified identifier for the subnetwork.
completion:
    flag:
        accelerator-type:
            - NVIDIA_TESLA_V100
            - NVIDIA_TESLA_T4
            - NVIDIA_TESLA_A100
            - NVIDIA_A100_80GB
            - NVIDIA_L4
        disk-type:
            - PD_STANDARD
            - PD_SSD
            - PD_BALANCED
            - PD_EXTREME
        post-startup-script-behavior:
            - POST_STARTUP_SCRIPT_BEHAVIOR_UNSPECIFIED
            - RUN_ONCE
            - RUN_EVERY_START
            - DOWNLOAD_AND_RUN_EVERY_START
documentation:
    command: |
        *(BETA)*  Create a Colab Enterprise runtime template, a VM configuration for your notebook runtimes.
    flag:
        accelerator-count: The number of accelerators used by the runtime.
        accelerator-type: 'The type of hardware accelerator used by the runtime. If specified, --accelerator-count must also be specified. _ACCELERATOR_TYPE_ must be one of: *NVIDIA_TESLA_V100*, *NVIDIA_TESLA_T4*, *NVIDIA_TESLA_A100*, *NVIDIA_A100_80GB*, *NVIDIA_L4*.'
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        description: The description of the runtime template.
        disk-size-gb: The disk size of the runtime in GB. If specified, the --disk-type must also be specified. The minimum size is 10GB and the maximum is 65536GB.
        disk-type: 'The type of the persistent disk. _DISK_TYPE_ must be one of: *PD_STANDARD*, *PD_SSD*, *PD_BALANCED*, *PD_EXTREME*.'
        display-name: The display name of the runtime template.
        enable-euc: Enable end user credential access for the runtime. Enabled by default, use *--no-enable-euc* to disable.
        enable-internet-access: Enable public internet access for the runtime. Enabled by default, use *--no-enable-internet-access* to disable.
        enable-secure-boot: Enables secure boot for the runtime. Disabled by default.
        idle-shutdown-timeout: The duration after which the runtime is automatically shut down. An input of 0s disables the idle shutdown feature, and a valid range is [10m, 24h]. See '$ gcloud topic datetimes' for details on formatting the input duration.
        kms-key: |-
            ID of the key or fully qualified identifier for the key.
            +
            To set the `kms-key` attribute:
            * provide the argument `--kms-key` on the command line.
        kms-keyring: |-
            KMS keyring id of the key.
            +
            To set the `kms-keyring` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-keyring` on the command line.
        kms-location: |-
            Cloud location for the key.
            +
            To set the `kms-location` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-location` on the command line.
        kms-project: |-
            Cloud project id for the key.
            +
            To set the `kms-project` attribute:
            * provide the argument `--kms-key` on the command line with a fully specified name;
            * provide the argument `--kms-project` on the command line.
        labels: Add labels to identify and group the runtime template.
        machine-type: The Compute Engine machine type selected for the runtime.
        network: |-
            ID of the network or fully qualified identifier for the network.
            +
            To set the `network` attribute:
            * provide the argument `--network` on the command line.
        network-tags: Applies the given Compute Engine tags to the runtime.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-enable-euc: Enable end user credential access for the runtime.
        no-enable-internet-access: Enable public internet access for the runtime.
        no-enable-secure-boot: Enables secure boot for the runtime. Disabled by default.
        post-startup-script: Post startup script in raw string to execute on the runtime.
        post-startup-script-behavior: The behavior of the post startup script. The default if passing a post-startup-script-url is RUN_ONCE.
        post-startup-script-url: Post startup script URL to execute on the runtime. This can be a public or private Google Cloud Storage object. In the form of gs://bucket/object or https://storage.googleapis.com/bucket/object.
        region: |-
            ID of the region or fully qualified identifier for the region.
            +
            To set the `region` attribute:
            * provide the argument `--region` on the command line;
            * set the property `colab/region`.
        runtime-template-id: The id of the runtime template. If not specified, a random id will be generated.
        set-env-vars: Set environment variables used by the runtime.
        subnetwork: |-
            ID of the subnetwork or fully qualified identifier for the subnetwork.
            +
            To set the `subnetwork` attribute:
            * provide the argument `--subnetwork` on the command line.
        subnetwork-region: |-
            Google Cloud region of this subnetwork https://cloud.google.com/compute/docs/regions-zones/#locations.
            +
            To set the `subnetwork-region` attribute:
            * provide the argument `--subnetwork` on the command line with a fully specified name;
            * provide the argument `--subnetwork-region` on the command line.
