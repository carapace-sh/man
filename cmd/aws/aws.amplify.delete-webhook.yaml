name: delete-webhook
description: Deletes a webhook.
flags:
    --webhook-id=!: The unique ID for a webhook.
documentation:
    command: Deletes a webhook.
    flag:
        webhook-id: The unique ID for a webhook.
