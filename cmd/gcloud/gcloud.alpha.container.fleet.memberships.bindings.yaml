name: bindings
description: '*(ALPHA)*  Membership Bindings for permissions.'
documentation:
    command: |-
        *(ALPHA)*  This command group allows for manipulation of bindings associated with
        membership-scope.

        ## EXAMPLES

        Manage Bindings:

          $ gcloud alpha container fleet memberships bindings --help
