name: describe
description: '*(ALPHA)*  Show details about a `WasmAction` resource.'
flags:
    --location=: The location ID.
documentation:
    command: '*(ALPHA)*  Show details about a `WasmAction` resource.'
    flag:
        location: |-
            The location ID.
            +

            To set the `location` attribute:
            * provide the argument `wasm_action` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * use global location.
