name: get-event-bridge-rule-template-group
description: Retrieves the specified eventbridge rule template group.
flags:
    --identifier=!: An eventbridge rule template group's identifier.
documentation:
    command: Retrieves the specified eventbridge rule template group.
    flag:
        identifier: An eventbridge rule template group's identifier. Can be either be its id or current name.
