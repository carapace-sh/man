name: describe
description: '*(ALPHA)*  Describe a condition in a Cloud Monitoring alerting policy.'
flags:
    --policy=: Name of the alerting policy.
documentation:
    command: '*(ALPHA)*  Describe a condition in a Cloud Monitoring alerting policy.'
    flag:
        policy: |-
            Name of the alerting policy.
            +
            To set the `policy` attribute:
            * provide the argument `condition` on the command line with a fully specified name;
            * provide the argument `--policy` on the command line.
