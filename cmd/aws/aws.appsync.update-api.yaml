name: update-api
description: Updates an `Api`.
flags:
    --api-id=!: The `Api` ID.
    --event-config=: The new event configuration.
    --name=!: The name of the Api.
    --owner-contact=: The owner contact information for the `Api`.
documentation:
    command: Updates an `Api`.
    flag:
        api-id: The `Api` ID.
        event-config: The new event configuration. This includes the default authorization configuration for connecting, publishing, and subscribing to an Event API.
        name: The name of the Api.
        owner-contact: The owner contact information for the `Api`.
