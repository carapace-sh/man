name: describe
description: '*(BETA)*  Get PrincipalAccessBoundaryPolicy instance.'
flags:
    --location=: The location id of the principalAccessBoundaryPolicy resource.
    --organization=: The organization id of the principalAccessBoundaryPolicy resource.
documentation:
    command: |
        *(BETA)*  Get PrincipalAccessBoundaryPolicy instance.
    flag:
        location: |-
            The location id of the principalAccessBoundaryPolicy resource.
            +
            To set the `location` attribute:
            * provide the argument `principal_access_boundary_policy` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        organization: |-
            The organization id of the principalAccessBoundaryPolicy resource.
            +
            To set the `organization` attribute:
            * provide the argument `principal_access_boundary_policy` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line.
