name: start
description: '*(BETA)*  Start serving specified versions.'
flags:
    --service=: If specified, only start versions from the given service.
documentation:
    command: |-
        *(BETA)*  This command starts serving the specified versions. It may only be used if the
        scaling module for your service has been set to manual.
    flag:
        service: If specified, only start versions from the given service.
