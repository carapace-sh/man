name: get-iam-policy
description: '*(BETA)*  Gets the IAM policy for a TagKey resource.'
documentation:
    command: |-
        *(BETA)*  Returns the IAM policy for a TagKey resource given the TagKey's display
        name and parent or the TagKey's numeric id.
