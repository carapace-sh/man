# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List all rules in a network security group.
flags:
    --include-default=: Include default security rules in the output.
    --nsg-name=!: Name of the network security group
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
documentation:
    flag:
        include-default: |-
            Include default security rules in the output.

            Default value: False
        nsg-name: Name of the network security group.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    List all rules in a network security group.: az network nsg rule list -g MyResourceGroup --nsg-name MyNsg
