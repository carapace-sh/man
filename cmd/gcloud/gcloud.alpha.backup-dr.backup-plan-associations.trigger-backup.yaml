name: trigger-backup
description: '*(ALPHA)*  Create an on-demand backup for a resource.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --backup-rule-id=: Name of an existing backup rule to use for creating an on-demand backup.
    --custom-retention-days=: Duration for which backup data will be retained.
    --labels=: Labels to be applied to the backup.
    --location=: The location of the Backup Plan Association.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --workload-project=: Cloud project id for the Backup Plan Association.
documentation:
    command: '*(ALPHA)*  Create an on-demand backup for a resource. Trigger an on demand backup for the given backup rule.'
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete. The default is *True*. Enabled by default, use *--no-async* to disable.
        backup-rule-id: Name of an existing backup rule to use for creating an on-demand backup.
        custom-retention-days: Duration for which backup data will be retained. If not specified, the default retention period will be used.
        labels: Labels to be applied to the backup.
        location: |-
            The location of the Backup Plan Association.
            +
            To set the `location` attribute:
            * provide the argument `BACKUP_PLAN_ASSOCIATION` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        workload-project: |-
            Cloud project id for the Backup Plan Association.
            +
            To set the `workload-project` attribute:
            * provide the argument `BACKUP_PLAN_ASSOCIATION` on the command line with a fully specified name;
            * provide the argument `--workload-project` on the command line;
            * provide the argument `--project` on the command line;
            * set the property `core/project`.
