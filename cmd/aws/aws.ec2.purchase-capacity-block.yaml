name: purchase-capacity-block
description: Purchase the Capacity Block for use with your account.
flags:
    --capacity-block-offering-id=!: The ID of the Capacity Block offering.
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --instance-platform=!: The type of operating system for which to reserve capacity.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --tag-specifications=: The tags to apply to the Capacity Block during launch.
completion:
    flag:
        instance-platform:
            - Linux/UNIX
            - Red Hat Enterprise Linux
            - SUSE Linux
            - Windows
            - Windows with SQL Server
            - Windows with SQL Server Enterprise
            - Windows with SQL Server Standard
            - Windows with SQL Server Web
            - Linux with SQL Server Standard
            - Linux with SQL Server Web
            - Linux with SQL Server Enterprise
            - RHEL with SQL Server Standard
            - RHEL with SQL Server Enterprise
            - RHEL with SQL Server Web
            - RHEL with HA
            - RHEL with HA and SQL Server Standard
            - RHEL with HA and SQL Server Enterprise
            - Ubuntu Pro
documentation:
    command: Purchase the Capacity Block for use with your account. With Capacity Blocks you ensure GPU capacity is available for machine learning (ML) workloads. You must specify the ID of the Capacity Block offering you are purchasing.
    flag:
        capacity-block-offering-id: The ID of the Capacity Block offering.
        dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is `DryRunOperation`. Otherwise, it is `UnauthorizedOperation`.
        instance-platform: The type of operating system for which to reserve capacity.
        tag-specifications: The tags to apply to the Capacity Block during launch.
