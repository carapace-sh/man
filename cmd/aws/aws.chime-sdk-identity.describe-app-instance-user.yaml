name: describe-app-instance-user
description: Returns the full details of an `AppInstanceUser`.
flags:
    --app-instance-user-arn=!: The ARN of the `AppInstanceUser`.
documentation:
    command: Returns the full details of an `AppInstanceUser`.
    flag:
        app-instance-user-arn: The ARN of the `AppInstanceUser`.
