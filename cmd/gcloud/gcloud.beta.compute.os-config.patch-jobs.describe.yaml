name: describe
description: '*(BETA)*  Describe a specified OS patch job.'
documentation:
    command: |-
        ## EXAMPLES

        To check the status of the patch job `job1`, run:

              $ gcloud beta compute os-config patch-jobs describe job1
