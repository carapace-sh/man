name: delete
description: '*(ALPHA)*  Delete an `SwpSecurityExtension` resource.'
hidden: true
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --location=: Cloud region in which the resource is located.
    --no-async&: Return immediately, without waiting for the operation in progress to
documentation:
    command: |
        *(ALPHA)*  Delete the specified `SwpSecurityExtension` resource.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        location: |-
            Cloud region in which the resource is located.
            +

            To set the `location` attribute:
            * provide the argument `swp_security_extension` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
