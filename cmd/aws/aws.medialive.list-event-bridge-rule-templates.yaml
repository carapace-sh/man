name: list-event-bridge-rule-templates
description: Lists eventbridge rule templates.
flags:
    --group-identifier=: An eventbridge rule template group's identifier.
    --max-results=: ""
    --next-token=: A token used to retrieve the next set of results in paginated list responses.
    --signal-map-identifier=: A signal map's identifier.
documentation:
    command: Lists eventbridge rule templates.
    flag:
        group-identifier: An eventbridge rule template group's identifier. Can be either be its id or current name.
        max-results: ""
        next-token: A token used to retrieve the next set of results in paginated list responses.
        signal-map-identifier: A signal map's identifier. Can be either be its id or current name.
