name: create
description: '*(BETA)*  Create a policy orchestrator.'
flags:
    --action=: Action to be taken on policy.
    --async: Return immediately, without waiting for the operation in progress to
    --folder=: Folder of the policy_orchestrator.
    --include-folders=: Applies policy to selected folders.
    --include-locations=: Applies policy to selected locations, e.g. `us-central1-a`.
    --include-projects=: Applies policy to selected projects.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --organization=: Organization of the policy_orchestrator.
    --policy-file=: Absolute path to the OS policy assignment file on your local client.
    --policy-id=: Policy id.
    --policy-type=!: Policy type to use.
    --state=: State of the policy orchestrator.
completion:
    flag:
        action:
            - delete
            - upsert
        policy-type:
            - os_policy_assignment_v1
        state:
            - active
            - stopped
documentation:
    command: |
        *(BETA)*  Create a policy orchestrator.
        ## EXAMPLES

        To create a policy orchestrator `my-orchestrator` in folder `123456` for OS policy assignment
        with config file `/path/to/file/config.yaml`, run:

          $ gcloud beta compute os-config policy-orchestrators create my-orchestrator --folder=123456 --policy-type os_policy_assignment_v1 \
              --policy-file=/path/to/file/config.yaml

        To create a policy orchestrator `my-orchestrator` in folder `123456` that deletes OS Policy
        assignments with id `my-policy`, run:

          $ gcloud beta compute os-config policy-orchestrators create my-orchestrator --folder=123456 --policy-type os_policy_assignment_v1 \
              --action delete --policy-id my-policy
    flag:
        action: |-
            Action to be taken on policy. _ACTION_ must be one of:
            +
            *delete*::: Delete a policy with a given name. `policy-id` must be specified.
            *upsert*::: Create or update a policy. `policy-file` must be specified.
            :::
            +
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        folder: |-
            Folder of the policy_orchestrator.
            +

            To set the `folder` attribute:
            * provide the argument `policy_orchestrator` on the command line with a fully specified name;
            * provide the argument `--folder` on the command line. Must be specified for resource of type [policy_orchestrator_folder].
        include-folders: |-
            Applies policy to selected folders. Comma-separated list of folder numbers. Can beused
            together with `--include-projects`.
        include-locations: Applies policy to selected locations, e.g. `us-central1-a`.
        include-projects: |-
            Applies policy to selected projects. Comma-separated list of project numbers. Can be used
            together with `--include-folders`.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        organization: |-
            Organization of the policy_orchestrator.
            +

            To set the `organization` attribute:
            * provide the argument `policy_orchestrator` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line. Must be specified for resource of type [policy_orchestrator_organization].
        policy-file: |-
            Absolute path to the OS policy assignment file on your local client. File must be in either JSON
            or YAML format. This file defines the OS policies that you want to apply to your VMs, the target
            VMs that you want to apply the policies to, and the rollout rate at which to apply the OS
            policies on a zonal level. For more information about this resource and sample OS policy
            assignment files, see https://cloud.google.com/compute/docs/os-configuration-management/working-with-os-policies#os-policy-assignment.
        policy-id: Policy id. Must be specified for `DELETE` action.
        policy-type: |-
            Policy type to use. _POLICY_TYPE_ must be (only one value is supported):
            +
            *os_policy_assignment_v1*::: OS policy assignment v1.
            :::
            +
        state: |-
            State of the policy orchestrator. _STATE_ must be one of:
            +
            *active*::: Creates a policy orchestrator in `ACTIVE` state.
            *stopped*::: Creates a policy orchestrator in `STOPPED` state.
            :::
            +
