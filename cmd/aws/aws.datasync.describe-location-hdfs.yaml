name: describe-location-hdfs
description: Provides details about how an DataSync transfer location for a Hadoop Distributed File System (HDFS) is configured.
flags:
    --location-arn=!: Specifies the Amazon Resource Name (ARN) of the HDFS location.
documentation:
    command: Provides details about how an DataSync transfer location for a Hadoop Distributed File System (HDFS) is configured.
    flag:
        location-arn: Specifies the Amazon Resource Name (ARN) of the HDFS location.
