# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: rub
description: Combines two entities together to perform an operation
group: editing commits
flags:
    -h, --help: Print help (see more with '--help')
documentation:
    command: The `rub` command is a versatile operation that combines two entities together to perform various operations depending on the types of entities provided.
