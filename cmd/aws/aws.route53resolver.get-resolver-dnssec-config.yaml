name: get-resolver-dnssec-config
description: Gets DNSSEC validation information for a specified resource.
flags:
    --resource-id=!: The ID of the virtual private cloud (VPC) for the DNSSEC validation status.
documentation:
    command: Gets DNSSEC validation information for a specified resource.
    flag:
        resource-id: The ID of the virtual private cloud (VPC) for the DNSSEC validation status.
