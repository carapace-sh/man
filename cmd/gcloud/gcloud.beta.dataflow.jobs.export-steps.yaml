name: export-steps
description: '*(BETA)*  Exports information about the steps for the given job.'
flags:
    --region=: Region ID of the job's regional endpoint.
documentation:
    command: '*(BETA)*  The only currently supported format is to a GraphViz dot file.'
    flag:
        region: Region ID of the job's regional endpoint. Defaults to 'us-central1'.
