name: delete
description: '*(ALPHA)*  Delete Package Rollouts Resource Bundle.'
flags:
    --force: If true, force deletion of any child resources.
    --location=: Google Cloud zone or region.
    --no-force&: If true, force deletion of any child resources.
documentation:
    command: '*(ALPHA)*  Delete Package Rollouts Resource Bundle.'
    flag:
        force: If true, force deletion of any child resources. Otherwise, attempting to delete a Resource Bundle with children will fail.
        location: Google Cloud zone or region.
        no-force: If true, force deletion of any child resources. Otherwise, attempting to delete a Resource Bundle with children will fail.
