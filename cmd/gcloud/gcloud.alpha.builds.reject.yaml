name: reject
description: '*(ALPHA)*  Reject a pending build.'
flags:
    --comment=: Optional comment to annotate the build's rejection.
    --url=: Optional URL to annotate the build's rejection.
documentation:
    command: '*(ALPHA)*  Reject a pending build.'
    flag:
        comment: Optional comment to annotate the build's rejection.
        url: Optional URL to annotate the build's rejection.
