name: add-labels
description: Add labels to Google Compute Engine snapshots.
flags:
    --labels=!: A list of labels to add.
documentation:
    command: |
        *gcloud compute snapshots add-labels* adds labels to a Google Compute Engine snapshot.
    flag:
        labels: A list of labels to add.
