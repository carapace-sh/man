name: clone-items
description: '*(BETA)*  Clone items from source address group of organization.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --location=: The location Id.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --organization=: Organization number.
    --source=: Source address group to be cloned from.
documentation:
    command: |
        *(BETA)*  Clone items from source address group of organization.
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        location: |-
            The location Id.
            +

            To set the `location` attribute:
            * provide the argument `address_group` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        organization: |-
            Organization number.
            +

            To set the `organization` attribute:
            * provide the argument `address_group` on the command line with a fully specified name;
            * provide the argument `--organization` on the command line.
        source: Source address group to be cloned from.
