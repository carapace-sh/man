name: describe-profile
description: '*(BETA)*  Describe the OS Login profile for the current user.'
documentation:
    command: |
        *(BETA)*  *gcloud beta compute os-login describe-profile* displays the OS Login profile for the currently
        authenticated user, including Posix accounts and SSH keys
        associated with the user.
