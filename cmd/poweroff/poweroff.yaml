# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: poweroff
description: poweroff the machine
documentation:
    command: '[AI] The "poweroff" command is a Linux utility used to shut down or power off a system completely. It initiates an orderly system shutdown, carrying out essential tasks such as saving open files and closing running applications before terminating the operating system and powering off the hardware. It''s important to note that this command requires superuser (root) privileges for execution.'
