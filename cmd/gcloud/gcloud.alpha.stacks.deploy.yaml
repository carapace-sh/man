name: deploy
description: '*(ALPHA)*  *(REMOVED)*  Create or replace an application from a YAML specification.'
hidden: true
flags:
    --region=: Region in which the resource can be found.
    --service-account=: Name of the service account to use when deploying the integration.
documentation:
    command: |4+
        *(ALPHA)*  *(REMOVED)*  Stacks is no longer supported.

        *(ALPHA)*  *(REMOVED)*  Create or replace an application from a YAML specification.

        Stacks is no longer supported.


    flag:
        region: Region in which the resource can be found. Alternatively, set the property [run/region].
        service-account: Name of the service account to use when deploying the integration.
