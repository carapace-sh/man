name: rbacrolebindings
description: '*(ALPHA)*  Fleet scope RBAC RoleBindings for permissions.'
documentation:
    command: |-
        *(ALPHA)*  This command group allows for manipulation of fleet namespace RBAC
        RoleBindings.

        ## EXAMPLES

        Manage RBAC RoleBindings:

          $ gcloud alpha container fleet scopes rbacrolebindings --help
