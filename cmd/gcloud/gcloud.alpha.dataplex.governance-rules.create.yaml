name: create
description: '*(ALPHA)*  Create a Dataplex Governance Rule.'
hidden: true
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --description=: Description of the Governance Rule.
    --display-name=: Display name of the Governance Rule.
    --labels=: List of label KEY=VALUE pairs to add.
    --location=: The location of the Dataplex resource.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-validate-only&: Validate the create action, but don't actually perform it.
    --rule-metadata-file=!: The name of the JSON or YAML file to define rule metadata.
    --validate-only: Validate the create action, but don't actually perform it.
documentation:
    command: '*(ALPHA)*  Governance Rule is a template for creating Governance Rules.'
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        description: Description of the Governance Rule.
        display-name: Display name of the Governance Rule.
        labels: |-
            List of label KEY=VALUE pairs to add.
            +
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
        location: |-
            The location of the Dataplex resource.
            +
            To set the `location` attribute:
            * provide the argument `governance_rule` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `dataplex/location`.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-validate-only: Validate the create action, but don't actually perform it.
        rule-metadata-file: The name of the JSON or YAML file to define rule metadata.
        validate-only: Validate the create action, but don't actually perform it.
