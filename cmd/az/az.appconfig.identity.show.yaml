# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Display managed identities for an App Configuration store.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --name=!: Name of the App Configuration store.
documentation:
    flag:
        name: Name of the App Configuration store. You can configure the default name using ``` az configure --defaults app_configuration_store=<name> ``` .
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Display managed identities for a task.: az appconfig identity show -g MyResourceGroup -n MyAppConfiguration
