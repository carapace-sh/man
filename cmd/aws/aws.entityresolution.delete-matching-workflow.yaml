name: delete-matching-workflow
description: Deletes the `MatchingWorkflow` with a given name.
flags:
    --workflow-name=!: The name of the workflow to be retrieved.
documentation:
    command: Deletes the `MatchingWorkflow` with a given name. This operation will succeed even if a workflow with the given name does not exist.
    flag:
        workflow-name: The name of the workflow to be retrieved.
