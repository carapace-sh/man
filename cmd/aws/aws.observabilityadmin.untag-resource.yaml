name: untag-resource
description: Removes tags from a telemetry rule resource.
flags:
    --resource-arn=!: The Amazon Resource Name (ARN) of the telemetry rule resource to remove tags from.
    --tag-keys=!: The list of tag keys to remove from the telemetry rule resource.
documentation:
    command: Removes tags from a telemetry rule resource.
    flag:
        resource-arn: The Amazon Resource Name (ARN) of the telemetry rule resource to remove tags from.
        tag-keys: The list of tag keys to remove from the telemetry rule resource.
