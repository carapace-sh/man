name: describe
description: '*(ALPHA)*  Describe the compliance states for OS policies that are applied to a VM.'
flags:
    --location=: Location of the VM instance to describe.
documentation:
    command: |
        *(ALPHA)*  Describe the compliance states for OS policies that are applied to a VM.
    flag:
        location: |-
            Location of the VM instance to describe. If not specified, the property
            `compute/zone` is used. For details on setting properties,
            see: https://cloud.google.com/sdk/docs/properties
