# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: version
description: Print the version number and exit
documentation:
    command: '[AI] The `gnome-keyring version` command is used to display the current version number of Gnome Keyring, a popular password manager in the GNOME desktop environment. This command is typically run from a terminal or command prompt in a system with Gnome Keyring installed. It returns the major and minor version numbers separated by a dot, for example "3.32.0" or "4.15.1".'
