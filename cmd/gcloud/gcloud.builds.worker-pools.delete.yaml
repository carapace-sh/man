name: delete
description: Delete a worker pool from Cloud Build.
flags:
    --region=: The Cloud region where the worker pool is.
documentation:
    command: Delete a worker pool from Cloud Build.
    flag:
        region: The Cloud region where the worker pool is.
