name: generate-provider-access-token
description: '*(ALPHA)*  generate networkMonitoringProviders.'
hidden: true
flags:
    --gcp-access-token=!: Google Cloud access token.
    --location=: The location id of the networkMonitoringProvider resource.
documentation:
    command: '*(ALPHA)*  generate networkMonitoringProviders'
    flag:
        gcp-access-token: Google Cloud access token.
        location: |-
            The location id of the networkMonitoringProvider resource.
            +
            To set the `location` attribute:
            * provide the argument `network_monitoring_provider` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
