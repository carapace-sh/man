name: delete
description: '*(ALPHA)*  Delete a BigQuery dataset.'
flags:
    --no-remove-tables&: Remove the dataset even if it contains one or more tables.
    --remove-tables: Remove the dataset even if it contains one or more tables.
documentation:
    command: '*(ALPHA)*  Delete a BigQuery dataset.'
    flag:
        no-remove-tables: |
            Remove the dataset even if it contains one or more tables. Tables will be removed
            before deleteing dataset.
        remove-tables: |-
            Remove the dataset even if it contains one or more tables. Tables will be removed
            before deleteing dataset.
