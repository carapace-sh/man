# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: delete
description: Deletes a managed cluster.
flags:
    --if-match=: The request should only proceed if an entity matches this string.
    --no-wait=: Do not wait for the long-running operation to finish.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=!: Name of resource group.
    -n, --name=!: Name of the managed cluster.
    -y, --yes=: Do not prompt for confirmation.
documentation:
    command: Delete a managed Kubernetes cluster.
    flag:
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        if-match: The value provided will be compared to the ETag of the managed cluster, if it matches the operation will proceed. If it does not match, the request will be rejected to prevent accidental overwrites.
        if-none-match: Set to '*' to allow deleting a cluster only if it exists. Other values will be ignored.
        ignore-pod-disruption-budget: Delete those pods on a node without considering Pod Disruption Budget.
        name: Name of the managed cluster.
        no-wait: |-
            Do not wait for the long-running operation to finish.

            Default value: False
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        "yes": |-
            Do not prompt for confirmation.

            Default value: False
examples:
    Delete a managed Kubernetes cluster.: az aks delete --name MyManagedCluster --resource-group MyResourceGroup
    Delete a managed Kubernetes cluster. (autogenerated): az aks delete --name MyManagedCluster --resource-group MyResourceGroup
