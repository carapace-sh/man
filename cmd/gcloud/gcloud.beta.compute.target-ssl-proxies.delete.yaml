name: delete
description: '*(BETA)*  Delete target SSL proxies.'
documentation:
    command: |
        *(BETA)*  *gcloud beta compute target-ssl-proxies delete* deletes one or more target SSL proxies.
