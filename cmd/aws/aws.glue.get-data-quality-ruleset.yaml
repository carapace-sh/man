name: get-data-quality-ruleset
description: Returns an existing ruleset by identifier or name.
flags:
    --name=!: The name of the ruleset.
documentation:
    command: Returns an existing ruleset by identifier or name.
    flag:
        name: The name of the ruleset.
