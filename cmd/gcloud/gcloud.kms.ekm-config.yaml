name: ekm-config
description: Update and retrieve the EkmConfig.
documentation:
    command: |-
        EkmConfig is a singleton resource that represents configuration
        parameters that apply to all CryptoKeys and CryptoKeyVersions of EXTERNAL_VPC
        protecion level.
