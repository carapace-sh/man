name: describe-domain
description: The description of the domain.
flags:
    --domain-id=!: The domain ID.
documentation:
    command: The description of the domain.
    flag:
        domain-id: The domain ID.
