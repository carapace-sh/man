name: get-id-namespace
description: Returns the `IdNamespace` with a given name, if it exists.
flags:
    --id-namespace-name=!: The name of the ID namespace.
documentation:
    command: Returns the `IdNamespace` with a given name, if it exists.
    flag:
        id-namespace-name: The name of the ID namespace.
