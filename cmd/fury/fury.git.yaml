# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: git
description: Git repository commands
documentation:
    command: '[AI] The command "fury git" is not a valid Git command as "fury" is not a recognized Git subcommand or tool. Git is a version control system, and commands are typically prefixed with "git", such as "git clone", "git pull", "git push", etc.'
