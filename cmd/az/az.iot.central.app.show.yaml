# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: show
description: Get the details of an IoT Central application.
flags:
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Name of resource group.
    -n, --name=!: IoT Central application name.
documentation:
    flag:
        name: IoT Central application name.
        resource-group: Name of resource group. You can configure the default group using ``` az configure --defaults group=<name> ``` .
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Show details for an app named 'my-first-iot-app' in the resource group 'MyResourceGroup'.: az iot central app show -n my-first-iot-app -g MyResourceGroup
    Show details for an app named 'my-first-iot-app'.: az iot central app show -n my-first-iot-app
