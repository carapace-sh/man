name: get-firewall-config
description: Retrieves the configuration of the firewall behavior provided by DNS Firewall for a single VPC from Amazon Virtual Private Cloud (Amazon VPC).
flags:
    --resource-id=!: The ID of the VPC from Amazon VPC that the configuration is for.
documentation:
    command: Retrieves the configuration of the firewall behavior provided by DNS Firewall for a single VPC from Amazon Virtual Private Cloud (Amazon VPC).
    flag:
        resource-id: The ID of the VPC from Amazon VPC that the configuration is for.
