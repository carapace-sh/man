# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: set-permission
description: Set the permissions for a file or folder in a Data Lake Store account.
flags:
    --ids=:
        description: One or more resource IDs (space-delimited).
        nargs: -1
    --path=!: The path in the specified Data Lake Store account where the action should take place.
    --permission=!: 'The octal representation of the permissions for user, group and mask (for example: 777 is full rwx for all entities)'
    --subscription=: Name or ID of subscription.
    -n, --account=: Name of the Data Lake Store account.
documentation:
    command: |-
        Command group 'dls' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        account: Name of the Data Lake Store account.
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        change-reference: The related change reference ID for this resource operation.
        ids: One or more resource IDs (space-delimited). It should be a complete resource ID containing all information of 'Resource Id' arguments. You should provide either --ids or other 'Resource Id' arguments.
        path: The path in the specified Data Lake Store account where the action should take place. In the format '/folder/file.txt', where the first '/' after the DNS indicates the root of the file system.
        permission: The octal representation of the permissions for user, group and mask.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
examples:
    Set the permissions for a file or folder in a Data Lake Store account. (autogenerated): az dls fs access set-permission --account dpreptestfiles --path / --permission 777 --subscription MySubscription
