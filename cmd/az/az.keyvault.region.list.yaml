# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: The List operation gets information about the regions associated with the managed HSM Pool.
flags:
    --hsm-name=!: Name of the HSM.
    --subscription=: Name or ID of subscription.
    -g, --resource-group=: Proceed only if Key Vault belongs to the specified resource group.
documentation:
    command: |-
        Command group 'keyvault region' is in preview and under development. Reference and support levels:
        https://aka.ms/CLI_refstatus
    flag:
        hsm-name: Name of the HSM.
        resource-group: Proceed only if Key Vault belongs to the specified resource group.
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
