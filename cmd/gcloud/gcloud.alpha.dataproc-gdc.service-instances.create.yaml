name: create
description: '*(ALPHA)*  Create dataproc gdc service instance.'
hidden: true
flags:
    --annotations=: The annotations to associate with this service instance.
    --async: Return immediately, without waiting for the operation in progress to
    --display-name=: User-provided human-readable name to be used in user interfaces.
    --gdce-cluster=!: ID of the cluster or fully qualified identifier for the cluster.
    --labels=: The labels to associate with this service instance.
    --location=: For resources [gdce-cluster, service_instance], provides fallback value for resource location attribute.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --service-account=: Requested service account to associate with ServiceInstance.
documentation:
    command: |
        *(ALPHA)*  Create dataproc gdc service instance.
    flag:
        annotations: |-
            The annotations to associate with this service instance. Annotations may be
            used to store client information, but are not used by the server.
            +
            *KEY*:::
            Sets `KEY` value.
            +
            *VALUE*:::
            Sets `VALUE` value.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --annotations=string=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --annotations='{"string": "string"}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --annotations=path_to_file.(yaml|json)
            +
            ```
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        display-name: User-provided human-readable name to be used in user interfaces.
        gdce-cluster: |-
            ID of the cluster or fully qualified identifier for the cluster.
            +
            To set the `cluster` attribute:
            * provide the argument `--gdce-cluster` on the command line.
        labels: |-
            The labels to associate with this service instance. Labels may be used for
            filtering and billing tracking.
            +
            *KEY*:::
            Keys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
            +
            *VALUE*:::
            Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --labels=string=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --labels='{"string": "string"}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --labels=path_to_file.(yaml|json)
            +
            ```
        location: For resources [gdce-cluster, service_instance], provides fallback value for resource location attribute. When the resource's full URI path is not provided, location will fallback to this flag value.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        service-account: Requested service account to associate with ServiceInstance.
