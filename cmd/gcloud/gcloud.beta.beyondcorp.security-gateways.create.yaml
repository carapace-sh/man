name: create
description: '*(BETA)*  Create securityGateways.'
flags:
    --async: Return immediately, without waiting for the operation in progress to
    --contextual-headers-output-type=: Default output type for all enabled headers.
    --device-info-output-type=: The output type of the delegated device info.
    --display-name=: An arbitrary user-provided name for the SecurityGateway.
    --group-info-output-type=: The output type of the delegated group info.
    --hubs=: Map of Hubs that represents regional data path deployment with Google Cloud Platform region
    --location=: The location id of the securityGateway resource.
    --no-async&: Return immediately, without waiting for the operation in progress to
    --no-proxy-protocol-config-client-ip&: Client IP configuration.
    --proxy-protocol-config-allowed-client-headers=: List of the allowed client header names.
    --proxy-protocol-config-client-ip: Client IP configuration.
    --proxy-protocol-config-gateway-identity=: Gateway identity configuration.
    --proxy-protocol-config-metadata-headers=: Custom resource specific headers along with the values.
    --request-id=: An optional request ID to identify requests.
    --resource-override-path=!: Contains uri path fragment where HTTP request is sent.
    --user-info-output-type=: The output type of the delegated user info.
completion:
    flag:
        contextual-headers-output-type:
            - json
            - none
            - protobuf
        device-info-output-type:
            - json
            - none
            - protobuf
        group-info-output-type:
            - json
            - none
            - protobuf
        proxy-protocol-config-gateway-identity:
            - resource-name
        user-info-output-type:
            - json
            - none
            - protobuf
documentation:
    command: '*(BETA)*  Create a securityGateway'
    flag:
        async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        contextual-headers-output-type: |-
            Default output type for all enabled headers. _CONTEXTUAL_HEADERS_OUTPUT_TYPE_ must be one of:
            +
            *json*::: JSON output type.
            *none*::: Explicitly disable header output.
            *protobuf*::: Protobuf output type.
            :::
            +
        device-info-output-type: |-
            The output type of the delegated device info. _DEVICE_INFO_OUTPUT_TYPE_ must be one of:
            +
            *json*::: JSON output type.
            *none*::: Explicitly disable header output.
            *protobuf*::: Protobuf output type.
            :::
            +
        display-name: |-
            An arbitrary user-provided name for the SecurityGateway.
            Cannot exceed 64 characters.
        group-info-output-type: |-
            The output type of the delegated group info. _GROUP_INFO_OUTPUT_TYPE_ must be one of:
            +
            *json*::: JSON output type.
            *none*::: Explicitly disable header output.
            *protobuf*::: Protobuf output type.
            :::
            +
        hubs: |-
            Map of Hubs that represents regional data path deployment with Google Cloud Platform region
            as a key.
            +
            *KEY*:::
            Sets `KEY` value.
            +
            *VALUE*:::
            Sets `VALUE` value.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --hubs=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --hubs='{"string": {}}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --hubs=path_to_file.(yaml|json)
            +
            ```
        location: |-
            The location id of the securityGateway resource. We support only global location.
            +
            To set the `location` attribute:
            * provide the argument `security_gateway` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
        no-async: |-
            Return immediately, without waiting for the operation in progress to
            complete.
        no-proxy-protocol-config-client-ip: Client IP configuration. The client IP address is included if true.
        proxy-protocol-config-allowed-client-headers: List of the allowed client header names.
        proxy-protocol-config-client-ip: Client IP configuration. The client IP address is included if true.
        proxy-protocol-config-gateway-identity: |-
            Gateway identity configuration. _PROXY_PROTOCOL_CONFIG_GATEWAY_IDENTITY_ must be (only one value is supported):
            +
            *resource-name*::: Resource name for gateway identity, in the format:
            projects/{project_id}/locations/{location_id}/securityGateways/{security_gateway_id}
            :::
            +
        proxy-protocol-config-metadata-headers: |-
            Custom resource specific headers along with the values.
            The names should conform to RFC 9110:
            > Field names SHOULD constrain themselves to alphanumeric characters, "-",
            and ".", and SHOULD begin with a letter.
            Field values SHOULD contain only ASCII printable characters and tab.
            +
            *KEY*:::
            Sets `KEY` value.
            +
            *VALUE*:::
            Sets `VALUE` value.
            +
            :::
            *Shorthand Example:*
            +
            ```
            +
            --proxy-protocol-config-metadata-headers=string=string
            +
            ```
            +
            *JSON Example:*
            +
            ```
            +
            --proxy-protocol-config-metadata-headers='{"string": "string"}'
            +
            ```
            +
            *File Example:*
            +
            ```
            +
            --proxy-protocol-config-metadata-headers=path_to_file.(yaml|json)
            +
            ```
        request-id: |-
            An optional request ID to identify requests. Specify a unique request ID
            so that if you must retry your request, the server will know to ignore the
            request if it has already been completed. The server will guarantee
            that for at least 60 minutes since the first request.
        resource-override-path: Contains uri path fragment where HTTP request is sent.
        user-info-output-type: |-
            The output type of the delegated user info. _USER_INFO_OUTPUT_TYPE_ must be one of:
            +
            *json*::: JSON output type.
            *none*::: Explicitly disable header output.
            *protobuf*::: Protobuf output type.
            :::
            +
