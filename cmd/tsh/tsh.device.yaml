# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: device
description: Manage this device. Requires Teleport Enterprise.
documentation:
    command: '[AI] The `tsh device` command is used in Tcl-based network automation tools like T шhreadable Shell (Tsh) to retrieve detailed information about a specific network device connected to the system. It interacts with the underling device driver or API to fetch hardware and software details such as interface statistics, MAC address, IP address, vendor name, model number, and other device-specific characteristics. The output is usually in a structured format that can be further processed using Tcl scripting.'
