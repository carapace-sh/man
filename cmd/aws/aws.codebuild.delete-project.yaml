name: delete-project
description: Deletes a build project.
flags:
    --name=!: The name of the build project.
documentation:
    command: Deletes a build project. When you delete a project, its builds are not deleted.
    flag:
        name: The name of the build project.
