name: describe
description: Show the latest status of an operation.
flags:
    --name=!: The full name of the Cloud Scheduler operation to describe.
documentation:
    command: |
        Show the latest status of an operation.
    flag:
        name: 'The full name of the Cloud Scheduler operation to describe. Format: projects/{project}/locations/{location}/operations/{operation}'
