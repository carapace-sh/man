name: remove-metadata
description: Remove project-wide metadata entries.
flags:
    --all: If provided, all metadata entries are removed.
    --keys=: The keys of the entries to remove.
    --no-all&: If provided, all metadata entries are removed.
documentation:
    command: '*gcloud compute project-info remove-metadata* can be used to remove project-wide metadata entries.'
    flag:
        all: If provided, all metadata entries are removed.
        keys: The keys of the entries to remove.
        no-all: If provided, all metadata entries are removed.
