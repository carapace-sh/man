# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: generate
description: Generate a LS_COLORS expression
flags:
    -h, --help: Prints help information
documentation:
    command: '[AI] The `vivid generate` command is a part of Vivid, a tool for creating and managing Kubernetes resources. It generates new Kubernetes YAML files based on given templates and input values. This can be useful for creating consistent and reusable configurations across various environments. Users provide a template file, and the desired input values to generate a new YAML file.'
    flag:
        help: '[AI] `vivid generate --help` is a command used with Vivid CLI, a tool for creating and managing applications in the Elixir programming language. This specific command displays detailed help information about the "generate" sub-command. It explains what the command does (creates new application files based on templates), provides usage examples, and lists available options to customize the generation process.'
