name: describe-export-tasks
description: Describes the specified export instance tasks or all of your export instance tasks.
flags:
    --export-task-ids=: The export task IDs.
    --filters=: the filters for the export tasks.
documentation:
    command: Describes the specified export instance tasks or all of your export instance tasks.
    flag:
        export-task-ids: The export task IDs.
        filters: the filters for the export tasks.
