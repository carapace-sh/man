name: describe-connector-entity
description: Provides details regarding the entity used with the connector, with a description of the data model for each field in that entity.
flags:
    --api-version=: The version of the API that's used by the connector.
    --connector-entity-name=!: The entity name for that connector.
    --connector-profile-name=: The name of the connector profile.
    --connector-type=: The type of connector application, such as Salesforce, Amplitude, and so on.
completion:
    flag:
        connector-type:
            - Salesforce
            - Singular
            - Slack
            - Redshift
            - S3
            - Marketo
            - Googleanalytics
            - Zendesk
            - Servicenow
            - Datadog
            - Trendmicro
            - Snowflake
            - Dynatrace
            - Infornexus
            - Amplitude
            - Veeva
            - EventBridge
            - LookoutMetrics
            - Upsolver
            - Honeycode
            - CustomerProfiles
            - SAPOData
            - CustomConnector
            - Pardot
documentation:
    command: Provides details regarding the entity used with the connector, with a description of the data model for each field in that entity.
    flag:
        api-version: The version of the API that's used by the connector.
        connector-entity-name: The entity name for that connector.
        connector-profile-name: The name of the connector profile. The name is unique for each `ConnectorProfile` in the Amazon Web Services account.
        connector-type: The type of connector application, such as Salesforce, Amplitude, and so on.
