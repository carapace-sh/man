name: upgrade
description: '*(ALPHA)*  Upgrade the version of the Config Management feature.'
flags:
    --location=: Location for the membership.
    --membership=!: ID of the membership or fully qualified identifier for the membership.
documentation:
    command: |-
        *(ALPHA)*  Upgrade a specified membership to any supported version of the Config
        Management feature.

        ## EXAMPLES

        To upgrade a membership named `MEMBERSHIP_NAME`, run:

          $ gcloud alpha container fleet config-management upgrade --membership=MEMBERSHIP_NAME --version=VERSION
    flag:
        location: |-
            Location for the membership.
            +
            To set the `location` attribute:
            * provide the argument `--membership` on the command line with a fully specified name;
            * provide the argument `--location` on the command line;
            * set the property `gkehub/location`.
        membership: |-
            ID of the membership or fully qualified identifier for the membership.
            +
            To set the `membership` attribute:
            * provide the argument `--membership` on the command line.
