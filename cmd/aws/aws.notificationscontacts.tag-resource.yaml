name: tag-resource
description: Attaches a key-value pair to a resource, as identified by its Amazon Resource Name (ARN).
flags:
    --arn=!: The ARN of the configuration.
    --tags=!: A list of tags to apply to the configuration.
documentation:
    command: Attaches a key-value pair to a resource, as identified by its Amazon Resource Name (ARN). Taggable resources in AWS User Notifications Contacts include email contacts.
    flag:
        arn: The ARN of the configuration.
        tags: A list of tags to apply to the configuration.
