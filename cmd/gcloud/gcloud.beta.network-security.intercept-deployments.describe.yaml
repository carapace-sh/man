name: describe
description: '*(BETA)*  Describe an Intercept Deployment.'
flags:
    --location=: Location of the intercept deployment.
documentation:
    command: |+
        *(BETA)*  Describe an intercept deployment.

        For more examples, refer to the EXAMPLES section below.

    flag:
        location: |-
            Location of the intercept deployment.
            +
            To set the `location` attribute:
            * provide the argument `INTERCEPT_DEPLOYMENT` on the command line with a fully specified name;
            * provide the argument `--location` on the command line.
