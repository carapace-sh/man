name: list
description: '*(BETA)*  List batch jobs in a project.'
flags:
    --filter=&: Apply a Boolean filter EXPRESSION to each resource item to be listed
    --limit=: Maximum number of resources to list.
    --page-size=: Some services group resource list output into pages.
    --region=: Dataproc region to use.
    --sort-by=: Comma-separated list of resource field key names to sort by.
documentation:
    command: |-
        *(BETA)*  List batch jobs in a project. Page-size sets the maximum number of jobs
          returned per page, and Page-token retrieves subsequent results.

        ## EXAMPLES

        List batch jobs in the "us-central1" region:

          $ gcloud beta dataproc batches list --region="us-central1"
    flag:
        filter: |-
            Apply a Boolean filter EXPRESSION to each resource item to be listed
            (the '=' equality operator is the only supported operator).
            If the expression evaluates true for an item, the item is listed.
            This flag interacts with other flags, which are applied in the
            following order: *--flatten*, *--sort-by*, *--filter*, *--limit*.
            For more information, run 'gcloud topic filters'.
        limit: |-
            Maximum number of resources to list. The default is *unlimited*.
            This flag interacts with other flags that are applied in this order:
            *--flatten*, *--sort-by*, *--filter*, *--limit*.
        page-size: |-
            Some services group resource list output into pages. This flag specifies
            the maximum number of resources per page. The default is *100*.
            Paging may be applied before or after *--filter* and *--limit* depending
            on the service.
        region: Dataproc region to use. Each Dataproc region constitutes an independent resource namespace constrained to deploying instances into Compute Engine zones inside the region. Overrides the default *dataproc/region* property value for this command invocation.
        sort-by: |-
            Comma-separated list of resource field key names to sort by. The
            default order is ascending. Prefix a field with ``~'' for descending
            order on that field. This flag interacts with other flags that are applied
            in this order: *--flatten*, *--sort-by*, *--filter*, *--limit*.
