name: describe
description: Show details about an access level.
flags:
    --level-format=&: The format in which to display the access level.
    --policy=: The ID of the access policy.
completion:
    flag:
        level-format:
            - as_defined
            - cel
documentation:
    command: Show details about an access level in a given access policy.
    flag:
        level-format: The format in which to display the access level.
        policy: |-
            The ID of the access policy.
            +

            To set the `policy` attribute:
            * provide the argument `level` on the command line with a fully specified name;
            * provide the argument `--policy` on the command line;
            * set the property `access_context_manager/policy`;
            * automatically, if the current account belongs to an organization with exactly one access policy..
