name: get-relay
description: Fetch the relay resource and it's attributes.
flags:
    --relay-id=!: A unique relay identifier.
documentation:
    command: Fetch the relay resource and it's attributes.
    flag:
        relay-id: A unique relay identifier.
