name: describe-scaling-plan-resources
description: Describes the scalable resources in the specified scaling plan.
flags:
    --max-results=: The maximum number of scalable resources to return.
    --next-token=: The token for the next set of results.
    --scaling-plan-name=!: The name of the scaling plan.
    --scaling-plan-version=!: The version number of the scaling plan.
documentation:
    command: Describes the scalable resources in the specified scaling plan.
    flag:
        max-results: The maximum number of scalable resources to return. The value must be between 1 and 50. The default value is 50.
        next-token: The token for the next set of results.
        scaling-plan-name: The name of the scaling plan.
        scaling-plan-version: The version number of the scaling plan. Currently, the only valid value is `1`.
