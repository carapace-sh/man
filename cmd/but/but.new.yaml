# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: new TARGET
description: Insert a blank commit before the specified commit, or at the top of a stack
group: branching and committing
flags:
    -h, --help: Print help
