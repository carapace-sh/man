name: remove-application-instance
description: Removes an application instance.
flags:
    --application-instance-id=!: An application instance ID.
documentation:
    command: Removes an application instance.
    flag:
        application-instance-id: An application instance ID.
