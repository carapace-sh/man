name: get-channel-namespace
description: Retrieves the channel namespace for a specified `Api`.
flags:
    --api-id=!: The `Api` ID.
    --name=!: The name of the `ChannelNamespace`.
documentation:
    command: Retrieves the channel namespace for a specified `Api`.
    flag:
        api-id: The `Api` ID.
        name: The name of the `ChannelNamespace`.
