name: delete-security-policy
description: Deletes an OpenSearch Serverless security policy.
flags:
    --client-token=: Unique, case-sensitive identifier to ensure idempotency of the request.
    --name=!: The name of the policy to delete.
    --type=!: The type of policy.
completion:
    flag:
        type:
            - encryption
            - network
documentation:
    command: Deletes an OpenSearch Serverless security policy.
    flag:
        client-token: Unique, case-sensitive identifier to ensure idempotency of the request.
        name: The name of the policy to delete.
        type: The type of policy.
