# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: create
description: Create file system for Azure Data Lake Storage Gen2 account.
flags:
    --account-key=: Storage account key.
    --account-name=: Storage account name.
    --auth-mode=: The mode in which to run the command.
    --blob-endpoint=: Storage data service endpoint.
    --connection-string=: Storage account connection string.
    --metadata=:
        description: Metadata in space-separated key=value pairs.
        nargs: -1
    --public-access=: Specify whether data in the file system may be accessed publicly and the level of access.
    --sas-token=: A Shared Access Signature (SAS).
    --subscription=: Name or ID of subscription.
    --timeout=: Request timeout in seconds.
    -d, --default-encryption-scope=: Specify the default encryption scope to set on the file system and use for all future writes.
    -n, --name=!: File system name (i.e. container name).
    -p, --prevent-encryption-scope-override=:
        description: If true, prevents any request from specifying a different encryption scope than the scope set on the file system.
        nargs: -1
completion:
    flag:
        auth-mode:
            - login
            - key
        prevent-encryption-scope-override:
            - "true"
            - "false"
        public-access:
            - "off"
            - file
            - filesystem
documentation:
    flag:
        account-key: 'Storage account key. Must be used in conjunction with storage account name or service endpoint. Environment variable: AZURE_STORAGE_KEY.'
        account-name: 'Storage account name. Related environment variable: AZURE_STORAGE_ACCOUNT. Must be used in conjunction with either storage account key or a SAS token. If neither are present, the command will try to query the storage account key using the authenticated Azure account. If a large number of storage commands are executed the API quota may be hit.'
        acquire-policy-token: Acquiring an Azure Policy token automatically for this resource operation.
        auth-mode: |-
            The mode in which to run the command. "login" mode will directly use your login credentials for the authentication. The legacy "key" mode will attempt to query for an account key if no authentication parameters for the account are provided. Environment variable: AZURE_STORAGE_AUTH_MODE.

            Accepted values: key, login
        blob-endpoint: 'Storage data service endpoint. Must be used in conjunction with either storage account key or a SAS token. You can find each service primary endpoint with ``` az storage account show ``` . Environment variable: AZURE_STORAGE_SERVICE_ENDPOINT.'
        change-reference: The related change reference ID for this resource operation.
        connection-string: 'Storage account connection string. Environment variable: AZURE_STORAGE_CONNECTION_STRING.'
        default-encryption-scope: Specify the default encryption scope to set on the file system and use for all future writes.
        metadata: Metadata in space-separated key=value pairs. This overwrites any existing metadata.
        name: File system name (i.e. container name).
        prevent-encryption-scope-override: |-
            If true, prevents any request from specifying a different encryption scope than the scope set on the file system. Default value is false.

            Accepted values: false, true
        public-access: |-
            Specify whether data in the file system may be accessed publicly and the level of access.

            Accepted values: file, filesystem, off
        sas-token: 'A Shared Access Signature (SAS). Must be used in conjunction with storage account name or service endpoint. Environment variable: AZURE_STORAGE_SAS_TOKEN.'
        subscription: Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`
        timeout: Request timeout in seconds. Applies to each call to the service.
examples:
    Create file system for Azure Data Lake Storage Gen2 account and enable public access.: az storage fs create -n fsname --public-access file --account-name mystorageaccount --account-key 0000-0000
    Create file system for Azure Data Lake Storage Gen2 account.: az storage fs create -n fsname --account-name mystorageaccount --account-key 0000-0000
    Create file system for Azure Data Lake Storage Gen2 account. (autogenerated): az storage fs create --account-name mystorageaccount --auth-mode login --name fsname
