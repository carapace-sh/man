# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: fs
description: Use the Charm file system.
documentation:
    command: '[AI] "charm fs" is not a standard Linux command, but it seems to be related to Charmed Kubernetes, a specific deployment method for OpenStack''s Kubernetes distribution, OpenStack Charms. It''s used within the OpenStack Charms ecosystem to manage and configure filesystem (FS) resources, such as creating new file systems or mounting existing ones for containers in Kubernetes clusters.'
